## 108

### Problem 1

This problem is worth 5 points (single-choice).

A proposition must have a definite truth value (true or false). Statements (A), (B), (D), (E) are declarative sentences with definite truth values. Statement (C) "垃圾不分藍綠" is ambiguous — it could mean "trash doesn't distinguish blue/green" (factual) or be a political double entendre. Its truth value is unclear, making it unlikely a proper proposition.
- Key concept: definition of a proposition — a declarative sentence with a definite truth value.

### Problem 2

This problem is worth 5 points (single-choice).

Fibonacci numbers grow exponentially: $F_n \approx \phi^n / \sqrt{5}$ where $\phi \approx 1.618$. A linear lower bound exists trivially ($F_n \ge n/2$ for large $n$). An exponential lower bound exists ($F_n \ge (\phi/2)^n$ eventually). An exponential upper bound exists ($F_n \le \phi^n$). However, a **linear upper bound** does NOT exist because Fibonacci numbers grow exponentially. Answer: **(D)**.
- Key concept: Fibonacci number growth rate — exponential growth precludes any linear upper bound.

### Problem 3

This problem is worth 5 points (single-choice).

By Fermat's little theorem, $3^{10} \equiv 1 \pmod{11}$. So $3^{302} = 3^{30 \times 10 + 2} = (3^{10})^{30} \cdot 3^2 \equiv 1^{30} \cdot 9 = 9 \pmod{11}$. Answer: **(D)**.
- Key concept: Fermat's little theorem — $a^{p-1} \equiv 1 \pmod{p}$ for prime $p$ and $\gcd(a,p)=1$.

### Problem 4

This problem is worth 5 points (single-choice).

In the poset $(\{1,...,9\}, |)$: not a total order (e.g., 2 and 3 are incomparable). Maximal elements exist (5,6,7,8,9 — not divided by any other element in $S$). 1 divides everything, so 1 is the greatest lower bound (minimum). But $R$ is not a lattice — e.g., $\text{lcm}(4,6)=12 \notin S$, so 4 and 6 have no least upper bound. Answer: **(C)**.
- Key concept: poset properties — divisibility relation, maximal/minimal elements, GLB, and lattice conditions.

### Problem 5

This problem is worth 5 points (single-choice).

Run Dijkstra from $a$: $a(0) \to c(3) \to b(4) \to d(9) \to e(10) \to f(14) \to g(15) \to z(19)$. Alternatively $a \to b(4) \to c(6)$... After $a$ and $c$ are finalized, $b$ has tentative distance $\min(4, 3+3)=4$ and $e$ has $3+6=9$, so $b$ is added next, not $d$. Shortest path cost to $z$ is 19, not 17. The shortest path doesn't necessarily have fewest edges. Multiple shortest paths may exist. Answer: **(E) None of the above**.
- Key concept: Dijkstra's algorithm execution — greedy selection of minimum-distance vertex and relaxation.

### Problem 6

This problem is worth 5 points (multiple-choice, −1 per wrong option).

A total order $R$ is reflexive, antisymmetric, transitive, and total (every pair is comparable).
(A) **True.** Totality: if $\neg R(x,y)$ then $R(y,x)$ (every pair is comparable).
(B) **True.** Antisymmetry: $R(x,y) \land R(y,x) \to x=y$.
(C) **Not necessarily true.** A total order on an infinite set may not have a minimum element.
(D) **True.** $R^{-1}$ is also a total order, so totality holds for $R^{-1}$.
(E) **False.** $R$ is reflexive, so $R^{-1}$ is also reflexive: $R^{-1}(x,x)$ holds.
- Key concept: total order properties — reflexive, antisymmetric, transitive, and total; inverse of a total order is also a total order.

### Problem 7

This problem is worth 5 points (multiple-choice, −1 per wrong option).

Recurrence: $p(n) = 2p(n/3) + \Theta(\sqrt{n}) + c$ (two recursive calls on size $n/3$ each, plus $\Theta(\sqrt{n})$ for Q). So $p(n) = 2p(n/3) + c_1\sqrt{n} + c_2$. By Master Theorem: $a=2, b=3, f(n) = \sqrt{n}$. $\log_3 2 \approx 0.631 > 0.5$. Case 1: $p(n) = \Theta(n^{\log_3 2})$.
(B) **True.** (E) **True.**
- Key concept: recurrence solving with the Master Theorem — comparing $f(n)$ growth with $n^{\log_b a}$.

### Problem 8

This problem is worth 5 points (multiple-choice, −1 per wrong option).

$i \sim j$ if $2i + 3j = 5n$ for some integer $n$, i.e., $2i + 3j \equiv 0 \pmod{5}$. Reflexive: $2i + 3i = 5i$ ✓. Symmetric: if $2i+3j \equiv 0$ then $2j+3i = 5(i+j)-(2i+3j) \equiv 0$ ✓. Transitive: if $2i+3j \equiv 0$ and $2j+3k \equiv 0$, then $2i+3k = (2i+3j)+(2j+3k)-5j \equiv 0$ ✓. So $\sim$ is an equivalence relation. Answer: **(D)**.
- Key concept: equivalence relation verification — checking reflexivity, symmetry, and transitivity for modular arithmetic.

### Problem 9

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** If $g \circ f$ is injective, then $f$ must be injective (if $f(a)=f(b)$, then $g(f(a))=g(f(b))$, contradicting injectivity of $g \circ f$).
(B) **False.** $g$ need not be injective — only on the range of $f$.
(C) **False.** $f$ need not be surjective.
(D) **True.** If $g \circ f$ is surjective, then for every $c \in C$, there exists $a$ with $g(f(a))=c$, so $g$ is surjective.
(E) **False.** $g \circ f$ bijective implies $f$ injective and $g$ surjective, but not both bijective individually.
- Key concept: composition of functions — injectivity/surjectivity preservation under composition.

### Problem 10

This problem is worth 5 points (multiple-choice, −1 per wrong option).

Solve $a_n = -6a_{n-1} + 7a_{n-2}$ with $a_0=-5, a_1=3$. Characteristic equation: $x^2+6x-7=0$, roots $x=1, x=-7$. General solution: $a_n = \alpha \cdot 1^n + \beta \cdot (-7)^n$. From $a_0=-5$: $\alpha+\beta=-5$. From $a_1=3$: $\alpha-7\beta=3$. Solving: $\beta=-1, \alpha=-4$. So $a_n = -4-(-7)^n$.
(A) Verify generating function form. (C) **True.** $a_n = -4-(-7)^n$.
- Key concept: solving linear recurrences — characteristic equation method and generating functions.

### Problem 11

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Eigenvalues of triangular matrices are diagonal entries.
(B) **True.** $(A^T)^{-1} = (A^{-1})^T$ exists if $A$ is invertible.
(C) **True.** $(AB)^{-1} = B^{-1}A^{-1}$.
(D) **True.** Matrix multiplication is not commutative in general.
(E) **True.** Example: $A = \begin{bmatrix} 0 & -1 \\ 1 & 0 \end{bmatrix}$, then $A^2 = -I$.
- Key concept: fundamental matrix properties — invertibility, transpose, commutativity, and special matrix constructions.

### Problem 12

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Real symmetric matrices have real eigenvalues and are orthogonally diagonalizable.
(B) **True.** If $A = PDP^{-1}$, then $A^{-1} = PD^{-1}P^{-1}$, so $A^{-1}$ is diagonalizable.
(C) **False.** For non-square $A$, invertibility doesn't apply; even for square matrices, $AB$ similar to $BA$ requires specific conditions.
(D) **False.** Product of diagonalizable matrices is not necessarily diagonalizable.
(E) **False.** A symmetric matrix with all positive entries can have negative eigenvalues (positive definiteness requires all eigenvalues > 0, not just positive entries).
- Key concept: diagonalizability, spectral theorem for symmetric matrices, and conditions for positive definiteness.

### Problem 13

This problem is worth 5 points (single-choice).

Sum of eigenvalues = trace of $A$ = $0.6 + 0.9 + 0.8 = 2.3$. Answer: **(B)**.
- Key concept: trace of a matrix equals the sum of its eigenvalues.

### Problem 14

This problem is worth 5 points (single-choice).

$A = LU$ where $L$ is lower triangular and $U$ is upper triangular. Solve $LUx = b$ by forward substitution ($Ly = b$) then back substitution ($Ux = y$). Compute step by step to find $x$.
- Key concept: solving linear systems via LU factorization — forward and back substitution.

### Problem 15

This problem is worth 5 points (single-choice).

Compute $A^{-1}$ using row reduction $[A|I] \to [I|A^{-1}]$ or the adjugate formula. Verify by checking $AA^{-1} = I$ for each answer choice.
- Key concept: matrix inverse computation — Gauss-Jordan elimination or cofactor method.

### Problem 16

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Complex eigenvalues of real matrices come in conjugate pairs, so $a-ib$ is also an eigenvalue.
(B) **True.** A complex eigenvalue requires a complex eigenvector (real matrix with complex eigenvalue cannot have a real eigenvector).
(C) **True.** Symmetric real matrices have only real eigenvalues (Spectral Theorem).
(D) **True.** Eigenspaces of distinct eigenvalues of a symmetric matrix are orthogonal.
(E) **False.** Eigenvalues of $A^TA$ are non-negative (they are squares of singular values), not arbitrary reals.
- Key concept: eigenvalue properties of real and symmetric matrices — conjugate pairs, real eigenvalues, orthogonal eigenspaces.

### Problem 17

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True** only when $A^TA$ is invertible (columns of $A$ are linearly independent).
(B) **False.** Even if $A^TA$ is not invertible, least-squares solutions exist (they may not be unique).
(C) **Not always true.** If $A$ has linearly independent columns, $\hat{x}$ is unique.
(D) **True.** If $b \in \text{Col}(A)$, the system is consistent and $\hat{x}$ is an exact solution.
(E) **False.** $\hat{x} = R^{-1}Q^Tb$ requires $A = QR$ with $R$ invertible (only when $A$ has linearly independent columns).
- Key concept: least-squares solutions — normal equations, existence, and uniqueness conditions.

### Problem 18

This problem is worth 5 points (multiple-choice, asks which are WRONG).

(A) **Wrong.** Invertibility doesn't imply diagonalizability (e.g., $\begin{bmatrix}1&1\\0&1\end{bmatrix}$).
(B) **Wrong.** QR factorization works for any matrix with linearly independent columns, not just square matrices.
(C) **Correct** (not wrong). $A = PDP^T$ requires $A$ to be symmetric (orthogonal diagonalization).
(D) **Wrong.** Spectral decomposition $A = \sum \lambda_i u_i u_i^T$ requires $A$ to be symmetric with orthonormal eigenvectors, not just having $n$ distinct eigenvalues.
(E) **Wrong.** SVD exists for all matrices, but the form is $A = U\Sigma V^T$; for symmetric $A$, eigendecomposition is more natural.
- Key concept: matrix factorization conditions — diagonalization, QR, spectral decomposition, and SVD prerequisites.

### Problem 19

This problem is worth 5 points (multiple-choice).

Cholesky factorization $A = R^TR$ requires: (A) square matrix ✓, (D) all eigenvalues positive (positive definite) ✓, (E) symmetric ✓. Linearly independent columns (B) is implied by positive definiteness. Enough eigenvectors (C) is guaranteed by symmetry.
- Key concept: Cholesky factorization requires a symmetric positive definite matrix.

### Problem 20

This problem is worth 5 points (multiple-choice).

$A = \begin{bmatrix}1&1&5\\1&5&1\\5&1&1\end{bmatrix}$ is symmetric. Find eigenvalues: $\det(A-\lambda I) = 0$. The matrix has eigenvalues $7, -2, -2$ (verify by trace = 7 and characteristic polynomial). Find orthonormal eigenvectors to form $P$ and diagonal $D$. Check which values do NOT appear in $P$ or $D$.
- Key concept: orthogonal diagonalization of symmetric matrices — eigenvalue computation and eigenvector orthonormalization.

## 109

### Problem 1

This problem is worth 1 point (True/False, −1 for wrong).

$AB = BA$ is **False** in general. Matrix multiplication is not commutative.
- Key concept: matrix multiplication is not commutative.

### Problem 2

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $AB = AC$ does not imply $B = C$ unless $A$ is invertible. Counterexample: $A = \begin{bmatrix}0&0\\0&0\end{bmatrix}$.
- Key concept: cancellation law fails for singular matrices.

### Problem 3

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Two non-zero matrices can multiply to the zero matrix. Example: $A = \begin{bmatrix}1&0\\0&0\end{bmatrix}$, $B = \begin{bmatrix}0&0\\1&0\end{bmatrix}$.
- Key concept: zero divisors exist in matrix algebra.

### Problem 4

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $(AB)^T = B^T A^T$, not $A^T B^T$.
- Key concept: transpose of a product reverses the order.

### Problem 5

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $(rA)^{-1} = \frac{1}{r} A^{-1}$, not $r A^{-1}$.
- Key concept: scalar multiplication and matrix inverse — the scalar factor is inverted.

### Problem 6

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $\det(A+B) \ne \det A + \det B$ in general. The determinant is not a linear function of the matrix.
- Key concept: determinant is multiplicative, not additive.

### Problem 7

This problem is worth 1 point (True/False, −1 for wrong).

**True.** $\det(AB) = \det A \cdot \det B$ (product rule for determinants).
- Key concept: determinant of a product equals the product of determinants.

### Problem 8

This problem is worth 1 point (True/False, −1 for wrong).

**True.** $\det(AB) = \det A \cdot \det B = \det B \cdot \det A = \det(BA)$.
- Key concept: determinant is commutative under product.

### Problem 9

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $\det(rA) = r^n \det A$ for an $n \times n$ matrix, not $r \det A$.
- Key concept: scalar multiplication scales the determinant by $r^n$.

### Problem 10

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $\det(A^{-1}) = 1/\det A$, not $(-1) \det A$.
- Key concept: determinant of inverse is the reciprocal of the determinant.

### Problem 11

This problem is worth 1 point (True/False, −1 for wrong).

**False.** If $b \ne 0$, the solution set of $Ax = b$ is an affine subspace (translated subspace), which is NOT a subspace (it doesn't contain the zero vector).
- Key concept: solution sets of non-homogeneous systems are affine, not linear subspaces.

### Problem 12

This problem is worth 1 point (True/False, −1 for wrong).

**True.** When $b \ne 0$ and the system is consistent, the solution set is an affine subspace, not a subspace.
- Key concept: non-homogeneous solution sets are not subspaces.

### Problem 13

This problem is worth 1 point (True/False, −1 for wrong).

**False.** If $b = 0$, the solution set (null space) is always a subspace.
- Key concept: the null space of a matrix is always a subspace.

### Problem 14

This problem is worth 1 point (True/False, −1 for wrong).

**False.** The null space ($b = 0$) is always a subspace.
- Key concept: homogeneous solution sets are always subspaces.

### Problem 15

This problem is worth 1 point (True/False, −1 for wrong).

**True.** The solution set of $Ax = 0$ is the null space of $A$, which is exactly a subspace.
- Key concept: null space is a subspace of $\mathbb{R}^n$.

### Problem 16

This problem is worth 1 point (True/False, −1 for wrong).

**True.** If $A = PBP^{-1}$, then $\det A = \det(PBP^{-1}) = \det B$.
- Key concept: similar matrices have the same determinant.

### Problem 17

This problem is worth 1 point (True/False, −1 for wrong).

**True.** If $A = PBP^{-1}$, then $A^2 = PB^2P^{-1}$, so $A^2$ is similar to $B^2$.
- Key concept: similarity is preserved under powers.

### Problem 18

This problem is worth 1 point (True/False, −1 for wrong).

**True.** If $A = PBP^{-1}$, then $A^T = (P^{-1})^T B^T P^T = (P^T)^{-1} B^T (P^{-1})^{-T}$... Actually, $A^T = (P^{-1})^T B^T P^T$, so $A^T$ is similar to $B^T$ (with change-of-basis matrix $(P^{-1})^T$).
- Key concept: transposes of similar matrices are similar.

### Problem 19

This problem is worth 1 point (True/False, −1 for wrong).

**True.** If $A = PBP^{-1}$, then $A^{-1} = PB^{-1}P^{-1}$, so $A^{-1}$ is similar to $B^{-1}$.
- Key concept: similarity is preserved under inversion.

### Problem 20

This problem is worth 1 point (True/False, −1 for wrong).

**True.** $AB = P(BA)P^{-1}$ where $P$ relates $A$ and $B$? Not directly. But if $A$ is similar to $B$ ($A = PBP^{-1}$), then $AB = PBP^{-1}B$ and $BA = BP B P^{-1}$... This requires careful analysis. In general, if $A$ is similar to $B$, $AB$ is not necessarily similar to $BA$.
- Key concept: similarity and products — requires careful consideration of basis transformations.

### Problem 21

This problem is worth 1 point (True/False, −1 for wrong).

**False.** A diagonalizable matrix doesn't need $n$ distinct eigenvalues — it needs $n$ linearly independent eigenvectors. Example: $I_n$ is diagonalizable with only one eigenvalue.
- Key concept: diagonalizability requires $n$ linearly independent eigenvectors, not necessarily distinct eigenvalues.

### Problem 22

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Diagonalizability doesn't imply invertibility or full column rank. Example: the zero matrix is diagonalizable but has no independent columns.
- Key concept: diagonalizability is independent of invertibility.

### Problem 23

This problem is worth 1 point (True/False, −1 for wrong).

**True** for $A^T$ (if $A = PDP^{-1}$, then $A^T = (P^{-1})^T D P^T$, so $A^T$ is diagonalizable). **False** for $A^{-1}$ if $A$ is not invertible (0 eigenvalue means $A^{-1}$ doesn't exist). So the statement is **False** overall.
- Key concept: transpose of a diagonalizable matrix is diagonalizable, but inverse may not exist.

### Problem 24

This problem is worth 1 point (True/False, −1 for wrong).

**False.** A diagonalizable matrix can have zero eigenvalues. Example: $\begin{bmatrix}1&0\\0&0\end{bmatrix}$.
- Key concept: zero eigenvalues are compatible with diagonalizability.

### Problem 25

This problem is worth 1 point (True/False, −1 for wrong).

**False.** If the matrix has fewer than $n$ distinct eigenvalues, it may still be diagonalizable but doesn't have $n$ distinct eigenspaces. The number of eigenspaces equals the number of distinct eigenvalues, which can be less than $n$.
- Key concept: the number of eigenspaces equals the number of distinct eigenvalues.

### Problem 26

This problem is worth 1 point (True/False, −1 for wrong).

**True.** The Fundamental Theorem of Linear Algebra: $\text{Nul}(A) = (\text{Row}(A))^\perp$.
- Key concept: four fundamental subspaces — null space is the orthogonal complement of row space.

### Problem 27

This problem is worth 1 point (True/False, −1 for wrong).

**True.** The orthogonal complement of any subset is always a subspace.
- Key concept: orthogonal complement is always a subspace.

### Problem 28

This problem is worth 1 point (True/False, −1 for wrong).

**True** when $W$ is a subspace. $(W^\perp)^\perp = W$ for any subspace $W$. Since the problem states $W$ is a subset, if $W$ is not a subspace, this may fail. But assuming $W$ is a subspace (standard assumption), **True**.
- Key concept: double orthogonal complement of a subspace returns the original subspace.

### Problem 29

This problem is worth 1 point (True/False, −1 for wrong).

**False** in general. $A$ having orthogonal columns means $A^T A$ is diagonal, but $(Av)^T(Aw) = v^T A^T A w$. If $v \perp w$, $v^T w = 0$, but $v^T A^T A w \ne 0$ unless $A^T A = cI$ (i.e., $A$ has orthonormal columns).
- Key concept: orthogonal columns preserve orthogonality only if columns are orthonormal.

### Problem 30

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $W + W^\perp = \mathbb{R}^n$ (direct sum), but $W \cup W^\perp \ne \mathbb{R}^n$ (union is not the same as sum for subspaces).
- Key concept: $\mathbb{R}^n = W \oplus W^\perp$ (direct sum), not union.

### Problem 31

This problem is worth 2 points (True/False, −2 for wrong).

Compute the $\beta$-matrix: $P^{-1}AP$ where $P = [b_1 | b_2 | b_3]$. Calculate $AP$ column by column, then solve $P^{-1}(AP)$ to get the $\beta$-matrix entries. Check if $0$ and $-1$ appear.
- Key concept: change of basis — $\beta$-matrix is $P^{-1}AP$ where $P$ is the basis change matrix.

### Problem 32

This problem is worth 2 points (True/False, −2 for wrong).

Continue computing $P^{-1}AP$ and check if $1$ and $-3$ appear in the result.
- Key concept: change of basis matrix computation.

### Problem 33

This problem is worth 2 points (True/False, −2 for wrong).

Check if $-2$ and $-4$ appear in the $\beta$-matrix $P^{-1}AP$.
- Key concept: change of basis matrix computation.

### Problem 34

This problem is worth 2 points (True/False, −2 for wrong).

Check if $-6$ and $-7$ appear in the $\beta$-matrix $P^{-1}AP$.
- Key concept: change of basis matrix computation.

### Problem 35

This problem is worth 2 points (True/False, −2 for wrong).

Check if $8$ and $-9$ appear in the $\beta$-matrix $P^{-1}AP$.
- Key concept: change of basis matrix computation.

### Problem 36

This problem is worth 2 points (True/False, −2 for wrong).

Perform QR factorization of the given $4 \times 3$ matrix using Gram-Schmidt. $R$ is upper triangular with $R_{ij} = q_i^T a_j$. Check if $1$ appears in $R$.
- Key concept: QR factorization via Gram-Schmidt — $R$ entries are inner products of orthonormal basis with original columns.

### Problem 37

This problem is worth 2 points (True/False, −2 for wrong).

Check if $3$ appears in the $R$ matrix from QR factorization.
- Key concept: QR factorization computation.

### Problem 38

This problem is worth 2 points (True/False, −2 for wrong).

Check if $5$ appears in the $R$ matrix from QR factorization.
- Key concept: QR factorization computation.

### Problem 39

This problem is worth 2 points (True/False, −2 for wrong).

Check if $7$ appears in the $R$ matrix from QR factorization.
- Key concept: QR factorization computation.

### Problem 40

This problem is worth 2 points (True/False, −2 for wrong).

Check if $9$ appears in the $R$ matrix from QR factorization.
- Key concept: QR factorization computation.

### Problem 41

This problem is worth 5 points (single-choice).

Exactly one person is lying. 神掌 and 柯P make contradictory statements (神掌 says "柯P did it," 柯P says "神掌 lied"). So exactly one of them is the liar. If 神掌 lies → 柯P didn't do it, 柯P tells truth (confirming 神掌 lied), 小英 says "神掌 did it" (must be true since only one liar), 韓總 says "I didn't" (true). So 神掌 did it? But 小英 saying "神掌 did it" being true conflicts with 神掌 lying about 柯P. Actually if 韓總 is the one lying → 韓總 did it. Trace through carefully: if 韓總 lies, he did it. Then 小英 (truth): "神掌 did it" — contradiction. So 韓總 isn't lying. The answer is **(B) 韓總**.
- Key concept: logic puzzle — using contradiction to identify the liar when exactly one person lies.

### Problem 42

This problem is worth 5 points (single-choice).

$\mathbb{N}, \mathbb{Z}, \mathbb{Q}$ are all countably infinite, so bijections exist between them. $\mathbb{R}$ is uncountable (Cantor's theorem), so no bijection from $\mathbb{Q}$ to $\mathbb{R}$ exists. Answer: **(C)**.
- Key concept: cardinality — countable vs. uncountable sets; Cantor's diagonalization argument.

### Problem 43

This problem is worth 5 points (single-choice).

$3^{302} \bmod 5$: $3^4 \equiv 1 \pmod{5}$, so $3^{302} = 3^{4 \times 75 + 2} \equiv 3^2 = 9 \equiv 4 \pmod{5}$. (A) ✓.
$3^{302} \bmod 7$: $3^6 \equiv 1 \pmod{7}$, so $3^{302} = 3^{6 \times 50 + 2} \equiv 9 \equiv 2 \pmod{7}$. (B) ✓.
$3^{302} \bmod 11$: by Problem 3, $\equiv 9$. (C) ✓.
$3^{302} \bmod 385$: $385 = 5 \times 7 \times 11$. By CRT: need residues mod 5,7,11 = 4,2,9. Check if $3^{302} \equiv 9 \pmod{385}$: $9 \bmod 5 = 4$ ✓, $9 \bmod 7 = 2$ ✓, $9 \bmod 11 = 9$ ✓. So (D) ✓. Answer: **(E) None of the above** (all are correct).
- Key concept: modular exponentiation using Fermat's little theorem and Chinese Remainder Theorem.

### Problem 44

This problem is worth 5 points (single-choice).

$R = \begin{bmatrix}0&1&0\\0&1&1\\0&0&1\end{bmatrix}$. (A) Antisymmetric: $R_{12}=1$ but $R_{21}=0$ ✓; $R_{24}=1$ but $R_{42}=0$ ✓. No pair with both $R_{ij}=1$ and $R_{ji}=1$ except diagonal. ✓. (B) Reflexive closure adds 1s to diagonal where missing: $R_{11}=0 \to 1$. Result matches ✓. (C) Symmetric closure: add $R_{ji}=1$ wherever $R_{ij}=1$. $R_{12}=1 \to R_{21}=1$; $R_{24}=1 \to R_{42}=1$. Result should be $\begin{bmatrix}0&1&0\\1&1&1\\0&1&1\end{bmatrix}$, not as given. **(C) is most inappropriate.** (E) Not an equivalence relation (not reflexive). Also inappropriate. Multiple answers may qualify; choose the MOST inappropriate.
- Key concept: relation closures — reflexive, symmetric, and transitive closures of a binary relation.

### Problem 45

This problem is worth 5 points (single-choice).

Graph isomorphism requires: same number of vertices, same number of edges, same degree sequence, and a bijection preserving adjacency. Conditions (A)-(D) are all necessary but none individually sufficient. Answer: **(E) All must hold** (but even all together are necessary, not sufficient — isomorphism requires an explicit bijection). However, (D) is actually sufficient by definition. Check carefully: (D) says every subgraph of $G_1$ has an isomorphic subgraph in $G_2$, which is essentially the definition. Answer: **(D)** or **(E)**.
- Key concept: graph isomorphism — necessary conditions vs. sufficient conditions.

### Problem 46

This problem is worth 5 points (single-choice).

Gödel's incompleteness theorems state that in any consistent formal system capable of expressing basic arithmetic, there are true statements that cannot be proven. This supports (b) (AI limitations), (d) (undecidable problems for Turing machines). It does NOT directly support (a) (graph isomorphism complexity) — that's a computational complexity question, not a foundational logic issue. Answer: **(a)**.
- Key concept: Gödel's incompleteness theorem — limitations of formal systems, not about computational complexity of specific problems.

### Problem 47

This problem is worth 5 points (single-choice).

(a) Set difference: $(A-B)-C \ne A-(B-C)$ in general. Not associative. (b) Division: $(a/b)/c \ne a/(b/c)$. Not associative. (c) Implication: $(p \to q) \to r \ne p \to (q \to r)$. Not associative. (d) GCD: $\gcd(\gcd(a,b),c) = \gcd(a,\gcd(b,c))$. **Associative** ✓. (e) Cartesian product: $(A \times B) \times C \ne A \times (B \times C)$ (different tuple structures). Not associative. Answer: **(d)**.
- Key concept: associativity of operations — GCD is associative; set difference, division, implication, and Cartesian product are not.

### Problem 48

This problem is worth 5 points (single-choice).

Recurrence: $T(n) = 4T(n/4) + \Theta(\sqrt{n}) + \Theta(\sqrt{n}) = 4T(n/4) + \Theta(\sqrt{n})$. Master Theorem: $a=4, b=4, f(n)=\sqrt{n}$. $\log_4 4 = 1 > 0.5$. Case 1: $T(n) = \Theta(n^1) = \Theta(n)$. Answer: **(a)**.
- Key concept: Master Theorem application — comparing polynomial exponents.

### Problem 49

This problem is worth 5 points (single-choice).

Let $a_n$ = number of ways to reserve $n$ ordered seats. For 1 person: 2 ways (email, web). For 2 persons: 3 ways (phone, email, web). Recurrence: $a_n = 2a_{n-1} + 3a_{n-2}$ with $a_1 = 2, a_2 = 2 \cdot 2 + 3 = 7$. Characteristic equation: $x^2 - 2x - 3 = 0$, roots $x = 3, -1$. General solution: $a_n = \alpha \cdot 3^n + \beta \cdot (-1)^n$. From initial conditions, solve for $\alpha$ and $\beta$. Answer: **(e) $\frac{3}{4}3^n + \frac{1}{4}(-1)^n$**.
- Key concept: linear recurrence with constant coefficients — characteristic equation and initial conditions.

### Problem 50

This problem is worth 5 points (single-choice).

10 people, each likes ≥ 4 others. Total "likes" ≥ 40. By pigeonhole, average "liked by" count = total likes / 10 ≥ 4. A person is "happy" if liked by ≥ 3. If everyone were liked by ≤ 2, total likes ≤ 20, contradicting ≥ 40. So some people must be liked by ≥ 3. Answer: **(c) It is impossible that no one is happy**.
- Key concept: pigeonhole principle and counting arguments in graph theory.

## 110

### Problem 1

This problem is worth 5 points (multiple-choice, asks which are INCORRECT).

(A) **Correct.** $\forall x P(x) \land \forall x Q(x) \equiv \forall x (P(x) \land Q(x))$. ✓
(B) **Incorrect.** $\forall x P(x) \lor \forall x Q(x) \not\equiv \forall x (P(x) \lor Q(x))$. The left side is stronger.
(C) **Incorrect.** $\exists x P(x) \land \exists x Q(x) \not\equiv \exists x (P(x) \land Q(x))$. Different witnesses may be needed.
(D) **Correct.** $\exists x P(x) \lor \exists x Q(x) \equiv \exists x (P(x) \lor Q(x))$. ✓
(E) **Correct.** With the given assumptions. ✓
- Key concept: quantifier distribution over logical connectives — $\forall$ distributes over $\land$, $\exists$ distributes over $\lor$.

### Problem 2

This problem is worth 5 points (multiple-choice, asks which are INCORRECT).

(A) Injective function from $A$ to $B$ means $|A| \le |B|$, not $|A| > |B|$. **Incorrect.**
(B) Bijective function means $|A| = |B|$, not $|A| > |B|$. **Incorrect.**
(C) Surjective function from $\mathbb{N}$ to a set means the set is at most countable, not necessarily larger. **Incorrect.**
(D) Surjective function from $\mathbb{Q}$ to a set means the set is at most countably infinite. **Incorrect.**
- Key concept: function types and set cardinality — injective (≤), surjective (≥), bijective (=).

### Problem 3

This problem is worth 5 points (multiple-choice, asks which are INCORRECT).

(A) **Correct.** Bézout's identity: $\gcd(a,b)=1 \Rightarrow \exists p,q: pa+qb=1$, so $pa+qb=n$ for any $n$ by scaling.
(B) **Incorrect claim.** The modular inverse exists when $\gcd(p,q)=1$, but $q>1$ is not strictly required ($q=1$ gives trivial case).
(C) **Incorrect.** CRT requires $m_i$ pairwise coprime, not just prime. If primes are distinct, they are coprime.
(D) **Incorrect.** Fermat's little theorem requires $\gcd(a,p)=1$; when $p | a$, $a^{p-1} \not\equiv 1$.
(E) **Incorrect.** In RSA, Alice encrypts with Bob's **public** key, and Bob decrypts with his **private** key (not the reverse).
- Key concept: number theory — Bézout's identity, modular inverse, CRT, Fermat's little theorem, and RSA encryption/decryption.

### Problem 4

This problem is worth 5 points (multiple-choice, asks which are INCORRECT).

Analyze the Hasse diagram to determine the partial order relation. Check each statement:
(A) $cRh$: trace if there's a path from $c$ up to $h$ in the Hasse diagram.
(B) $mRm$: reflexivity — every element is related to itself. ✓
(C) $dRa$: $d$ is above $a$ in the diagram, so $aRd$ (not $dRa$). **Incorrect.**
(D) $a$ is minimal if no element is below it. ✓
(E) Lattice requires every pair to have a join and meet.
- Key concept: Hasse diagram interpretation — reading partial order, minimal/maximal elements, and lattice conditions.

### Problem 5

This problem is worth 5 points (multiple-choice).

Analyze the directed graph with edges: $b \to a, a \to e, e \to d, d \to b, b \to c, c \to d$.
(A) Strongly connected: check if every vertex can reach every other. $a \to e \to d \to b \to a$ ✓, $b \to c \to d$ ✓, but can we reach $a$ from $c$? $c \to d \to b \to a$ ✓. **True.**
(B) Weakly connected: if strongly connected, also weakly connected. **True.**
(C) Euler path: requires at most 2 vertices with odd degree (undirected) or specific in/out degree conditions. Check in/out degrees.
(D) Hamilton path: visits each vertex exactly once. Check if one exists.
(E) Planar: 5 vertices, can always be drawn without crossings (a graph with ≤ 5 vertices is planar unless it's $K_5$). **True.**
- Key concept: graph connectivity, Euler/Hamilton paths, and planarity for directed graphs.

### Problem 6

This problem is worth 5 points (multiple-choice).

$R = \{(x,y) : x$ has no more prime factors than $y\}$ (counting multiplicity or distinct?). If counting distinct prime factors: reflexive (same count ✓), not antisymmetric (6 and 10 both have 2 distinct prime factors), transitive (if $\omega(x) \le \omega(y)$ and $\omega(y) \le \omega(z)$ then $\omega(x) \le \omega(z)$). Not a partial order (not antisymmetric). Not total (depends on interpretation).
(C) **True** — reflexive.
- Key concept: properties of relations defined by prime factor counts.

### Problem 7

This problem is worth 5 points (multiple-choice).

$S$ = set of all AI software (binary strings). Binary strings are countable ($|S| = |\mathbb{N}| = \aleph_0$). $|V| = |\mathbb{R}| = \mathfrak{c} > \aleph_0$. So $|V| > |S|$, meaning AI cannot solve all valuable problems.
(C) **True** — $|S|$ is countably infinite.
(D) **True** — $|V| \ne |S|$ and $|V| > |S|$.
- Key concept: cardinality arguments — countable programs vs. uncountable problems.

### Problem 8

This problem is worth 5 points (multiple-choice).

Ways to buy $n$ dollars using \$1 and \$2 bills: last bill is \$1 (remaining: $a_{n-1}$ ways) or \$2 (remaining: $a_{n-2}$ ways). So $a_n = a_{n-1} + a_{n-2}$. Answer: **(D)**.
- Key concept: Fibonacci-type recurrence for counting compositions.

### Problem 9

This problem is worth 5 points (multiple-choice).

(A) **True.** Merge sort is $\Theta(n \log n)$ in all cases.
(B) **True.** Euclidean algorithm: $\Theta(\log \max(a,b))$.
(C) **False.** Bubble sort is $\Theta(n^2)$ average/worst.
(D) **False.** Warshall's algorithm is $\Theta(n^3)$ (three nested loops over $n$ vertices).
(E) **True.** TSP is NP-hard.
- Key concept: algorithm complexity — merge sort, Euclidean GCD, bubble sort, Warshall's, and TSP.

### Problem 10

This problem is worth 5 points (multiple-choice).

$a_n = a_{n-1} + 20a_{n-2}$, $a_0=-1, a_1=-1$. Characteristic equation: $x^2 - x - 20 = 0$, roots $x = 5, -4$. General solution: $a_n = \alpha \cdot 5^n + \beta \cdot (-4)^n$. From $a_0=-1$: $\alpha + \beta = -1$. From $a_1=-1$: $5\alpha - 4\beta = -1$. Solving: $\alpha = -5/9, \beta = -4/9$... Check generating function approach.
(B) **True.** $G(z) = (-1-z)/(1-z-20z^2)$.
(D) **True.** Partial fractions yield the given form.
(E) **True.** $a_n = (-2/3) \cdot 5^n + (-1/3) \cdot (-4)^n$.
- Key concept: solving recurrences via generating functions and characteristic equations.

### Problem 11

$A = PDP^{-1}$ factorization. Necessary conditions: square matrix with enough linearly independent eigenvectors. Answer: **(D)**.

### Problem 12

$A = QR$ factorization. Necessary condition: linearly independent columns. Answer: **(C)**.

### Problem 13

$A = PDP^T$ factorization. Necessary condition: symmetric. Answer: **(B)**.

### Problem 14

Spectral decomposition. Necessary conditions: symmetric with orthonormal eigenvectors. Answer: **(B)**.

### Problem 15

Cholesky $A = R^TR$. Necessary conditions: symmetric and positive definite. Answer: **(B), (E)**.
- Key concept: matrix factorization prerequisites — each factorization has specific structural requirements.

### Problem 16

This problem is worth 1 point (True/False, −1 for wrong).

**False.** A diagonalizable matrix can have 0 as an eigenvalue. Example: $\text{diag}(1,0)$.
- Key concept: diagonalizability doesn't exclude zero eigenvalues.

### Problem 17

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Diagonalizable doesn't require distinct eigenvalues. Example: $I_n$ has only eigenvalue 1.
- Key concept: repeated eigenvalues are compatible with diagonalizability.

### Problem 18

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Diagonalizable doesn't imply invertible (can have zero eigenvalues).
- Key concept: diagonalizability and invertibility are independent properties.

### Problem 19

This problem is worth 1 point (True/False, −1 for wrong).

**True.** If $A = PDP^{-1}$, then $A^T = (P^{-1})^T D^T P^T = (P^{-1})^T D P^T$, so $A^T$ is diagonalizable.
- Key concept: transpose of a diagonalizable matrix is diagonalizable.

### Problem 20

This problem is worth 1 point (True/False, −1 for wrong).

**False.** The diagonalization is not unique — different orderings of eigenvalues and different eigenvector bases yield different $P$ and $D$.
- Key concept: diagonalization is not unique due to eigenvector/eigenvalue ordering freedom.

### Problem 21

This problem is worth 1 point (True/False, −1 for wrong).

Compute $M = C^{-1} A P_\beta$ where $P_\beta = [b_1 | b_2]$ and $C = [c_1|c_2|c_3]$. First compute $A \cdot P_\beta$, then solve $C \cdot M_{\text{col}} = (A \cdot P_\beta)_{\text{col}}$ for each column. Check if $1, 2, 4$ appear in $M$.
- Key concept: transformation matrix relative to given bases — $M = C^{-1}AP_\beta$.

### Problem 22

This problem is worth 1 point (True/False, −1 for wrong).

Check if $1, -2, 4$ appear in the transformation matrix $M$.
- Key concept: transformation matrix computation.

### Problem 23

This problem is worth 1 point (True/False, −1 for wrong).

Check if $1, -2, -4$ appear in the transformation matrix $M$.
- Key concept: transformation matrix computation.

### Problem 24

This problem is worth 1 point (True/False, −1 for wrong).

Check if $7, 14, 28$ appear in the transformation matrix $M$.
- Key concept: transformation matrix computation.

### Problem 25

This problem is worth 1 point (True/False, −1 for wrong).

Check if $7, -14, 28$ appear in the transformation matrix $M$.
- Key concept: transformation matrix computation.

### Problem 26

This problem is worth 1 point (True/False, −1 for wrong).

**True.** If $Ax = b$ is inconsistent, then $b \notin \text{Col}(A)$.
- Key concept: inconsistency means $b$ is not in the column space.

### Problem 27

This problem is worth 1 point (True/False, −1 for wrong).

**False.** The least-squares solution $\hat{x}$ is unique when $A$ has linearly independent columns. It may not be unique otherwise.
- Key concept: uniqueness of least-squares solution depends on column independence.

### Problem 28

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Even if $A^TA$ is not invertible, least-squares solutions still exist (they are just not unique).
- Key concept: $A^TA$ singularity affects uniqueness, not existence, of least-squares solutions.

### Problem 29

This problem is worth 1 point (True/False, −1 for wrong).

**False.** $A\hat{x}$ is always in $\text{Col}(A)$ by definition (it's a linear combination of columns of $A$).
- Key concept: any matrix-vector product $Ax$ is in the column space of $A$.

### Problem 30

This problem is worth 1 point (True/False, −1 for wrong).

**True.** The residual $b - A\hat{x}$ is orthogonal to $\text{Col}(A)$, which means it's in $\text{Nul}(A^T)$ (since $\text{Col}(A)^\perp = \text{Nul}(A^T)$).
- Key concept: the least-squares residual lies in the left null space of $A$.

### Problem 31

This problem is worth 1 point (True/False, −1 for wrong).

**True.** A covariance matrix is symmetric positive semi-definite, so by the spectral theorem it has $n$ orthogonal eigenvectors.
- Key concept: covariance matrices are symmetric → spectral theorem guarantees orthogonal eigenvectors.

### Problem 32

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Eigenvalues can repeat (e.g., covariance matrix of identical variables).
- Key concept: symmetric matrices have real eigenvalues, but they need not be distinct.

### Problem 33

This problem is worth 1 point (True/False, −1 for wrong).

**False.** Covariance matrices are positive semi-definite, so eigenvalues are $\ge 0$. They can be zero but not negative.
- Key concept: covariance matrix eigenvalues are non-negative.

### Problem 34

This problem is worth 1 point (True/False, −1 for wrong).

**True.** Any positive semi-definite matrix $C$ can be decomposed as $C = AA^T$ (e.g., via Cholesky or eigendecomposition: $C = U\Lambda U^T = (U\sqrt{\Lambda})(U\sqrt{\Lambda})^T$).
- Key concept: positive semi-definite decomposition into $AA^T$.

### Problem 35

This problem is worth 1 point (True/False, −1 for wrong).

**False.** A covariance matrix is positive semi-definite, not necessarily positive definite (some eigenvalues can be 0 when variables are linearly dependent).
- Key concept: covariance matrices are PSD, not necessarily PD.

### Problem 36

This problem is worth 5 points (multiple-choice).

(A) **False.** LU factorization costs $O(n^3/3)$, proportional to $n^3$.
(B) **True.** Back substitution on a dense upper triangular matrix is $O(n^2)$.
(C) **True.** With ≤ 3 nonzeros per row, each Jacobi iteration is $O(n)$ (sparse matrix-vector multiply).
(D) **False.** Contradicts (C).
- Key concept: computational costs of linear algebra operations — LU is $O(n^3)$, back substitution is $O(n^2)$, sparse Jacobi is $O(n)$.

### Problem 37

This problem is worth 5 points (multiple-choice).

Symmetric diagonally dominant matrices are positive definite, so all standard methods work: LU (A) ✓, normal equations (B) ✓, Gauss-Seidel (C) ✓ (converges for diagonally dominant), Jacobi (D) ✓ (converges for diagonally dominant).
- Key concept: diagonally dominant matrices guarantee convergence of iterative methods.

### Problem 38

This problem is worth 5 points (multiple-choice).

Check linear independence for each set: (A) compute determinant. (B) $\det = 0$ (rows are arithmetic progressions). (C) check. (D) orthogonal vectors, clearly independent ✓.
- Key concept: basis verification via linear independence — compute determinants or row reduce.

### Problem 39

This problem is worth 5 points (multiple-choice).

Row reduce $A$ to find the null space. The null space basis vectors satisfy $Ax = 0$. Verify each candidate by multiplying $A \cdot x$ and checking if the result is zero.
- Key concept: null space computation via row reduction and verification.

### Problem 40

This problem is worth 5 points (multiple-choice).

The Python function computes the **determinant** using cofactor expansion along the first row. Matrices with $\det = 0$: (A) has two identical columns → $\det = 0$ ✓. (B) row 4 = 3 × row 1 → $\det = 0$ ✓. (C) check if linearly dependent rows exist. (D) product of two matrices — $\det(AB) = \det(A)\det(B)$; check if either has $\det = 0$.
- Key concept: determinant computation via cofactor expansion — zero determinant indicates linear dependence.

## 111

### Problem 1

This problem is worth 5 points (multiple-choice).

$Q(x,y): x+y = x-y$, which simplifies to $2y = 0$, i.e., $y = 0$.
(A) $\exists x \exists y: y=0$ ✓ **True.**
(B) $\forall x \exists y: y=0$ ✓ (choose $y=0$) **True.**
(C) $\exists y \forall x: y=0$ ✓ ($y=0$ works for all $x$) **True.**
(D) $\forall y \exists x$: need $y=0$ for all $y$ — **False** (fails when $y \ne 0$).
(E) $\forall x \forall y$: need $y=0$ always — **False.**
- Key concept: quantifier logic — evaluating truth of nested quantifiers.

### Problem 2

This problem is worth 5 points (multiple-choice).

75 hours, at least 1 match/hour, at most 125 total. Let $s_i$ = cumulative matches after hour $i$. $s_0=0$, $s_{75} \le 125$. Consider $s_i$ and $s_i + 24$: by pigeonhole, since $s_{75} \le 125$, there exist $i < j$ with $s_j - s_i = 24$ (the sequences $s_i$ and $s_i+24$ overlap in $[1, 125]$). Similarly for other values.
(C) **True** — there's a consecutive period with exactly 24 matches.
- Key concept: pigeonhole principle on cumulative sums — guaranteeing a specific difference exists.

### Problem 3

This problem is worth 5 points (multiple-choice).

(A) **True.** RSA correctness uses Euler's theorem (generalization of Fermat's) and CRT for efficient computation.
(B) **False.** The two primes are used to generate $n = pq$; public/private keys are $(e, n)$ and $(d, n)$.
(C) **True.** $(M^e)^d = M^{ed} = M \pmod{n}$ by RSA construction.
(D) **False.** Alice encrypts with her **private** key (for signing) and Bob decrypts with Alice's **public** key. The description reverses public/private.
- Key concept: RSA cryptosystem — key generation, encryption/decryption, and digital signatures.

### Problem 4

This problem is worth 5 points (multiple-choice).

Euler's formula for connected planar graphs: $v - e + r = 2$, so $r = e - v + 2$.
(A) **True** (correcting to $r = e - v + 2$). The given formula $r = e + v - 2$ is **wrong**. So **(A) is False.**
(B) **True.** For simple connected planar graphs with $v \ge 3$: $e \le 3v - 6$.
(C) **True.** Consequence of $e \le 3v - 6$: average degree $< 6$, so some vertex has degree $\le 5$.
(D) **False.** Kuratowski's theorem: nonplanar iff contains subdivision of $K_5$ or $K_{3,3}$. $K_{2,3}$ is planar.
(E) **True.** Kuratowski: subgraph homeomorphic to $K_5$ implies nonplanar.
- Key concept: planar graph theory — Euler's formula, edge bounds, and Kuratowski's theorem.

### Problem 5

This problem is worth 5 points (multiple-choice).

For a full $m$-ary tree: $n = mi + 1$ (total vertices), $l = (m-1)i + 1$ (leaves), $n = l + i$.
(A) $i = (n-1)/m$ ✓ **True.**
(B) $l = [(m-1)n+1]/m$ ✓ **True.**
(C) $l = (m-1)i + 1$ ✓ **True.**
(D) $n = (ml-1)/(m-1)$ ✓ **True.**
(E) $i = (l-1)/(m-1)$ ✓ **True.**
- Key concept: full $m$-ary tree formulas relating vertices, internal nodes, and leaves.

### Problem 6

This problem is worth 5 points (multiple-choice).

$|A|=8, |B|=3$. (A) Total functions: $3^8$ ✓. (B) One-to-one: impossible since $|A| > |B|$, so count = 0. **False.** (C) Onto functions: use inclusion-exclusion, not $8^3$. **False.** (D) $f^{-1}$: since no bijection exists ($|A| \ne |B|$), no inverse function exists. Number = 0. **True.** (E) Bijection: impossible since $|A| \ne |B|$. Number = 0. **True.**
- Key concept: counting functions — total, injective, surjective, and bijective between sets of different sizes.

### Problem 7

This problem is worth 5 points (multiple-choice).

The given properties: reflexive, antisymmetric (for distinct elements), transitive, total ($\forall i,j: p(i,j) \lor p(j,i)$), and has a unique minimum ($\exists! k: \forall i \ne k, p(k,i)$).
(B) **True** — reflexive + antisymmetric + transitive = partial order.
(C) **True** — partial order + totality = total order.
(D) **True** — total order is a lattice (every pair has meet and join).
(E) Total order with a minimum on a finite set is a well-order. **True** if $S$ is finite.
- Key concept: order relations — partial order, total order, lattice, and well-ordering.

### Problem 8

This problem is worth 5 points (multiple-choice).

Algorithm X: $k=2$, $T(n) = 2T(n/2) + \Theta(\sqrt{n}) + \Theta(n)$. Dominant term: $\Theta(n)$. By Master: $a=2, b=2, f(n)=n$. $\log_2 2 = 1 = $ exponent of $f$. Case 2: $T(n) = \Theta(n \log n)$.

Algorithm Y: $k=4$, $T(n) = 4T(n/4) + \Theta(\sqrt{n}) + \Theta(\sqrt{n}) = 4T(n/4) + \Theta(\sqrt{n})$. Master: $a=4, b=4, \log_4 4 = 1 > 0.5$. Case 1: $T(n) = \Theta(n)$.

Algorithm Z: $k=9$, $T(n) = 9T(n/9) + \Theta(\sqrt{n}) + \Theta(n)$. Dominant: $\Theta(n)$. Master: $a=9, b=9, \log_9 9 = 1$. Case 2: $T(n) = \Theta(n \log n)$.

(D) **True** — X and Z have the same complexity $\Theta(n \log n)$.
(E) **True** — X is $\Theta(n \log n)$.
- Key concept: comparing algorithm complexities via Master Theorem with different branching factors.

### Problem 9

This problem is worth 5 points (multiple-choice).

(A) **True.** $\binom{r}{k} = \frac{r!}{k!(r-k)!} = \frac{r}{k} \cdot \frac{(r-1)!}{(k-1)!(r-k)!} = \frac{r}{k}\binom{r-1}{k-1}$.
(B) **True.** $(1-1)^n = \sum_{k=0}^n \binom{n}{k}(-1)^k = 0$ for $n \ge 1$. For $n=0$: $\binom{0}{0}(-1)^0 = 1 \ne 0$. **False** for $n=0$.
(C) **True.** Hockey stick identity: $\sum_{k=m}^n \binom{k}{m} = \binom{n+1}{m+1}$.
(D) **True.** Vandermonde-like identity for upper indices.
(E) **False.** Pascal's rule: $\binom{r}{k} = \binom{r-1}{k-1} + \binom{r-1}{k}$, not $\binom{r}{k-1}$.
- Key concept: binomial coefficient identities — absorption, alternating sum, hockey stick, and Pascal's rule.

### Problem 10

This problem is worth 5 points (multiple-choice).

$(a_{n-1} - a_{n-2}) = 2(a_n - a_{n-1})$, i.e., $2a_n = 3a_{n-1} - a_{n-2}$. $a_0 = 5, a_1 = 9$. Solve using generating functions or characteristic equation. Characteristic: $2x^2 - 3x + 1 = 0$, roots $x = 1, 1/2$. General solution: $a_n = \alpha + \beta(1/2)^n$. From $a_0=5$: $\alpha + \beta = 5$. From $a_1=9$: $\alpha + \beta/2 = 9$. Solving: $\beta = -8, \alpha = 13$. So $a_n = 13 - 8(1/2)^n$.
(D) **True.** $a_n = 13 - 8 \times (1/2)^n$.
- Key concept: solving second-order linear recurrences via characteristic equation.

### Problem 11

This problem is worth 5 points (multiple-choice).

(A) **False.** $A = \begin{bmatrix}0&1\\1&0\end{bmatrix}$: $A^2 = I$ (diagonal), but $A$ is not diagonal.
(B) **False.** Orthonormal eigenvectors don't imply Hermitian — unitary matrices (which are normal but not necessarily Hermitian) also have orthonormal eigenvectors.
(C) **True.** $AA^H = A^HA$ means $A$ is normal, and normal matrices are unitarily diagonalizable.
(D) **True.** If rank is $r < n$, the null space has dimension $n-r$, so 0 is an eigenvalue with multiplicity $\ge n-r$.
(E) **True.** If the eigenspace has dimension > 1, multiple eigenvectors exist for the same eigenvalue.
- Key concept: eigenvalue/eigenvector properties — normal matrices, rank-nullity, and eigenspace dimensions.

### Problem 12

This problem is worth 5 points (multiple-choice).

The $197 \times 197$ tridiagonal matrix with 2 on diagonal and −1 on off-diagonals. Eigenvalues: $\lambda_k = 2 - 2\cos(k\pi/198)$ for $k=1,...,197$. All eigenvalues are positive (range: $2-2\cos(\pi/198)$ to $2+2\cos(\pi/198)$, all > 0).
(A) $\det = \prod \lambda_k = 198$. $\text{mod}(198, 5) = 3$. **True.**
(B) **True.** Any matrix $A = \frac{A+A^T}{2} + \frac{A-A^T}{2}$. Since $A$ is symmetric, $C = 0$ (skew part is 0). Still valid decomposition.
(C) **True.** $A$ is symmetric, so $A = BCB^T$ with orthogonal $B$ and diagonal $C$ (spectral theorem).
(D) **True.** $A$ is symmetric positive definite, so $A = LDL^T$ (LDLT factorization).
(E) **False.** All eigenvalues are positive (no negative eigenvalues).
- Key concept: tridiagonal matrix properties — eigenvalue formula, decompositions, and positive definiteness.

### Problem 13

This problem is worth 5 points (single-choice, −2 for wrong).

$A^5 = \begin{bmatrix}32&100000\\0&3125\end{bmatrix}$. $A$ is upper triangular, so $A = \begin{bmatrix}a&b\\0&c\end{bmatrix}$ with $a^5=32 \Rightarrow a=2$ and $c^5=3125 \Rightarrow c=5$. $\det A = 10$. Compute $b$ from $A^5_{12} = 100000$: using the formula for powers of upper triangular matrices, find $b$. Then $N = \text{round}(2+b+0+5)$, and compute $\text{mod}(10 + 13N, 5)$.
- Key concept: powers of upper triangular matrices — diagonal entries are powers of original diagonal entries.

### Problem 14

This problem is worth 5 points (single-choice, −2 for wrong).

First system: $\det = k^3 - 3k + 2 = (k-1)^2(k+2)$. Multiple solutions when $k=1$ (det=0 and consistent). No solution when... check $k=-2$: $\det = 0$ but system may be inconsistent. Compute $|a-b|$ and continue with the second system's least-squares solution.
- Key concept: system analysis via determinant — conditions for unique, multiple, or no solutions; least-squares.

### Problem 15

This problem is worth 5 points (single-choice, −2 for wrong).

Compute $\det(A)$ by cofactor expansion or row reduction. Compute $\det(B)$ similarly. Then $K = |M/N| / 10000$ and determine its range.
- Key concept: determinant computation for 5×5 and 4×4 matrices.

### Problem 16

This problem is worth 5 points (multiple-choice).

(A) **True.** Similar matrices: $\det(PBP^{-1}) = \det B$.
(B) **False.** Similar matrices have the same eigenvalues but different eigenvectors ($v_A = Pv_B$).
(C) **True.** $A^2 = PB^2P^{-1}$.
(D) **True.** If $A = PBP^{-1}$, then $AB = PBP^{-1}B$ and we need to show similarity to $BA$. $AB = PB(P^{-1}B)$ and $BA = B(PBP^{-1}) = B \cdot A$... This requires careful analysis. Actually $AB$ and $BA$ are always similar when $A$ (or $B$) is invertible.
(E) **False.** $A^2$ similar to $B^2$ does not imply $A$ similar to $B$.
- Key concept: similarity invariants — determinant, eigenvalues, trace, and powers.

### Problem 17

This problem is worth 5 points (multiple-choice).

(A) **True.** Real matrices can have complex eigenvalues (in conjugate pairs).
(B) **True.** $\lambda = 0 \Rightarrow \det(A) = 0 \Rightarrow A$ is not invertible.
(C) **False.** Repeated eigenvalues don't prevent diagonalizability if eigenspaces have sufficient dimension.
(D) **False.** Symmetric matrices have real eigenvalues, but they can be negative or zero (not necessarily positive).
(E) **True.** Different orderings of eigenvalues/eigenvectors give different $P$ and $D$.
- Key concept: eigenvalue properties — complex eigenvalues, invertibility, diagonalizability conditions.

### Problem 18

This problem is worth 5 points (multiple-choice).

(A) **False.** The set of columns of $A$ is not necessarily a subspace (not closed under addition/scaling unless it spans one).
(B) **True.** Orthogonal complement is always a subspace.
(C) **True** when $W$ is a subspace: $(W^\perp)^\perp = W$. But $W$ here is just columns, which may not be a subspace.
(D) **False.** $W \cap W^\perp = \{0\}$ (contains the zero vector), not the empty set.
(E) **True.** QR factorization requires linearly independent columns, so $m \ge n$.
- Key concept: orthogonal complements, subspace properties, and QR factorization prerequisites.

### Problem 19

This problem is worth 5 points (multiple-choice, asks which are NOT related to least-squares).

(A) Inconsistent system — **related** (least-squares solves inconsistent systems).
(B) Eigenvector — **NOT related** to least-squares.
(C) Diagonalizable — **NOT related** to least-squares.
(D) Orthogonal projection — **related** (least-squares = projection onto column space).
(E) Gram-Schmidt — **related** (used in QR factorization for least-squares).
- Key concept: least-squares problem components — normal equations, projection, QR factorization.

### Problem 20

This problem is worth 5 points (multiple-choice).

Spectral decomposition of $\begin{bmatrix}3&4\\4&9\end{bmatrix}$: find eigenvalues and orthonormal eigenvectors. $A = \lambda_1 u_1 u_1^T + \lambda_2 u_2 u_2^T$. Eigenvalues: $\lambda^2 - 12\lambda + 11 = 0$, $\lambda = 1, 11$. Find normalized eigenvectors and identify which fractions appear.
- Key concept: spectral decomposition of symmetric matrices — eigenvalues and orthonormal eigenvector outer products.

## 112

### Problem 1

This problem is worth 5 points (multiple-choice).

Analyze the relation matrix $A$. (A) Reflexive: diagonal entries $A_{11}=0$, not all 1. **False.** (B) Anti-symmetric: check if $A_{ij}=1$ and $A_{ji}=1$ implies $i=j$. $A_{34}=1, A_{43}=1$ and $3 \ne 4$. **False.** (C) Transitive: check if $A_{ij}=1$ and $A_{jk}=1$ implies $A_{ik}=1$ for all $i,j,k$. Verify systematically.
- Key concept: relation properties verification from matrix representation.

### Problem 2

This problem is worth 5 points (multiple-choice).

(A) Reflexive closure: add 1s to diagonal where $A_{ii}=0$. Count: positions (1,1), (2,2), (3,3), (5,5) are 0. Need 4 additions. **True.**
(B) Symmetric closure: for each $A_{ij}=1$ with $A_{ji}=0$, add $A_{ji}=1$. Count the additions.
(C) Transitive closure: use Warshall's algorithm to find additional 1s needed.
(D) Check if reflexive closure is a partial order.
(E) Check if symmetric closure is an equivalence relation.
- Key concept: computing relation closures — reflexive (add diagonal), symmetric (mirror entries), transitive (Warshall's).

### Problem 3

This problem is worth 5 points (multiple-choice).

Analyze the graph representation of $R$: nodes 1-6, directed edges based on $A_{ij}=1$. Check connectivity, longest simple path, Hamilton path in transitive closure, and strong connectivity of $R^{-1}$.
- Key concept: graph properties derived from relation representation.

### Problem 4

This problem is worth 5 points (multiple-choice).

The symmetric and reflexive closure of $R$ creates a relation that can model Boolean operators. Check which Boolean operators ($\land, \lor, \neg, \to, \leftrightarrow$) match the truth-table pattern of the closure relation.
- Key concept: Boolean operator representation as binary relations.

### Problem 5

This problem is worth 5 points (multiple-choice).

Identify sufficient but NOT necessary conditions for each goal:
(A) Isomorphism is much stronger than both having Euler circuits. **Sufficient but not necessary** ✓.
(B) $g = O(f)$ but not $\Theta(f)$ means $g = o(f)$. Is this sufficient but not necessary? $g = o(f)$ means $g/f \to 0$. **Check carefully.**
(C) Equivalence relation ↔ partition (they are equivalent, not just sufficient). **Necessary AND sufficient.** So not an answer.
(D) Well-order enables transfinite induction, which generalizes mathematical induction. **Sufficient but may not be necessary.**
(E) $P$ false makes $P \to Q$ true. Is this sufficient but not necessary? Yes — $P \to Q$ is also true when $Q$ is true. **Sufficient but not necessary** ✓.
- Key concept: distinguishing sufficient vs. necessary conditions in discrete mathematics.

### Problem 6

This problem is worth 5 points (multiple-choice).

(A) **False.** Connected graph with $n$ vertices: longest simple path has at most $n-1$ edges (length $n-1$), not $n$.
(B) **True.** Handshaking lemma: sum of degrees = $2|E|$, always even.
(C) **False.** Connected graph needs at least $n-1$ edges (tree), giving $\sum D(i) = 2(n-1) = 2n-2 < 2n$.
(D) **True.** Pigeonhole: degrees range from 0 to $n-1$, but 0 and $n-1$ can't coexist. So $n$ values in $n-1$ slots → two vertices share a degree.
(E) **True.** Reflexive and transitive closure of a connected graph's adjacency makes all pairs related → complete graph.
- Key concept: graph theory fundamentals — handshaking lemma, pigeonhole on degrees, and closure properties.

### Problem 7

This problem is worth 5 points (multiple-choice).

Procedure P: calls B ($\Theta(\sqrt{n})$), loops through array (3 conditions per element → $\Theta(n)$), creates 3 sub-arrays of size $n/9$ each (elements where $i \bmod 9 = 0, 3, 6$), recursively calls P on each. So $F_n = 3F_{n/9} + \Theta(\sqrt{n}) + \Theta(n) = 3F_{n/9} + \Theta(n)$.
(D) **True.** $F_n = 3F_{n/9} + \Theta(n)$.
- Key concept: recurrence derivation from code analysis — identifying subproblem sizes and non-recursive costs.

### Problem 8

This problem is worth 5 points (multiple-choice).

From Problem 7: $F_n = 3F_{n/9} + \Theta(n)$. Master Theorem: $a=3, b=9, f(n)=n$. $\log_9 3 = 0.5 < 1$. Case 3: $F_n = \Theta(n)$. So $O(n)$ is correct, and $O(n \log n)$ is also valid (but not tight).
(B) **True.** $O(n \log n)$ is a valid upper bound (though $\Theta(n)$ is tighter).
- Key concept: Master Theorem application for complexity bounds.

### Problem 9

This problem is worth 5 points (multiple-choice).

Legal strings avoid consecutive pairs from $\{\alpha_1\alpha_1, \alpha_1\alpha_2, \alpha_2\alpha_1, \alpha_2\alpha_2\}$. Position $n$: if last char is $\alpha_1$ or $\alpha_2$ (2 choices restricted), previous char has restrictions. If last char is $\alpha_3$-$\alpha_8$ (6 choices), no restriction on previous. This leads to a recurrence.
$P_0 = 8$ (single characters), $P_1 = 8 \times 8 - 4 = 60$? Or $P_1 = 64 - 4 = 60$? Check: 8 first chars × 8 second chars − 4 forbidden pairs = 60. But the problem says $P_{n-1}$ is length $n$ strings... **Check indexing carefully.**
- Key concept: constrained string counting via recurrence relations.

### Problem 10

This problem is worth 5 points (multiple-choice).

Convert the recurrence from Problem 9 into generating function form and solve using partial fractions.
- Key concept: generating functions for constrained counting problems.

### Problem 11

This problem is worth 5 points (multiple-choice).

(A) **True.** Spectral theorem: symmetric matrices have complete orthonormal eigenvectors regardless of eigenvalue multiplicity.
(B) **False.** $A+B$ is symmetric ✓, but $AB$ is symmetric only if $AB = BA$, which isn't guaranteed.
(C) **True.** Rank $r < n$ means $n-r$ zero eigenvalues (rank-nullity theorem + symmetric matrix properties).
(D) **True.** Eigenvalues of symmetric matrices are always real.
- Key concept: symmetric matrix properties — spectral theorem, real eigenvalues, and rank-eigenvalue relationship.

### Problem 12

This problem is worth 5 points (multiple-choice).

$A$ is $m \times n$ with $m < n$.
(A) **False.** Rank-nullity: rank + nullity = $n$ (number of columns), not $m$.
(B) **True.** If rank = $m$, then $A$ has $m$ independent rows, so $AA^T$ is $m \times m$ with rank $m$, hence invertible.
(C) **False.** Row rank = column rank always.
(D) **True.** Gaussian elimination produces $m - r$ zero rows.
- Key concept: rank-nullity theorem and properties of wide matrices ($m < n$).

### Problem 13

This problem is worth 5 points (multiple-choice).

(A) **False.** Positive pivots don't guarantee positive eigenvalues — pivots relate to $LDU$ decomposition, not directly to eigenvalues for general matrices.
(B) **False.** Commuting diagonalizable matrices share eigenvectors but not necessarily eigenvalues.
(C) **True.** Similar matrices have the same eigenvalues (same characteristic polynomial).
(D) **True.** $AA^T$ and $A^TA$ have the same nonzero eigenvalues (singular value relationship).
- Key concept: eigenvalue relationships — similarity, commutativity, and $AA^T$ vs. $A^TA$.

### Problem 14

This problem is worth 5 points (single-choice, −2 for wrong).

Compute $A^{-1}$ by row reduction $[A|I] \to [I|A^{-1}]$. Sum all 16 entries of $A^{-1}$, take absolute value, round, and compute mod 5.
- Key concept: matrix inverse via Gauss-Jordan elimination.

### Problem 15

This problem is worth 5 points (single-choice, −2 for wrong).

$A$ is $11 \times 11$ with all entries 1 except diagonal entries 0.9. $A = J - 0.1I$ where $J$ is the all-ones matrix. $J$ has eigenvalues 11 (once) and 0 (10 times). So $A$ has eigenvalues $11-0.1=10.9$ (once) and $0-0.1=-0.1$ (10 times). $\det(A) = 10.9 \times (-0.1)^{10} = 10.9 \times 10^{-10}$. $d \times 10^{11} = 10.9 \times 10 = 109$. $c = |109 + 7| = 116$. $\text{mod}(116,5) = 1$. Answer: **(B)**.
- Key concept: determinant of rank-1 perturbation of identity — using eigenvalue analysis.

### Problem 16

This problem is worth 5 points (single-choice, −2 for wrong).

Change of basis: $B = P^{-1}AP$ where $P = [[1,1,0]^T, [0,1,1]^T, [1,2,2]^T]$. Compute $P^{-1}$, then $B = P^{-1}AP$. Sum all entries, take absolute value, round, and compute mod 5.
- Key concept: change of basis for linear transformations — similarity transformation.

### Problem 17

This problem is worth 5 points (single-choice, −2 for wrong).

Gram-Schmidt on $v_1, v_2, v_3$: $u_1 = v_1 = [1,1,1,1]$. $u_2 = v_2 - \text{proj}_{u_1}v_2$. Compute the projection and subtract. Then $u_3 = v_3 - \text{proj}_{u_1}v_3 - \text{proj}_{u_2}v_3$. Find integer entries $a,b,c,d,e,f$ and compute the answer.
- Key concept: Gram-Schmidt orthogonalization process.

### Problem 18

This problem is worth 5 points (single-choice, −2 for wrong).

Diagonalize $A$: find eigenvalues and eigenvectors. Then $A^{12} = PD^{12}P^{-1}$. Sum all entries of $B = A^{12}$, take absolute value, compute mod 5.
- Key concept: matrix powers via diagonalization — $A^n = PD^nP^{-1}$.

### Problem 19

This problem is worth 5 points (single-choice, −2 for wrong).

Least-squares: $\hat{x} = (A^TA)^{-1}A^Tb$. Compute $A^TA$, $A^Tb$, solve for $\hat{x}$. Sum entries, round, take absolute value, compute mod 5.
- Key concept: least-squares solution via normal equations.

### Problem 20

This problem is worth 5 points (single-choice, −2 for wrong).

$A$ is symmetric, so $P^TAP = D$ with orthogonal $P$. Find eigenvalues of $A$: $\det(A-\lambda I) = 0$. Find orthonormal eigenvectors to form $P$. Compute the product of first row of $P$ and product of diagonal of $D$, then the final answer.
- Key concept: orthogonal diagonalization of symmetric matrices.

## 113

### Problem 1

This problem is worth 5 points (single-choice, −2 for wrong).

$A = LU$ decomposition. Perform Gaussian elimination on $A = \begin{bmatrix}2&4&2\\1&5&2\\4&-1&9\end{bmatrix}$. Row operations give $L$ (lower) and $U$ (upper) factors. Sum all 9 entries $a+b+c+d+e+f+g+h+i$, floor, and compute mod 5.
- Key concept: LU decomposition via Gaussian elimination.

### Problem 2

This problem is worth 5 points (single-choice).

Transition matrix $S$ from basis $[1, x, x^2]$ to $[1, 2x, 4x^2-2]$: express old basis vectors in terms of new basis. $1 = 1 \cdot 1$, $x = \frac{1}{2}(2x)$, $x^2 = \frac{1}{4}(4x^2-2) + \frac{1}{2}(1)$. Form $S$ and count zeros.
- Key concept: change of basis in polynomial spaces — transition matrix construction.

### Problem 3

This problem is worth 5 points (single-choice, −2 for wrong).

From RREF $U$: column 3 = $-1 \times$ col1 + $3 \times$ col2. Apply same relationship to $A$'s columns: col3 of $A$ = $-1 \times [3,2,-1,2]^T + 3 \times [1,1,-2,3]^T = [0,1,-5,7]^T$. Sum = $0+1-5+7 = 3$. $\lfloor 3 \rfloor \% 5 = 3$. Answer: **(d)**.
- Key concept: column relationships are preserved between a matrix and its RREF.

### Problem 4

This problem is worth 5 points (single-choice).

Construct rotation matrices for yaw (45°), pitch (−90°), and roll (−45°). Multiply them in the correct order. Sum all 9 entries, take absolute value, round, compute mod 5.
- Key concept: 3D rotation matrices — yaw, pitch, roll composition.

### Problem 5

This problem is worth 5 points (single-choice).

$A$ is upper triangular with all diagonal entries = 1. Only eigenvalue is 1 (multiplicity 4). The eigenspace dimension = $\dim(\text{Nul}(A-I))$. $A - I = \begin{bmatrix}0&2&3&4\\0&0&2&3\\0&0&0&2\\0&0&0&0\end{bmatrix}$, rank = 3. Eigenspace dimension = $4 - 3 = 1$. Answer: **(b)**.
- Key concept: eigenspace dimension — nullity of $A - \lambda I$ for repeated eigenvalues.

### Problem 6

This problem is worth 5 points (single-choice).

Find eigenvalues of $A$. Characteristic polynomial: $\lambda^3 - 20\lambda^2 + 92\lambda - 120 = (\lambda-4)(\lambda-6)(\lambda-10)=0$? Check: trace = 20 ✓, sum of 2×2 minors = 92? $30+42+27-9-18-6 = 66$? Verify. Then $B = A^3 - 20A^2 + 92A - 120I$. By Cayley-Hamilton, if the characteristic polynomial is $p(\lambda) = \lambda^3 - 20\lambda^2 + 92\lambda - 120$, then $p(A) = 0$, so $B = 0$ and $\det(B) = 0$. $|0| \% 5 = 0$. Answer: **(a)**.
- Key concept: Cayley-Hamilton theorem — a matrix satisfies its own characteristic polynomial.

### Problem 7

This problem is worth 5 points (single-choice).

Gram-Schmidt on $v_1, v_2, v_3$ in $\mathbb{R}^4$. First $t_1$ is given. Compute $t_2$ and $t_3$ (orthonormalized). Extract values $n$ and $s$, compute $1/(n^2 s^2)$, round, mod 5.
- Key concept: Gram-Schmidt orthonormalization in $\mathbb{R}^4$.

### Problem 8

This problem is worth 5 points (single-choice).

Decompose $w = v + u$ where $v \in U^\perp$ and $u \in U$. $u = \text{proj}_U w = (w \cdot t_1)t_1 + (w \cdot t_2)t_2 + (w \cdot t_3)t_3$. Then $v = w - u$. Compute $c^2 + g^2$, round, mod 5.
- Key concept: orthogonal decomposition — projection onto subspace and its complement.

### Problem 9

This problem is worth 5 points (single-choice).

Diagonalize $A$: find eigenvalues from $\det(A - \lambda I) = 0$. Find eigenvectors to form $P$. Arrange so $a \le b \le c$. Sum all entries, take absolute value, round, mod 5.
- Key concept: matrix diagonalization — eigenvalue/eigenvector computation and arrangement.

### Problem 10

This problem is worth 5 points (single-choice).

Least-squares fit of parabola $C + Dt + Et^2$ to data points. Set up the system $Ax = v$ where $A$ has columns $[1, t, t^2]$ evaluated at $t = 0,1,3,4$. Solve normal equations $(A^TA)\hat{x} = A^Tv$. Compute $|C+D+E| \times 256$, round, mod 5.
- Key concept: polynomial least-squares fitting via normal equations.

### Problem 11

This problem is worth 5 points (multiple-choice).

For each nonplanar graph, check if removing any single vertex (and its incident edges) produces a planar graph.
(A) $K_5$: removing a vertex gives $K_4$, which is planar. **True.**
(B) $K_6$: removing a vertex gives $K_5$, which is nonplanar. **False.**
(C) $K_{3,3}$: removing a vertex gives $K_{2,3}$ or $K_{3,2}$, which is planar. **True.**
(D) $K_{3,4}$: removing from the 4-side gives $K_{3,3}$, nonplanar. **False.**
(E) $K_{4,4}$: removing a vertex gives $K_{3,4}$ or $K_{4,3}$, check planarity. $K_{3,4}$ has $e=12, v=7$, $3v-6=15 \ge 12$ — need to check more carefully. **Check if $K_{3,4}$ is planar.**
- Key concept: planarity testing — vertex removal and Kuratowski/Wagner characterization.

### Problem 12

This problem is worth 5 points (multiple-choice).

Knight's move graph on 8×8 chessboard: (a) Bipartite: knight alternates square colors → **True.** (b) Maximum degree: center squares have 8 moves, not 10. **False.** (c) Minimum degree: corner squares have 2 moves. **False** (minimum is 2, not 4). (d) Four vertices of degree 2: the 4 corners. **True.** (e) Eight vertices of degree 3: check squares adjacent to corners.
- Key concept: knight's graph on chessboard — degree analysis and bipartiteness.

### Problem 13

This problem is worth 5 points (multiple-choice).

Use Erdős–Gallai theorem or Hakimi's algorithm to check if each sequence is graphic (realizable as degree sequence of a simple graph). Sum of degrees must be even. Apply degree-elimination algorithm.
- Key concept: graphic sequences — Erdős–Gallai theorem for realizability.

### Problem 14

This problem is worth 5 points (multiple-choice).

From the graph description: left quadrilateral $a$-$b$-$c$-$d$ connected to right quadrilateral $e$-$f$-$g$-$h$ via edge $c$-$e$. Cut vertices: removing $c$ or $e$ disconnects the graph. Cut edges: edge $\{c,e\}$ is a bridge.
(c) **True** — $\{c,e\}$ is the only cut edge.
- Key concept: cut vertices and bridges — identifying critical connectivity points in graphs.

### Problem 15

This problem is worth 5 points (multiple-choice).

(a) **True.** Hamiltonian path between any two vertices → Hamiltonian cycle exists (connect endpoints).
(b) **False.** Having a Hamiltonian path from every vertex doesn't guarantee a cycle.
(c) **False.** Eulerian trail between any two vertices doesn't imply Eulerian circuit (need all vertices even degree).
(d) **True.** $K_{m,n}$ is Hamiltonian iff $m = n \ge 2$.
(e) **False.** $K_{m,n}$ is Eulerian iff $m$ and $n$ are both even (all vertices have even degree).
- Key concept: Hamiltonian and Eulerian properties of complete bipartite graphs.

### Problem 16

This problem is worth 5 points (multiple-choice).

$R(P,Q)$: "$P \to Q$ is true." (a) Reflexive: $P \to P$ is always true ✓. **True.** (b) Anti-symmetric: $P \to Q$ true and $Q \to P$ true implies $P = Q$? No — both can be true (e.g., both true propositions). **False.** (c) Transitive: if $P \to Q$ and $Q \to R$ are both true, then $P \to R$ is true ✓. **True.** (d) Not a partial order (fails anti-symmetry). **False.** (e) Transitive closure of transitive relation is itself ✓. **True.**
- Key concept: properties of the implication relation — reflexive and transitive but not anti-symmetric.

### Problem 17

This problem is worth 5 points (multiple-choice).

$|S|=3$, $f: S \times S \to S$. $|S \times S| = 9$. (a) $3^6$? No, domain has 9 elements. **False.** (b) $3^9$ total functions. **True.** (c) 1-to-1: impossible since $|S \times S| = 9 > 3 = |S|$. Count = 0. **False.** (d) Onto: inclusion-exclusion on 9-element domain, 3-element codomain. $3^9 - \binom{3}{1}2^9 + \binom{3}{2}1^9 = 3^9 - 3 \cdot 2^9 + 3$. **True.** (e) Bijection: impossible since $|S \times S| \ne |S|$. **False.**
- Key concept: counting functions from $S \times S$ to $S$ — total, injective, surjective.

### Problem 18

This problem is worth 5 points (multiple-choice).

Josephus problem recurrence: $J(2n) = 2J(n) - 1$ and $J(2n+1) = 2J(n) + 1$.
(b) **True.** $J(2n) = 2J(n) - 1$.
(e) **True.** $J(2n+1) = 2J(n) + 1$.
- Key concept: Josephus problem recurrence — separate cases for even and odd $n$.

### Problem 19

This problem is worth 5 points (multiple-choice).

(a) **False.** If numbers are already factorized, GCD is simply the product of common factors — no need for Euclidean algorithm. Euclidean is best when factors are unknown.
(b) **False.** Finding minimum-weight Hamiltonian path is NP-hard, not uncomputable. It's decidable but computationally hard.
(c) **True.** A lattice is a poset where every pair has a join and meet.
(d) **True.** Bipartite graphs can represent functions (domain → codomain).
(e) **True.** AND, OR, NOT form a functionally complete set.
- Key concept: discrete math fundamentals — computability vs. complexity, lattice definition, functional completeness.

### Problem 20

This problem is worth 5 points (multiple-choice).

$2a_n = 7a_{n-1} - 6a_{n-2}$. Characteristic equation: $2x^2 - 7x + 6 = 0$, roots $x = 2, 3/2$. General solution: $a_n = \alpha \cdot 2^n + \beta \cdot (3/2)^n$. From $a_0 = 3/2$: $\alpha + \beta = 3/2$. From $a_1 = 11/4$: $2\alpha + 3\beta/2 = 11/4$. Solving: $\alpha = 1, \beta = 1/2$. So $a_n = 2^n + (1/2)(3/2)^n$.
(a) **True.** Characteristic polynomial: $2x^2 - 7x + 6 = 0$ → $6x^2 - 7x + 2 = 0$ after dividing by $x^2$ and substituting $1/x$... Actually the characteristic equation for $2a_n - 7a_{n-1} + 6a_{n-2} = 0$ is $2r^2 - 7r + 6 = 0$. Multiplying by a constant: $6x^2 - 7x + 2 = 0$ is different. **Check carefully.**
(c) **True.** Verify generating function form.
(e) **True.** $a_n = (1/2)^{n+1} + 2^n = (1/2) \cdot (1/2)^n + 2^n$. Check: $(1/2)(3/2)^n \ne (1/2)^{n+1}$. **Verify.**
- Key concept: solving recurrences with non-unit leading coefficient — characteristic equation and generating functions.
