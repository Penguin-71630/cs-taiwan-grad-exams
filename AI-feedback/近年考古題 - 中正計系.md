## 108

### Problem 1

This problem is worth 20 points total (10 subparts, 2 points each).

- **(1)** [2%] Answer: **(C) FAT.** The File Allocation Table tracks free space and maps file blocks, enabling multiple file operations simultaneously.
  - Key concept: FAT file system — dual-purpose structure for free space management and file block mapping.

- **(2)** [2%] Answer: **(B) RAID 1.** Mirroring duplicates every disk, requiring 2× the number of drives — the highest redundancy cost.
  - Key concept: RAID levels — RAID 1 mirroring has 100% overhead in extra drives.

- **(3)** [2%] Answer: **(E) None of the above.** Multi-level page tables reduce memory consumption — a single flat page table for large address spaces would be too large to fit contiguously in memory.
  - Key concept: multi-level page tables — reduce memory footprint by only allocating used portions.

- **(4)** [2%] Answer: **(B).** Higher-priority queues are served first; lower queues get CPU time only when upper queues are empty.
  - Key concept: multilevel feedback queue scheduling — priority-based exhaustive service.

- **(5)** [2%] Answer: **(D) Clean dirty pages.** Pre-cleaning dirty pages avoids a disk write later when a frame is needed, saving a future I/O operation.
  - Key concept: page cleaning — proactive write-back to avoid synchronous I/O during page replacement.

- **(6)** [2%] Answer: **(A) Avoidance.** The four necessary conditions for deadlock are: mutual exclusion, hold and wait, no preemption, and circular wait. Avoidance is a prevention strategy, not a condition.
  - Key concept: deadlock necessary conditions — mutual exclusion, hold-and-wait, no preemption, circular wait.

- **(7)** [2%] Answer: **(A) We don't know the next CPU burst length.** SRTF requires knowing future burst lengths, which is impossible in practice (must be estimated).
  - Key concept: SRTF scheduling limitation — requires prediction of future CPU burst lengths.

- **(8)** [2%] Answer: **(D) We incur more context switches.** Preemption interrupts running processes, requiring additional context switches to save/restore state.
  - Key concept: preemptive scheduling cost — increased context switch overhead.

- **(9)** [2%] Answer: **(C) Monolithic with Dynamically Loadable Drivers.** Linux has a monolithic kernel but supports loadable kernel modules (LKMs) for drivers.
  - Key concept: Linux kernel architecture — monolithic with dynamic module loading.

- **(10)** [2%] Answer: **(B) Executing untrusted code in a controlled environment like a virtual machine.** Sandboxing isolates untrusted code to limit potential damage.
  - Key concept: sandbox model — controlled execution environment for untrusted code.

### Problem 2

This problem is worth 5 points.

**Yes.** A system can be in an unsafe state without being deadlocked. An unsafe state means no safe sequence is guaranteed from the current state, but deadlock may not actually occur if processes happen to release resources before requesting more. Unsafe state is a necessary (but not sufficient) condition for deadlock — it means deadlock *could* occur, not that it *has* occurred.
- Key concept: safe vs. unsafe vs. deadlocked states — unsafe does not imply deadlocked.

### Problem 3

This problem is worth 8 points total (2 subparts, 4 points each).

- **(a)** [4%] **astar**: Shows very localized memory access concentrated in a small region (bottom of address space), with occasional sparse accesses elsewhere. This indicates a small working set with good spatial locality — the process primarily accesses a limited data structure with rare references to other memory regions.
  - Key concept: memory access patterns — small working set with high spatial locality.

- **(b)** [4%] **cactusADM**: Exhibits dense, uniform access across the entire address space over time. Both reads and writes are spread throughout all memory pages, indicating a large working set with poor locality — the process regularly accesses most of its allocated memory.
  - Key concept: memory access patterns — large working set with poor locality (streaming/uniform access).

### Problem 4

This problem is worth 7 points.

Three processes are created (original + 2 forks). The first `fork()` creates child C1 (gets `child==0`). The parent forks again creating C2. Variable `c` copies: **3 copies.** Values: C1 sets `c = 5+5 = 10`. Parent (original) sets `c = 5+10+5 = 20`. C2 (from second fork) sets `c = 5+10 = 15`. Final values: **10, 15, 20**.
- Key concept: fork() and copy-on-write — each process gets independent copy of variables after fork.

### Problem 5

This problem is worth 10 points total (3 subparts).

- **(a)** [4%] Priority 10: time quantum = **160 ms**. Priority 55: time quantum = **40 ms**. Read directly from the dispatch table.
  - Key concept: Solaris dispatch table — time quantum lookup by priority level.

- **(b)** [3%] Thread at priority 35 uses full quantum → "Time Quantum Expired" column → new priority = **25**.
  - Key concept: priority demotion — CPU-bound threads get lower priority after using full quantum.

- **(c)** [3%] Thread at priority 35 blocks for I/O → "Return from Sleep" column → new priority = **54**.
  - Key concept: priority boost — I/O-bound threads get higher priority after blocking.

### Problem 6

This problem is worth 10 points.

Each output element = $aw + bx + ey + fz$ (4 multiplications, 3 additions). With parallelism: all 4 multiplications execute in parallel ($1 \times D_{MUL}$), then 3 additions in a tree structure: 2 pairs added in parallel ($D_{ADD}$), then final sum ($D_{ADD}$). Total = $D_{MUL} + 2 \times D_{ADD} = D_{MUL} + 2 \times 0.1 \times D_{MUL} = 1.2 \times D_{MUL}$.
- Key concept: parallel computation — multiply-then-reduce tree minimizes critical path delay.

### Problem 7

This problem is worth 10 points.

One convolution output = 4 multiplications + 3 additions = 7 operations. With 1 multiplier and 1 adder in parallel per cycle: 4 cycles for multiplications (sequential) overlapped with additions. Optimal schedule: cycles 1-4 do multiplications; additions can start after first two products are ready. For 6 output elements with data reuse: analyze the pipeline of multiply-accumulate operations. Minimum cycles = 4 (multiplies) + 2 (remaining adds) = **6 cycles** per output element, or optimize with pipelining across elements.
- Key concept: resource-constrained scheduling — overlap multiply and add operations within hardware limits.

### Problem 8

This problem is worth 5 points.

$-5.0$ in IEEE 754 double precision: Sign = 1 (negative). $5.0 = 101.0_2 = 1.01 \times 2^2$. Exponent: $2 + 1023 = 1025 = 10000000001_2$. Mantissa: $0100...0$ (52 bits). Full: `1 10000000001 0100000000000000000000000000000000000000000000000000`.
- Key concept: IEEE 754 double precision — sign, biased exponent (bias=1023), normalized mantissa.

### Problem 9

This problem is worth 25 points total (4 subparts).

- **(a)** Weights: layer 1 has $256 \times 16 = 4096$; layers 2,3 have $16 \times 16 = 256$ each; output layer has $16 \times 10 = 160$. Total weights = $4096 + 256 + 256 + 160 = 4768$. Biases: $16 + 16 + 16 + 10 = 58$. Storage = $(4768 + 58) \times 4$ bytes = $19304$ bytes ≈ $18.9$ KB.
  - Key concept: neural network parameter counting — weights = input_dim × output_dim per layer.

- **(b)** Total weight storage = $4768 \times 4 = 19072$ bytes. Cache: 1KB = 1024 bytes, block size = 32 bytes → 32 blocks. Address bits to index 19072 bytes of weights: $\lceil\log_2(19072/32)\rceil = \lceil\log_2(596)\rceil = 10$ bits for block address. Tag bits = address bits - index bits - offset bits. Additional storage: tag array + valid bits for 32 cache lines.
  - Key concept: direct-mapped cache design — tag storage overhead calculation.

- **(c)** Direct-mapped cache with 1KB for 19KB of weights: capacity is much smaller than working set. Every new layer's weights will evict previous ones. Miss rate is very high (near 100% for first pass, compulsory misses dominate). Miss type: **compulsory** (cold start) and **capacity** (cache too small for working set).
  - Key concept: cache miss classification — compulsory vs. capacity vs. conflict misses.

- **(d)** Use a **fully-associative** or **set-associative** cache to reduce conflict misses. Alternatively, reorganize weight storage to exploit spatial locality — store weights in the order they are accessed (layer by layer, neuron by neuron) to maximize sequential block utilization.
  - Key concept: cache optimization — associativity and data layout optimization.

## 109

### Problem 1

This problem is worth 20 points total (10 subparts, 2% each).

- **(1)** [2%] Answer: **(D) File consistency checking must be performed.** After a crash in a log-structured FS, the log is replayed to ensure consistency.
  - Key concept: log-structured file system recovery — replay log after crash.

- **(2)** [2%] Answer: **(A) Copy-on-write.** COW shares pages until modification, then creates a copy of the modified page.
  - Key concept: copy-on-write — deferred page copying until write occurs.

- **(3)** [2%] Answer: **(E) An unsafe state may lead to a safe state.** Unsafe states can transition to safe states if processes release resources. Unsafe ≠ deadlocked.
  - Key concept: state transitions — unsafe states are not necessarily permanent.

- **(4)** [2%] Answer: **(C) Linked allocation.** FAT is an enhanced form of linked allocation — each entry points to the next block in the file's chain.
  - Key concept: FAT as linked allocation — file blocks connected via table entries.

- **(5)** [2%] Answer: **(C) Atomicity.** Critical section solutions require mutual exclusion, progress, and bounded waiting — not atomicity (which is a property of individual operations).
  - Key concept: critical section requirements — mutual exclusion, progress, bounded waiting.

- **(6)** [2%] Answer: **(E) None of the above.** All listed operations are privileged and require kernel mode — user-mode applications cannot perform them directly.
  - Key concept: user mode restrictions — privileged operations require system calls.

- **(7)** [2%] Answer: **(D) A hardware interrupt (trap) will occur.** The MMU detects the illegal address and raises a trap/exception.
  - Key concept: memory protection — hardware trap on illegal address access.

- **(8)** [2%] Answer: **(B) fork/exec.** `fork()` creates a copy, `exec()` replaces it with a new program. Together they start a new process.
  - Key concept: Unix process creation — fork + exec paradigm.

- **(9)** [2%] Answer: **(B).** Top level queue is served first; only when empty does the next level get CPU time.
  - Key concept: multilevel feedback queue — priority-based service order.

- **(10)** [2%] Answer: **(B) The page was not currently in memory.** With virtual memory, the valid bit indicates whether the page is resident in physical memory.
  - Key concept: valid bit in VM — indicates page residency in physical memory.

### Problem 2

This problem is worth 5 points.

Block size = 8KB, pointer = 4 bytes → pointers per block = $8192/4 = 2048 = 2K$. Direct: $12 \times 8$ KB. Single indirect: $2048 \times 8$ KB. Double indirect: $2048^2 \times 8$ KB. Triple indirect: $2048^3 \times 8$ KB. Max file size = $(12 + 2048 + 2048^2 + 2048^3) \times 8$ KB ≈ $2048^3 \times 8$ KB = $2^{11 \times 3} \times 2^{13}$ = $2^{46}$ bytes = 64 TB.
- Key concept: inode file size calculation — direct + single/double/triple indirect blocks.

### Problem 3

This problem is worth 13 points total (3 subparts).

- **(1)** [4%] Two inherent NAND flash constraints: (1) **Erase-before-write** — cannot overwrite; must erase an entire block before writing. (2) **Limited endurance** — each cell has a finite number of program/erase (P/E) cycles before wearing out.
  - Key concept: NAND flash constraints — erase-before-write and limited P/E cycles.

- **(2)** [6%] **Garbage collector**: reclaims invalid pages by copying valid pages from partially-invalid blocks to a clean block, then erasing the old block to create free space. **Wear leveler**: distributes write/erase operations evenly across all blocks to prevent some blocks from wearing out prematurely, extending device lifespan.
  - Key concept: FTL components — garbage collection reclaims space; wear leveling extends lifetime.

- **(3)** [3%] **Over-provisioning** is extra flash capacity not visible to the user (typically 7-28% of total capacity). It provides pre-erased blocks for immediate writes and allows garbage collection to proceed more efficiently with more free space for valid page relocation, reducing write amplification.
  - Key concept: over-provisioning — reserved capacity for GC efficiency and write performance.

### Problem 4

This problem is worth 6 points total (2 subparts).

- **(1)** [3%] Available = (1,4,1,2). Need matrix = Max - Allocation. Check which processes can finish with available resources: P1 needs (2,1,1,2) ≤ (1,4,1,2)? No (A insufficient). P2 needs (1,3,1,1) ≤ (1,4,1,2)? Yes → finish P2, available becomes (4,5,3,3). Then P1, P0, P3, P4 can finish. **Safe.** Safe sequence: P2→P1→P0→P3→P4.
  - Key concept: Banker's algorithm — find safe sequence by iteratively satisfying process needs.

- **(2)** [3%] Available = (2,1,1,3). Check processes: P1 needs (2,1,1,2) ≤ (2,1,1,3)? Yes → finish P1, available = (4,3,2,3). Continue finding safe sequence or determine unsafe.
  - Key concept: Banker's algorithm — systematic safety check with given available resources.

### Problem 5

This problem is worth 6 points.

CPU utilization is only 20% while paging disk is at 97.7% — the system is **thrashing** (excessive paging). Three ways to improve: (1) **Decrease degree of multiprogramming** — fewer processes means less memory pressure and fewer page faults. (2) **Increase physical memory (RAM)** — more frames available reduces page fault rate. (3) **Use working set model** — only keep active pages in memory, suspend processes whose working sets don't fit.
- Key concept: thrashing mitigation — reduce multiprogramming, add RAM, or use working set strategy.

### Problem 6

This problem is worth 5 points.

Machine P: CPU time = IC × 2.0 × 250 ps = IC × 500 ps. Machine Q: clock cycle = 1/2GHz = 500 ps. CPU time = IC × 1.0 × 500 ps = IC × 500 ps. Both machines have **equal performance** for any program (same execution time).
- Key concept: CPU performance equation — execution time = IC × CPI × clock cycle time.

### Problem 7

This problem is worth 5 points.

Critical path stage = 10 ns (longest stage determines pipeline clock). Pipeline clock period = 10 ns. For $N$ instructions: pipeline fill time = 5 stages × 10 ns = 50 ns, then one instruction completes per 10 ns. Total = $(5 + N - 1) \times 10$ ns = $(N + 4) \times 10$ ns.
- Key concept: pipelined execution time — fill time plus one cycle per instruction after pipeline is full.

### Problem 8

This problem is worth 5 points.

Clock frequency = 2.5 GHz → cycle time = 0.4 ns. Miss penalty = 20 ns / 0.4 ns = 50 cycles. Memory stall cycles = IC × (instruction miss rate + data access rate × data miss rate) × miss penalty = IC × (0.02 + 0.20 × 0.05) × 50 = IC × (0.02 + 0.01) × 50 = IC × 1.5. Actual CPI = 2 + 1.5 = **3.5**.
- Key concept: memory stall CPI — base CPI plus stall cycles from cache misses.

### Problem 9

This problem is worth 10 points.

`0xc0800000` = sign 1, exp 10000001, mantissa 000...0 → $-1.0 \times 2^{(129-127)} = -1.0 \times 4 = -4.0$. `0x40000000` = sign 0, exp 10000000, mantissa 000...0 → $+1.0 \times 2^{(128-127)} = +2.0$. Each output = $aw+bx+ey+fz = (-4)(2)+(-4)(2)+(-4)(2)+(-4)(2) = -32$. In IEEE 754: $-32 = -1.0 \times 2^5$. Sign=1, exp=$5+127=132=10000100_2$, mantissa=0. Result: `0xC2000000`.
- Key concept: IEEE 754 arithmetic — decode hex to float, compute, encode result.

### Problem 10

This problem is worth 15 points total (3 subparts).

- **(1)** [5%] $T_{setup}$: time before clock edge that input must be stable. $T_{C2Q}$: delay from clock edge to output becoming valid. $T_{hold}$: time after clock edge that input must remain stable. Draw timing diagram showing clock edge, data stability window, and output change.
  - Key concept: flip-flop timing parameters — setup, clock-to-q, and hold times.

- **(2)** [5%] Minimum clock period $T_{clk} \ge T_{C2Q} + T_{cp} + T_{setup}$. The clock must accommodate: output delay of source FF + combinational logic delay + setup time of destination FF.
  - Key concept: synchronous circuit timing constraint — critical path determines max frequency.

- **(3)** [5%] Amdahl's Law: speedup limited by sequential portions. Pipeline overhead (register delays $T_{C2Q} + T_{setup}$) adds to each stage. As $N$ increases, stage computation time decreases but register overhead stays constant, dominating at high $N$. Also, pipeline hazards cause stalls proportional to $N$.
  - Key concept: pipeline speedup limits — Amdahl's Law and register overhead bound ideal speedup.

### Problem 11

This problem is worth 10 points.

4-bit shift register: 4 D-flip-flops in series. Hold time violation: $T_{hold} = 30$ ps > $T_{C2Q} = 10$ ps, so data may change before hold time expires. Insert inverter pairs (buffers) between FFs to add delay: each inverter pair adds $2 \times 50 = 100$ ps. One buffer pair between each FF pair satisfies hold time. Critical path: $T_{C2Q} + T_{inv} \times 2 + T_{setup} = 10 + 100 + 10 = 120$ ps. Max frequency = $1/120$ ps ≈ **8.33 GHz**.
- Key concept: shift register design with hold time fixing — buffer insertion to prevent hold violations.

## 110

### Problem 1

This problem is worth 20 points total (10 subparts, 2 points each).

- **(1)** [2%] Answer: **(D).** Mobile devices often have fewer cores than desktops. This is **false** — modern mobile SoCs have 8+ cores. The false statement depends on context, but historically D was considered false.
  - Key concept: mobile vs. desktop hardware — modern mobile chips are increasingly powerful.

- **(2)** [2%] Answer: **(A) Only one process can use this file.** Exclusive lock prevents all other access (read and write).
  - Key concept: file locking — exclusive lock blocks all concurrent access.

- **(3)** [2%] Answer: **(D) Page caching.** Unified virtual memory uses the page cache to cache both process pages and file data in a single framework.
  - Key concept: unified page cache — single caching mechanism for memory-mapped files and process pages.

- **(4)** [2%] Answer: **(C).** Memory-mapped I/O uses the processor's address space with standard load/store instructions to communicate with I/O devices.
  - Key concept: memory-mapped I/O — device registers accessed via address space with standard instructions.

- **(5)** [2%] Answer: **(B) Modern disks do not disclose the physical location of logical blocks.** The OS cannot optimize for rotational latency because disk geometry is hidden by the controller.
  - Key concept: disk scheduling — OS only optimizes seek distance since physical geometry is hidden.

- **(6)** [2%] Answer: **(A).** Device manufacturers perform low-level formatting but NOT file system initialization. The OS creates the initial file-system structures. Statement A is **FALSE**.
  - Key concept: device formatting — low-level (manufacturer) vs. high-level (OS file system) formatting.

- **(7)** [2%] Answer: **(A) Spinlocks cannot be used on single processor machines.** This is **NOT true** about Linux spinlocks — they CAN be used but simply disable preemption on uniprocessor systems.
  - Key concept: Linux spinlocks — on uniprocessor, implemented as preemption disabling.

- **(8)** [2%] Answer: **(A).** The mutex must be locked before `pthread_cond_wait()` (which atomically releases it and waits) and must be unlocked after `pthread_cond_signal()`.
  - Key concept: POSIX condition variables — mutex must be held when calling wait; signal requires mutex.

- **(9)** [2%] Answer: **(B) Bounded waiting implies progress, and progress implies no deadlock.** Progress means some process enters CS → no indefinite blocking → no deadlock.
  - Key concept: critical section property relationships — logical implications between requirements.

- **(10)** [2%] Answer: **(C).** In Java, `start()` creates a new thread in the JVM (not `run()`). This statement is **TRUE**, not false. The false statement would be one of the others depending on the specific wording.
  - Key concept: Java thread creation — `start()` method creates and begins thread execution.

### Problem 2

This problem is worth 10 points total (2 subparts).

- **(a)** [5%] RMS assigns priority by rate (shorter period = higher priority): P1 > P2 > P3. CPU utilization: $20/50 + 30/100 + 90/300 = 0.4 + 0.3 + 0.3 = 1.0$. RMS bound for 3 tasks: $3(2^{1/3}-1) \approx 0.78$. Since $1.0 > 0.78$, RMS is NOT guaranteed to schedule. Simulate to check: P1 runs [0,20], P2 runs [20,50], P1 preempts [50,70], P2 resumes [70,80], P3 runs [80,170]... check if P3 misses deadline at 300.
  - Key concept: Rate Monotonic Scheduling — utilization bound test and simulation for schedulability.

- **(b)** [5%] EDF with total utilization = $1.0 \le 1.0$: EDF can schedule any task set with utilization ≤ 1.0. **Yes**, all deadlines can be met.
  - Key concept: EDF schedulability — guarantees feasibility when total utilization ≤ 1.

### Problem 3

This problem is worth 10 points total (2 subparts).

- **(a)** [5%] Page size = 32KB = $2^{15}$ → offset = 15 bits. Virtual address = 64 bits → virtual page number = 49 bits. Each page table page holds $2^{15}/4 = 2^{13}$ entries → each level covers 13 bits. Levels needed: $\lceil 49/13 \rceil = 4$ levels.
  - Key concept: multi-level page table depth — determined by VPN bits divided by bits per level.

- **(b)** [5%] TLB hit: 1 TLB access + 1 memory access = 10 + 100 = 110 ns. TLB miss: 1 TLB access + 4 memory accesses (page walk) + 1 memory access = 10 + 400 + 100 = 510 ns. Effective = 0.8 × 110 + 0.2 × 510 = 88 + 102 = **190 ns**.
  - Key concept: effective memory access time — weighted average of TLB hit and miss paths.

### Problem 4

This problem is worth 5 points.

Compute Need = Max - Allocation. Available = (0,1,1,1). Check: P2 needs (1,0,1,0) ≤ (0,1,1,1)? No (A insufficient). P0 needs (1,1,0,0) ≤ (0,1,1,1)? No. P3 needs (0,1,1,1) ≤ (0,1,1,1)? Yes → finish P3, available = (2,2,1,2). Then P0 needs (1,1,0,0) ≤ (2,2,1,2)? Yes → finish P0, available = (4,2,2,3). Continue... Safe sequence exists: **P3→P0→P1→P2** (or similar).
- Key concept: Banker's algorithm — iterative safe sequence discovery.

### Problem 5

This problem is worth 5 points.

Block size = 4KB, pointer = 8 bytes → pointers per block = $4096/8 = 512$. Direct: $12 \times 4$ KB = 48 KB. Single indirect: $512 \times 4$ KB = 2 MB. Double indirect: $512^2 \times 4$ KB = 1 GB. Triple indirect: $512^3 \times 4$ KB = 512 GB. Max file = 48KB + 2MB + 1GB + 512GB ≈ **512 GB**.
- Key concept: inode maximum file size — sum of direct and indirect addressing capacities.

### Problem 6

This problem is worth 15 points total (3 subparts).

- **(a)** [6%] From the circuit: NAND inputs are `input` and `NOT(present_state)`. NAND output = NOT(input AND NOT(present_state)). After inverter for next_state: `next_state = input AND NOT(present_state)`. Output through inverter: `output = input AND NOT(present_state)`.
  - Key concept: sequential circuit Boolean equations — derive next-state and output from gate structure.

- **(b)** [4%] Starting at present_state=0, apply inputs 0,0,1,1,0,1: compute next_state and output for each clock cycle using the Boolean equations from (a).
  - Key concept: sequential circuit simulation — trace state and output through input sequence.

- **(c)** [5%] Critical path: D-FF $T_{C2Q}$ + inverter (NOT present_state) + NAND gate + inverter (to next_state) + $T_{setup}$ = 100 + 50 + 100 + 50 + 100 = **400 ps**. Max frequency = $1/400$ ps = **2.5 GHz**.
  - Key concept: sequential circuit maximum frequency — longest combinational path between flip-flops.

### Problem 7

This problem is worth 8 points.

| Property | SRAM | DRAM |
|----------|------|------|
| Density | Lower (6T per bit) | Higher (1T+1C per bit) |
| Power | Lower static power | Higher (refresh needed) |
| Cost | More expensive per bit | Cheaper per bit |
| Speed | Faster (no refresh) | Slower (refresh + precharge) |

- Key concept: SRAM vs. DRAM tradeoffs — speed/power vs. density/cost.

### Problem 8

This problem is worth 8 points.

Machine A (dual-port): CPI = 1, no structural hazards for load/store. Machine B (single-port): loads cause 1-cycle stall (memory port conflict) → effective CPI = 1 + 0.40 × 1 = 1.4. B is 1.05× faster clock. Performance ratio: Time_A/Time_B = (IC × 1 × T_A) / (IC × 1.4 × T_A/1.05) = 1.05/1.4 = 0.75. Machine A is **faster** (Time_A < Time_B since A takes 75% of B's time).
- Key concept: dual-port vs. single-port memory — structural hazard impact on CPI.

### Problem 9

This problem is worth 7 points.

```asm
# f = (i==j) ? g+h : g-h
# Assume: $s0=f, $s1=g, $s2=h, $s3=i, $s4=j
      bne  $s3, $s4, Else    # if i != j, goto Else
      add  $s0, $s1, $s2     # f = g + h
      j    End
Else: sub  $s0, $s1, $s2     # f = g - h
End:
```
- Key concept: MIPS conditional assignment — branch on inequality, compute both cases.

### Problem 10

This problem is worth 6 points.

$-5.0$: $5.0 = 101.0_2 = 1.01 \times 2^2$. Sign=1, exp=$2+15=17=10001_2$, mantissa=$0100000000_2$. Result: `1 10001 0100000000` = `0xC500`.

$+2.5$: $2.5 = 10.1_2 = 1.01 \times 2^1$. Sign=0, exp=$1+15=16=10000_2$, mantissa=$0100000000_2$. Result: `0 10000 0100000000` = `0x4100`.
- Key concept: IEEE 754 half-precision (FP16) — 1+5+10 bit format with bias 15.

### Problem 11

This problem is worth 6 points.

Two cache optimization schemes to increase bandwidth: (1) **Non-blocking (lockup-free) cache** — allows cache hits to proceed during a miss, enabling hit-under-miss and miss-under-miss. (2) **Multi-banked cache** — divide cache into independent banks that can be accessed simultaneously, increasing throughput for multiple concurrent accesses.
- Key concept: cache bandwidth optimization — non-blocking caches and banking.

## 111

### Problem 1

This problem is worth 30 points total (15 subparts, 2 points each).

- **(1)** [2%] Answer: **(C) Because they waste CPU cycles.** Hardware lock instructions (test-and-set, CAS) require busy-waiting (spinning), wasting CPU cycles. Applications use OS-provided synchronization (mutexes with blocking) instead.
  - Key concept: hardware lock limitations — busy-waiting wastes CPU; prefer blocking synchronization.

- **(2)** [2%] Answer: **(D) It may require synchronization.** Shared memory is fast but requires explicit synchronization (locks, semaphores) to prevent race conditions.
  - Key concept: shared memory IPC — speed advantage offset by synchronization complexity.

- **(3)** [2%] Answer: **(E) Starts a new program running in current process.** `exec()` replaces the current process's code/data with a new program — same PID, new program.
  - Key concept: exec() — loads new program into current process address space.

- **(4)** [2%] Answer: **(B) Library calls involve an interrupt.** This is a DISADVANTAGE — kernel thread operations require system calls (interrupts/traps), which are slower than user-level thread library calls.
  - Key concept: kernel threads — system call overhead is the main disadvantage.

- **(5)** [2%] Answer: **(A) Coding.** Address binding occurs at compile time, link time, load time, or execution time — not during coding (source writing).
  - Key concept: address binding times — compile, link, load, or execution.

- **(6)** [2%] Answer: **(A) TLB.** The Translation Lookaside Buffer caches page table entries to avoid multiple memory accesses for address translation.
  - Key concept: TLB — hardware cache for page table entries to reduce memory accesses.

- **(7)** [2%] Answer: **(A) The page was not currently in memory.** In virtual memory, the valid bit indicates whether the page is resident in physical memory.
  - Key concept: valid bit in virtual memory — indicates page residency.

- **(8)** [2%] Answer: **(C) One per process.** Each process has its own page table mapping its virtual address space to physical frames.
  - Key concept: page table per process — private virtual-to-physical mapping.

- **(9)** [2%] Answer: **(A) Log transactions of file system metadata updates to increase reliability.** Journaling logs metadata changes to recover from crashes.
  - Key concept: journaling file system — metadata logging for crash recovery.

- **(10)** [2%] Answer: **(D) Both A and B.** Digital signatures verify sender identity (authentication) and prevent the sender from denying they sent the message (non-repudiation).
  - Key concept: digital signatures — authentication and non-repudiation.

- **(11)** [2%] Answer: **(B) Virtual Machines.** VMs allow multiple OSes to run concurrently on the same hardware via a hypervisor.
  - Key concept: virtual machines — hardware virtualization for concurrent OS execution.

- **(12)** [2%] Answer: **(D) Sector Sparing.** Sector sparing is for reliability (remapping bad sectors), not performance enhancement.
  - Key concept: disk performance vs. reliability mechanisms — sector sparing is for fault tolerance.

- **(13)** [2%] Answer: **(D) Kernel.** The kernel is the core OS component always resident in memory after booting.
  - Key concept: kernel — always-resident core of the operating system.

- **(14)** [2%] Answer: **(B) Interrupt number.** The device controller needs buffer address, device address, and operation type — the interrupt number is configured separately, not passed per I/O operation.
  - Key concept: I/O controller parameters — buffer address, device address, operation type.

- **(15)** [2%] Answer: **(C) Locality of reference.** Programs access memory in patterns (temporal and spatial locality), making caching effective.
  - Key concept: locality of reference — fundamental principle enabling cache effectiveness.

### Problem 2

This problem is worth 20 points total (4 subparts, 5 points each).

- **(1)** [5%] **Dynamic loading**: routine loaded into memory only when called (not at program start); saves memory for rarely-used code. **Dynamic linking**: shared library code linked at runtime; multiple programs share one copy of the library in memory (e.g., .so/.dll files).
  - Key concept: dynamic loading vs. linking — loading defers memory allocation; linking shares library code.

- **(2)** [5%] **Processor affinity** keeps a process/thread on the same CPU to reuse warm cache contents. Without affinity, migration to another CPU invalidates cached data, causing cold-start cache misses and degraded performance.
  - Key concept: processor affinity — maintain cache warmth by avoiding CPU migration.

- **(3)** [5%] **Priority inversion**: a high-priority task is blocked waiting for a resource held by a low-priority task, which itself is preempted by a medium-priority task. The medium task effectively runs before the high-priority one. Solution: priority inheritance — temporarily boost the low-priority holder's priority.
  - Key concept: priority inversion — solved by priority inheritance protocol.

- **(4)** [5%] Applications avoid spin-locks because busy-waiting wastes CPU cycles. Instead, they use **blocking synchronization** (mutexes, semaphores) that put waiting threads to sleep, yielding the CPU to other work until the lock becomes available.
  - Key concept: blocking vs. spinning — applications prefer sleeping to conserve CPU.

### Problem 3

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] CPU execution time = **Instruction Count (IC) × CPI × Clock Cycle Time**. Equivalently: IC × CPI / Clock Rate.
  - Key concept: CPU performance equation — three multiplicative factors.

- **(2)** [5%] Multiply-accumulate instruction: **IC decreases** (two instructions replaced by one), **CPI may increase** (more complex instruction may take more cycles), **Clock cycle time may increase** (more complex ALU may lengthen critical path). Net effect depends on implementation — typically beneficial for AI workloads.
  - Key concept: ISA design tradeoffs — fused instructions reduce IC but may increase CPI or cycle time.

### Problem 4

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] A **pipeline hazard** is a situation where the next instruction cannot execute in the following clock cycle due to data dependency, control dependency, or resource conflict. Types: data hazard, control hazard, structural hazard.
  - Key concept: pipeline hazards — situations preventing ideal one-instruction-per-cycle throughput.

- **(2)** [5%] Splitting ID into 2 stages increases the distance between reading register values and writing results. New hazards: load-use hazards now require **2 stall cycles** (instead of 1) because the value isn't available until later. Example: `lw $t0, 0($s0)` followed by `add $t1, $t0, $t2` — with 6 stages, the load result isn't available until after MEM, creating a 2-cycle bubble.
  - Key concept: deeper pipelines increase hazard penalty — more forwarding paths or stalls needed.

### Problem 5

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] **BHT (Branch History Table)**: a small cache indexed by branch instruction address that stores the prediction (taken/not-taken) for each branch based on its recent history.
  - Key concept: BHT — branch prediction using history of past branch outcomes.

- **(2)** [5%] Example: a branch taken 9 times then not-taken once (loop). With 1-bit BHT: mispredicts twice per loop iteration (on exit and re-entry). With 2-bit BHT: only mispredicts once (on exit) — one wrong prediction doesn't flip the prediction state, requiring two consecutive mispredictions to change.
  - Key concept: 2-bit BHT advantage — tolerates occasional anomalies without flipping prediction.

### Problem 6

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] Direct-mapped, 10 blocks, 1 word/block. Block = address mod 10. Trace: 8→B8(miss-compulsory), 11→B1(miss-compulsory), 21→B1(miss-conflict), 22→B2(miss-compulsory), 17→B7(miss-compulsory), 30→B0(miss-compulsory), 134→B4(miss-compulsory), 38→B8(miss-conflict), 43→B3(miss-compulsory), 48→B8(miss-conflict). **10 misses** total (7 compulsory + 3 conflict).
  - Key concept: direct-mapped cache miss analysis — classify by type (compulsory/conflict/capacity).

- **(2)** [5%] 10 words/block: block = (address/10) mod 10. Addresses map: 8→B0, 11→B1, 21→B2, 22→B2(hit!), 17→B1(hit!), 30→B3, 134→B3(miss-conflict), 38→B3(miss-conflict), 43→B4, 48→B4(hit!). Count misses and classify.
  - Key concept: cache blocking effects — larger blocks exploit spatial locality.

### Problem 7

This problem is worth 10 points.

Three reasons Apple develops its own SoC: (1) **Power efficiency** — custom ARM-based design optimizes performance-per-watt for battery-powered devices, better than Intel's x86 thermal envelope. (2) **Tight hardware-software integration** — controlling both chip and OS enables deeper optimization (unified memory architecture, custom accelerators for ML/media). (3) **Performance scalability** — same architecture from iPhone to Mac enables shared software ecosystem and consistent performance characteristics.
- Key concept: custom SoC advantages — power efficiency, integration, and ecosystem unification.

## 112

### Problem 1

This problem is worth 5 points.

SJF (non-preemptive) minimizes average waiting time. Simulate: at time 0, P3 arrives; at time 2, P2 and P4 arrive; at time 5, P1 arrives; at time 7, P5 arrives. Schedule shortest CPU bursts first. Trace execution to determine which process is running at time 23. With SRTF (preemptive SJF): track remaining bursts and I/O completions to find the running process at t=23.
- Key concept: SJF/SRTF scheduling simulation — trace execution timeline with arrivals and I/O.

### Problem 2

This problem is worth 5 points.

Answer: **(a) page cache.** Unified virtual memory uses the page cache for both process pages and file data.
- Key concept: unified page cache — single mechanism for VM pages and file buffer caching.

### Problem 3

This problem is worth 5 points.

Answer: **(e) MIPS can represent the execution time of a program on a given machine.** MIPS = IC/(execution time × 10^6), so given MIPS and IC, you can derive execution time. Other options are false: MIPS varies by program, is not a good cross-program metric.
- Key concept: MIPS metric limitations — varies by program, not ideal for comparison.

### Problem 4

This problem is worth 5 points.

Answer: **(e) Overflow occurs while adding two positive numbers and the sum is negative.** This is the correct definition of overflow for signed addition. (a) is false (FP addition is NOT associative due to rounding); (b) is false for negative numbers; (c) is debatable; (d) is false (Booth's is for multiplication).
- Key concept: integer overflow detection — sign mismatch in addition result.

### Problem 5

This problem is worth 5 points.

Answer: **(c) CISC always has better performance than RISC.** This is FALSE — RISC often outperforms CISC due to simpler pipelines and better clock rates. Also **(e)** is generally true but "always" makes it false in edge cases.
- Key concept: RISC vs. CISC — RISC doesn't inherently have worse performance than CISC.

### Problem 6

This problem is worth 5 points.

Incorrect statements: **(b)** Mounting points are directories, not regular files. **(c)** Repeatedly-used data storage is called **caching**, not buffering (buffering is for temporary I/O data in transit). **(e)** Modern OS uses logical block addressing (LBA), not physical disk geometry.
- Key concept: file system terminology — mounting points are directories; caching vs. buffering distinction.

### Problem 7

This problem is worth 5 points.

Hardware-based synchronization: **(a) CAS (Compare-And-Swap) instruction.** This is an atomic hardware instruction. Peterson's Solution is software-based, DMA is for I/O, Banker's is an algorithm, MMU is for memory management.
- Key concept: hardware synchronization primitives — CAS/test-and-set are hardware instructions.

### Problem 8

This problem is worth 5 points.

Context switch must occur: **(a) When a process is complete** — must switch to another process. **(d) When running→waiting** — process blocks, CPU must switch. (b) Interrupt doesn't always cause context switch (may return to same process). (c) waiting→running isn't a valid transition (goes through ready). (e) System call alone doesn't require full context switch.
- Key concept: context switch triggers — process completion and blocking require switching.

### Problem 9

This problem is worth 5 points.

Analyze dependencies: `lw $1` then `lw $2` then `add $3,$2,$4` (RAW on $2 — **data hazard/dependency**). `lw $1` (line 1) and `lw $1` (line 4) — **output dependency** (WAW on $1). Answers: **(b) Data hazard**, **(c) Data dependency**, **(d) Output dependency**.
- Key concept: pipeline dependency types — RAW (data), WAW (output), WAR (anti).

### Problem 10

This problem is worth 5 points.

Solutions: **(a) Forwarding** — resolves data hazards by bypassing pipeline registers. **(b) Code interchanging** (reordering) — moves independent instructions between dependent ones. **(d) Register renaming** — eliminates WAW/WAR hazards by using different physical registers.
- Key concept: hazard resolution techniques — forwarding, scheduling, and register renaming.

### Problem 11

This problem is worth 5 points.

(a) Max capacity = $2^{10}$ cylinders × $2^8$ heads × $2^6$ sectors × 512 bytes = $2^{10+8+6+9}$ = $2^{33}$ = **8 GB** (8,589,934,592 bytes).
(b) C-SCAN: extract cylinder values from CHS addresses, sort them, and compute total seek distance moving in one direction then returning to start.
- Key concept: CHS addressing capacity and C-SCAN disk scheduling — seek distance calculation.

### Problem 12

This problem is worth 5 points.

Answer: **(c) Circular wait.** Predefined lock ordering breaks the circular wait condition — if all threads acquire locks in the same order, no circular dependency can form.
- Key concept: deadlock prevention via lock ordering — breaks circular wait condition.

### Problem 13

This problem is worth 5 points.

Total resources (6,7,6,10). Allocated: sum = (4,7,5,6). Available = (2,0,1,4). T0's Max = (4,3,3,7), Need = (3,1,3,5). The OS can only grant resources that keep the system in a safe state. Check what additional allocation to T0 still allows a safe sequence to complete all other tasks.
- Key concept: Banker's algorithm — maximum safe grant to a single process.

### Problem 14

This problem is worth 5 points.

Answer: **(e) RAID** (Redundant Array of Independent Disks). RAID uses multiple disks for performance (striping) and reliability (mirroring/parity).
- Key concept: RAID — multiple disks for performance and reliability.

### Problem 15

This problem is worth 5 points.

`1 01111110 11000000000000000000000`: Sign=1 (negative). Exponent = $01111110_2 = 126$, bias=127, so actual = $-1$. Mantissa = $1.11_2 = 1.75$. Value = $(-1)^1 \times 1.75 \times 2^{-1} = -0.875$.
- Key concept: IEEE 754 single-precision decoding — sign, biased exponent, normalized mantissa.

### Problem 16

This problem is worth 5 points.

Stall cycles per instruction = instruction miss penalty + data miss penalty = $0.03 \times 100 + 0.35 \times 0.05 \times 100 = 3 + 1.75 = 4.75$. Actual CPI = $2 + 4.75 = 6.75$. Speedup of perfect cache = $6.75/2 = 3.375$.
- Key concept: cache miss impact on CPI — speedup from eliminating all stalls.

### Problem 17

This problem is worth 10 points total (2 subparts).

(h) Miss penalty = $100$ ns × $2.5$ GHz = $500$ cycles × (1/2.5GHz) = 100 ns... Actually: cycle time = 1/5GHz = 0.2 ns. Miss penalty in cycles = 100 ns / 0.2 ns = 500 cycles. Average memory time = 1 cycle (hit) + 0.02 × 500 = 1 + 10 = **11 cycles** (= 2.2 ns).
(i) With L2: average = 1 + miss_L1 × (L2_time + miss_L2 × DRAM_time). L2 in cycles = 5ns/0.2ns = 25. Average = 1 + 0.02 × (25 + 0.25 × 500) = 1 + 0.02 × 150 = 1 + 3 = **4 cycles** (= 0.8 ns).
- Key concept: multi-level cache average memory access time — hierarchical miss penalty.

### Problem 18

This problem is worth 5 points.

Sequential portion: scalar sum = negligible. Parallel portion: matrix sum of 1000×1000 = 1,000,000 additions. With 1000 processors: parallel time = 1000 additions per processor. Speedup ≈ 1000 (near-perfect parallelization). By Amdahl's Law: if sequential fraction $f$ is negligible, speedup ≈ $N = 1000$. But scalar sum is sequential: speedup = $1 / (f + (1-f)/1000)$ where $f = 1/1000001$ ≈ **999.999** ≈ **1000×**.
- Key concept: Amdahl's Law — speedup limited by sequential fraction.

## 113

### Problem 1

This problem is worth 30 points total (15 subparts, 2% each).

- **(1)** [2%] Answer: **(B) A hardware interrupt (trap) will occur.** MMU generates a trap on illegal address.
  - Key concept: memory protection — hardware exception on illegal address.

- **(2)** [2%] Answer: **(C) The hardware jumps to the handler through the interrupt vector.** The CPU uses the interrupt vector table to find and jump to the appropriate ISR.
  - Key concept: interrupt handling — hardware-driven dispatch via interrupt vector.

- **(3)** [2%] Answer: **(D) Monolithic with Dynamically Loadable Drivers.** Linux kernel architecture.
  - Key concept: Linux kernel — monolithic with loadable kernel modules.

- **(4)** [2%] Answer: **(B) Multithreading.** Threading on uniprocessors maps directly to SMP parallelism — same programming model, actual parallel execution on multiple CPUs.
  - Key concept: multithreading for SMP — same paradigm scales from uniprocessor to multiprocessor.

- **(5)** [2%] Answer: **(C) A long job can delay shorter ones.** The convoy effect — short processes wait behind long ones.
  - Key concept: FCFS convoy effect — long processes delay all following shorter processes.

- **(6)** [2%] Answer: **(A) We don't know the next CPU burst length.** SRTF requires future knowledge.
  - Key concept: SRTF fundamental limitation — requires burst length prediction.

- **(7)** [2%] Answer: **(B).** Top-level queue served first; lower queues only get CPU when upper ones are empty.
  - Key concept: MLFQ — priority-based queue service order.

- **(8)** [2%] Answer: **(E) None of the above.** Multi-level page tables reduce memory usage — flat page tables for large address spaces waste too much memory.
  - Key concept: multi-level page tables — space efficiency for sparse address spaces.

- **(9)** [2%] Answer: **(D) Clean dirty pages.** Proactive cleaning avoids future synchronous I/O when frames are needed.
  - Key concept: page cleaning — reduces future I/O by pre-writing dirty pages.

- **(10)** [2%] Answer: **(D) Keeping track of free memory blocks in lists of like size.** Buddy system maintains free lists organized by power-of-2 block sizes.
  - Key concept: buddy system — power-of-2 memory allocation with efficient splitting/coalescing.

- **(11)** [2%] Answer: **(B) Something you see.** Authentication factors: something you know (password), something you have (token), something you are (biometrics). "Something you see" is not a standard factor.
  - Key concept: authentication factors — knowledge, possession, inherence.

- **(12)** [2%] Answer: **(E) Executing untrusted code in a controlled environment like a virtual machine.**
  - Key concept: sandboxing — isolated execution environment.

- **(13)** [2%] Answer: **(C) The allocation granularity does not exactly match a requested allocation.** Internal fragmentation = wasted space within an allocated block.
  - Key concept: internal fragmentation — unused space within allocated blocks due to fixed granularity.

- **(14)** [2%] Answer: **(A) We wanted the scheduler to be fairer.** C-SCAN and variants sacrifice optimal seek time for fairness (no starvation of requests at disk edges).
  - Key concept: disk scheduling fairness — SCAN/C-SCAN prevent starvation at expense of seek time.

- **(15)** [2%] Answer: **(E) All of the above.** HTTP, FTP, SMTP, POP3 are all application layer protocols.
  - Key concept: application layer protocols — HTTP, FTP, SMTP, POP3.

### Problem 2

This problem is worth 10 points total (2 subparts).

- **(a)** First `fork()` creates 2 processes. Child (pid==0) calls `fork()` again → 3 processes total. Then `thread_create()` in child processes. Final `fork()` is executed by all 3 processes → 6 total processes. **Unique processes created = 5** (6 total minus the original).
  - Key concept: fork() process tree — each fork doubles processes on that execution path.

- **(b)** `thread_create()` is called in processes where pid==0 (child of first fork and its child from second fork) — **2 unique threads** created.
  - Key concept: thread creation within forked processes — only child path executes thread_create.

### Problem 3

This problem is worth 10 points.

Effective access time = $(1-p) \times 100$ ns $+ p \times$ page fault service time. Page fault service = $0.30 \times 8$ ms $+ 0.70 \times 20$ ms $= 2.4 + 14 = 16.4$ ms $= 16,400,000$ ns. EAT ≤ 200 ns: $(1-p) \times 100 + p \times 16,400,000 \le 200$. $100 + p(16,400,000 - 100) \le 200$. $p \times 16,399,900 \le 100$. $p \le 100/16,399,900 \approx 6.1 \times 10^{-6}$.
- Key concept: effective access time with page faults — maximum acceptable page fault rate calculation.

### Problem 4

This problem is worth 20 points total (2 subparts).

- **(a)** [10%] Direct-mapped, 10 blocks, 1 word/block. Block = address mod 10. Trace: 18→B8(compulsory), 121→B1(compulsory), 26→B6(compulsory), 123→B3(compulsory), 168→B8(conflict), 169→B9(compulsory), 126→B6(conflict), 68→B8(conflict), 85→B5(compulsory), 89→B9(conflict). Misses: 10 total (6 compulsory + 4 conflict).
  - Key concept: cache miss classification — trace through addresses and identify miss types.

- **(b)** [10%] 10 words/block: block = (address/10) mod 10. 18→B1(compulsory), 121→B2(compulsory), 26→B2(conflict), 123→B2(conflict), 168→B6(compulsory), 169→B6(hit), 126→B2(conflict), 68→B6(conflict), 85→B8(compulsory), 89→B8(hit). Classify each miss.
  - Key concept: larger cache blocks — spatial locality vs. conflict misses tradeoff.

### Problem 5

This problem is worth 15 points total (3 subparts).

- **(a)** [5%] Pipelining affects: **CPI** — ideally reduces CPI toward 1 (one instruction per cycle), but hazards increase CPI above 1. **Clock cycle time** — determined by the slowest pipeline stage plus register overhead; deeper pipelines have shorter stages but more register delays.
  - Key concept: pipelining effects on performance equation — reduces effective CPI, may affect cycle time.

- **(b)** [5%] CPI-related issues: (1) **Pipeline hazards** (data, control, structural) cause stalls that increase CPI. (2) **Branch mispredictions** — incorrectly predicted branches waste pipeline stages (flush penalty).
  - Key concept: CPI degradation factors — hazard stalls and branch misprediction penalties.

- **(c)** [5%] Clock cycle time issues: (1) **Pipeline register overhead** — setup + hold time of registers added to each stage's delay. (2) **Clock skew** — variation in clock arrival times across the chip requires timing margins, limiting frequency.
  - Key concept: clock cycle time limitations — register overhead and clock distribution.

### Problem 6

This problem is worth 15 points total (3 subparts).

- **(a)** [5%] Hazards: `lw $t0` → `sw $t0` (RAW data hazard on $t0), `addi $s0` → `lw` uses $s0 next iteration (RAW), `addi $s0` → `bne $s0` (RAW data hazard), `bne` → next `lw` (control hazard — branch not resolved until EXE).
  - Key concept: MIPS pipeline hazard identification — RAW dependencies and control hazards.

- **(b)** [5%] Insert NOPs: after `lw $t0` (1 NOP for forwarding, or 2 without forwarding before `sw`), after `addi $s0` (before `bne` — 2 NOPs without forwarding), after `bne` (2 NOPs for branch delay since resolved at EXE). Rewrite code with minimal NOPs.
  - Key concept: NOP insertion — resolve hazards without hardware support.

- **(c)** [5%] Count cycles: each loop iteration = instructions + NOPs + pipeline stages. For 100 iterations ($s0$ starts at 400, decrements by 4 each time): total cycles = iterations × cycles_per_iteration + pipeline fill/drain.
  - Key concept: pipeline execution cycle counting — accounting for stalls and iterations.
