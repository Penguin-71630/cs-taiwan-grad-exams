## 108

**Part I. 資料結構 (50%)**

### Problem 1

This problem is worth 12 points total (3 subparts, 4% each).

- **(1)** [4%] **False.** DFS with an adjacency matrix takes $O(v^2)$ time (checking all $v$ entries for each of $v$ vertices), not $O(e^2)$. The complexity depends on the representation: adjacency matrix → $O(v^2)$; adjacency list → $O(v+e)$.
  - Key concept: DFS time complexity depends on graph representation — adjacency matrix is $O(v^2)$.

- **(2)** [4%] **False.** The MST path from $u$ to $v$ minimizes the maximum edge weight along the path, but is NOT necessarily the shortest path (minimum total weight). Counterexample: triangle graph with edges (u,w)=1, (w,v)=1, (u,v)=3 — MST uses edges 1,1 but shortest path u→v is 3 directly (same), however in other configurations they differ.
  - Key concept: MST paths minimize bottleneck weight, not total weight — different from shortest paths.

- **(3)** [4%] **Partially true, partially false.** Open addressing worst case is $O(n)$ ✓. However, chaining with a balanced BST gives $O(\log n)$ per bucket, but the statement says "reduced to $O(\log n)$" which is correct for chaining with balanced trees. With simple linked-list chaining, worst case is still $O(n)$.
  - Key concept: hash collision resolution — open addressing vs. chaining with different secondary structures.

### Problem 2

This problem is worth 18 points total (3 subparts).

- **(1)** [7%] Compute earliest event time $e(i)$ and latest event time $l(i)$ for each activity. Forward pass from $V_0$: compute earliest start times. Backward pass from $V_8$: compute latest start times. $e(i)$ = earliest start of activity $a_i$; $l(i)$ = latest start without delaying project. Slack = $l(i) - e(i)$.
  - Key concept: AOE network analysis — forward/backward pass for earliest and latest event times.

- **(2)** [7%] Critical activities have zero slack ($e(i) = l(i)$). Identify all activities where earliest start equals latest start. The critical path length equals the longest path from source to sink.
  - Key concept: critical activities — zero-slack activities that determine project duration.

- **(3)** [4%] List all paths from $V_0$ to $V_8$ that consist entirely of critical activities. Multiple critical paths may exist if different routes share the same total length.
  - Key concept: critical path identification — all paths consisting entirely of critical activities.

### Problem 3

This problem is worth 20 points total (4 subparts, 5% each).

- **(1)** [5%] Build a winner tree (tournament tree) with 8 leaves (one per run). Compare pairs: smallest element from each run competes. Winner (smallest) moves up. Root holds the overall minimum. Draw the complete binary tree with 8 leaves showing the winners at each internal node.
  - Key concept: winner tree (tournament tree) — external sorting with k-way merge.

- **(2)** [5%] Output the root's record (overall minimum), replace it in its run with the next record, then replay matches from that leaf to the root. Redraw the tree with updated winners.
  - Key concept: winner tree restructuring — replay matches along the path from updated leaf to root.

- **(3)** [5%] Convert the winner tree to a loser tree: each internal node stores the loser of the comparison (the non-advancing element), while the winner advances upward. The overall winner is stored above the root.
  - Key concept: loser tree — stores losers at internal nodes for more efficient restructuring.

- **(4)** [5%] Total time = $n \cdot \lceil\log_2 k\rceil$ comparisons. Each of the $n$ records requires $\lceil\log_2 k\rceil$ comparisons to find the next minimum (height of the tournament tree). Total: $O(n \log k)$.
  - Key concept: k-way merge complexity — $O(n \log k)$ using tournament trees.

**Part II. 演算法 (50%)**

### Problem 4

This problem is worth 10 points.

To prove problem $L$ is NP-complete: (1) Show $L \in NP$ — given a certificate, verify it in polynomial time. (2) Show $L$ is NP-hard — take a known NP-complete problem $L'$ and show $L' \le_P L$ (polynomial-time reduction from $L'$ to $L$). The reduction must transform any instance of $L'$ into an instance of $L$ in polynomial time, preserving yes/no answers.
- Key concept: NP-completeness proof — membership in NP plus NP-hardness via polynomial reduction.

### Problem 5

This problem is worth 10 points.

Run DFS on the undirected graph. If the graph has more than $V-1$ edges, it must contain a cycle (by pigeonhole on a forest). During DFS, if a back edge is found (edge to an already-visited vertex that is not the parent), a cycle exists. DFS visits at most $V$ vertices and examines at most $2(V-1)$ edge endpoints in a forest; any additional edge creates a cycle. Total time: $O(V)$ — stop after visiting $V$ vertices or finding $V$ edges.
- Key concept: cycle detection in $O(V)$ — DFS terminates early once $|E| \ge |V|$ or a back edge is found.

### Problem 6

This problem is worth 10 points total (5 subparts, 2% each).

- **(a)** [2%] **False.** The 0-1 knapsack problem requires dynamic programming, not greedy. Greedy works for the fractional knapsack but fails for 0-1 because items cannot be divided.
  - Key concept: 0-1 knapsack is not solvable by greedy — items are indivisible.

- **(b)** [2%] **False.** DFS on an undirected graph produces tree edges and **back edges** only (no cross edges). Cross edges only appear in directed graph DFS.
  - Key concept: undirected DFS edge classification — only tree edges and back edges.

- **(c)** [2%] **True.** Memoization (top-down DP) stores results of subproblems to avoid redundant computation, reducing exponential time to polynomial for overlapping subproblems.
  - Key concept: memoization — caching subproblem results in top-down dynamic programming.

- **(d)** [2%] **False.** The unweighted longest simple path problem does NOT have optimal substructure. A longest simple path from $u$ to $v$ via $w$ does not decompose into longest simple paths $u \to w$ and $w \to v$ because the simplicity constraint (no repeated vertices) creates dependencies between subproblems.
  - Key concept: longest simple path lacks optimal substructure — subpath independence is violated.

- **(e)** [2%] **True.** After topological sorting, all edges go from earlier to later vertices. Running DFS in topological order means we never encounter a back edge (no edge goes from a later vertex to an earlier one in the DFS tree).
  - Key concept: topological sort eliminates back edges in subsequent DFS traversal.

### Problem 7

This problem is worth 10 points.

$T(n) = T(2n/3) + 1$. Master Theorem: $a=1, b=3/2, f(n)=1$. $\log_{3/2} 1 = 0$. $f(n) = \Theta(n^0) = \Theta(1)$. Case 2: $T(n) = \Theta(\log n)$.
- Key concept: Master Theorem Case 2 — $f(n) = \Theta(n^{\log_b a})$ gives $\Theta(n^{\log_b a} \log n)$.

### Problem 8

This problem is worth 10 points.

BFS from node 0 (visit smaller numbers first): Level 0: {0}. Level 1: neighbors of 0 sorted = {1, 2, 4}. Level 2: unvisited neighbors of 1,2,4 sorted = {3, 5, 6, 7}. The BFS spanning tree has edges: 0-1, 0-2, 0-4, 1-3, 1-6, 2-5, 4-7 (or 5-7 depending on which reaches 7 first).
- Key concept: BFS spanning tree — level-by-level exploration visiting smaller-numbered neighbors first.

## 109

**Part I. 資料結構 (50%)**

### Problem 1

This problem is worth 30 points total (5 subparts, 6% each).

- **(1)** [6%] **Partially true.** Open addressing worst case $O(n)$ ✓. Using balanced BST for chaining reduces worst-case search to $O(\log n)$ per chain ✓. The statement is **True**.
  - Key concept: hashing with balanced BST chaining — $O(\log n)$ worst-case search.

- **(2)** [6%] **False.** The sum of degrees equals $2e$ (handshaking lemma), not $e$. Correct formula: $e = \frac{1}{2}\sum_{i=0}^{n-1} d_i$.
  - Key concept: handshaking lemma — sum of degrees equals twice the number of edges.

- **(3)** [6%] **False.** MST path does not guarantee shortest path. MST minimizes total edge weight of the spanning tree, not individual pairwise shortest paths.
  - Key concept: MST vs. shortest path — MST optimizes global tree weight, not pairwise distances.

- **(4)** [6%] **False.** DFS with adjacency lists runs in $O(v + e)$ time, not $O(ev^2)$. Each vertex is visited once and each edge is examined twice (once from each endpoint).
  - Key concept: DFS with adjacency lists — $O(V+E)$ time complexity.

- **(5)** [6%] **Not necessarily true.** A topological order CAN be unique if the AOV network has a unique topological ordering (e.g., a simple chain). The statement claims it's always non-unique, which is **False**.
  - Key concept: topological order uniqueness — depends on graph structure; not always unique but can be.

### Problem 2

This problem is worth 10 points.

After $u$ updates, each bit remains 0 with probability $(1 - 1/m)^{uh}$ (each of $u$ keys sets $h$ positions). Probability a specific bit is still 0: $(1 - 1/m)^{uh} \approx e^{-uh/m}$. For a filter error (false positive), all $h$ bits for the query key must be 1. Probability of filter error: $(1 - (1-1/m)^{uh})^h \approx (1 - e^{-uh/m})^h$.
- Key concept: Bloom filter false positive probability — $(1 - e^{-kn/m})^k$ for $k$ hash functions, $n$ elements, $m$ bits.

### Problem 3

This problem is worth 10 points.

Apply Kruskal's or Prim's algorithm: sort edges by weight, greedily add the smallest edge that doesn't create a cycle. For 5 vertices, the MST has 4 edges. Starting with smallest: $a$-$b$(7), $a$-$c$(9) or $a$-$e$(9), etc. Build the tree and sum edge weights for minimum total cost.
- Key concept: minimum spanning tree construction — Kruskal's or Prim's algorithm on weighted undirected graph.

**Part II. 演算法 (50%)**

### Problem 4

This problem is worth 10 points.

$5^{n+1} = 5 \cdot 5^n = O(5^n)$ — **Yes**, since $5^{n+1}/5^n = 5$ (constant). $6^{2n} = 36^n$ and $6^n$: $6^{2n}/6^n = 6^n \to \infty$, so $6^{2n} \ne O(6^n)$ — **No**.
- Key concept: exponential growth rate comparison — constant factor vs. exponential factor.

### Problem 5

This problem is worth 10 points.

$T(n) = \sqrt{n} \cdot T(\sqrt{n}) + n$. Substitution: let $n = 2^m$, $S(m) = T(2^m)$. Then $S(m) = 2^{m/2} S(m/2) + 2^m$. Dividing by $2^m$: $S(m)/2^m = S(m/2)/2^{m/2} \cdot (1/2^{m/2}) + 1$... Use substitution method. After analysis: $T(n) = \Theta(n \log \log n)$.
- Key concept: recurrence with $\sqrt{n}$ subproblem size — substitution $n = 2^m$ transforms to standard form.

### Problem 6

This problem is worth 10 points.

Use **randomized quicksort** (random pivot selection) or **median-of-medians** (deterministic linear-time median finding). With random pivots, expected partition is balanced: $T(n) = 2T(n/2) + O(n) = O(n \log n)$ expected. With median-of-medians as pivot selection, worst-case $O(n \log n)$ is guaranteed by ensuring balanced partitions.
- Key concept: guaranteed $O(n \log n)$ quicksort — randomized pivot or median-of-medians.

### Problem 7

This problem is worth 10 points.

LCS of $X = \langle 1,0,0,1,0,1,0,1 \rangle$ and $Y = \langle 0,1,0,1,1,0,1,1,0 \rangle$. Build the DP table $c[i][j]$: if $x_i = y_j$, $c[i][j] = c[i-1][j-1]+1$; else $c[i][j] = \max(c[i-1][j], c[i][j-1])$. LCS length = 6. One LCS: $\langle 1,0,1,0,1,0 \rangle$ or $\langle 0,0,1,0,1,1 \rangle$. Trace back through the table to construct the subsequence.
- Key concept: LCS dynamic programming — $O(mn)$ table construction and backtracking.

### Problem 8

This problem is worth 10 points.

$T(n) = 27T(n/3) + \Theta(n^3/\lg n)$. Master Theorem: $a=27, b=3, \log_3 27 = 3$. $f(n) = n^3/\lg n$. Compare with $n^{\log_b a} = n^3$: $f(n) = n^3/\lg n = o(n^3)$ but $n^3/\lg n$ is not polynomially smaller (the ratio is $1/\lg n$, not $n^{-\epsilon}$). Master Theorem does not directly apply (falls in the gap between Cases 2 and 1). By extended Master Theorem or substitution: $T(n) = \Theta(n^3 \lg \lg n)$.
- Key concept: Master Theorem gap case — when $f(n)$ differs from $n^{\log_b a}$ by a logarithmic factor.

## 110

**Part I. 資料結構 (50%)**

### Problem 1

This problem is worth 10 points.

Insert keys in preorder order into BST: 50 (level 1), 15 (level 2, left of 50), 11 (level 3), 3 (level 4), 2 (level 5), 22 (level 3), 84 (level 2, right of 50), 80 (level 3), 77 (level 4), 90 (level 3), 95 (level 4). The BST height reflects the preorder insertion sequence. Report the level of each node.
- Key concept: BST construction from preorder traversal — each key is inserted following BST property.

### Problem 2

This problem is worth 10 points.

Build Huffman tree: repeatedly merge two lowest-frequency nodes. R(1)+L(3)=4, then 4+I(5)=9, M(6)+9=15... Continue until all merged. Compute total bits = $\sum f_i \times d_i$ where $d_i$ is the depth of character $i$ in the Huffman tree.
- Key concept: Huffman coding — greedy construction of optimal prefix-free code; total bits = weighted path length.

### Problem 3

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] Sum of ASCII values of each string: "abcdef" = 97+98+99+100+101+102 = 597. All four strings have the same characters, so same sum = 597. 597 mod 11 = 597 - 54×11 = 597-594 = 3. All four strings hash to index 3. Collisions = 3 (after the first insertion, each subsequent is a collision).
  - Key concept: hash collisions — permutation-invariant hash functions cause collisions for anagrams.

- **(2)** [5%] Hash = $\sum(\text{ASCII} \times \text{position})$ mod 11. Positions 1-6. Compute for each string: "abcdef" = 97×1+98×2+...+102×6 = different from "bcdefa" = 98×1+99×2+...+97×6. Calculate each value mod 11 and count collisions.
  - Key concept: position-weighted hash function — reduces collisions for string permutations.

### Problem 4

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] First loop: $\sum_{i=1}^{8} i = 36$. Second loop: $\sum_{i=4}^{1} i = 4+3+2+1 = 10$. Total sum = 36 + 10 = 46. Output: **46**.
  - Key concept: loop summation — compute arithmetic series for each loop.

- **(2)** [5%] First loop runs $2n$ iterations, second runs $n$ iterations. Total: $3n$ iterations. $T(n) = O(n)$ (linear time).
  - Key concept: time complexity analysis — sum of linear loops is linear.

### Problem 5

This problem is worth 10 points.

Apply Kruskal's algorithm: sort edges by weight. Add edges in order: 1-6(2), 2-4(2), 3-4(3), 5-6(4), 1-5(5) or 2-3(5) — need 5 edges for 6 vertices. Sum the weights of the 5 MST edges. Total MST cost = 2+2+3+4+5 = 16 (verify with specific graph edges).
- Key concept: MST cost computation — Kruskal's algorithm with sorted edge weights.

**Part II. 演算法 (50%)**

### Problem 6

This problem is worth 15 points total (5 subparts, 3% each).

- **(1)** [3%] $T(n) = 2T(n/4) + \sqrt{n}$. $a=2, b=4, \log_4 2 = 0.5$. $f(n) = n^{0.5} = \Theta(n^{\log_4 2})$. Case 2: $T(n) = \Theta(\sqrt{n} \log n)$.
  - Key concept: Master Theorem Case 2.

- **(2)** [3%] $T(n) = T(n-1) + n\lg n$. Not applicable — Master Theorem requires $T(n/b)$ form. By expansion: $T(n) = \sum_{i=1}^{n} i\lg i = \Theta(n^2 \log n)$.
  - Key concept: Master Theorem does not apply to subtract-and-conquer recurrences.

- **(3)** [3%] $T(n) = 3T(n/4) + n\lg n$. $a=3, b=4, \log_4 3 \approx 0.79$. $f(n) = n\lg n = \Omega(n^{0.79+\epsilon})$. Case 3 (with regularity check): $T(n) = \Theta(n \log n)$.
  - Key concept: Master Theorem Case 3.

- **(4)** [3%] $T(n) = 4T(n/2) + n^2\lg n$. $a=4, b=2, \log_2 4 = 2$. $f(n) = n^2\lg n$. This is $n^2$ times $\lg n$ — not polynomially larger than $n^2$. Extended Case 2: $T(n) = \Theta(n^2 \log^2 n)$.
  - Key concept: Master Theorem extended Case 2 — extra log factor.

- **(5)** [3%] $T(n) = 7T(n/2) + \Theta(n^2)$. $a=7, b=2, \log_2 7 \approx 2.807$. $f(n) = n^2 = O(n^{2.807-\epsilon})$. Case 1: $T(n) = \Theta(n^{\log_2 7}) \approx \Theta(n^{2.807})$.
  - Key concept: Master Theorem Case 1 — Strassen-like recurrence.

### Problem 7

This problem is worth 15 points total (5 subparts, 3% each).

- **(1)** [3%] **False (with caveat).** If NP-complete problem $A \le_P L$ and $L \in NP$, then $L$ is NP-complete. But the statement doesn't specify $L \in NP$. If $L \notin NP$, $L$ is NP-hard but not NP-complete. So **False** as stated (missing $L \in NP$ condition).
  - Key concept: NP-completeness = NP-hard + in NP — reduction alone only proves NP-hardness.

- **(2)** [3%] **False.** If $A \le_P B$ and $A \in P$, we cannot conclude $B \in P$. The reduction shows $A$ is "no harder than" $B$, not that $B$ is easy. $B$ could be much harder.
  - Key concept: polynomial reduction direction — $A \le_P B$ means $A$ is at most as hard as $B$.

- **(3)** [3%] **True.** $P \subseteq NP$. Any problem solvable in polynomial time can be verified in polynomial time (use the solver as verifier).
  - Key concept: $P \subseteq NP$ — polynomial-time solvability implies polynomial-time verifiability.

- **(4)** [3%] **False.** If $P \ne NP$, there is NO constant-factor approximation algorithm for the general TSP (unless $P = NP$). The inapproximability result holds for the general (metric-unrestricted) TSP.
  - Key concept: general TSP inapproximability — no polynomial-time constant-factor approximation unless $P = NP$.

- **(5)** [3%] **False.** Maximum elements in a heap of height $h$ is $2^{h+1} - 1$ (a complete binary tree). With $h$ starting from 0: height-0 tree has 1 node, height-$h$ tree has at most $2^{h+1}-1$ nodes. If height starts from 1, max = $2^h - 1$.
  - Key concept: heap capacity — maximum nodes in a complete binary tree of given height.

### Problem 8

This problem is worth 10 points.

(1) $S[i][0] = S[i-1][0]$ && $(x[i-1] == z[i-1])$ — first $i$ chars of $z$ match first $i$ chars of $x$.
(2) $S[0][j] = S[0][j-1]$ && $(y[j-1] == z[j-1])$ — first $j$ chars of $z$ match first $j$ chars of $y$.
(3) $S[i][j] = (S[i-1][j]$ && $x[i-1] == z[i+j-1])$ || $(S[i][j-1]$ && $y[j-1] == z[i+j-1])$ — either the $(i+j)$-th char of $z$ matches $x[i]$ (extending from $S[i-1][j]$) or matches $y[j]$ (extending from $S[i][j-1]$).
- Key concept: shuffle verification via 2D DP — checking interleaving of two sequences.

### Problem 9

This problem is worth 10 points total (3 subparts).

- **(1)** [2%] Floyd-Warshall time complexity: $O(V^3)$ — three nested loops over $V$ vertices.
  - Key concept: Floyd-Warshall all-pairs shortest paths — $O(n^3)$ time.

- **(2)** [3%] $d_{ij}^{(k)} = \min(d_{ij}^{(k-1)},\ d_{ik}^{(k-1)} + d_{kj}^{(k-1)})$. Either the shortest path doesn't use vertex $k$, or it goes through $k$.
  - Key concept: Floyd-Warshall recurrence — optimal substructure via intermediate vertices.

- **(3)** [5%] Run Floyd-Warshall on the 6-vertex graph to compute all-pairs shortest paths. Sum $dist(1,5) + dist(2,5) + dist(3,5) + dist(4,5) + dist(6,5)$. Compute each shortest distance using the algorithm's final matrix $D^{(6)}$.
  - Key concept: applying Floyd-Warshall to compute specific shortest path sums.

## 111

**Part I. 資料結構 (50%)**

### Problem 1

This problem is worth 4 points.

- **(A)** `stk->top = stk->top->prev;` — update top pointer to the previous node before freeing.
- **(B)** `new_node->prev = stk->top;` — link the new node's prev pointer to the current top.
  - Key concept: stack implementation with linked list — push/pop pointer manipulation.

### Problem 2

This problem is worth 2 points.

Union by height (rank): attach the shorter tree under the root of the taller tree. Left tree (root 1) has height 2. Right tree (root 6) has height 3 (path 6→7→10→12). Since right tree is taller, root 1 becomes a child of root 6. Answer: **(A)**.
- Key concept: union by rank/height — attach shorter tree under taller tree's root.

### Problem 3

This problem is worth 5 points total (2 subparts).

- **(i)** [2%] Insert 62 into the red-black tree: 62 goes to the right of 60. This causes a red-red violation (60 is red, 62 is red). Uncle 70 is also red → recolor: 60 and 70 become black, 65 becomes red. Then check 65 (now red) with parent 80 (red) → uncle 50's child... Apply rotations/recoloring as needed. Answer: **(A)** (after recoloring and possible rotation).
  - Key concept: red-black tree insertion — recoloring and rotations to maintain properties.

- **(ii)** [3%] Convert the resulting red-black tree to its 2-3-4 tree counterpart: red nodes merge with their black parent to form 3-nodes or 4-nodes. Draw the equivalent 2-3-4 tree.
  - Key concept: red-black tree to 2-3-4 tree correspondence — red nodes merge with parent.

### Problem 4

This problem is worth 4 points.

Compute hash values for 8 and 9 using the three functions. For $x=8$: $f_1(8) = \lfloor \text{frac}(8 \times 0.31) \times 16 \rfloor = \lfloor \text{frac}(2.48) \times 16 \rfloor = \lfloor 0.48 \times 16 \rfloor = \lfloor 7.68 \rfloor = 7$. $f_2(8) = \lfloor \text{frac}(1.92) \times 16 \rfloor = \lfloor 0.92 \times 16 \rfloor = \lfloor 14.72 \rfloor = 14$. $f_3(8) = \lfloor \text{frac}(1.04) \times 16 \rfloor = \lfloor 0.04 \times 16 \rfloor = \lfloor 0.64 \rfloor = 0$. Check bits 7, 14, 0: bit[7]=0. Not all 1 → 8 is **not in**. Answer includes **(A)**.
For $x=9$: compute similarly and check bits. If all 3 bits are 1, answer is "may be in" **(E)**; if any bit is 0, "not in" **(D)**.
- Key concept: Bloom filter query — check all hash positions; any 0 means definitely not present.

### Problem 5

This problem is worth 2 points.

Delete 70, 10, 60, 95 in order from the order-3 B-tree with root [50, 80]. After each deletion, rebalance by borrowing from siblings or merging nodes. Track the tree structure after all 4 deletions.
- Key concept: B-tree deletion — rebalancing through borrowing and merging.

### Problem 6

This problem is worth 5 points total (2 subparts).

- **(i)** [2%] Insert 1001, 1100, 0000, 0001 into the Patricia trie. Each insertion finds the first differing bit position with the existing key found during search, then creates a new node at that bit position. Draw the resulting Patricia after all 4 insertions.
  - Key concept: Patricia trie insertion — compare bit positions to determine branching.

- **(ii)** [3%] Convert the Patricia to its compressed binary trie representation: expand the Patricia's bit-position comparisons into a full trie structure with internal nodes at each compared bit position.
  - Key concept: Patricia to compressed binary trie conversion.

### Problem 7

This problem is worth 2 points.

DFS from node 1: check each candidate sequence. A valid DFS sequence must follow adjacency relationships and visit unvisited neighbors in some order. Verify each option against the graph's adjacency structure. Node 1 connects to nodes 3 and 6. From 3: connects to 5, 6, 8. Trace each sequence to check validity.
- Key concept: DFS traversal validation — sequence must respect adjacency and visit order.

### Problem 8

This problem is worth 4 points.

Hash function $f(x) = x \bmod 17$. Insert 23→6, 52→1, 11→11, 1→1(collision→2), 50→16, 99→14, 65→14(collision→15), 20→3, 35→1(collision→3→4, but 3 taken→4), 34→0, 82→14(collision→15→16→0→1→2→3→4→5). Fill blanks: (A)=52, (B)=1, (C)=20, (D)=35, (E)=82.
- Key concept: hash table with linear probing — collision resolution by sequential search.

### Problem 9

This problem is worth 4 points.

- **(A)** `i++` — increment the partition index.
- **(B)** `return i` — return the pivot's final position.
- **(C)** `arr, front, pivot - 1` — sort left subarray.
- **(D)** `arr, pivot + 1, end` — sort right subarray.
- Key concept: quicksort partition — Lomuto partition scheme implementation.

### Problem 10

This problem is worth 4 points total (2 subparts).

- **(i)** [2%] The function recursively processes left and right subtrees first, then swaps them. This is **(C) postorder** traversal (process children before current node).
  - Key concept: tree traversal order — postorder processes subtrees before the current node.

- **(ii)** [2%] The function mirrors the tree (swaps left and right children at every node). After mirroring, the rightmost leaf of the original tree becomes the leftmost, and vice versa. The original leftmost leaf is 3 (leftmost of 13's left). After mirroring, this becomes the rightmost path. Trace the mirrored tree to find the rightmost leaf.
  - Key concept: tree mirroring — swapping left/right children recursively.

### Problem 11

This problem is worth 6 points total (3 subparts).

- **(i)** [2%] Extract min (6): replace root with last element (26), sift down. After heapify: 8 becomes root, children rearrange. $Q[4]$ after extraction: trace the heap array after removing 6 and sifting 26 down. Answer: **(C) 20** or verify by simulation.
  - Key concept: min-heap extract-min — replace root with last element and sift down.

- **(ii)** [2%] Extract next min (8): replace with last element, sift down again. $Q[5]$ after this second extraction.
  - Key concept: successive heap extract-min operations.

- **(iii)** [2%] Insert priority 11: add at end, sift up. $Q[9]$ after insertion.
  - Key concept: min-heap insertion — add at end and sift up.

### Problem 12

This problem is worth 8 points total (4 subparts).

- **(i)** [2%] (1) = **(A)** $x.key < y.key$ — cut when heap property is violated (child key < parent key in min-heap).
  - Key concept: Fibonacci heap decrease-key — cut when min-heap property violated.

- **(ii)** [2%] (2) = **(A)** NIL — after cutting $x$ from parent $y$, set $x.p = \text{NIL}$ (moved to root list).
  - Key concept: Fibonacci heap cut operation — detach node from parent.

- **(iii)** [2%] Amortized time of DecreaseKey: **(A)** $O(1)$.
  - Key concept: Fibonacci heap amortized complexity — decrease-key is $O(1)$ amortized.

- **(iv)** [2%] Insert into F-heap: **(A)** $O(1)$ — add to root list and update min pointer.
  - Key concept: Fibonacci heap insertion — $O(1)$ by adding to root list.

**Part II. 演算法 (50%)**

### Problem 13

This problem is worth 10 points.

$T(n) = 2T(n/4) + \sqrt{n}\log n$. Master Theorem: $a=2, b=4, \log_4 2 = 0.5$. $f(n) = n^{0.5}\log n$. Compare with $n^{0.5}$: $f(n) = n^{0.5}\log n$ exceeds $n^{0.5}$ by a $\log n$ factor (not polynomial). Extended Case 2: $T(n) = \Theta(\sqrt{n} \log^2 n)$.
- Key concept: Master Theorem extended Case 2 — extra log factor gives additional $\log$ in result.

### Problem 14

This problem is worth 10 points.

Optimal BST: use the DP algorithm. Build tables $e[i,j]$ (expected cost) and $w[i,j]$ (weight sum) for all subproblems. $e[i,j] = \min_{i \le r \le j} \{e[i,r-1] + e[r+1,j] + w[i,j]\}$. With 5 keys and 6 dummy keys, fill the $5 \times 5$ table bottom-up. Report the minimum expected search cost $e[1,5]$.
- Key concept: optimal binary search tree — DP algorithm minimizing expected search cost.

### Problem 15

This problem is worth 10 points.

A directed strongly connected graph contains a cycle of odd length iff it is NOT bipartite. Algorithm: run BFS/DFS to attempt 2-coloring. If the graph is 2-colorable (bipartite), all cycles are even-length. If not 2-colorable, an odd cycle exists. Time: $O(V+E)$.
- Key concept: odd cycle detection in directed graphs — bipartiteness test via 2-coloring.

### Problem 16

This problem is worth 10 points.

Build a constraint graph: for each constraint $x_i - x_j \le b_{ij}$, add edge $(j, i)$ with weight $b_{ij}$. Add source vertex $v_0$ with zero-weight edges to all variables. Run Bellman-Ford from $v_0$. If no negative-weight cycle exists, the shortest-path distances give a feasible solution: $x_i = \delta(v_0, v_i)$. Check for negative cycles to determine feasibility.
- Key concept: system of difference constraints — Bellman-Ford on constraint graph.

### Problem 17

This problem is worth 10 points.

The greedy set cover algorithm achieves an approximation ratio of $\delta = H(\max|S|) \le H(|X|) = \ln|X| + 1$, where $H(k) = \sum_{i=1}^{k} 1/i$ is the $k$-th harmonic number. The minimum value of $\delta$ (tightest bound) is $H(\max_{S \in F}|S|)$.
- Key concept: greedy set cover approximation ratio — $O(\ln n)$ approximation via harmonic number bound.

## 112

**Part I. 資料結構 (50%)**

### Problem 1

This problem is worth 5 points.

(A) **False.** A BST with $n$ nodes can be skewed (height $n$), requiring up to $n$ comparisons. Only balanced BSTs guarantee $O(\log n)$.
(B) **True.** Full binary tree of height $h$ (root at height 1) has $2^h - 1$ nodes.
(C) **True.** In array representation with root at index 1, parent of node $i$ is $\lfloor i/2 \rfloor$.
(D) **False.** A compressed trie is not necessarily a binary tree (nodes can have multiple children based on string labels). AVL and red-black trees are binary trees.
- Key concept: binary tree properties — height vs. node count, array representation, and tree type classification.

### Problem 2

This problem is worth 5 points.

Remove max (85): replace with last element, sift down to restore max-heap property. After removal: 55 or the last leaf takes root position, then percolates down. Check each statement against the resulting heap structure.
(E) **True.** Removal from a heap is $O(\log n)$ — sift down takes at most $\log n$ steps.
- Key concept: max-heap deletion — replace root with last element, sift down.

### Problem 3

This problem is worth 5 points.

Analyze MST $T$ of graph $G$. (A) Check Prim's from $L$: the last edge added depends on the order edges are processed. (B) MST uniqueness: unique iff all edge weights are distinct. (C) Removing edge $(M,E)$: if $(M,E) \notin T$, MST unchanged. (D) Removing edge $(B,F)$: if $(B,F) \notin T$, MST unchanged.
- Key concept: MST properties — uniqueness conditions and effect of edge removal.

### Problem 4

This problem is worth 5 points.

AVL tree: root 22, left child 17 (right child 20), right child 35 (right child 36). Check balance after each operation:
(A) Insert 18: goes to left of 20. Path 22→17→20→18. Balance factors change; check if violation occurs at 17 (left height 0, right height 2) → unbalanced. **Yes, needs restructuring.**
(B) Insert 33: goes to left of 35. Balance factor of 35 becomes 1 (balanced). No restructuring needed.
(D) Delete 35: 36 replaces 35. Check balance at 22: right subtree height = 1, left = 2. May need restructuring.
- Key concept: AVL tree balance checking — insert/delete operations and rotation triggers.

### Problem 5

This problem is worth 5 points.

Insert 6, 14, 11 into the red-black tree. Each insertion may trigger recoloring or rotations. After all three insertions, check which statements about node colors and positions are correct.
- Key concept: red-black tree sequential insertions — tracking recoloring and rotations.

### Problem 6

This problem is worth 5 points.

(A) **True.** Maximum data items in m-way search tree of height $h$: each node has at most $m-1$ keys, total nodes at most $\sum_{i=0}^{h-1} m^i = (m^h-1)/(m-1)$, total keys at most $(m-1) \times (m^h-1)/(m-1) = m^h - 1$. ✓
(B) **False.** 2-3-4 tree is a B-tree of order 4, not 5.
(C) **True.** B-tree of order $m$: internal nodes (except root) have at least $\lceil m/2 \rceil$ children. ✓
(D) **True.** All external (leaf) nodes in a B-tree are at the same level. ✓
(E) **False.** Red-black tree corresponds to 2-3-4 tree, not 2-3 tree.
- Key concept: B-tree and m-way search tree properties — order, node capacity, and correspondence.

### Problem 7

This problem is worth 5 points.

Verify each insertion order produces the given hash table. $h(k) = k \% 10$. Items: 22→2, 43→3, 14→4, 32→2(→3→4→5), 26→6, 53→3(→4→5→6→7). Check each option against the final table to find valid insertion orders.
- Key concept: hash table linear probing — verifying insertion order from final table state.

### Problem 8

This problem is worth 5 points.

Min-Max heap: even levels are min-levels, odd levels are max-levels. Array: [3, 20, 13, 6, 5, 4, 8, 10, 15, 12, 9, 7]. After swapping each pair, verify the min-max heap property holds. The swap that violates the property is NOT a valid min-max heap.
- Key concept: min-max heap property verification — alternating min/max levels.

### Problem 9

This problem is worth 5 points.

Insert 0110 into Patricia trie: find the first differing bit with the key found during search. Create a new node at that bit position. Then delete 1100: find 1100, remove it, and adjust pointers. After both operations, determine what node 1101 points to.
- Key concept: Patricia trie insertion and deletion — bit-position-based branching and pointer adjustment.

### Problem 10

This problem is worth 5 points.

(A) **True.** Tries never compare full keys — they branch by individual bits/characters, requiring fewer comparisons than digital search trees.
(B) **True.** Compressed tries eliminate single-child internal nodes, reducing storage.
(C) **True.** Patricia further compresses by skipping non-branching bit positions.
(D) **False.** Patricia has less storage overhead than digital search trees (no redundant nodes).
(E) **True.** Patricia requires fewer comparisons than digital search trees.
- Key concept: comparison of trie variants — tries, compressed tries, Patricia, and digital search trees.

**Part II. 演算法 (50%)**

### Problem 11

This problem is worth 10 points.

$T(n) = 27T(n/3) + \Theta(n^3/\lg n)$. Master Theorem: $a=27, b=3, \log_3 27 = 3$. $f(n) = n^3/\lg n$. Compare with $n^3$: $f(n)/n^3 = 1/\lg n \to 0$, but not polynomially smaller. Master Theorem does not directly apply (gap between Case 1 and Case 2). By substitution or extended analysis: $T(n) = \Theta(n^3 \lg\lg n)$.
- Key concept: Master Theorem gap case — logarithmic difference from critical exponent.

### Problem 12

This problem is worth 10 points.

Matrix chain multiplication DP. Fill in: (a) $j = i + l - 1$; (b) $q = m[i,k] + m[k+1,j] + p_{i-1} \cdot p_k \cdot p_j$; (c) $s[i,j] = k$.
- Key concept: matrix chain order DP — recurrence for minimum scalar multiplications.

### Problem 13

This problem is worth 10 points.

Build max-heap from $S = [5,13,2,25,7,17,20,8,4]$ using bottom-up heapify. Then repeatedly extract max and place at end. Show the array state after each extraction until fully sorted in ascending order.
- Key concept: heapsort — build-max-heap followed by repeated extract-max.

### Problem 14

This problem is worth 10 points.

Optimal BST with 7 keys: use DP table. $e[i,j] = \min_{r=i}^{j}\{e[i,r-1] + e[r+1,j] + w[i,j]\}$ where $w[i,j] = \sum_{k=i}^{j} p_k + \sum_{k=i-1}^{j} q_k$. Fill tables for all chain lengths $l = 1$ to $7$. Report minimum cost $e[1,7]$ and the root structure from table $s$.
- Key concept: optimal BST construction — $O(n^3)$ DP for expected search cost minimization.

### Problem 15

This problem is worth 10 points.

Maximum flow from $s$ to $t$: apply Ford-Fulkerson method (BFS-based Edmonds-Karp). Find augmenting paths from $s$ to $t$, compute bottleneck capacity along each path, update residual graph. Repeat until no augmenting path exists. Sum of flows on edges leaving $s$ (or entering $t$) gives maximum flow.
- Key concept: maximum flow — Ford-Fulkerson/Edmonds-Karp algorithm with augmenting paths.

## 113

**Part I. 資料結構 (50%)**

### Problem 1

This problem is worth 5 points total (2 subparts).

- **(i)** [3%] Insert keys 15,8,13,18,17,6,11,14,5 into empty BST. 15 is root. 8→left of 15. 13→right of 8. 18→right of 15. 17→left of 18. 6→left of 8. 11→left of 13. 14→right of 13. 5→left of 6. Level of 5: 15(1)→8(2)→6(3)→5(4). Answer: **(D) 4**.
  - Key concept: BST insertion — following BST property to determine node placement and level.

- **(ii)** [2%] Delete 15: replace with largest in left subtree (14) or smallest in right subtree (17). Both are valid. Answers: **(B) 14** or **(C) 17**.
  - Key concept: BST deletion of internal node — inorder predecessor or successor replacement.

### Problem 2

This problem is worth 5 points total (3 subparts).

- **(i)** [1%] Build max-heap from sequence 20,5,10,18,4,22,11,32,21. After heapify: 32 at root, then arrange. Level of 5: trace the heap structure.
  - Key concept: max-heap construction — bottom-up heapify for sequential insertions.

- **(ii)** [1%] Build min-heap: 4 at root. Check each statement about node positions.
  - Key concept: min-heap construction and property verification.

- **(iii)** [3%] Symmetric min-max heap: root's left subtree is a min-heap, right subtree is a max-heap (or vice versa). Build and verify statements about node positions and deletion behavior.
  - Key concept: symmetric min-max heap — combined min-heap and max-heap structure.

### Problem 3

This problem is worth 5 points total (2 subparts).

- **(i)** [2%] Reconstruct binary tree from postfix (postorder) and infix (inorder). Last element of postorder (Q) is root. Find Q in inorder: left subtree = {Z,Y,P,L,C}, right subtree = {K,R,W,S}. Recurse. Then perform level-order traversal on the reconstructed tree.
  - Key concept: tree reconstruction from postorder and inorder traversals.

- **(ii)** [3%] Preorder traversal of the reconstructed tree: visit root first, then left subtree, then right subtree recursively.
  - Key concept: preorder traversal from reconstructed tree.

### Problem 4

This problem is worth 5 points total (2 subparts).

- **(i)** [2%] Verify DFS/BFS traversal claims against the graph structure. Check adjacency for each traversal step. For DFS: after visiting A,C,I,N,M,H, the stack-based backtracking determines the next vertex.
  - Key concept: DFS and BFS traversal order verification on weighted graph.

- **(ii)** [3%] Maximum cost spanning tree: negate all edge weights and find MST (or greedily select largest edges). Sum the maximum-weight spanning tree edges. Check each statement about the resulting tree.
  - Key concept: maximum cost spanning tree — greedy selection of largest edges without cycles.

### Problem 5

This problem is worth 5 points total (2 subparts).

- **(i)** [3%] Compute hash values: $f_1(k) = 3k \bmod 15$, $f_2(k) = (2k+1) \bmod 15$, $f_3(k) = k^2 \bmod 15$. For each query/insert/delete, check the corresponding bit positions. Verify each statement.
  - Key concept: Bloom filter operations — insert, query, and deletion limitations.

- **(ii)** [2%] Optimal number of hash functions: $k_{opt} = (m/n) \ln 2$ where $m=15$ (bits), $n=5$ (elements). $k_{opt} = 3 \ln 2 \approx 2.08$. Answer: **(C) $3\ln(2)$**.
  - Key concept: optimal Bloom filter hash function count — minimizes false positive rate.

### Problem 6

This problem is worth 5 points.

Delete minimum (4) from height-based leftist tree: remove root, merge left and right subtrees. Leftist tree merge: always merge along right paths, then swap children if right path is longer than left. Check each statement about the resulting tree structure.
- Key concept: leftist tree delete-min — merge left and right subtrees maintaining leftist property.

### Problem 7

This problem is worth 5 points.

Perform Fibonacci heap operations sequentially: decrease key 14 by 5 (new key 9), decrease key 21 by 14 (new key 7), delete key 12, insert key 14, delete-min. Track cascading cuts, tree consolidation, and root list changes. Verify each statement about the resulting F-heap structure.
- Key concept: Fibonacci heap operations — decrease-key with cascading cuts, delete, and consolidation.

### Problem 8

This problem is worth 5 points.

Insert 62 into the red-black tree: 62 goes between 47 and 70, specifically as right child of 47. 47 is red (child of 36, which is black child of 70). Inserting 62 as red child of red 47 → red-red violation. Uncle 90's child 81 is red... Apply fix-up: recolor or rotate. Compare with each answer option.
- Key concept: red-black tree insertion fix-up — case analysis for recoloring and rotations.

### Problem 9

This problem is worth 5 points.

B+-tree operations: (A) Insert 49 — goes into node N [45,46,48]. Adding 49 makes [45,46,48,49] which overflows (capacity 3). Split: [45,46] and [48,49], promote 48 to parent D. Check if 45 moves to root. (B) Delete 72 — from node P [72,73]. After deletion [73]. Check if node E changes. Verify each statement.
- Key concept: B+-tree insertion with overflow/split and deletion with underflow/merge.

### Problem 10

This problem is worth 5 points.

Convert binary trie to Patricia: compress paths with single children into single edges labeled with the bit position that differs. Internal nodes only exist where branching occurs. Compare the given binary trie structure with each Patricia option to find the correct transformation.
- Key concept: binary trie to Patricia conversion — path compression at non-branching nodes.

**Part II. 演算法 (50%)**

### Problem 11

This problem is worth 10 points.

Procedure F computes matrix multiplication with 7 recursive calls (not 8 — $C_{22}$ only has one call instead of two). Recurrence: $T(n) = 7T(n/2) + \Theta(n^2)$. Master Theorem: $a=7, b=2, \log_2 7 \approx 2.807$. $f(n) = n^2 = O(n^{2.807-\epsilon})$. Case 1: $T(n) = \Theta(n^{\log_2 7})$.
- Key concept: Strassen-like matrix multiplication — 7 recursive subproblems give $\Theta(n^{\log_2 7})$.

### Problem 12

This problem is worth 10 points.

Neural network computation is equivalent to matrix chain multiplication. Weight matrices: $W^1$ is $P_1 \times P_2$, $W^2$ is $P_2 \times P_3$, etc. Input vector is $P_1 \times 1$. Total multiplications = optimal parenthesization of $W^5 \cdot W^4 \cdot W^3 \cdot W^2 \cdot W^1 \cdot v$. With dimensions $\langle 10,6,12,5,50,3 \rangle$, apply matrix chain order DP to find minimum multiplications.
- Key concept: neural network forward pass as matrix chain multiplication — optimize computation order.

### Problem 13

This problem is worth 10 points.

LCS recursive formula:
$$c[i,j] = \begin{cases} 0 & \text{if } i=0 \text{ or } j=0 \\ c[i-1,j-1]+1 & \text{if } i,j>0 \text{ and } x_i = y_j \\ \max(c[i-1,j],\ c[i,j-1]) & \text{if } i,j>0 \text{ and } x_i \ne y_j \end{cases}$$
- Key concept: LCS recurrence — match extends diagonal, mismatch takes max of excluding either element.

### Problem 14

This problem is worth 10 points.

Lower bound for comparison-based sorting: $\Omega(n \log n)$. The decision tree has $n!$ leaves (all permutations). A binary tree with $n!$ leaves has height $\ge \log_2(n!) = \Theta(n \log n)$ (by Stirling's approximation). Therefore any comparison-based sort requires $\Omega(n \log n)$ comparisons in the worst case.
- Key concept: comparison sort lower bound — decision tree argument gives $\Omega(n \log n)$.

### Problem 15

This problem is worth 10 points.

Build constraint graph with edges for each inequality $x_i - x_j \le b_{ij}$: add edge $(j \to i)$ with weight $b_{ij}$. Add source $v_0$ with zero-weight edges to all vertices. Run Bellman-Ford from $v_0$. Check for negative-weight cycles. If none, solution: $x_i = \delta(v_0, x_i)$. The system has constraints: $x_1-x_3 \le 1$, $x_2-x_3 \le 4$, $x_4-x_5 \le -2$, $x_3-x_4 \le 4$, $x_5-x_1 \le 3$, $x_4-x_2 \le -7$, $x_1-x_2 \le -2$, $x_5-x_3 \le 1$. Check: $x_1-x_2 \le -2$ and $x_2-x_3 \le 4$ gives $x_1-x_3 \le 2$ (consistent with $x_1-x_3 \le 1$). Continue checking for negative cycles to determine feasibility.
- Key concept: difference constraints — constraint graph construction and Bellman-Ford feasibility check.
