## 108

### Problem 1

This problem is worth 25 points total (11 subparts).

- **1.1** [2%] `cout` is **(a) an object** — it is an instance of the `ostream` class defined in `<iostream>`.
  - Key concept: C++ I/O stream objects — `cout`, `cin` are global objects, not functions or operators.

- **1.2** [2%] Answer: **(b) `bool errorFlag = true;`**. The `bool` type with lowercase `true`/`false` literals is standard C++.
  - Key concept: C++ Boolean type — `bool` with `true`/`false` keywords.

- **1.3** [2%] Answer: **(a) only in the current class.** Private members are accessible only within the class itself (not derived classes or external code).
  - Key concept: C++ access specifiers — private restricts access to the declaring class only.

- **1.4** [2%] Answer: **(b) functions inc1 and inc3.** `inc1` passes by reference and increments directly; `inc3` dereferences pointer and increments the pointed-to value. `inc2` increments the pointer itself; `inc4` won't compile (modifying const reference).
  - Key concept: pass-by-reference vs. pass-by-pointer — both can modify the original, but syntax differs.

- **1.5** [2%] Answer: **(c) `delete [] ptr;`**. Array new requires array delete to properly deallocate.
  - Key concept: C++ `new[]`/`delete[]` pairing — array allocation must use array deallocation.

- **1.6** [2%] Answer: **(e) none is correct** — actually **(a)** only `sum1` is correct. Default arguments must go right-to-left without gaps; `sum2` and `sum3` violate this rule. The correct answer depends on reading: only `sum1` compiles.
  - Key concept: C++ default arguments — must be trailing (right-to-left, no gaps).

- **1.7** [2%] Answer: **(d) all of the above.** Copy constructor is invoked for initialization from another object, pass-by-value, and return-by-value.
  - Key concept: copy constructor invocation — initialization, pass-by-value, return-by-value.

- **1.8** [2%] Answer: **(d) none of the above.** Correct form is `virtual void f() = 0;` (no function body with `= 0`). Options (a)-(c) all have bodies `{ }` which is incorrect for pure virtual declaration.
  - Key concept: pure virtual function syntax — `= 0` with no body in the declaration.

- **1.9** [3%] `cin >> i >> d >> c >> str`: reads 30 into `i`, 40 into `d` (as 40.0), 'K' into `c`, "ing" into `str` (stops at space). Output: **(e) ing**.
  - Key concept: C++ stream extraction — skips whitespace, reads word-by-word for strings.

- **1.10** [3%] `f(x, x)`: both p1 and p2 reference x. `p1 = p2 + 14` → but p2 IS x, so after `p1 = x + 14`, x becomes `2 + 14 = 16`. Then `y = x = 16`, `f(y,y)`: y becomes `y + 14 = 30`. But we print `x` which is still 16. Answer: **(d) it prints the value 16**.
  - Key concept: reference aliasing — both parameters reference the same variable.

- **1.11** [3%] Answer: **(a)**. After a failed extraction, the stream enters a fail state; checking `if(iFile)` detects this.
  - Key concept: C++ stream error detection — stream evaluates to false on extraction failure.

### Problem 2

This problem is worth 8 points total (2 subparts).

- **(a)** [3%] Insert 65, 50, 80, 70, 60, 62, 90, 10 into BST. Build tree following BST rules; for each node compute `leftSize` = (number of nodes in left subtree) + 1.
  - Key concept: BST construction with augmented data — maintaining subtree size information.

- **(b)** [5%] To find k-th smallest: at root, compare k with `leftSize`. If k == leftSize, root is the answer. If k < leftSize, recurse into left subtree. If k > leftSize, recurse into right subtree with k' = k - leftSize. This runs in O(log n) for balanced trees.
  - Key concept: order-statistic tree — O(log n) k-th element selection using augmented subtree sizes.

### Problem 3

This problem is worth 6 points total (2 subparts).

- **(a)** [3%] Build max heap by inserting 7, 16, 49, 82, 5, 31, 6, 2, 44 one by one (bubble up after each insert). Final max heap: 82, 44, 49, 16, 5, 31, 6, 2, 7.
  - Key concept: max heap construction via successive insertions — bubble-up (sift-up) after each insert.

- **(b)** [3%] Perform 5 iterations of heap sort: swap root with last, reduce heap size, heapify. After 5 swaps, the sorted portion has the 5 largest elements, and the remaining heap has the rest.
  - Key concept: heap sort process — repeated extract-max builds sorted array from the end.

### Problem 4

This problem is worth 4 points total (2 subparts).

- **(a)** [2%] Dijkstra's from vertex 0: since edges go FROM vertex 4 and 3, vertex 0 has no outgoing edges in the given graph. Process vertices by shortest distance from source. Order depends on actual edge connectivity from vertex 0.
  - Key concept: Dijkstra's algorithm — greedy selection of minimum-distance unvisited vertex.

- **(b)** [2%] BFS from vertex 0: visit all neighbors at distance 1, then distance 2, etc. When multiple choices exist, select the smallest edge (vertex) first.
  - Key concept: BFS traversal — level-by-level exploration with tie-breaking.

### Problem 5

This problem is worth 2 points.

Hash function: key % 17. Insert keys 6, 12, 34, 29, 28, 11, 23, 7, 0, 33 with linear probing. For each key, compute hash, if slot occupied probe next slots linearly until empty slot found.
- Key concept: hash table with linear probing — collision resolution by sequential search.

### Problem 6

This problem is worth 5 points total (2 subparts).

- **(a)** [2%] Maximum nodes in a k-ary tree of height h: $(k^{h+1} - 1)/(k - 1)$ (full k-ary tree, counting root at height 0).
  - Key concept: k-ary tree maximum size — geometric series sum for complete tree.

- **(b)** [3%] Prove by induction: level $i$ has at most $k^i$ nodes (0-indexed). Sum from $i=0$ to $h$: $\sum_{i=0}^{h} k^i = (k^{h+1}-1)/(k-1)$.
  - Key concept: geometric series proof — induction on tree levels.

### Problem 7

This problem is worth 6 points total (3 subparts).

- **(a)** [2%] **False.** Finding LCS of an arbitrary number of sequences is NP-hard (only 2-sequence LCS is polynomial O(mn)).
  - Key concept: multi-sequence LCS — NP-hard generalization of the 2-sequence polynomial case.

- **(b)** [2%] **True.** Longest simple path in general graphs is NP-hard (even with non-negative weights), as it can encode Hamiltonian path.
  - Key concept: longest path problem — NP-hard due to reduction from Hamiltonian path.

- **(c)** [2%] **False.** BFS does not classify all edge types (cannot identify cross edges vs. back edges in directed graphs). DFS is the algorithm that classifies edges into tree, back, forward, and cross edges.
  - Key concept: edge classification — DFS (not BFS) provides complete edge classification.

### Problem 8

This problem is worth 5 points.

Stable sorting algorithms: **(a) Counting sort, (c) Mergesort, (d) Insertion sort.** Quicksort and Heapsort are NOT stable (they may reorder equal elements).
- Key concept: sorting stability — algorithms that preserve relative order of equal elements.

### Problem 9

This problem is worth 8 points.

- **(a)** Bubble sort: O(n²) worst/average — **NOT** asymptotically optimal (comparison-based lower bound is Ω(n log n)).
- **(b)** Quicksort: O(n log n) average but O(n²) worst — **NOT** asymptotically optimal (worst case exceeds lower bound).
- **(c)** Mergesort: O(n log n) worst case — **asymptotically optimal** (matches Ω(n log n) lower bound).
- **(d)** Heapsort: O(n log n) worst case — **asymptotically optimal**.
- Key concept: asymptotic optimality — matching the Ω(n log n) comparison-based sorting lower bound in worst case.

### Problem 10

This problem is worth 1 point.

The matrix representation where entry (v, u) stores the edge weight from u to v is called an **adjacency matrix** (or specifically a **weighted adjacency matrix** / **cost matrix**).
- Key concept: graph representation — weighted adjacency matrix stores edge weights.

### Problem 11

This problem is worth 4 points.

Johnson's algorithm reweights edges to eliminate negative weights: run Bellman-Ford from a new source to get vertex potentials h(v), then reweight each edge (u,v): w'(u,v) = w(u,v) + h(u) - h(v). After reweighting, all edges become non-negative, then run Dijkstra from each vertex.
- Key concept: Johnson's algorithm — reweighting with Bellman-Ford potentials to enable Dijkstra.

### Problem 12

This problem is worth 10 points total (2 subparts).

- **(a)** [3%] Output: ` or C`. Since `x=2 > y=10` is false, the outer `if` body (including the inner `if-else`) is skipped entirely. Then `printf(" or C")` executes unconditionally. Note: the `else` binds to the nearest `if` (dangling else problem).
  - Key concept: dangling else — `else` associates with the nearest unmatched `if`.

- **(b)** [7%] `sizeof(struct node)`: on 64-bit system with alignment: char* (8) + double (8) + long long (8) = 24 bytes (with padding considerations). `sizeof(short)` = 2. After loops, `p` points past the first 10 chars "abcdefghij" and copies 3 chars from `q` (starting at text[3]="def"). String `q` now points to text[6]. `printf("%s", q)` prints from text[6] onward: "ghijdef".
  - Key concept: pointer arithmetic, struct alignment, and string manipulation in C.

### Problem 13

This problem is worth 7 points.

Errors to fix: (1) `q` is uninitialized — should `strcpy` to allocated memory or use `text` directly. (2) Loop syntax uses commas instead of semicolons. (3) `strcpy` should be `strcat` or use pointer arithmetic with `strcpy`. (4) `text += strlen(q)` — can't increment array name; use a pointer instead. (5) `int *p` is unused/wrong type.
- Key concept: C string manipulation bugs — uninitialized pointers, array vs. pointer, strcat vs. strcpy.

### Problem 14

This problem is worth 8 points total (2 subparts).

- **(a)** [2%] Error: array initialization uses `()` instead of `{}`. Correct: `int A[] = {0, 1, 2, 3, 4, 5};`. Note: `(0, 1, 2, 3, 4, 5)` is the comma operator, evaluating to just `5`.
  - Key concept: C array initialization syntax — use braces `{}`, not parentheses.

- **(b)** [6%] After fixing initialization to `{0,1,2,3,4,5}`: `p = A` (points to A[0] as char*), `p = &A[*(p+2)]` — complex pointer arithmetic. Then loop modifies 3 elements starting at computed position.
  - Key concept: pointer arithmetic with type casting — char pointer on int array.

## 109

### Problem 1

This problem is worth 5 points total (3 subparts).

- **1.** [1%] **True.** A class can have multiple derived (child) classes — this is basic inheritance.
  - Key concept: class hierarchy — one base class, multiple derived classes.

- **2.** [2%] **False.** A class can have only ONE destructor (no overloading, no parameters).
  - Key concept: destructors — exactly one per class, no overloading.

- **3.** [2%] **True.** If no constructor is defined, the compiler provides a default constructor (no-arg).
  - Key concept: implicit default constructor — compiler-generated when none is explicitly defined.

### Problem 4

This problem is worth 20 points.

Bugs in bubble sort: Line 5: parameters should be references (`int &a, int &b`). Line 8: should be `b = temp` (not `temp = b`). Line 10: `vector<int>&` (missing template parameter and reference). Line 11: `i--` (not `i++`). Line 12: `int j = 0` (missing type). Line 13: `>` (not `<` — ascending order). Line 18: `intArray + 10` (not 9, for all 10 elements). Line 20: `BubbleSort(intVector)` (not `&intVector`). Line 22: `<` (not `<=`). Line 23: `<<` (not `>>`).
- Key concept: C++ debugging — pass-by-reference, vector template syntax, operator usage, loop bounds.

### Problem 5

This problem is worth 10 points.

Declare a struct with `char name[32]` and a `next` pointer. Function takes head pointer and name string. Allocate new node, copy name with `strcpy`, set new node's link to current head, return new node as new head. This is standard linked list prepend (insert at front).
- Key concept: linked list insertion at head — allocate, copy data, repoint head.

### Problem 6

This problem is worth 5 points.

Complete the swap function using pointers: `void swap(int *a, int *b)` with body swapping `*a` and `*b` via temp variable. Call: `swap(&x, &y)`.
- Key concept: C swap via pointers — pass addresses, dereference to exchange values.

### Problem 7

This problem is worth 10 points.

Read lines with `fgets()`, track longest line seen so far. Use `strlen()` to compare lengths, store the longest in a buffer. After reading all input (until `fgets` returns NULL), print the stored longest line. Remove trailing newline from `fgets` output for accurate comparison.
- Key concept: fgets() line reading — track maximum length string from input.

### Problem 8

This problem is worth 4 points total (2 subparts).

- **(a)** [2%] `a*(b+c/d)-e` → postfix: `a b c d / + * e -`.
  - Key concept: infix to postfix conversion — operator precedence and stack-based algorithm.

- **(b)** [2%] `(a+c)*b+d*e` → postfix: `a c + b * d e * +`.
  - Key concept: infix to postfix — parentheses override default precedence.

### Problem 9

This problem is worth 4 points.

Number of distinct binary trees with n nodes = Catalan number $C_n = \frac{1}{n+1}\binom{2n}{n}$. For n=2: 2, n=3: 5, n=4: 14, n=5: 42.
- Key concept: Catalan numbers — count distinct binary tree structures.

### Problem 10

This problem is worth 8 points.

Insert 50, 10, 80, 90, 70, 60, 65, 62 into AVL tree: perform rotations (single/double) when balance factor exceeds ±1. For Red-Black tree: maintain properties (root black, red nodes have black children, equal black-height) with recoloring and rotations after each insert.
- Key concept: AVL and Red-Black tree insertion — balancing via rotations and recoloring.

### Problem 11

This problem is worth 4 points total (2 subparts).

- **(a)** [2%] Pattern `abaabaab`: failure function computes longest proper prefix that is also a suffix. Result: [-1, -1, 0, 0, 1, 2, 3, 4] (0-indexed, using the given algorithm).
  - Key concept: KMP failure function — longest prefix-suffix matching.

- **(b)** [2%] Pattern `abcababcabc`: compute failure function similarly. Result: [-1, -1, -1, 0, 1, -1, 0, 1, 2, 3, 4].
  - Key concept: KMP failure function computation — iterative prefix matching.

### Problem 12

This problem is worth 5 points.

Quicksort with first element as pivot: 55 is pivot, partition → elements ≤55 on left, >55 on right. Show sequence after each partition step. Continue recursively on each sublist.
- Key concept: quicksort partition — trace through each recursive level showing element rearrangement.

### Problem 13

This problem is worth 18 points total (6 subparts, 3% each).

- **(1)** [3%] **False.** Counting sort runs in O(n + k) where k is the range of values. If k is exponential in n (e.g., values up to $2^n$), it's not polynomial in input size n.
  - Key concept: counting sort complexity — depends on value range, not just input size.

- **(2)** [3%] **True.** Heap sort runs in O(n log n) which is polynomial in n.
  - Key concept: heap sort — O(n log n) guaranteed, polynomial time.

- **(3)** [3%] **True.** DFS with adjacency matrix visits all V vertices and checks all V neighbors for each → Θ(V²).
  - Key concept: DFS time complexity — O(V+E) with adjacency list, O(V²) with adjacency matrix.

- **(4)** [3%] **False.** Not every DP corresponds to shortest path. Some DPs use max operations or more complex combining functions that don't map to shortest path.
  - Key concept: DP and DAG relationship — subproblem DAG structure doesn't always map to shortest path.

- **(5)** [3%] **False.** If X reduces TO an NP-hard problem, that tells us nothing about X's hardness. X must have an NP-hard problem reduce TO it to be NP-hard.
  - Key concept: reduction direction — problem must have NP-hard problems reduce TO it.

- **(6)** [3%] **False.** Using DFS for augmenting paths in Ford-Fulkerson can lead to poor performance (potentially non-terminating with irrational capacities). BFS (Edmonds-Karp) guarantees O(VE²).
  - Key concept: Ford-Fulkerson path selection — DFS doesn't improve complexity; BFS (Edmonds-Karp) does.

### Problem 14

This problem is worth 7 points total (3 subparts).

- **(a)** [3%] Max flow = min cut. Find augmenting paths: S→A→C→T (capacity min(7,4,9)=4), S→B→D→T (min(6,3,5)=3), S→A→D→T (min(7-4,2,5-3)=2), S→B→A→C→T (min(6-3,2,4-4,9-4)=...). Max flow = **9** with min cut {S→A, S→B} capacity 7+6=13... Recompute carefully using augmenting paths.
  - Key concept: max-flow min-cut theorem — Ford-Fulkerson augmenting path method.

- **(b)** [2%] Draw residual graph: for each edge with flow f and capacity c, add forward edge (c-f) and backward edge (f). Mark vertices reachable from S and vertices that can reach T.
  - Key concept: residual graph — forward/backward edges representing remaining capacity.

- **(c)** [2%] Bottleneck edges: edges on ALL min-cuts, where increasing capacity increases max flow. Identify by checking if edge is saturated in all max-flow solutions.
  - Key concept: bottleneck edges — edges whose capacity limits the maximum flow.

## 110

### Problem 1

This problem is worth 10 points total.

- **1.1(a)** [2%] `int &* a1;` — **No**, cannot have pointer to reference. References are not objects with addresses.
  - Key concept: C++ type legality — pointer-to-reference is illegal.

- **1.1(b)** [2%] `int *& a2;` — **No** (won't compile without initialization), but the TYPE is valid (reference to pointer). As a declaration it needs an initializer.
  - Key concept: reference-to-pointer — valid type but requires initialization.

- **1.2(a)** [2%] `const int** b1 = &cip;` — **Compiles.** `cip` is `const int*`, `&cip` is `const int**` which matches.
  - Key concept: pointer-to-pointer const correctness.

- **1.2(b)** [2%] `int* const * b2 = &cip;` — **Violates const-ness.** Would allow modifying the pointed-to int through the const pointer.
  - Key concept: const pointer type mismatch — can't drop const qualification.

- **1.2(c)** [2%] `int ** const b3 = &cip;` — **Violates const-ness.** Drops the const from the pointed-to type.
  - Key concept: const correctness in multi-level pointers.

### Problem 2

This problem is worth 15 points total.

- **(a)** [3%] `A* ap = new B;` → calls `A()` constructor (B inherits from A), then `B()` constructor. `delete ap;` → calls `~A()` only (no virtual destructor in A — undefined behavior, but most likely just A's destructor). Functions called: **A::A, B::B** (construction), then **A::~A** (deletion).
  - Key concept: virtual destructor importance — non-virtual destructor with base pointer causes incomplete cleanup.

- **(b)** [12%]
  - `bp1->f1()`: B::f1 (B's own f1, non-virtual override). Output: **"B::f1, "**
  - `bp1->f2()`: A::f2 (virtual, b is type B which doesn't override f2). Output: **"A::f2, "**
  - `bp2->f2()`: C::f2 (virtual dispatch to C's override since object is C). C::f2 calls f3() → B::f3. Output: **"B::f3, C::f2, "**
  - `bp2->f3()`: B::f3 (virtual, C doesn't override f3). Output: **"B::f3, "**
  - `ap1->f1()`: A::f1 (f1 not virtual in A, static dispatch). A::f1 calls f2() → virtual → C::f2 → calls f3() → B::f3. Output: **"B::f3, C::f2, A::f1, "**
  - `ap1->f3()`: **Compiler error** — f3 not declared in class A.
  - Key concept: virtual dispatch, static vs. dynamic binding, inheritance method resolution.

### Problem 3

This problem is worth 9 points total (4 subparts).

- **3.1** [2%] DFS from A (alphabetical order): A→B→D→E→(back to A)→C→F→(E already visited via back edge)→G. Wait — must trace carefully with alphabetical order. Answer depends on exact DFS traversal with smallest vertex first.
  - Key concept: DFS spanning tree construction — vertex ordering affects tree structure.

- **3.2** [2%] Assign DFS numbers in order of first visit during depth-first traversal starting from A.
  - Key concept: DFS numbering — sequential assignment during traversal.

- **3.3** [3%] Compute low(v) = min(dfn(v), min of dfn(w) for all back edges (v,w), min of low(child) for all tree edges). Low values detect connections to ancestors.
  - Key concept: low function — minimum reachable DFS number through subtree and back edges.

- **3.4** [2%] Articulation points: root is articulation point if it has ≥2 children in DFS tree. Non-root vertex u is articulation point if some child v has low(v) ≥ dfn(u).
  - Key concept: articulation point detection — using DFS numbers and low values.

### Problem 4

This problem is worth 3 points.

Reconstruct binary tree from in-order and post-order: last element of post-order (A) is root. Find A in in-order to split left/right subtrees. Recursively build. Then output pre-order (root, left, right).
- Key concept: binary tree reconstruction — use post-order for roots, in-order for subtree division.

### Problem 5

This problem is worth 8 points total (3 subparts).

- **5.1** [3%] Dijkstra's from A: select minimum-distance unvisited vertex at each step. With negative edges, Dijkstra's may give incorrect results — but select answer based on greedy execution.
  - Key concept: Dijkstra's algorithm execution — greedy vertex selection (unreliable with negative edges).

- **5.2** [3%] Shortest path costs from A to each vertex after running Dijkstra's. Note: Dijkstra's with negative edges may produce incorrect shortest paths.
  - Key concept: single-source shortest path distances.

- **5.3** [2%] Identify the actual shortest path from A to D based on computed distances.
  - Key concept: shortest path reconstruction — backtrack through predecessor information.

### Problem 6

This problem is worth 5 points.

Pattern "acacabacacabacacac" (18 chars). Compute KMP failure function values at positions 0, 3, 5, 10, 13, 17. Use the definition: f(j) = largest k < j such that prefix of length k+1 equals suffix ending at j.
- Key concept: KMP failure function computation — longest proper prefix that is also suffix.

### Problem 7

This problem is worth 4 points.

$T(n) = T(n/4) + T(n/5) + 6n$. Guess $T(n) = O(n)$. Verify by substitution: $T(n) \le cn/4 + cn/5 + 6n = cn(1/4 + 1/5) + 6n = cn(9/20) + 6n \le cn$ when $c \ge 6/(1 - 9/20) = 6/(11/20) = 120/11 \approx 11$. So $T(n) = O(n)$.
- Key concept: substitution method — guess and verify recurrence bounds.

### Problem 8

This problem is worth 4 points.

$T(n) = 4T(n/2) + n^2$. By Master theorem: $a=4, b=2, f(n)=n^2$, $n^{\log_b a} = n^2$. Case 2: $f(n) = \Theta(n^{\log_b a})$, so $T(n) = \Theta(n^2 \log n)$. For lower bound: $T(n) = \Omega(n^2 \log n)$.
- Key concept: Master theorem Case 2 — $f(n) = \Theta(n^{\log_b a})$ gives extra log factor.

### Problem 9

This problem is worth 3 points.

The statement "every comparison-based sorting algorithm has time complexity Ω(n lg n)" is **true for worst case** but **false in general** — best case of insertion sort is O(n). However, the **worst-case lower bound** is Ω(n log n) for comparison-based sorting, provable via decision tree argument (n! leaves require height ≥ log(n!) = Ω(n log n)).
- Key concept: comparison-based sorting lower bound — decision tree argument proves Ω(n log n) worst case.

### Problem 10

This problem is worth 3 points.

Optimal substructure does NOT imply locally optimal = globally optimal. That's the greedy-choice property. Dynamic programming has optimal substructure but may not have greedy-choice property (hence needs full enumeration of subproblems). Counter-example: 0-1 knapsack has optimal substructure but greedy doesn't give optimal solution.
- Key concept: optimal substructure vs. greedy-choice property — DP doesn't guarantee greedy optimality.

### Problem 11

This problem is worth 5 points.

For a DAG with negative edges, Bellman-Ford runs in O(VE). Johnson's algorithm: O(VE + V²log V) (Bellman-Ford for reweighting + V × Dijkstra). If the graph is a DAG, single-source shortest path can be solved in O(V+E) via topological sort + relaxation — faster than both Johnson's and Bellman-Ford.
- Key concept: DAG shortest paths — topological order enables O(V+E) solution.

### Problem 12

This problem is worth 6 points.

- **(a)** Fractional knapsack: (1) optimal substructure ✓; (2) greedy-choice ✓; (3) overlapping subproblems — not necessarily (greedy solves directly).
- **(b)** 0-1 knapsack: (1) optimal substructure ✓; (2) greedy-choice ✗ (greedy fails); (3) overlapping subproblems ✓ (hence solved by DP).
- Key concept: knapsack problem properties — fractional allows greedy; 0-1 requires DP.

### Problem 13

This problem is worth 10 points.

Errors: (1) `scanf("%d", X)` → should be `scanf("%d", &X)` (need address). (2) `X&2 == 0` → operator precedence: `==` binds tighter than `&`. Should be `(X & 1) == 0` (also should be `&1` not `&2` for even/odd check, since bit 0 determines parity).
- Key concept: C operator precedence and scanf — `&` for address, bitwise AND precedence, parity check.

### Problem 14

This problem is worth 10 points.

Declare: `typedef struct Node { char key[32]; struct Node *left, *right; } Node;`. Function: traverse BST comparing key with `strcmp()`. If match return pointer; if key < node go left; if key > node go right; if NULL return NULL.
- Key concept: BST search with string keys — use strcmp for comparison, return node pointer or NULL.

### Problem 15

This problem is worth 5 points total (2 subparts).

- **(a)** [2%] `strcmp(const char *s1, const char *s2)`: compares two strings lexicographically. Returns 0 if equal, negative if s1 < s2, positive if s1 > s2. Parameters: two const char pointers. Return: int.
  - Key concept: strcmp — lexicographic string comparison returning int.

- **(b)** [3%] `strdup(const char *s)`: allocates memory (via malloc) and copies the string into it. Returns a pointer to the new string (caller must free). Parameter: const char pointer. Return: char pointer (dynamically allocated copy).
  - Key concept: strdup — malloc + strcpy combined, returns heap-allocated string copy.

## 111

### Problem 1

This problem is worth 2 points.

Binary search of 2000 elements: max comparisons = $\lceil\log_2(2000)\rceil + 1 = 11$. Answer: **(c) 11**.
- Key concept: binary search maximum comparisons — ⌈log₂(n)⌉ + 1 for n elements.

### Problem 2

This problem is worth 2 points.

Answer: **(c) `sizeof(r)/sizeof(int)`**. Total array size divided by element size gives element count.
- Key concept: array size calculation in C — sizeof(array)/sizeof(element).

### Problem 3

This problem is worth 2 points.

Answer: **(a) `*(bPtr + 3)`**. Pointer arithmetic: `bPtr + 3` points to the 4th element, dereferencing gives `b[3]`.
- Key concept: pointer-array equivalence — `a[i]` equals `*(a + i)`.

### Problem 4

This problem is worth 2 points.

Answer: **(b) The leftmost bit of x is set to zero.** Left shift discards MSB, right shift fills with 0 (for unsigned) or sign bit (for signed). For positive numbers, the leftmost bit becomes 0. For negative (signed), implementation-defined, but conceptually MSB is cleared.
- Key concept: bit shifting — left shift then right shift clears the MSB.

### Problem 5

This problem is worth 2 points.

`mystery(4)` = 4 × mystery(3) = 4 × 3 × mystery(2) = 4 × 3 × 2 × mystery(1) = 4 × 3 × 2 × 1 = 24. Answer: **(d) 24**. This is factorial.
- Key concept: recursive factorial — base case returns 1, recursive case multiplies by n.

### Problem 6

This problem is worth 15 points.

Reverse integer digits: extract digits with `x % 10`, build reversed number with `rev = rev * 10 + digit`, check overflow before each multiplication/addition. Handle negative numbers by noting sign, working with absolute value.
- Key concept: integer digit reversal with overflow detection — modular arithmetic and bounds checking.

### Problem 7

This problem is worth 14 points.

A=6 (constructor), B=5 (destructor), C=4 (data member), D=0 (member function), E=8 (private — only class code can access), F=7 (public — any code can access), G=1 (recursive — calls itself).
- Key concept: OOP terminology matching — constructors, destructors, access specifiers, recursion.

### Problem 8

This problem is worth 10 points.

| Property | char* | string |
|----------|-------|--------|
| 1. Contain null chars | True (manually) | True |
| 2. Auto de-allocated | False (manual free) | True |
| 3. Characters changeable | True | True |
| 4. O(1) k-th access | True | True |
| 5. `=` makes deep copy | False (copies pointer) | True |

- Key concept: char* vs. std::string — memory management, copying semantics.

### Problem 9

This problem is worth 1 point.

Answer: **(B) The same generic code to operate on different types of data.** Templates provide type parameterization — same algorithm/container works with any type.
- Key concept: C++ templates — generic programming with type parameters.

### Problem 10

This problem is worth 3 points.

Insert 51, 26, 11, 6, 8, 4, 7 into AVL tree with rotations. After all insertions and rebalancing, represent as array (level-order). Trace each insertion and rotation to get final tree structure. Answer: **(c) (8, 6, 26, 4, 7, 11, 51)**.
- Key concept: AVL tree construction with rotations — maintain balance factor ≤ 1.

### Problem 11

This problem is worth 3 points.

Post-order: E D A C G F B H. Build tree using children count information. Root is H (last in post-order, 2 children). Reconstruct tree using post-order (children before parent) and child counts. Then output pre-order. Answer: **(d) Yes, (H A D E B F C G)** — tree is unique given post-order and child counts.
- Key concept: tree reconstruction from post-order + child count — unique determination.

### Problem 12

This problem is worth 3 points.

Build max-heap from array [45, 83, 7, 61, 12, 99, 44, 77, 14, 29] using bottom-up heapify. Then perform 2 extract-max operations (swap root with last, heapify remaining). Show array state after removing two largest elements (99, 83).
- Key concept: heap sort phases — build heap, then repeatedly extract maximum.

### Problem 13

This problem is worth 2 points.

Hash table size 11, division method (key % 11), linear probing. Insert 25, 42, 96, 101, 102, 162, 197, 201. Compute: 25%11=3, 42%11=9, 96%11=8, 101%11=2, 102%11=3→4(collision), 162%11=8→10(collision), 197%11=10→0(collision), 201%11=3→5(collision). Answer: **(d)**.
- Key concept: hash table with linear probing — collision chain computation.

### Problem 14

This problem is worth 3 points.

Convert general tree to binary tree (left-child right-sibling representation), then perform post-order traversal on the resulting binary tree. In left-child/right-sibling, first child becomes left child, next sibling becomes right child.
- Key concept: general tree to binary tree conversion — left-child right-sibling representation.

### Problem 15

This problem is worth 3 points.

Convert $B - E/(F/A - A*D/F) + E*C*D - (A+B)*E$ to prefix. Apply operator precedence and associativity, then write in prefix (operator before operands). Answer: **(a)**.
- Key concept: infix to prefix conversion — operators precede their operands.

### Problem 16

This problem is worth 4 points total (3 subparts).

- **16.1** [1%] DFS from B (alphabetical order): B→A→C→..., building spanning tree edges.
  - Key concept: DFS spanning tree — edges followed during first visit.

- **16.2** [2%] Compute low values for each vertex using DFS tree and back edges.
  - Key concept: low function — minimum ancestor reachable through subtree.

- **16.3** [1%] Articulation points: vertices whose removal disconnects the graph, detected via low values.
  - Key concept: articulation points — biconnectivity analysis using DFS.

### Problem 17

This problem is worth 4 points total (2 subparts).

- **17.1** [2%] Critical path in AOE network: longest path from source to sink. Compute earliest/latest event times. The critical path has zero slack on all activities.
  - Key concept: critical path method — longest path in activity-on-edge network.

- **17.2** [2%] Dijkstra's shortest path from vertex 1: greedy selection of minimum-distance vertex.
  - Key concept: Dijkstra's algorithm — greedy single-source shortest path.

### Problem 18

This problem is worth 4 points.

- **(a)** Quicksort: average O(n log n) but worst case O(n²) — **NOT** asymptotically optimal (worst case exceeds lower bound).
- **(b)** Mergesort: O(n log n) worst case — **asymptotically optimal** (matches Ω(n log n) lower bound).
- Key concept: asymptotic optimality — requires O(n log n) worst-case for comparison sorts.

### Problem 19

This problem is worth 17 points total (4 subparts).

- **(a)** [2%] Dijkstra's fails on graphs with negative edges — it may produce incorrect results because it assumes relaxed vertices are final. With negative cycles or edges, a later path through a negative edge could be shorter.
  - Key concept: Dijkstra's limitation — incorrect with negative edge weights.

- **(b)** [4%] Reweight: add a new vertex s with 0-weight edges to all vertices. Run Bellman-Ford from s to get h(v). Reweight: w'(u,v) = w(u,v) + h(u) - h(v). All new weights are non-negative.
  - Key concept: Johnson's reweighting — Bellman-Ford potentials make edges non-negative.

- **(c)** [8%] Floyd-Warshall: initialize D⁰ from adjacency matrix. Iterate k=1..4: D^k[i][j] = min(D^(k-1)[i][j], D^(k-1)[i][k] + D^(k-1)[k][j]). Show D¹, D², D³, D⁴ and predecessor matrices.
  - Key concept: Floyd-Warshall — dynamic programming all-pairs shortest paths via intermediate vertices.

- **(d)** [3%] Floyd-Warshall: O(V³). Johnson's: O(VE + V²log V). Johnson's is faster when the graph is sparse (E << V²). If E = O(V²), Floyd-Warshall may be comparable or faster due to lower constant factors.
  - Key concept: algorithm selection — sparse graphs favor Johnson's; dense graphs favor Floyd-Warshall.

### Problem 20

This problem is worth 4 points.

Ternary Huffman code: merge three lowest-probability symbols at each step (instead of two). Since (7-1)%(3-1) = 6/2 = 3 ≡ 0, no dummy symbol needed. Build tree bottom-up: merge θ(0.08)+η(0.05)+ε(0.13)=0.26, then merge δ(0.14)+γ(0.15)+β(0.2)=0.49, then merge remaining. Assign 0, 1, 2 to each branch.
- Key concept: ternary Huffman coding — merge 3 symbols per step, assign ternary codes.

## 112

### Problem 1

This problem is worth 10 points total (2 subparts).

- **(a)** [5%] `printf("%#o", 100);` — The `#` flag adds leading 0 for octal, `%o` formats as octal. 100 decimal = 144 octal. Output: `0144`.
  - Key concept: C printf format specifiers — `%o` for octal, `#` for prefix.

- **(b)** [5%] `scanf("%[0-9]", n);` — Character class scanset reads only digit characters until non-digit encountered.
  - Key concept: scanf scanset — `%[...]` reads characters matching the specified set.

### Problem 2

This problem is worth 10 points.

Trace pointer operations: `char *p = s` (points to '0'). `*p++` → prints '0', p moves to '1'. `*++p` → p moves to '2', prints '2'. `*(++p)` → p moves to '3', prints '3'. `*(p++)` → prints '3', p moves to '4'. `(*p)++` → prints '4' (then increments the char in place). Output: **02334**.
- Key concept: pointer increment vs. dereference precedence — pre/post increment with `*`.

### Problem 3

This problem is worth 5 points.

Trace nested loops: x starts at 3. Count how many (i,j) pairs satisfy each condition: i+j==2 (only (1,1)→x+=3→x=6), i+j==3 (pairs (1,2),(2,1)→x+=4 twice→x=14), i+j==4 (pairs (1,3),(2,2),(3,1)→x+=5 three times→x=29). Final: **x = 29**.
- Key concept: nested loop counting — enumerate pairs satisfying conditions.

### Problem 4

This problem is worth 4 points.

Problem: an integer can legitimately have the value 0, which is the same bit pattern as NULL (4 null bytes). Using 0 as a sentinel means you cannot store the integer value 0 in the array. The sentinel is indistinguishable from valid data.
- Key concept: sentinel value conflicts — NULL/0 terminator conflicts with valid integer value 0.

### Problem 5

This problem is worth 3 points.

Declarations but NOT definitions: **(C)** and **(D)** — function declarations without bodies (prototypes). `int i;` is a definition (allocates storage). `int i=1;` is a definition. `typedef` is neither a definition nor produces storage.
- Key concept: C/C++ declaration vs. definition — declarations introduce names; definitions allocate storage.

### Problem 6

This problem is worth 12 points total (4 subparts).

- **甲** [3%] Answer: **(C).** `this` is a pointer to the calling instance. It's not a reference (A wrong), you cannot change what it points to (B wrong).
  - Key concept: `this` pointer — const pointer to the calling object.

- **乙** [3%] Answer: **(E) Depends on if the member function is const.** In non-const method: `Thing* const`. In const method: `const Thing* const`.
  - Key concept: `this` type varies with const qualification of member function.

- **丙** [3%] Answer: **(B).** A const variable must be initialized at declaration. (A is false — const prevents modification even through references. C is false — you CAN add const to a reference.)
  - Key concept: const variable rules — must initialize at declaration.

- **丁** [3%] Answer: **(F) None.** Pointers and references don't copy — they reference the original. Only value semantics (like `string x = y;`) would copy.
  - Key concept: pointer/reference vs. copy — pointers and references don't copy the object.

### Problem 7

This problem is worth 6 points total (2 subparts).

- **甲** [3%] Answer: **(B) and (C).** Constructors set initial state (B) and can be overloaded (C). A describes destructors. D is false — constructors are called for all allocations.
  - Key concept: constructor properties — initialization and overloading.

- **乙** [3%] Answer: **(B) No return on a constructor.** Constructors have no return type (not even void).
  - Key concept: constructor return type — none (not even void).

### Problem 8

This problem is worth 10 points total (5 subparts).

- **(1)** [2%] **True.** Binary tree with 9 levels (root at level 1): max nodes = $2^9 - 1 = 511$.
  - Key concept: full binary tree size — $2^h - 1$ nodes for h levels.

- **(2)** [2%] **False.** $f_1$ and $f_2$ being O(g(n)) means they're both bounded above by g(n), but they need not be equal to each other.
  - Key concept: Big-O — upper bound doesn't imply equality between functions.

- **(3)** [2%] **True.** The lower bound for worst-case comparison-based sorting is Ω(n log n), proven by decision tree argument.
  - Key concept: comparison sorting lower bound — Ω(n log n) worst case.

- **(4)** [2%] **False.** A biconnected graph has NO articulation points (removing any single vertex leaves graph connected). Having two articulation points would mean it's NOT biconnected.
  - Key concept: biconnected graph — no articulation points; remains connected after any single vertex removal.

- **(5)** [2%] **False.** Pre-order and post-order together do NOT uniquely determine a binary tree (ambiguity when a node has only one child).
  - Key concept: tree reconstruction — need in-order + (pre-order or post-order) for uniqueness.

### Problem 9

This problem is worth 15 points total (5 subparts).

- **(1)** [3%] DFS from node 1 (smallest ID first): visit 1, then smallest unvisited neighbor. Follow edges systematically choosing smallest available vertex.
  - Key concept: DFS traversal with tie-breaking — always choose smallest available vertex.

- **(2)** [3%] Bottom-up heapify of {19, 5, 27, 3, 16, 11, 69, 18}: start from last non-leaf, sift down. Result: **(b) {69, 18, 27, 5, 16, 11, 19, 3}**.
  - Key concept: bottom-up heap construction — O(n) heapify via sift-down.

- **(3)** [3%] AVL tree insertion with rotations for sequence {89, 70, 52, 4, 25, 99, 59, 20, 79, 62, 44, 38}. Trace insertions and balance.
  - Key concept: AVL tree — insert with single/double rotations to maintain balance.

- **(4)** [3%] Catalan number $C_8 = \frac{1}{9}\binom{16}{8} = 1430$. Answer: **(a) 1430**.
  - Key concept: Catalan numbers — number of distinct binary trees with n nodes.

- **(5)** [3%] Red-black tree insertion of {50, 10, 80, 90, 70, 60, 65, 62}: trace color flips and rotations maintaining RB properties.
  - Key concept: red-black tree insertion — recoloring and rotation rules.

### Problem 10

This problem is worth 8 points total (4 subparts).

- **(a)** [2%] **False** (partially). Quicksort's expected time is O(n log n) which matches the lower bound, but worst case is O(n²) so it's not worst-case optimal.
  - Key concept: Quicksort optimality — average-case optimal but not worst-case.

- **(b)** [2%] **False.** Maximally unbalanced partitioning creates a degenerate tree (one child per node), which is NOT a full binary tree.
  - Key concept: Quicksort decision tree — unbalanced partitions create skewed trees.

- **(c)** [2%] **False.** Insertion sort IS both in-place AND stable. The statement says "not stable" which is incorrect.
  - Key concept: insertion sort properties — O(1) extra space (in-place) and stable.

- **(d)** [2%] **False.** Counting sort is stable but NOT in-place (requires O(k) auxiliary space for counting array).
  - Key concept: counting sort — stable but requires extra space proportional to value range.

### Problem 11

This problem is worth 11 points total (4 subparts).

- **(a)** [2%] **No.** Dijkstra's is not applicable with negative edge weights — it may produce incorrect results since it assumes once a vertex is finalized, its distance is optimal.
  - Key concept: Dijkstra's limitation — requires non-negative edge weights.

- **(b)** [2%] **Yes.** BFS finds shortest path when all edges have weight 1 (unweighted graph).
  - Key concept: BFS shortest path — works for unweighted (unit-weight) graphs.

- **(c)** [2%] **No.** The graph contains a cycle (b→d→c→b), so topological sort is not applicable (requires DAG).
  - Key concept: topological sort — only applicable to directed acyclic graphs.

- **(d)** [5%] Johnson's reweighting: run Bellman-Ford to get h(v), reweight w'(u,v) = w(u,v) + h(u) - h(v). Show the reweighted graph with all non-negative edges.
  - Key concept: Johnson's algorithm reweighting — eliminate negative edges preserving shortest paths.

### Problem 12

This problem is worth 6 points.

Ford-Fulkerson on the 4-node network: find augmenting paths from s to t, update residual graph until no more paths exist. Sum of flows on edges leaving s = max flow. Show each augmenting path and its bottleneck capacity.
- Key concept: Ford-Fulkerson method — iterative augmenting path discovery until saturation.

## 113

### Problem 1

This problem is worth 3 points.

After inserting 43 into the AVL tree (root 65, left subtree has 40, 35, 50, 45, 55; right has 70, 80): 43 goes as left child of 45. Check balance — node 50 becomes unbalanced (left-heavy). Perform rotation. Array representation of rebalanced tree. Answer: **(C)**.
- Key concept: AVL insertion with rotation — rebalance after insert, represent as level-order array.

### Problem 2

This problem is worth 3 points.

Post-order traversal of the AVL tree from Q1: visit left subtree, right subtree, then root recursively. Answer: **(C)**.
- Key concept: post-order traversal — left, right, root recursion.

### Problem 3

This problem is worth 3 points.

Remove node 65 (root) from AVL tree: replace with in-order successor or predecessor, then rebalance if needed. Convert to array representation. Answer depends on replacement choice and resulting balance.
- Key concept: AVL deletion — replace with successor/predecessor, rebalance.

### Problem 4

This problem is worth 3 points.

Build expression tree for $B - E/(A-D) + E + C*D - A*B$ respecting precedence and left-to-right evaluation for same-precedence operators. Level-order (BFS) traversal of the expression tree gives the answer.
- Key concept: expression tree construction — operator precedence determines tree structure.

### Problem 5

This problem is worth 3 points.

Prefix form: write operators before their operands. Convert the expression tree to prefix notation by pre-order traversal (root, left, right). Answer: **(C)**.
- Key concept: prefix notation — pre-order traversal of expression tree.

### Problem 6

This problem is worth 3 points.

KMP failure function for "AAABAAABAAA" (11 chars): compute longest proper prefix that is also suffix for each position. f(0)=-1, f(1)=0, f(2)=1, f(3)=-1, f(4)=0, f(5)=1, f(6)=2, f(7)=3 (since prefix "AAAB" matches), etc.
- Key concept: KMP failure function — prefix-suffix matching for pattern preprocessing.

### Problem 7

This problem is worth 3 points.

Reconstruct from in-order (A,B,G,E,D,I,J,F,H,C) and post-order (G,E,J,I,H,F,D,C,B,A): A is root (last in post-order). Find A in in-order → all elements are in right subtree of A. Build recursively. Output pre-order. Answer: **(C)**.
- Key concept: tree reconstruction and pre-order output — in-order + post-order determines unique tree.

### Problem 8

This problem is worth 2 points.

Dijkstra's from A on the directed graph: select closest unvisited vertex at each step. A(0)→B(10)→D(12)→E(14)→F(13)→C(15). Order: A, B, D, F, E, C. Answer: **(A)**.
- Key concept: Dijkstra's algorithm — greedy vertex selection by minimum distance.

### Problem 9

This problem is worth 2 points.

Topological ordering (smallest alphabetical first): A has no incoming edges → select A. Then B, C, D have no remaining incoming. Select B (smallest). Continue: D→E→F and C→E. Select based on available in-degree-0 vertices alphabetically. Answer: **(E) None of the above** or verify specific option.
- Key concept: topological sort with alphabetical tie-breaking — Kahn's algorithm with priority.

### Problem 10

This problem is worth 10 points.

`num = 250`. `%X` prints hexadecimal uppercase: 250 = FA. `%.2f` prints float with 2 decimal places: 250.00. Output: `FA` and `250.00`.
- Key concept: printf format specifiers — %X for hex, %.2f for fixed-point float.

### Problem 11

This problem is worth 10 points.

Loop adds arr[i] when i is even (i=0,2,4): sum = arr[0]+arr[2]+arr[4] = 10+30+50 = **90**.
- Key concept: conditional array summation — modulo-based index filtering.

### Problem 12

This problem is worth 5 points.

`fun1` reverses the string in-place: "13579" → "97531". `atoi("97531")` = 97531. `97531 % 10` = **1**.
- Key concept: string reversal + integer operations — in-place swap algorithm.

### Problem 13

This problem is worth 5 points.

Line (A): `cin >> number` (read integer from user). Line (B): `number % factor != 0` (keep decrementing until factor divides number evenly).
- Key concept: largest factor finding — decrement from n-1 until divisibility found.

### Problem 14

This problem is worth 20 points.

A=6 (function declaration — specification of name, params, return type), B=7 (if-else — selects between two choices), C=8 (break — causes execution to leave switch/loop), D=2 (pass by value — default C++ argument passing), E=1 (local variable — declared inside function), F=4 (void — type with no values), G=3 (float — type of 5.0F constant), H=5 (cast — explicit type conversion), I=10 (promotion — implicit char→int, float→double), J=9 (overloaded function — multiple definitions with same name).
- Key concept: C++ terminology — matching definitions to programming concepts.

### Problem 15

This problem is worth 2 points.

$T(n) = 3T(n/5) + n$. Master theorem: $a=3, b=5, f(n)=n$. $n^{\log_5 3} \approx n^{0.68}$. Since $f(n) = n = \Omega(n^{\log_5 3 + \epsilon})$, Case 3 applies: $T(n) = \Theta(n)$. Answer: **(b) n**.
- Key concept: Master theorem Case 3 — f(n) dominates.

### Problem 16

This problem is worth 2 points.

$T(n) = 4T(n/4) + n\log n$. $a=4, b=4$, $n^{\log_4 4} = n$. $f(n) = n\log n$. Since $f(n) = n\log n = \Omega(n^{1+\epsilon})$? No — $n\log n / n = \log n$ which is not polynomial. This falls between Cases 2 and 3 (Master theorem gap). Extended case: $T(n) = \Theta(n\log^2 n)$. Answer: **(c)**.
- Key concept: Master theorem extended — log factor cases.

### Problem 17

This problem is worth 2 points.

$T(n) = 4T(n/2) + n^3$. $a=4, b=2$, $n^{\log_2 4} = n^2$. $f(n) = n^3 = \Omega(n^{2+\epsilon})$ with $\epsilon=1$. Case 3: $T(n) = \Theta(n^3)$. Answer: **(d) n³**.
- Key concept: Master theorem Case 3 — f(n) polynomially larger than $n^{\log_b a}$.

### Problem 18

This problem is worth 2 points.

**False.** The max-flow min-cut theorem and Ford-Fulkerson/Edmonds-Karp algorithms find minimum s-t cut in polynomial time (equivalent to max flow).
- Key concept: min-cut — solvable in polynomial time via max-flow algorithms.

### Problem 19

This problem is worth 2 points.

**True.** Both Merge (in merge sort) and Partition (in quicksort) process each element at most once in a single pass → O(n) linear time operations.
- Key concept: merge and partition — both are linear-time subroutines.

### Problem 20

This problem is worth 2 points.

**False.** Median can be found in O(n) time using the median-of-medians (select) algorithm, which is a comparison-based linear-time selection algorithm.
- Key concept: linear-time selection — median-of-medians achieves O(n) for finding median.

### Problem 21

This problem is worth 2 points.

**False.** MST minimizes total edge weight, not individual path lengths. The path between s and t in MST may be longer than the shortest path in G. MST paths are only guaranteed to minimize the maximum edge weight (minimax paths).
- Key concept: MST vs. shortest path — MST doesn't preserve shortest distances between vertices.

### Problem 22

This problem is worth 3 points.

Check if current flow is maximum: look for augmenting paths in the residual graph. If S-side vertices can reach T-side in residual graph, flow is not maximum. If not maximum, find augmenting paths and compute max flow.
- Key concept: max flow verification — check for augmenting paths in residual graph.

### Problem 23

This problem is worth 8 points.

Applicable algorithms for shortest paths on the graph with negative edges: **(b) Dijkstra's** — NO (negative edges). **(d) Johnson's** — YES (handles negative edges via reweighting). **(e) Bellman-Ford** — YES (handles negative edges, single source). **(g) Floyd-Warshall** — YES (all-pairs, handles negative edges). Not applicable: (a) Kruskal's (MST), (c) Ford-Fulkerson (max flow), (f) Jarvis's march (convex hull), (h) Huffman (coding).
- Key concept: algorithm applicability — matching graph properties to algorithm requirements.
