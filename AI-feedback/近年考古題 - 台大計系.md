## 108

### Problem 1

This problem is worth 4 points total (2 points each for two True/False questions).

a) **FALSE.** Software pipelining and global instruction scheduling are compiler optimization techniques that reorder instructions to reduce stalls; they can benefit even single-issue pipelined machines by hiding latencies without requiring multi-issue hardware.
- Key concept: understanding the distinction between hardware multi-issue and compiler-based ILP optimizations (software pipelining, instruction scheduling).

b) **FALSE.** Data dependencies only cause stalls if the dependent instruction needs the result before it is available; forwarding (bypassing) paths can resolve many data hazards without stalling.
- Key concept: data hazards and forwarding in pipelined processors.

### Problem 2

This problem is worth 12 points total (4 points each for three multiple-choice questions).

a) **(4) Reduces the number of completed page walks.** With a 2 GB working set accessed randomly in 4-byte chunks, there are ~500M distinct pages (4KB) but only ~1000 pages (2MB). Larger pages drastically reduce TLB misses and thus completed page walks.
- Key concept: TLB coverage and the impact of page size on address translation overhead.

b) **(4) It exploits data-level parallelism primarily by the out-of-order execution mechanism.** Out-of-order execution exploits ILP (instruction-level parallelism); DLP is exploited by SIMD/vector units.
- Key concept: distinguishing ILP from DLP in superscalar architectures.

c) **(2) Blockchain cryptocurrency mining.** Mining is computationally intensive and not security-sensitive in nature; it does not require TEE protection, unlike fingerprint auth, key storage, keystore, or RNG.
- Key concept: Trusted Execution Environment (TEE) use cases — protecting security-critical operations, not general computation.

### Problem 3

This problem is worth 8 points total across four sub-questions on ISA addressing modes.

a) A pseudo instruction is an assembly instruction not directly supported by hardware but translated by the assembler into one or more real instructions. The advantage is improved code readability and programmer convenience without increasing hardware complexity.
- Key concept: pseudo instructions and the role of the assembler.

b) Pre/post-increment instructions reduce instruction count by combining address arithmetic with load/store in a single instruction. They benefit loops and array traversal (e.g., stack push/pop, sequential memory access patterns).
- Key concept: addressing modes that combine data transfer with address update for loop/array access.

c) Itanium supports only post-increment because it simplifies the hardware — the address is available at the start of execution (no need to compute the new address before the memory access), aligning with VLIW's predictable pipeline behavior.
- Key concept: VLIW pipeline simplicity and its impact on ISA design choices.

d) Disadvantages include increased instruction complexity, wider instruction encoding, and more complex pipeline control logic (the instruction writes two results: the loaded data and the updated base register).
- Key concept: ISA complexity trade-offs — instruction encoding width and pipeline write-back conflicts.

### Problem 4

This problem is worth 8 points total across three sub-questions on exceptions and interrupts.

a) **Exceptions** (synchronous, caused by instruction execution): Page fault, TLB miss, Floating point arithmetic underflow, Undefined instruction, Execution abort, System call. **Interrupts** (asynchronous, caused by external events): I/O device request, User defined interrupt.
- Key concept: distinguishing synchronous exceptions (generated by instruction execution) from asynchronous interrupts (external events).

b) Ranking from most difficult to simplest: Speculative execution > Out-of-order superscalar > Superscalar implementation > Pipelined implementation > Hierarchical data caches > Single issue In-order processor. Speculative execution requires precise exception handling with rollback of speculated state.
- Key concept: precise exceptions and the difficulty of maintaining architectural state in speculative/out-of-order machines.

c) Ranking from most difficult to simplest: Virtual Machines > GPGPU > Hyper-threaded processor > Superscalar processor > Pipelined processor > Containers. VMs add a layer of indirection for interrupt routing; GPGPUs have thousands of threads with no traditional interrupt model.
- Key concept: interrupt virtualization and the challenge of managing interrupts across abstraction layers.

### Problem 5

This problem is worth 9 points total across three sub-questions on parallel execution.

a) ILP = Instruction-Level Parallelism; DLP = Data-Level Parallelism; MLP = Memory-Level Parallelism; TLP = Thread-Level Parallelism.
- Key concept: taxonomy of parallelism types in computer architecture.

b) ILP: out-of-order execution, superscalar issue, branch prediction. DLP: SIMD/vector units, GPU SIMT. MLP: non-blocking caches, hardware prefetching, out-of-order memory access. TLP: multi-core processors, simultaneous multithreading (SMT).
- Key concept: mapping hardware/microarchitecture techniques to the type of parallelism they exploit.

c) ILP: software pipelining, instruction scheduling by compiler. DLP: loop vectorization, SIMD intrinsics. MLP: data prefetch instructions, memory access reordering by compiler. TLP: multi-threaded programming (pthreads, OpenMP).
- Key concept: software techniques that expose or increase each form of parallelism.

### Problem 6

This problem is worth 9 points total on big.LITTLE multi-core design.

a) Object to $B_{2020}=6, L_{2020}=2$. Most mobile workloads are bursty and idle most of the time; having more big cores wastes power during the dominant idle/light phases. The big.LITTLE philosophy is to use little cores for background tasks (majority of time) and big cores for peak demand. 6 big cores would consume excessive power and thermal budget for diminishing performance returns.
- Key concept: energy-proportional computing and the asymmetric multi-core design philosophy (big.LITTLE / DynamIQ).

b) Cache coherence between the two clusters is maintained using a snoop-based or directory-based coherence protocol (e.g., MOESI) over a coherent interconnect (like ARM CCI or DSU). When one cluster modifies a cache line, the interconnect snoops or invalidates copies in the other cluster to ensure a consistent view of memory.
- Key concept: cache coherence protocols (snooping/directory) in heterogeneous multi-cluster architectures.

### Problem 7

This problem is worth 25 points total on process management and OS concepts.

a-1) **Real UID**: the actual user who created the process (identifies the user). **Effective UID**: determines the permissions the process currently has for accessing resources. **Saved UID**: stores a previous effective UID so that a process can temporarily drop and later regain elevated privileges.
- Key concept: UNIX process credentials and privilege management via UID types.

a-2) The **effective UID** should be changed (e.g., via setuid bit on an executable) to gain additional permissions.
- Key concept: setuid mechanism for privilege escalation.

b-1) **Memory**: multi-process uses separate address spaces (more memory overhead); multi-thread shares the same address space (less overhead). **CPU scheduling**: threads within the same process can be scheduled independently with lower context-switch cost. **Resource sharing**: threads share code, data, and files; processes require explicit IPC for sharing.
- Key concept: processes vs. threads — isolation, overhead, and resource sharing trade-offs.

b-2) No, the parent's variable will still be 'P'. After `fork()`, the child gets a copy of the parent's address space (copy-on-write). The child's modification to `sharedVariable='C'` only affects the child's copy; the parent retains the original value 'P'.
- Key concept: `fork()` creates a separate address space via copy-on-write; parent and child do not share stack/data variables.

b-3) Yes, the addresses printed will appear the same because both parent and child use the same virtual address space layout (virtual addresses are identical), but they map to different physical frames after copy-on-write.
- Key concept: virtual vs. physical addresses — fork preserves virtual layout but maps to different physical pages.

b-4) With `vfork()`, the child shares the parent's address space until it calls `exec()` or `_exit()`. The child's modification `sharedVariable='C'` directly modifies the parent's memory, so the parent prints 'C'. Also, the parent is blocked until the child exits or execs.
- Key concept: `vfork()` shares the parent's address space — no copy, so modifications by child are visible to parent.

### Problem 8

This problem is worth 25 points total on transaction processing and distributed systems.

a) A priority-based scheduling that processes higher-value transactions first (e.g., Highest Value First or revenue-weighted priority queue) maximizes revenue for the provider.
- Key concept: priority scheduling for revenue maximization in transaction systems.

b) Use Lamport timestamps or vector clocks to establish a happened-before ordering; since there is no global clock, logical timestamps based on message arrival order (causal ordering) ensure "earlier sent" is processed first.
- Key concept: logical clocks (Lamport/vector) for ordering events in distributed systems without a global clock.

c) Use distributed locking or optimistic concurrency control (e.g., two-phase commit, distributed transactions) to synchronize inventory updates. Alternatively, use a partitioned inventory with per-partition locks to minimize contention.
- Key concept: distributed concurrency control — 2PL, OCC, or partitioning to prevent overselling while maximizing parallelism.

d) Use a journaling/write-ahead log (WAL) file system: transactions are first written to a log before being committed. Cancelled transactions are rolled back using the log. This ensures both atomicity (trustfulness) and sequential I/O (efficiency).
- Key concept: write-ahead logging (WAL) for atomic, recoverable transactions in file systems.

e) DDoS attacks (flood of fake requests), replay attacks (resubmitting transactions), and race conditions (double-spending inventory). Mitigations: rate limiting, request deduplication with idempotency keys, CAPTCHA, and atomic compare-and-swap for inventory checks.
- Key concept: security threats in high-throughput transaction systems — DDoS, replay attacks, and their mitigations.

## 109

### Problem 1

This problem is worth 50 points total (10 sub-questions, 5 points each) on 1D convolution and computer architecture.

(a) Write a nested loop: outer loop over output index $n$ from $M$ to $N-1-M$, inner loop over kernel index $m$ from $-M$ to $M$, accumulating `result[n] += f[n-m] * g[m+M]`.
- Key concept: implementing convolution as a sliding-window dot product in C.

(b) Convert the C loops to assembly using load/store for array access, a multiply-accumulate sequence, and branch instructions for loop control. Use registers for loop counters and accumulators.
- Key concept: translating high-level loop constructs to assembly with explicit register allocation and memory addressing.

(c) Data hazards occur between the multiply and accumulate (RAW on the product register), and control hazards at the loop branch. Without forwarding, each RAW hazard causes 2 stall cycles (result available after WB, needed at EX). The branch causes 1 stall (resolved in EX).
- Key concept: identifying RAW data hazards and control hazards in a 5-stage pipeline without forwarding.

(d) Add a branch predictor (e.g., 2-bit saturating counter) that predicts "taken" for loop branches. On correct prediction, no stalls; on misprediction, flush 1 cycle. Since loop branches are mostly taken, the predictor accuracy is high (~99%), nearly eliminating control hazard stalls.
- Key concept: branch prediction to reduce control hazard penalties in loops.

(e) Unroll the loop by a factor $k$: compute $k$ output elements per iteration, reducing branch frequency by $k\times$ and exposing more ILP for instruction scheduling to hide data hazard stalls between independent multiply-accumulate chains.
- Key concept: loop unrolling to reduce branch overhead and expose ILP for better pipeline utilization.

(f) For $M=1$: kernel size=3, reuse of $f$ elements across adjacent outputs means ~2/3 of accesses hit cache. For $M=4$: kernel size=9, cache holds 256 bytes = 64 floats in 16 blocks; input array (N>>256 bytes) thrashes cache, miss rate ≈ 1/4 (every 4th element is a new block). For $M=8$: larger kernel, similar thrashing, miss rate ≈ 1/4 but with more conflict misses.
- Key concept: cache miss rate analysis based on access patterns, block size, and working set size relative to cache capacity.

(g) Yes, blocking can help if $N$ is large: tile the output computation so that the input segment accessed fits in the cache. Rewrite as blocks of consecutive outputs sharing input elements within a cache-sized tile, reducing capacity misses from ~25% to near-compulsory only.
- Key concept: cache blocking (tiling) to improve temporal locality for sliding-window computations.

(h) Yes, since independent input sequences have no data dependencies, each sequence can be processed by a separate thread. Assign different input sequences to different threads; each thread has its own working set, achieving near-linear speedup.
- Key concept: embarrassingly parallel workloads — independent data partitioning for multithreading.

(i) Yes, applying different kernels to one input sequence can be parallelized: each processor applies a different kernel to the (read-shared) input. Since reads are shared and outputs are independent, no synchronization is needed — classic data parallelism.
- Key concept: read-sharing and independent outputs enabling multiprocessor parallelism without synchronization.

(j) Arithmetic intensity = (2M+1)*2 FLOPs per output / ((2M+1+1)*4 bytes loaded per output) ≈ 1 FLOP/byte for small M. Without cache, performance is memory-bandwidth limited (below the roofline knee). With a cache, effective bandwidth increases as data is reused, moving the kernel rightward on the roofline. Adding a 4× vector unit raises the roofline ceiling by 4×, but if still memory-bound, attainable performance doesn't change unless bandwidth also increases.
- Key concept: roofline model analysis — arithmetic intensity determines whether a kernel is compute-bound or memory-bound.

### Problem 2

This problem is worth 20 points total (10 True/False sub-questions, 2 points each).

(a) **No.** More threading can cause overhead (context switches, contention, cache thrashing) that degrades performance — Amdahl's law and diminishing returns apply.
- Key concept: threading overhead and diminishing returns.

(b) **No (in general).** Bélády's anomaly shows that for some replacement algorithms (e.g., FIFO), more frames can actually increase page faults. However, for stack algorithms (LRU), more frames never increases page faults.
- Key concept: Bélády's anomaly — more frames don't always reduce page faults.

(c) **No.** Larger time quantum reduces context switch overhead but can increase waiting time for short jobs, potentially increasing average turnaround time (approaches FCFS behavior).
- Key concept: time quantum trade-off — too large approximates FCFS, hurting short-job turnaround.

(d) **Yes.** Multiprocessor systems increase throughput by executing multiple processes/threads simultaneously, even if individual task latency doesn't improve.
- Key concept: throughput vs. latency in multiprocessor systems.

(e) **Yes.** Firmware code can execute faster in RAM because RAM has lower access latency than ROM/flash. Systems often copy firmware to RAM ("shadowing") for faster execution.
- Key concept: ROM shadowing — copying firmware to faster RAM for improved execution speed.

(f) **Yes.** Modularity (separation of concerns) is a reason for process cooperation: dividing a system into cooperating processes enables modular design.
- Key concept: modularity as a motivation for inter-process cooperation.

(g) **Yes.** A safe state guarantees that resources can be allocated in some order to avoid deadlock; by definition, a safe state cannot be a deadlocked state.
- Key concept: safe state definition in deadlock avoidance (Banker's algorithm).

(h) **Yes.** UNIX semantics means writes are immediately visible to other users who have the same file open (shared file pointer, writes to the same inode are visible immediately).
- Key concept: UNIX file sharing semantics — immediate visibility of writes.

(i) **No.** A real-time scheduler must meet deadlines, not just schedule by priority in the fastest manner. It ensures timing constraints (deadlines) are met, which is different from fastest-possible execution.
- Key concept: real-time scheduling focuses on meeting deadlines, not just speed.

(j) **No (partially).** Two-phase locking ensures conflict serializability but does NOT prevent deadlock — deadlocks can still occur and require detection/resolution.
- Key concept: 2PL guarantees serializability but is susceptible to deadlock.

### Problem 3

This problem is worth 5 points on disk management.

Sector sparing reserves spare sectors; when a bad sector is detected, the controller remaps it to a spare sector. Sector slipping shifts all sectors after a bad one by one position. Sparing may cause non-sequential physical layout, affecting sequential scheduling; slipping preserves sequentiality but requires shifting many sectors.
- Key concept: disk bad-sector management — sparing vs. slipping and their impact on disk scheduling locality.

### Problem 4

This problem is worth 10 points on the working-set model.

The working-set model can also be used for **page prepaging (prefetching)**: by tracking a process's working set, the OS can preload the working set pages when a process is resumed (e.g., after a context switch or swap-in), reducing initial page faults. It can also be used for **memory allocation decisions** — allocating frames proportional to each process's working set size.
- Key concept: working-set model applications beyond thrashing prevention — prepaging and proportional memory allocation.

### Problem 5

This problem is worth 15 points on implementing a network packet collector with timestamps.

Write a daemon that uses asynchronous I/O to receive packets, buffers them with their transmission timestamps, and every 10 seconds selects and prints the packet with the earliest timestamp. Since network delay is unbounded, use a bounded waiting window (e.g., wait slightly beyond 10s) and print the earliest-timestamped packet received so far. Use `select()`/`epoll()` for async I/O and a min-heap keyed by timestamp for efficient earliest extraction.
- Key concept: asynchronous I/O with bounded-delay assumptions for ordering events from distributed sources without a global clock.

## 110

### Problem 1

This problem is worth 10 points total on OS architecture (4 sub-questions).

(a) In MS-DOS (monolithic), memory management services are in the **Kernel layer (C)** since all OS services reside in a single kernel layer.
- Key concept: monolithic OS architecture — all services in the kernel.

(b) In Linux (layered), standard I/O libraries are in the **System Services layer (B)** as user-space libraries above the kernel.
- Key concept: layered OS — libraries reside in the system services layer.

(c) In Containers, scheduling services are in the **Kernel layer (C)** because containers share the host kernel's scheduler.
- Key concept: containers share the host kernel — scheduling is a kernel service.

(d) In micro-kernel OS (Mach/QNX), device drivers are in the **System Services layer (B)** or even **Applications layer (A)** — they run as user-space servers outside the minimal kernel.
- Key concept: micro-kernel architecture — drivers run in user space as servers.

### Problem 2

This problem is worth 10 points on process memory layout.

(a) Stack segment → **(D)** (high address, grows downward).
(b) Text segment → **(A)** (lowest address).
(c) Uninitialized data segment (BSS) → **(B)** (above initialized data, below heap).
(d) Free space → **(C)** (between heap growing up and stack growing down).
- Key concept: virtual address space layout — text, data, BSS, heap, and stack segment positions.

### Problem 3

This problem is worth 10 points on CPU scheduling (5 sub-questions, 2 points each).

(a) With Round-Robin (quantum=3): Simulating the Gantt chart with I/O bursts, PID 5 arrives at t=10 and completes at **t=27**.
- Key concept: Round-Robin scheduling simulation with I/O bursts.

(b) Non-preemptive priority: PID 5 (priority 1, arrives at 10) waits for current job to finish, then runs immediately after. Completion time is **t=14** (arrives at 10, runs 4 units).
- Key concept: non-preemptive priority scheduling — highest priority runs after current finishes.

(c) Preemptive SJF (SRTF): PID 5 arrives at t=10 with burst=4. Depending on remaining bursts of running processes, PID 5 completes at **t=14**.
- Key concept: Shortest Remaining Time First (preemptive SJF) scheduling.

(d) LISC (Last In, Shortest CPU — preemptive): With PID 5 arriving at t=16, it preempts and runs to completion at **t=20**.
- Key concept: LISC scheduling policy simulation.

(e) LIF (Longest In, First — preemptive): With PID 5 burst=8, it gets preempted by longer-waiting processes. Completion time depends on the specific simulation.
- Key concept: LIF scheduling — prioritizing processes that have been in the system longest.

### Problem 4

This problem is worth 10 points on I/O performance (5 sub-questions, 2 points each).

(a) **(B) Greater than.** `fsync()` forces data to disk synchronously, increasing clock time compared to the baseline (buffered in page cache).
- Key concept: `fsync()` forces synchronous disk writes, increasing wall-clock time.

(b) **(C) Similar to.** Doubling buffer size from 4KB to 8KB doesn't significantly change User CPU time; the user-space work per byte is the same.
- Key concept: buffer size primarily affects system call frequency, not user CPU computation.

(c) **(A) Less than.** Buffered I/O with `puts()` reduces the number of system calls (batches writes in user-space buffer), so System CPU time decreases.
- Key concept: buffered I/O reduces system call overhead by batching writes.

(d) **(B) Greater than.** Fully-buffered `puts()` requires the library to manage buffers in user space, slightly increasing User CPU time for buffer management vs. raw `write()`.
- Key concept: user-space buffering overhead in standard I/O library.

(e) **(B) Greater than.** Adding `fflush()+fsync()` forces both user-space buffer flush and kernel-to-disk sync, increasing System CPU time due to forced I/O operations.
- Key concept: combining `fflush()` and `fsync()` creates maximum system call / kernel overhead.

### Problem 5

This problem is worth 10 points on UNIX signals (5 sub-questions, 2 points each).

(a) The process first suspends at `sigsuspend()` (approximately line 33 in the code). `sigsuspend` atomically sets the signal mask to `waitmask` (blocking SIGUSR1) and suspends the process.
- Key concept: `sigsuspend()` atomically changes signal mask and suspends the process.

(b) **(B) Resume.** SIGUSR2 is not in the `waitmask` (only SIGUSR1 is blocked during suspend), so SIGUSR2 is delivered and handled, causing the process to resume from `sigsuspend`.
- Key concept: `sigsuspend` only blocks signals in the provided mask; unblocked signals can wake the process.

(c) **(D) Empty.** The `sa_mask` for the handler was set to empty (`sigemptyset`), and the current signal (SIGUSR2) is automatically blocked during handler execution, so the mask during handling is {SIGUSR2}. But considering only what was explicitly set: the mask is effectively {SIGUSR2} (auto-blocked).
- Key concept: signal mask during handler execution — the caught signal is auto-blocked, plus any in `sa_mask`.

(d) After `sigsuspend` returns, the signal mask is restored to the `newmask` state (SIGINT blocked). SIGINT would be pending; when `sigprocmask` unblocks it (line 37-38), the handler runs. Execution resumes from line 37 (`pr_mask("after return from sigsuspend")`).
- Key concept: signal delivery and mask restoration after `sigsuspend`.

(e) **(D) Empty.** With `signal()` instead of `sigaction()`, after the handler returns, the signal mask is reset to the state before the signal was caught (which was empty after `sigprocmask` restores `oldmask`).
- Key concept: `signal()` vs `sigaction()` — `signal()` has unreliable semantics with automatic mask reset.

### Problem 6

This problem is worth 20 points on cache coherence in SMP.

(a) Cache: 4KiB, direct-mapped, 16-byte lines → 256 lines. Tag bits = 32 - log2(16) - log2(256) = 32 - 4 - 8 = 20 bits. Tag array size = 256 × 20 = 5120 bits.
- Key concept: calculating tag array size from cache parameters (capacity, block size, associativity).

(b) No. The arrays A, B, C are contiguous and the access pattern `A[i]+B[i]→C[i]` for the given small range (i=0..7) maps to distinct cache sets. Conflict misses don't occur with this sequential pattern; increasing associativity won't help because there are no conflicts.
- Key concept: analyzing whether conflict misses exist to determine if associativity helps.

(c) With 16-byte cache lines and 4-byte ints, each line holds 4 elements. P0 accesses indices 0-3, P1 accesses indices 4-7. Array C: C[0]-C[3] in one line, C[4]-C[7] in another — no sharing. Arrays A, B are read-only — no coherence misses. c1=0 coherence misses, c2=0 false-sharing misses.
- Key concept: coherence misses and false sharing analysis based on cache line boundaries and access patterns.

(d) With 32-byte lines, each line holds 8 ints. C[0]-C[7] all in one line. P0 writes C[0]-C[3] and P1 writes C[4]-C[7] — same cache line! Worst case: d1=8 coherence misses (ping-pong on every write). All are d2=8 false-sharing misses since the processors access different words in the same line.
- Key concept: false sharing — different processors writing to different words in the same cache line causes coherence traffic.

### Problem 7

This problem is worth 25 points on pipelined RISC-V processor.

(a) The MEM stage determines the clock cycle time: D-Mem R/W (280ps) + pipeline register R/W (30ps) = 310ps. Checking other stages: IF = 280+30=310ps, ID = max path through registers/control, EX = ALU(200)+mux(25)+30=255ps, WB = mux(25)+register(180)+30=235ps. The MEM or IF stage (both 310ps) determines the cycle time.
- Key concept: pipeline clock cycle determined by the slowest stage's critical path.

(b) When `lw` (#1) is in MEM: with hazard detection causing a stall bubble after #1, b1: #3 (add x2,x2,x4) is in ID (or a bubble), b2: #2 (add x2,x10,x2) is in EX. Due to the load-use hazard, a bubble is inserted, so b1=#3 in ID is unlikely; more precisely b1=bubble, b2=#2.
- Key concept: load-use hazard stalls and pipeline stage occupancy analysis.

(c) When `sub` (#4) is in EX: it needs x2 (from #3, which is in MEM) for rs1 → ForwardA = 01 (forward from EX/MEM). It needs x6 (no recent writer) for rs2 → ForwardB = 00 (no forwarding needed). c1=01, c2=00.
- Key concept: forwarding control signal values based on pipeline register contents.

(d) For `sub rd, rs1, rs2`: RegWrite=1 (writes to rd), MemtoReg=0 (ALU result, not memory), MemRead=0 (no memory read), MemWrite=0 (no memory write). d1=1, d2=0, d3=0, d4=0.
- Key concept: control signal generation for R-type instructions in the RISC-V pipeline.

### Problem 8

This problem is worth 5 points (must select all correct answers).

Correct answers: **(3) and (4).**
- (1) FALSE: MIPS doesn't account for instruction complexity differences.
- (2) FALSE: LRU is not optimal; Bélády's OPT algorithm is optimal.
- (3) TRUE: Vector instructions exploit DLP in applications with data parallelism.
- (4) TRUE: SMT interleaves instructions from multiple threads to improve utilization.
- Key concept: performance metrics limitations (MIPS), cache replacement optimality, and SMT/vector execution benefits.

## 111

### Problem 1

This problem is worth 5 points on page table address translation.

Page size = 4 bytes, so offset = 2 bits. Logical address space = 16 entries → 4 pages. Variable "g" is at logical address 6: page 6/4=1, offset 6%4=2. Page 1 → physical frame 6. Physical address = 6×4+2 = 26. Variable "o" is at logical address 14: page 14/4=3, offset 14%4=2. Page 3 → physical frame 2. Physical address = 2×4+2 = 10. Answer: **(C) 22 and 10** — Wait, recalculating: g at addr 6, page=1, offset=2, phys frame=6, phys addr=6*4+2=26; o at addr 14, page=3, offset=2, phys frame=2, phys addr=2*4+2=10. Answer: **(A) 26 and 10**.
- Key concept: page table address translation — page number lookup and physical address computation.

### Problem 2

This problem is worth 5 points on page table sizing.

24-bit logical address, 4KiB pages → page offset = 12 bits, page number = 24-12 = 12 bits. Number of entries = $2^{12}$ = 4096. Each entry = 1 byte. Page table size = 4096 × 1 = 4096 bytes = 4KiB. Answer: **(D) 12 and 4 KiB**.
- Key concept: computing page table size from logical address width, page size, and entry size.

### Problem 3

This problem is worth 5 points on file system operations.

When `fd == -1`, the `open()` call failed (file doesn't exist with O_EXCL, or error). The sequence of operations when opening fails: the system searches the directory structure first. Answer: **(B) E -> B** — the kernel searches the directory (in secondary storage via operation E), then returns error to user (B).
- Key concept: file open operation sequence in the kernel — directory lookup and error return path.

### Problem 4

This problem is worth 5 points on CPU performance metrics.

CPI = Execution Time / (Instruction Count × Clock Cycle Time). C1: CPI = 1.1s / (1.0×10⁹ × 1ns) = 1.1. C2: CPI = 1.5s / (1.2×10⁹ × 1ns) = 1.25. Answer: **(C) C1=1.1; C2=1.25**.
- Key concept: CPI calculation from execution time, instruction count, and clock cycle time.

### Problem 5

This problem is worth 5 points on pipeline hazards without forwarding.

Without forwarding, a register written in WB (cycle 5 of the producing instruction) can be read in ID (cycle 2 of the consuming instruction). `ld x29` (#2) completes WB at cycle 7 (with stalls). `sub x17, x29, x14` (#3) has RAW on x29 from `ld` — needs 2 stall cycles (load-use). #3 starts ID at cycle 7, completes at cycle 9. #4 depends on x17 from #3 (available after WB of #3). With stalls, #4 completes at cycle 11. Answer: **(D) 9; 11**.
- Key concept: pipeline stall analysis for load-use hazards without forwarding — counting completion cycles.

### Problem 6

This problem is worth 5 points on RISC-V calling conventions and reverse engineering.

Analyzing the assembly: `slli x5, x12, 5` means x5 = c<<5 = c*32 (c is in x12). The function calls `m(a, b)` (x10=a, x11=b via jal), then sets up x11=c*32 and x12=0x11232334, calls `g` via tail call. So `return g(m(a,b), c*32, 0x11232334)`. Answer: **(E) return g(m(a, b), c\*32, 0x11232334)**.
- Key concept: reverse-engineering C code from RISC-V assembly using calling conventions and instruction semantics.

### Problem 7

This problem is worth 5 points on shared-memory concurrency.

Each core executes: load [x2], add 1, store [x2], load [x3], add [x2]+[x3], store [x3]. Initial [x2]=[x3]=1. For [x2]: both cores do read-modify-write; depending on interleaving, final [x2] = 2 (lost update) or 3 (serialized). For [x3]: depends on values of [x2] read and [x3] read; possible values are 3,4,5,6. Answer: **(B) [x2]=2 or 3, [x3]=3 or 4 or 5 or 6**.
- Key concept: race conditions in shared-memory concurrent execution — analyzing all possible interleavings.

### Problem 8

This problem is worth 4 points on process management (wait/zombies).

Correct: **(A)** Cascading termination definition is correct. **(C)** If no child exists, wait() returns immediately with error (ECHILD) — effectively "no wait". **(D)** Orphan processes are re-parented to init which periodically calls wait(). (B) is FALSE: zombie PID is NOT released until parent calls wait().
- Key concept: zombie processes, orphan adoption, and cascading termination in UNIX.

### Problem 9

This problem is worth 4 points on threading concepts.

Correct: **(A)** Implicit threading definition is correct. **(B)** Thread pools limit concurrent threads — correct. (C) is FALSE: a single core supports concurrency via time-slicing. (D) is FALSE: cancellation points are for deferred (not asynchronous) cancellation.
- Key concept: implicit threading, thread pools, concurrency vs. parallelism, and thread cancellation types.

### Problem 10

This problem is worth 4 points on rate-monotonic scheduling.

With rate-monotonic: P1 (period=30) > P2 (period=70) > P3 (period=100) in priority. P1 runs 10 units every 30; P2 runs 10 units every 70; P3 gets remaining time. (A) P2 worst-case turnaround: P2 arrives at t=10, may be preempted by P1. Worst case: arrives just before P1, waits 10, then runs → turnaround = 20, not 10. FALSE. (B) P3 worst-case turnaround = 30 — analyzing schedule, P3 can be delayed by P1 and P2 → TRUE. (D) Arrival time changes don't affect worst-case analysis for periodic tasks under RMS → TRUE.
- Key concept: rate-monotonic scheduling analysis — priority assignment by period and worst-case response time.

### Problem 11

This problem is worth 5 points on virtual memory properties.

Correct: **(A)** Each process has its own virtual space — TRUE. **(B)** VM allows more processes than physical memory — TRUE. **(C)** Shared memory between processes is supported — TRUE. **(D)** Uses SEVERAL bases (one per page) — TRUE. (E) is FALSE (uses multiple base addresses via page table, not one).
- Key concept: virtual memory properties — per-process address space, overcommit, sharing, and page-based translation.

### Problem 12

This problem is worth 5 points on file system consistency semantics.

Correct: **(B)** Consistency semantics specify when modifications are observable — TRUE. **(E)** They specify how multiple processes access shared files — TRUE. (A) is about locking, not consistency semantics. (C) UNIX semantics is hard for distributed FS. (D) describes immutable shared files semantics, not session semantics.
- Key concept: file system consistency semantics — UNIX, session, and immutable semantics definitions.

### Problem 13

This problem is worth 5 points on synchronization and deadlock.

Correct: **(E)** Freedom from starvation implies freedom from deadlock (if no process starves, then no process is stuck forever in deadlock). (A) FALSE: cycle is necessary but not sufficient for deadlock with multi-instance resources. (B)(C)(D) all FALSE by counterexamples.
- Key concept: relationships between progress, bounded waiting, deadlock-freedom, and starvation-freedom.

### Problem 14

This problem is worth 5 points on VIPT cache design.

For VIPT to work without aliasing: the index bits must come entirely from the page offset. Page size = 4KiB → 12 offset bits. Cache: 16KiB, 32-byte blocks. Index bits = log2(sets). For n-way: sets = 16KiB/(n×32). Need index+offset_in_block ≤ 12. Block offset = 5 bits. Index = log2(16384/(n×32)). For direct-mapped: index = 9 bits, total = 14 > 12 — FAILS. 2-way: index = 8, total = 13 > 12 — FAILS. 4-way: index = 7, total = 12 = 12 — OK. 8-way and 16-way also work. Answer: **(C)(D)(E)**.
- Key concept: VIPT cache — ensuring virtual index bits don't exceed page offset to avoid aliasing.

### Problem 15

This problem is worth 5 points on performance analysis with instruction mix.

Total cycles = 12M×1 + 15M×2 + 50M×4 + 13M×2 = 12+30+200+26 = 268M cycles. Time = 268M/3.2G = 83.75ms. (A) Removing 80% INT: saves 12M×0.8×1=9.6M cycles → 9.6/268 = 3.58% > 3% — TRUE. (B) Even if FP CPI→0: saves 30M/268M = 11.2% → speedup = 1.126x, cannot reach 2x — TRUE (it IS impossible). (C) Reducing L/S CPI to 1.2: saves 50M×(4-1.2)=140M → new total=128M, speedup=268/128=2.09x > 2x — TRUE. (D) New INT: 6M instructions × CPI=? with 10% clock penalty: need detailed calculation — likely FALSE. (E) Adding 30M×10=300M cycles → total=568M, ratio=568/268=2.12x > 2x — TRUE.
- Key concept: Amdahl's law and performance analysis with instruction mix and CPI optimization.

### Problem 16

This problem is worth 5 points on Intel SGX / TEE.

Correct: **(B)** SGX supports remote attestation to verify enclave binary integrity — TRUE. **(D)** A malicious OS can modify page tables affecting enclave execution (page-fault side-channel attacks) — TRUE. **(E)** SGX sealing allows persistent encryption with hardware keys — TRUE. (A) FALSE: TEE code can still crash (bugs, exceptions). (C) FALSE: entire OS kernel cannot run in an enclave (SGX is for user-space applications).
- Key concept: Intel SGX security model — remote attestation, sealing, and known side-channel vulnerabilities.

### Problem 17

This problem is worth 5 points on RISC-V ISA register encoding.

Correct: **(A)** Halving registers (16 instead of 32) frees 1 bit per register field; I-type has 1 rs and 1 rd → 2 extra bits for immediate — TRUE. **(D)** More registers means wider bit fields, potentially longer instructions — TRUE. (B) FALSE: fewer register bits don't change the number of I-type instruction encodings (opcode/funct determines instruction type). (C) FALSE: shift amounts are determined by data width, not register count. (E) FALSE: fewer registers might increase code size due to more spills.
- Key concept: ISA encoding trade-offs — register count vs. immediate field width and instruction length.

### Problem 18

This problem is worth 8 points on optimal scheduling for minimizing average turnaround time.

Optimal preemptive scheduling for minimum average turnaround: use SRTF (Shortest Remaining Time First). With the given pattern ($P_i$ arrives at $i-1$ with burst $2n-2i+2$), SRTF always runs the newest arrival (shortest burst). Computing the average turnaround time as a polynomial in $n$ and checking which coefficient equations hold. Through derivation: the minimized average turnaround = $(2/3)n^2 + (1/3)n$ approximately. Checking: **(A)** W+X+Y+Z = 0+2/3+1/3+0 = 1 — likely TRUE.
- Key concept: SRTF optimality for minimizing average turnaround time and polynomial analysis of scheduling metrics.

### Problem 19

This problem is worth 10 points on architecture concepts.

(A) TRUE: ARM big.LITTLE is designed for energy efficiency (use little cores for light tasks).
(B) FALSE: By Amdahl's law, 5% serial → max speedup = 1/0.05 = 20x, not 90x. Wait — "at most 5% can't be serialized" means 95% is serial? No — "5% of codes that can't be serialized" likely means 5% serial, 95% parallel → max speedup = 1/(0.05 + 0.95/100) = ~16.8x, still not 90x. FALSE.
(C) FALSE: RISC vs CISC performance depends on implementation, not ISA type alone.
(D) TRUE: Larger blocks mean more words per line, increasing probability of false sharing.
(E) TRUE: Larger blocks bring in more consecutive data on each miss, reducing compulsory misses (spatial locality).
- Key concept: big.LITTLE energy efficiency, Amdahl's law limitations, RISC vs CISC, and cache block size effects.

## 112

### Problem 1

This problem is worth 2 points on cache miss rate analysis.

Each inner iteration: loads x[i], y[i] (reused across j), x[j], y[j] (sequential), stores distance[j+i*N]. With 32-byte blocks (8 floats), x[j] and y[j] arrays are accessed sequentially → 1 miss per 8 accesses. x[i], y[i] stay in cache if it's large enough for a few lines. Miss rate ≈ 2 misses / (5×8 accesses per block) ≈ 5%. Answer: **(C) Between 5% and 10%**.
- Key concept: cache miss rate estimation from access patterns and cache block size.

### Problem 2

This problem is worth 2 points on arithmetic intensity.

Per iteration: 5 FLOPs (2 sub, 2 mul, 1 add + sqrt ≈ 6 FLOPs). Bytes transferred: without cache, 5 floats × 4 bytes = 20 bytes per iteration. AI = 6/20 = 0.3. Answer: **(A) Less than 0.35**.
- Key concept: arithmetic intensity calculation — FLOPs divided by bytes transferred from memory.

### Problem 3

This problem is worth 2 points on arithmetic intensity with cached data.

If x[] and y[] are fully cached, only distance[] is transferred: 1 store × 4 bytes = 4 bytes per iteration (with write-allocate, 8 bytes for the block). FLOPs ≈ 6. AI = 6/4 = 1.5 or 6/8 = 0.75. Answer: **(C) Between 0.7 and 1** or **(D) Between 1 and 2**.
- Key concept: arithmetic intensity changes when working set fits in cache — only non-cached data counts.

### Problem 4

This problem is worth 2 points on roofline model performance estimation.

N=1024: total FLOPs ≈ 6 × 1024² ≈ 6.3M FLOPs. With AI ≈ 0.3 (memory-bound), attainable performance from roofline: 0.3 × 16.4 GB/s = 4.92 GFLOPs/s (single precision). Time = 6.3M / 4.92G ≈ 0.0013s. Wait — that seems too fast. Recalculating with correct total: 6 × 1024 × 1024 = 6.3M FLOPs. 6.3M / 4.92G = 1.28ms. Answer: **(A) Less than 1,000 seconds**.
- Key concept: using roofline model to estimate execution time from arithmetic intensity and peak bandwidth.

### Problem 5

This problem is worth 2 points on performance optimization strategy.

Since the kernel is memory-bandwidth limited (low arithmetic intensity), the most effective improvement is **(C) Enable data prefetch** — it hides memory latency to better utilize available bandwidth. Alternatively **(B) Increase memory bandwidth** directly raises the roofline slope.
- Key concept: identifying the performance bottleneck (memory-bound) and choosing the appropriate optimization.

### Problem 6

This problem is worth 6 points on memory consistency models.

(A) TRUE: In Case 0 with store buffers, both stores can be buffered and loads see the old value 0, so x0=x1=0 is possible. (B) FALSE: Under TSO, store-buffer forwarding allows each processor to see its own store but the other's load may still see 0. Both x0=x1=0 IS possible under TSO (this is the classic litmus test for store buffer). So (B) is FALSE. (C) In Case 1 under TSO: loads happen before stores, so x0=x1=0 initially, then stores happen. x0=0, x1=0 is possible. For both to be 1, loads must see the other's store — possible only if stores complete before the other's load, which can happen. TRUE. (D) In Case 2 under TSO: Proc1 does load B then load A. Proc0 stores A then B. TSO preserves store order. If Proc1 sees B=1 (x0=1), then Proc0's store to B completed, which means store to A also completed (store order preserved), so x1 must be 1. x0=1,x1=0 is NOT possible under TSO. FALSE.
- Key concept: TSO memory model — store buffer behavior, store ordering guarantees, and litmus test analysis.

### Problem 7

This problem is worth 5 points on pipeline NOP insertion.

Without hazard detection or forwarding, we must manually insert NOPs to resolve RAW hazards. Analyzing dependencies: `or→ld` (x10), `ld→add` (x5, x10), `add→sd` (x5, x10). Each RAW needs 2 NOPs (result available after WB stage 5, needed at ID stage 2 of consumer = 2 cycle gap). Total NOPs needed: 2 (after or) + 2 (after first ld) + 2 (after add) = 6. Wait — checking more carefully with the specific code. Answer: **(D) 6**.
- Key concept: manual NOP insertion for RAW hazards without forwarding — 2 NOPs per dependent pair.

### Problem 8

This problem is worth 5 points on ISA and cache statements.

(D) TRUE: Increasing associativity requires checking more ways (more comparators, longer mux chains), increasing hit time. (E) TRUE: Smaller blocks reduce the amount of data per block, so more blocks fit → fewer capacity misses. Actually (E) is debatable — smaller blocks reduce spatial locality benefit but reduce capacity misses. The most clearly true statement is **(D)**.
- Key concept: cache design trade-offs — associativity vs. hit time, block size vs. miss types.

### Problem 9

This problem is worth 8 points on nested page tables.

With 39-bit VA, 4KB pages: 3-level page table (each level has 512 entries = 9 bits per level, 3 levels for 27 bits + 12 offset = 39 bits). For 2-stage translation: each PT walk needs 3 memory accesses, and each IPA→PA translation in NPT also needs 3 accesses. Total: Guest PT walk = 3 levels, each level requires NPT walk (3 accesses) + 1 final access = 4 per level × 3 levels + final data NPT walk (3) = 3×4+3 = 15? With 2M pages reducing one level: possible values include 10, 12, 13. Answer: **(A)(C)(D)** are possible.
- Key concept: nested page table walk cost — multiplicative effect of two-stage translation.

### Problem 10

This problem is worth 8 points on multi-processor memory ordering.

Analyzing all possible interleavings of 3 CPUs with store/load/add/store sequences on shared location A (initially 0). Possible final values depend on which CPU's final store "wins" (last writer). Possible values: CPU0 can write 8, CPU1 can write various values (2,4,8,10,12...), CPU2 can write various (0,4,6,8,12...). Checking: (A) 8 — possible (CPU0 final). (B) 9 — possible if CPU1 reads 4 from CPU0, adds 1→5, stores 5, then reads 5, doubles→10... checking more carefully. (C) 10 — possible. (D) 16 — possible. Answer: **(A)(C)(D)** and possibly **(B)**.
- Key concept: analyzing final memory values under arbitrary instruction interleaving across multiple processors.

### Problem 11

This problem is worth 8 points on cache address decoding.

4-way set associative, 4-byte blocks, 8 sets. Address bits: offset = 2 bits, index = 3 bits, tag = 13-2-3 = 8 bits. Checking each address: (A) 0x71A = 0111 0001 1010 → tag=0x38(0111000), index=110=6, offset=10. Set 6: check tag 0x38 — Way 0 has tag 38 but V=0 → miss. Way 3 has tag 38, V=1 → hit! Byte offset 2 = 0xEB? Checking byte 2 of way 3: FA 93 EB 48 → byte[2]=EB, not DB. (B) (C) (D) — need detailed checking. Answer requires careful bit extraction.
- Key concept: cache address decoding — extracting tag, index, offset and checking valid bits for hit/miss.

### Problem 12

This problem is worth 5 points on file allocation methods.

Correct: **(A)** Contiguous allocation has external fragmentation — TRUE. **(D)** FAT is a variation of linked allocation — TRUE. **(E)** Traversing free list is not a frequent action — TRUE. (B) FALSE: linked allocation is bad for random access. (C) FALSE: indexed allocation supports both sequential and random access.
- Key concept: file allocation methods — contiguous, linked, indexed — and their trade-offs.

### Problem 13

This problem is worth 5 points on security.

Correct: **(A)** Fork bomb is a DoS attack — TRUE. **(B)** MAC verifies sender authenticity — TRUE. **(C)** MAC verifies message integrity — TRUE. **(E)** Buffer overflow enables code injection — TRUE. (D) FALSE: MAC alone doesn't prevent replay attacks (needs nonces/timestamps).
- Key concept: security primitives — MAC properties (authenticity, integrity), DoS attacks, and buffer overflow vulnerabilities.

### Problem 14

This problem is worth 10 points on two-level paging.

For outer page table to fit in one page ($2^X$ bytes): entries in outer PT = $2^X / 2^Y$. Inner PT entries per page = $2^X / 2^Z$. Pages needed: $2^{W-X}$ total pages. Inner PTs: each covers $2^X / 2^Z$ pages → number of inner PTs = $2^{W-X} / (2^X / 2^Z) = 2^{W-2X+Z}$. Outer PT entries = $2^{W-2X+Z}$. For outer PT to fit in one page: $2^{W-2X+Z} \times 2^Y \leq 2^X$ → $W-2X+Z+Y \leq X$ → $W+Y+Z \leq 3X$.
Checking: (A) 16+6+6=28, 3×8=24 → NO. (B) 16+16+8=40, 3×12=36 → NO. (C) 32+8+4=44, 3×16=48 → YES. (D) 32+6+10=48, 3×24=72 → YES. (E) 64+20+20=104, 3×32=96 → NO.
Answer: **(C)(D)**.
- Key concept: two-level page table sizing — fitting the outer page table within a single page.

### Problem 15

This problem is worth 10 points on probabilistic optimal page replacement.

With the Markov chain transition probabilities, we compute expected page faults for an optimal (clairvoyant about probabilities) replacement algorithm. The 1st reference is always a fault. The 2nd is always a fault (frames initially empty, first 2 distinct pages). For the 3rd and 4th references, the optimal algorithm evicts the page least likely to be referenced next. Detailed computation yields expected faults. Answer: **(A)** E[2nd page fault] = 1 — TRUE (2 frames, 3 pages, first 2 refs guaranteed to miss).
- Key concept: optimal page replacement with probabilistic future references — Markov chain analysis.

### Problem 16

This problem is worth 10 points on real-time scheduling with soft deadlines.

With EDF (optimal for dynamic priority): (A) TRUE — if utilization > 1 with S1=S2=1 (hard deadlines), EDF cannot schedule it. (B) TRUE — EDF is optimal and U≤1 is sufficient for schedulability. For S2=2: P2 only needs to meet half its deadlines, effective utilization of P2 is C2/(2×T2). (D) If C1/T1 + C2/(2×T2) ≤ 1, schedulable — plausible but depends on exact formulation.
- Key concept: EDF schedulability analysis and soft real-time deadline relaxation.

### Problem 17

This problem is worth 10 points on Banker's algorithm safe sequences.

Computing Need = Max - Allocation, then finding all valid safe sequences where each thread can be satisfied with Available + released resources. This requires systematic enumeration. The answer encodes as 1000W+100X+10Y+Z. After careful analysis of which threads can run first (T0 and T2 have small needs relative to available), counting all valid orderings. Checking the arithmetic constraints to determine W,X,Y,Z.
- Key concept: Banker's algorithm — safe sequence enumeration and resource allocation verification.

## 113

### Problem 1

This problem is worth 5 points on power management.

(B) TRUE: Power ∝ V². P_new = 10 × (1.1/1.3)² = 10 × 0.716 = 7.16W ≈ 7.2W. (C) TRUE: For memory-bound programs, reducing CPU frequency has minimal impact since the bottleneck is memory. (A) FALSE: reducing voltage reduces max frequency. (D) FALSE: DVFS reduces dynamic power, not leakage. (E) FALSE: turning off caches loses dirty data.
- Key concept: CMOS power equation (P ∝ CV²f), DVFS effects, and memory-bound program behavior.

### Problem 2

This problem is worth 5 points on pipeline timing.

Stage latencies: IF=500, ID=700, EX=300, MEM=600, WB=400. Pipeline clock = max stage = 700ps. Non-pipeline store = IF+ID+EX+MEM = 500+700+300+600 = 2100ps (no WB for store). (A) Store needs 2100ps non-pipelined, not 2500 — FALSE? Actually all instructions pass through all 5 stages in pipelined design, but non-pipelined latency for store = sum of stages used. (B) Load latency pipelined = 5×700 = 3500ps; non-pipelined = 2500ps; ratio = 3500/2500 = 1.4x — TRUE. (C) Data memory used by Load(25%) + Store(20%) = 45% → utilization = 45% < 50% — TRUE. (D) Throughput: pipelined = 1/700ps; non-pipelined avg = 1/(weighted avg latency). Non-pipeline avg ≈ 2340ps. Speedup = 2340/700 = 3.34x > 3x — TRUE.
- Key concept: pipeline throughput vs. latency analysis with varying instruction types.

### Problem 3

This problem is worth 5 points on memory hierarchy concepts.

(E) TRUE: Write-invalidate protocol causes additional coherence misses in shared-memory multithreaded programs on multiprocessors. (A) FALSE: compilers cannot fully optimize for cache — programmers' help needed. (B) FALSE: three Cs are compulsory, capacity, conflict (not coherence). (C) FALSE: write-through writes immediately to lower level (not write-back). (D) FALSE: VIVT has aliasing problems; VIPT is the common solution.
- Key concept: cache coherence protocol effects, three Cs model, and write policies.

### Problem 4

This problem is worth 5 points on FP8 floating-point formats.

E4M3: exponent 4 bits (bias 7), mantissa 3 bits. Max value = (1+7/8) × 2^(14-7) = 1.875 × 128 = 240. E5M2: exponent 5 bits (bias 15), mantissa 2 bits. Max value = (1+3/4) × 2^(30-15) = 1.75 × 32768 = 57344. (A) E4M3 max (240) < E5M2 max (57344) — FALSE. (D) E5M2 max (57344) < unsigned 16-bit max (65535) — FALSE. Actually E5M2 with special exponent handling: max = 57344 < 65535. Hmm. (C) E4M3 max (240) < unsigned 8-bit max (255) — FALSE. Need to check exact FP8 specs. The standard E4M3 max = 448, E5M2 max = 57344. (C) 448 > 255 — TRUE. (D) 57344 < 65535 — FALSE.
- Key concept: floating-point format range analysis — exponent bits determine range, mantissa bits determine precision.

### Problem 5

This problem is worth 5 points on LLM training systems.

(B) TRUE: Benchmark results can estimate full training time (article mentions Venigalla's 2-day estimate from 11-minute benchmark). (D) TRUE: Parallel computing is critical — from 256 to 3584 accelerators significantly reduces training time. (E) TRUE: Going from 256 (430min) to 512 (65min) accelerators shows >6x speedup, indicating improved efficiency. (A) FALSE: H100 has 80GB memory; 175B params × 1 byte = 175GB > 80GB per GPU. (C) FALSE: benchmark evaluates speed, not accuracy.
- Key concept: LLM training scalability, memory requirements, and benchmark interpretation.

### Problem 6

This problem is worth 5 points on MLPerf benchmark interpretation.

(C) TRUE: 80 of 88 entries used Nvidia GPUs — most results are on GPUs. (D) TRUE: H100 uses TSMC 5nm, and about half the entries use H100. (E) TRUE: The article mentions dataset privacy risks for recommendation benchmarks. (A) FALSE: Intel Gaudi2 (not GPU) also competes. (B) FALSE: Even with FP8 (2x improvement), Gaudi2 is "competitive" but not confirmed faster per-chip than H100.
- Key concept: MLPerf benchmark ecosystem — GPU dominance, manufacturing process, and data privacy challenges.

### Problem 7

This problem is worth 10 points on LLM workload characterization with roofline model.

(B) TRUE: The generation stage has AI=1 (memory-bound at 1 TFLOPS vs peak 165 TFLOPS); increasing memory bandwidth directly improves performance. (E) TRUE: Compressing FP16→FP8 halves the data to load, effectively doubling arithmetic intensity from 1 to 2, approximately doubling the attainable throughput (still memory-bound). (A) FALSE: More ALUs don't help when memory-bound. (C) FALSE: Higher clock increases power quadratically, hurting efficiency. (D) FALSE: LLM inference is dominated by matrix-vector operations, not control flow.
- Key concept: roofline model application to LLM — identifying memory-bound workloads and effective optimization strategies.

### Problem 8

This problem is worth 10 points on DGX GH200 system architecture for LLM training.

(A) TRUE: 32×144GB = 4.6TB > 1T params × 1 byte; performance = 32 × 4900 GB/s × 1 FLOP/byte ≈ 157 TFLOPS, vs peak 128 PFLOPS → about 0.1% utilization — the calculation makes the point about bandwidth limitation. (B) Scalability from the article actually shows good scaling (256→3584 nearly linear). (D) SSD bandwidth (256GB/s) vs CPU memory (500GB/s) — not that far, but much slower than HBM. Still feasible as poor man's solution. (E) FALSE: cache blocking increases data reuse, not block size.
- Key concept: system architecture analysis for LLM training — memory capacity, bandwidth bottlenecks, and data movement optimization.

### Problem 9

This problem is worth 20 points (10 True/False, 2 points each) on OS concepts.

A. **No.** Less threading can sometimes improve performance by reducing contention, synchronization overhead, and cache thrashing.
- Key concept: threading overhead vs. parallelism benefit.

B. **No (in general).** Bélády's anomaly shows more frames can increase page faults for some algorithms (FIFO). For LRU, fewer frames do increase faults.
- Key concept: Bélády's anomaly.

C. **No.** Smaller quantum increases context switch overhead but can reduce waiting time for short jobs, potentially decreasing average turnaround time (better responsiveness).
- Key concept: time quantum trade-off in Round-Robin scheduling.

D. **No.** Firmware executes faster in RAM than ROM because RAM has lower access latency.
- Key concept: ROM vs. RAM access speed and firmware shadowing.

E. **No (not necessarily).** Working set model can be implemented purely in software by tracking page reference bits via periodic timer interrupts; MMU support (reference bits) helps but isn't strictly required.
- Key concept: working set model implementation — software vs. hardware support.

F. **Yes.** By definition, from a safe state, there exists a sequence in which all processes can complete; thus deadlock cannot occur.
- Key concept: safe state guarantees deadlock avoidance.

G. **No (partially).** Context switch is primarily done in software (saving/restoring registers). Some hardware support exists (e.g., task state segment in x86), but the statement is misleading — it's not "usually" a single hardware instruction.
- Key concept: context switch implementation — mostly software with some hardware assist.

H. **Yes.** Session semantics means changes are visible only to sessions opened after the file is closed (not to concurrent sessions).
- Key concept: session semantics in file systems (AFS-style).

I. **No.** Real-time schedulers schedule based on deadlines (EDF) or periods (RMS), not just "real-time priorities." The goal is meeting timing constraints, not generic priority ordering.
- Key concept: real-time scheduling — deadline-driven vs. priority-driven.

J. **No.** Two-phase locking ensures serializability but does NOT prevent deadlock.
- Key concept: 2PL guarantees serializability but allows deadlock.

### Problem 10

This problem is worth 5 points on device driver and event handling.

The mouse driver (bottom half / tasklet) reads the mouse displacement data from the device buffer, updates the cursor position in kernel data structures, and notifies the window manager. The driver is called by the top-half ISR (interrupt handler) scheduling the bottom-half work. The window manager knows to redraw because it receives an event notification (e.g., via input event subsystem, poll/select on /dev/input, or signal/callback mechanism).
- Key concept: interrupt handling split (top-half/bottom-half), device driver role, and event notification to user-space.

### Problem 11

This problem is worth 10 points on intrusive linked lists.

Intrusive linked lists embed the link pointers within the data structure itself, so each element requires only ONE memory allocation (the item itself). Traditional linked lists require a SEPARATE allocation for the list node containing a pointer to the data, requiring two allocations per element and an extra pointer dereference. Benefits: (1) fewer memory allocations (less fragmentation, less allocator overhead), (2) better cache locality (link and data are contiguous), (3) no extra indirection for accessing data from the list node.
- Key concept: intrusive vs. non-intrusive linked lists — memory allocation efficiency and cache locality in kernel data structures.

### Problem 12

This problem is worth 15 points on Round-Robin scheduler implementation.

Round-Robin with 1ms preemption: Use a hardware timer interrupt every 1ms. The ISR saves the current task's context (registers, PC), moves it to the end of the ready queue, and dispatches the next task from the front of the queue. Resume is automatic via the circular queue traversal.

For daemon T (run 1s, block 1s): Count timer ticks; after 1000 ticks (1 second of running), initiate an asynchronous I/O operation (e.g., timer-based sleep) and voluntarily yield/block. The I/O completion callback or timer interrupt after 1 second re-enqueues T.

```
// Pseudo-code for daemon T
while (true) {
    work_start = get_tick();
    while (get_tick() - work_start < 1000) { do_work(); } // run 1s
    async_sleep(1000, &callback);  // non-blocking sleep request
    block_self();  // remove from ready queue
}
// callback: re-enqueue T to ready queue
```
- Key concept: timer-interrupt-driven preemption for Round-Robin and using asynchronous I/O for precise blocking intervals.

## 114

### Problem 1

This problem is worth 5 points on critical-section solutions.

Answer: **(D)**. Mutex locks enforce mutual exclusion: acquire before entering critical section, release after leaving. This prevents concurrent updates to shared memory. (A) Memory barriers enforce ordering, not mutual exclusion. (B) Atomic instructions do prevent concurrent updates, but the statement's wording about "completing load and store" is misleading — they provide atomicity, not just completion. (C) Semaphores with value=1 enforce mutual exclusion; value>1 allows multiple access — but "can allow more than one" doesn't mean they always do, so this is a misleading choice. (D) is the most clearly correct.
- Key concept: mutual exclusion mechanisms — mutex locks provide acquire/release semantics for critical sections.

### Problem 2

This problem is worth 5 points on RAID.

Answer: **(B)**. RAID 5 uses block-interleaved distributed parity to provide redundancy. (A) FALSE: reliability comes from redundancy, not parallelism. (C) MTBF calculation: mean time to data loss = MTBF²/(2×MTTR) = (100000)²/(2×10) = 500,000,000 hours = 57,077 years ≈ not 5,700 years — so FALSE (actual is ~57,000 years, not 5,700). Actually let me recalculate: 100000²/(2×10) = 10^10/20 = 5×10^8 hours / 8760 = 57,077 years. The answer says 5,700 — that's off by 10x. FALSE. (D) RAID 10 is mirrored first, then striped (RAID 1+0), not "stripped first mirrored later" — that would be RAID 0+1. FALSE.
- Key concept: RAID levels — RAID 5 distributed parity, RAID 10 vs RAID 01, and reliability calculations.

### Problem 3

This problem is worth 3 points on fork() behavior.

After `fork()`: child sets value=20, prints A=20. Parent waits, then prints B=10 (original value, since fork creates separate address space). A=20, B=10. Check: (A) A+B²=20+100=120 — TRUE. Answer: **(A)**.
- Key concept: fork() creates independent address spaces — child modifications don't affect parent.

### Problem 4

This problem is worth 7 points on paging system calculations.

Physical memory = 64KB → P = 16 bits. Page size = 2KB → offset = 11 bits. Page table has 1024 entries → logical pages = 1024 → page number bits = 10 → L = 10+11 = 21 bits. EMA with page fault: hit case = 2×180ns (page table + memory access) with 60% hit; page fault case = 30,000,000ns. X = 0.6×(2×180) + 0.4×30,000,000 = 216 + 12,000,000 = 12,000,216ns. Inverted page table entries N = physical frames = 64KB/2KB = 32. With TLB (90% hit, 20ns search): Y = 0.9×(20+180) + 0.1×(20+2×180) = 0.9×200 + 0.1×380 = 180 + 38 = 218ns. Speedup = X/Y = 12,000,216/218 ≈ 55,046x. A=55045, B=55047 → checking choices with L=21, P=16, N=32.
- Key concept: effective memory access time with page tables, TLBs, and page faults.

### Problem 5

This problem is worth 10 points on working-set page replacement.

32-bit address, 8KB pages → offset D=13 bits, page number P=19 bits. Extracting page numbers from hex addresses: 4DF6BC24→page=4DF6B(hex)>>1=0x9BECB? Actually: page = address >> 13. Let me compute: 0x4DF6BC24 >> 13 = 0x4DF6BC24 / 8192 = 0x26FB5 (page). Computing all page numbers and tracking working set with W=6, applying the locality-based replacement algorithm to count faults F. Then check which polynomial equation is satisfied.
- Key concept: working-set page replacement with locality-based eviction policy.

### Problem 6

This problem is worth 5 points on file open operations.

(D) TRUE: When "foo.txt" is already open in the system, the file control block (FCB) is already in kernel memory, so there's no need to read it from secondary storage (Operation ⑤ skipped). (C) TRUE: When file doesn't exist, a new directory entry is created.
- Key concept: file open sequence — leveraging cached FCBs for already-open files.

### Problem 7

This problem is worth 5 points on Round-Robin scheduling.

All arrive at 0, bursts: 10,8,5,2,4. With RR quantum=4 (assumed standard): Simulate the Gantt chart. Each process runs up to quantum, then goes to back of queue. Computing waiting times for each process and averaging. Average waiting time = sum of waiting times / 5. Check which arithmetic constraint is satisfied.
- Key concept: Round-Robin scheduling simulation and average waiting time calculation.

### Problem 8

This problem is worth 5 points on deadline-monotonic scheduling.

Deadline-monotonic assigns priority by deadline (shorter deadline = higher priority). Order: P3(D=5) > P1(D=10) = P2(D=10) = P4(D=10) = P5(D=10). (C) Check P4 schedulability: P4 has period=20, burst=10, deadline=10. Higher priority: P3(burst=5, period=800). Response time of P4: R4 = 10 + ceil(10/800)×5 = 10+5 = 15 > D4=10 — NOT schedulable. FALSE? Need more careful analysis with all higher-priority tasks.
- Key concept: deadline-monotonic scheduling — priority assignment by relative deadline and response time analysis.

### Problem 9

This problem is worth 5 points on OS/architecture concepts.

(B) TRUE: A single-core system supports concurrency via time-sharing without parallelism. (C) TRUE: `pthread_join()` blocks the calling thread until the target terminates — synchronous threading. (A) FALSE: 2 cores don't guarantee 2x speedup (Amdahl's law, synchronization). (D) FALSE: waiting→ready, not waiting→running. (E) FALSE: compile-time binding is static binding.
- Key concept: concurrency vs. parallelism, pthread synchronization, and process state transitions.

### Problem 10

This problem is worth 8 points on x86-64 assembly reverse engineering.

From the assembly: offset 0x120 = 288 bytes for `lst` field. Structure size for `a[i]`: `lea (rdi,rdi,4), rax` → rax = 5i; `lea (rsi,rax,8), rax` → rax = up + 40i. So each np_struct is 40 bytes. `mov 0x8(rax), rdx` → np->idx at offset 8. `mov rcx, 0x10(rax, rdx, 8)` → np->x[np->idx] at offset 16 + 8×idx. np_struct = {long idx (8 bytes at offset 8? — offset 0 might be padding), long x[] starting at offset 16}. Size=40: if first 8 bytes unused, idx at 8, x[] at 16 with (40-16)/8 = 3 longs. CN: offset of lst = 4(fst) + CN×40 = 288 → CN×40 = 284 → not integer. Recheck: `up->fst` at offset 0 (4 bytes), `up->lst` at 0x120=288. 288 - 4 = 284 for array → 284/40 = 7.1 — doesn't divide evenly. With padding: fst(4) + pad(4) + a[CN]×40 + lst → 8 + 40×CN = 288 → CN=7. np_struct: 40 bytes = {long idx(8), long idx1(8), long x[3](24)} = 40. Answer: **(D) CN=7, nt_struct = {long idx, long idx1, long x[3]}**.
- Key concept: reverse-engineering structure layout from x86-64 assembly addressing patterns.

### Problem 11

This problem is worth 5 points on compiler performance comparison.

(A) CPI_A = 1.5s/(1.0×10⁹ × 1ns) = 1.5 — TRUE. (B) CPI_B = 1.8s/(1.5×10⁹ × 1ns) = 1.2, not 1.5 — FALSE. (C) Same execution time: Clock_A × CPI_A × IC_A = Clock_B × CPI_B × IC_B. If times equal on different processors: f_A/f_B = (CPI_B × IC_B)/(CPI_A × IC_A) = (1.2×1.5G)/(1.5×1.0G) = 1.8/1.5 = 1.2, not 1.37 — FALSE. (D) Compiler C: time = 4×10⁸ × 0.8 × 1ns = 0.32s. Speedup vs A = 1.5/0.32 = 4.69 < 4.7 — TRUE. (E) Speedup vs B = 1.8/0.32 = 5.625 > 4 — FALSE (it's MORE than 4x, so "less than 4x" is false).
- Key concept: CPI calculation and cross-compiler/cross-processor performance comparison.

### Problem 12

This problem is worth 6 points on pipeline timing analysis.

Stage latencies: IF=150, ID=250, EX=50, MEM=200, WB=100. Pipeline clock = max = 250ps. (A) FALSE: clock cycle = 250ps, not 350ps. (B) Non-pipelined LW = 150+250+50+200+100 = 750ps, not 850 — FALSE. (C) Pipelined LW latency = 5×250 = 1250ps — TRUE. (D) Data memory utilization: LW(20%)+SW(15%)=35% — TRUE.
- Key concept: pipeline clock cycle determination and instruction latency in pipelined vs. non-pipelined datapaths.

### Problem 13

This problem is worth 5 points on load delay slot behavior.

`addi x3, x0, 0x18` → x3 = 24. `lw x3, 8(x3)` → x3 = MEM[24+8] = MEM[32] = -4. If load delay slot is IGNORED (meaning the load result IS available immediately): `add x4, x3, x3` → x4 = (-4)+(-4) = -8. Answer: **(C) -8**.
- Key concept: load delay slot — when ignored, the loaded value is available to the next instruction.

### Problem 14

This problem is worth 4 points on cache data store sizing.

2-way set associative, tag store = 13×2⁸ = 13×256 bits per way? Total tag store = 13×256 bits. Per set: 2 ways × (valid+dirty+LRU+tag) bits. Sets × 2 × (1+1+1+tag) = 13×256. With page=1KB, VA=1MB(20 bits), block=8B(3 offset bits), sets=S, index=log2(S) bits. Physical tag bits = 20-10+phys... Need careful derivation. Data store = sets × ways × block_size = S × 2 × 8. From tag store equation: S × 2 × (1+1+1+tag_bits) = 13×256 → S×2×(3+tag) = 3328. If tag=10: S×26=3328→S=128. Data=128×2×8=2048=2KB. Answer: **(B) 2KB**.
- Key concept: deriving cache data store size from tag store specifications.

### Problem 15

This problem is worth 4 points on VIPT virtual index bits.

Cache: 2KB data (128 sets × 2 ways × 8 bytes). Index bits = log2(128) = 7. Block offset = 3 bits. Total bits from address for indexing = 7+3 = 10. Page offset = log2(1KB) = 10 bits. Virtual index bits from page number = total index bits - (page_offset - block_offset) = 7 - (10-3) = 0. All index bits come from page offset. Answer: **(A) 0 bits** — wait, that's not an option. Reconsidering: index = 7 bits, offset = 3 bits → bits 3-9 used for index. Page offset = 10 bits (bits 0-9). So all 7 index bits are within page offset → 0 bits from virtual page number. But (A) says 1 bit. Let me recheck with data store = 2KB assumption. Actually if data store is larger, e.g. 4KB: sets=256, index=8, bits 3-10 for index, page offset bits 0-9. Bit 10 comes from VPN → 1 bit. Answer: **(A) 1 bit**.
- Key concept: VIPT aliasing — counting how many index bits extend beyond the page offset into the virtual page number.

### Problem 16

This problem is worth 4 points on pipeline execution cycle counting.

Loop iterations = 1099. Per iteration: `fld f0`(6 cycles latency), `fld f1`(6), `fadd.d f2,f0,f1`(needs f0,f1 → stalls for fld latency), `fsd f2`(needs f2 → stalls for fadd latency), `addi`, `ble`. With latencies: fld=6 means result available after 6 cycles. fadd=4. Counting stalls per iteration and total cycles. Total ≈ 23 cycles × 1099 + startup = ~25277 ≈ 25283. Answer: **(D) 25283**.
- Key concept: pipeline cycle counting with floating-point instruction latencies and data hazard stalls.

### Problem 17

This problem is worth 8 points on loop optimization with register moves.

Using `fmv.d` to carry values between iterations: f0 from previous iteration's f1, f1 from previous f2. This eliminates one `fld` per iteration. Optimized loop: `fmv.d f0,f1; fmv.d f1,f2; fld f1,-8(x11)` — wait, better: keep f0=A[i-2] and f1=A[i-1] across iterations, only load nothing new? Actually each iteration produces A[i]=A[i-1]+A[i-2], so next iteration needs A[i] and A[i-1]. Use fmv to shift: f0←f1, f1←f2 (the just-computed value). This eliminates both fld's after the first iteration. Cycles per iteration ≈ 7. Total ≈ 7×1099 + startup ≈ 7703. Answer: **(A) 7703**.
- Key concept: register renaming/moving to eliminate redundant loads across loop iterations.

### Problem 18

This problem is worth 6 points on superscalar out-of-order execution.

Thread X: A1(2 cycles), A2(depends A1), A3(conflicts A1), A4(depends A2). Thread Y: B1, B2(conflicts B1), B3, B4(depends B2). Each core has 2 functional units, out-of-order. Core 0 (Thread X): Cycle 1: issue A1+A3(conflict!)→only A1. Cycle 2: A1 continues + A3 starts. Cycle 3: A2 starts (A1 done) + A3 finishes? A2 depends on A1(done cycle 2). Cycle 3: A2 executes. Cycle 4: A4 executes. Total X = 4 cycles, 4 issue slots used out of 8 → 4 wasted. Core 1 (Thread Y): Cycle 1: B1+B3 (no conflict between them). Cycle 2: B2 (B1 done) + B4 waits. Cycle 3: B4 (B2 done). Total Y = 3 cycles, slots: 2+1+1=4 used out of 6→2 wasted. But both threads total: 4 cycles (longer thread), total slots = 4×2×2=16, used=8? Answer: **(B) 4 cycles, 3 slots wasted** — checking: X takes 4 cycles with 2 FUs → 8 slots, 4 used (A1,A1,A2,A3,A4=5 slots?). Need more careful analysis. Answer: **(A) 4 cycles, 7 slots wasted** seems more likely for the combined system view.
- Key concept: superscalar out-of-order execution — issue slot utilization with structural and data hazards.
