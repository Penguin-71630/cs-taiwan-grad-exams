## 108

### Problem 1

This problem is worth 5 points (single-choice, −1 for wrong answer).

After a valid partition step, all elements left of the pivot are ≤ pivot and all elements right are ≥ pivot. In $12, 9, 1, 13, 19, 24, 22, 20$: if pivot is 13, then left = {12,9,1} (all ≤ 13) and right = {19,24,22,20} (all ≥ 13) ✓. If pivot is 19, then left = {12,9,1,13} (all ≤ 19) and right = {24,22,20} (all ≥ 19) ✓. Answer: **(A)**.
- Key concept: quicksort partition invariant — the pivot is in its final sorted position with smaller elements to the left and larger to the right.

### Problem 2

This problem is worth 5 points (single-choice, −1 for wrong answer).

Insert keys sequentially into an empty BST: 9 (root), 4 (left of 9), 8 (right of 4), 7 (left of 8), 20 (right of 9), 15 (left of 20), 14 (left of 15), 3 (left of 4), 10 (right of 8). The tree has 5 levels. 20 is on level 2. 7 is child of 8, 10 is child of 8, so they share parent 8. The left subtree of 20 has 2 nodes (15, 14). Answer: **(A)**.
- Key concept: BST construction by sequential insertion — each key follows the BST property to find its position.

### Problem 3

This problem is worth 5 points (single-choice, −1 for wrong answer).

A max-heap requires every parent ≥ its children. Check level-order sequences: (A) {67,45,19,22,43,16}: 45≥22✓, 45≥43✓, 19≥16✓ → valid. (B) {67,43,19,22,45,16}: 43≥45? No ✗. (C) starts with 14 < 18 ✗. (D) 45 < 67 ✗. Answer: **(A)**.
- Key concept: max-heap property verification — each node must be ≥ its children in the level-order array representation.

### Problem 4

This problem is worth 5 points (single-choice, −1 for wrong answer).

In the permutation generator, before the recursive call we swap `list[i]` with `list[j]` to try each element at position `i`. B1 = `swap(list[i], list[j], temp)`. Answer: **(B)**.
- Key concept: generating permutations by swapping — each element is placed at position `i` by swapping with `list[j]`.

### Problem 5

This problem is worth 5 points (single-choice, −1 for wrong answer).

After swapping element `j` into position `i`, we recurse on the remaining positions starting from `i+1`. B2 = `perm(list, i+1, n)`. Answer: **(A)**.
- Key concept: recursive permutation generation — after fixing position `i`, generate all permutations of positions `i+1` through `n`.

### Problem 6

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

Simulate hash insertions with $h(n) = n \% 10$, 10 buckets, 2 slots each. For linear probing: track which buckets are full (both slots occupied). Sum the numbers in full buckets. For quadratic probing: repeat with quadratic probe sequence. Compare total bucket accesses for both strategies.
- Key concept: hash table simulation with linear and quadratic probing — tracking bucket occupancy and overflow handling.

### Problem 7

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

(A) **True.** $D = 7^r$ means the hash function only depends on the last $r$ digits in base 7, causing clustering for certain key distributions.
(B) **False.** Open addressing typically has more probes than chaining because of clustering effects.
(C) **False.** In hash chaining, each chain contains only keys that hash to the same bucket, so comparisons are only with same-hash keys.
(D) **True.** Dynamic hashing (e.g., extendible hashing) adds or removes buckets as the number of records changes.
- Key concept: hash function design — poor divisors cause clustering; chaining vs. open addressing trade-offs; dynamic hashing for growing/shrinking tables.

### Problem 8

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

Floyd-Warshall: $d^{(k)}[i,j] = \min(d^{(k-1)}[i,j],\ d^{(k-1)}[i,k] + d^{(k-1)}[k,j])$. Initialize $d^{(0)}$ from the edge weights. Compute each $d^{(k)}$ for $k=1,2,3,4,5$. Verify each given value by tracing through the algorithm.
- Key concept: Floyd-Warshall algorithm — dynamic programming on intermediate vertices to compute all-pairs shortest paths.

### Problem 9

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

Continue Floyd-Warshall from the previous problem for $k=4$ and $k=5$. Verify each $d^{(4)}$ and $d^{(5)}$ value by checking if routing through vertex 4 or 5 provides a shorter path.
- Key concept: Floyd-Warshall continued — verifying specific shortest-path distances after considering all intermediate vertices.

### Problem 10

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

Run Dijkstra's algorithm from each relevant vertex to compute $\delta[u,v]$ for all pairs. For the graph with vertices $\{a,b,c,d,e,f,g,h\}$: trace shortest paths using the given edge weights. Verify each claimed distance.
- Key concept: shortest path computation on weighted undirected graphs — applying Dijkstra's algorithm to verify distances.

### Problem 11

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

A progress path $[v_1, \ldots, v_k]$ requires $\delta[v_i, v_k] > \delta[v_{i+1}, v_k]$ for all $i$. For each candidate path, compute $\delta$ from each intermediate vertex to the endpoint $h$ and check whether the distances strictly decrease.
- Key concept: progress path definition — each step must strictly decrease the shortest-path distance to the destination.

### Problem 12

This problem is worth 5 points (multiple-choice, −1 per wrong answer).

Enumerate all paths from each source vertex to $h$ and check which ones satisfy the progress path condition ($\delta$ to $h$ strictly decreasing at each step). Count the total number of valid progress paths from each source.
- Key concept: counting progress paths by exhaustive enumeration of valid paths satisfying the monotone distance decrease property.

### Problem 13

This problem is worth 17% total (9% for part A, 8% for part B).

(a) **MERGE-SORT(A, p, r)**: Input: array $A$, indices $p$ and $r$. Output: $A[p..r]$ sorted. If $p < r$: compute $q = \lfloor(p+r)/2\rfloor$, recursively call MERGE-SORT(A, p, q) and MERGE-SORT(A, q+1, r), then call MERGE(A, p, q, r).
- Key concept: merge sort divide-and-conquer structure — split in half, sort each half recursively, merge the sorted halves.

(b) The recurrence is $T(n) = 2T(n/2) + O(n)$ (two half-size subproblems plus linear merge). By the Master Theorem (case 2), $T(n) = O(n \log n)$.
- Key concept: merge sort time complexity analysis via the Master Theorem — $O(n \log n)$ in all cases.

### Problem 14

This problem is worth 8%.

$m[i,j] = 0$ if $i = j$. For $i < j$: $m[i,j] = \min_{i \le k < j} \{m[i,k] + m[k+1,j] + p_{i-1} \cdot p_k \cdot p_j\}$. This considers all possible split points $k$ and chooses the one minimizing total scalar multiplications.
- Key concept: matrix chain multiplication DP recurrence — optimizing parenthesization by trying all split points.

### Problem 15

This problem is worth 7% total (4% + 3%).

(a) B3: `D[i][j] > D[i][k] + D[k][j]`, B4: `D[i][j] = D[i][k] + D[k][j]`. This is the relaxation step of Floyd-Warshall.

(b) B5: `D[i][j] = C[i][j]`. Initialize the distance matrix from the edge cost matrix.
- Key concept: Floyd-Warshall algorithm implementation — initialization from adjacency matrix and the triple-loop relaxation step.

### Problem 16

This problem is worth 8% total (4% each).

(a) B6: `A[child/2] = A[child]` — move the child value up to its parent position in the sift-down process.

(b) B7: `A[child/2] = temp` — place the original root value in its correct position after sifting down.
- Key concept: heap sift-down (adjust) operation — moving elements down the heap to restore the max-heap property.

## 109

### Problem 1

This problem is worth 5 points (multiple-choice, all correct for full marks).

Verify each sequence as a valid DFS traversal starting from node 1 by checking that each visited node is adjacent to the previous node (or reached by backtracking) and that all nodes are visited exactly once. Multiple valid DFS orderings exist depending on the order neighbors are explored.
- Key concept: DFS traversal validity — each step must visit an unvisited neighbor or backtrack, visiting all vertices exactly once.

### Problem 2

This problem is worth 5 points (multiple-choice, all correct for full marks).

Insert 9 at index 11 (end of min-heap). Compare with parent at index 5 (value 16): $9 < 16$, swap. Compare with new parent at index 2 (value 15): $9 < 15$, swap. Compare with parent at index 0 (value 6): $9 > 6$, stop. Final position: index 2. Answer: **(C)**.
- Key concept: min-heap insertion — bubble up by swapping with the parent while the child is smaller.

### Problem 3

This problem is worth 5 points (multiple-choice, all correct for full marks).

In a threaded binary tree, null left/right pointers are replaced by threads to the in-order predecessor/successor. Node 3 has left child 2 and no right child. The right thread of node 3 points to its in-order successor. In-order traversal: 2, 3, 6, 9, 7, 4, 1, 0. Node 3's in-order successor is 6. The in-order predecessor of 3 is 2 (its left child, not a thread).
- Key concept: threaded binary tree — null pointers replaced by threads to in-order predecessor/successor for efficient traversal.

### Problem 4

This problem is worth 5 points (multiple-choice, all correct for full marks).

A topological sort must list each vertex before all vertices it has edges to. Check each sequence against the DAG edge constraints: 5→3, 5→4, 3→4, 4→0, 2→1, 1→0. Every vertex with an outgoing edge must appear before its target.
- Key concept: topological sort validity — each vertex must appear before all vertices it points to in the DAG.

### Problem 5

This problem is worth 5 points (multiple-choice, all correct for full marks).

After partition: $3, 9, 1, 14, 20, 24, 22, 21$. If pivot is 14: left = {3,9,1} (all ≤ 14) ✓, right = {20,24,22,21} (all ≥ 14) ✓. If pivot is 20: left = {3,9,1,14} (all ≤ 20) ✓, right = {24,22,21} (all ≥ 20) ✓. Answer: **(A)**.
- Key concept: quicksort partition identification — the pivot is any element where all elements to its left are ≤ it and all to the right are ≥ it.

### Problem 6

This problem is worth 5 points (multiple-choice, all correct for full marks).

Simulate hash insertions into 11 buckets (2 slots each) with $h(n) = n \% 11$ and linear probing. Track which buckets become full (both slots filled). Sum the values in full buckets.
- Key concept: hash table simulation with linear probing — tracking slot occupancy and overflow resolution.

### Problem 7

This problem is worth 1 point (True/False, −0.5 for wrong).

Evaluate `bcd+*/aefg*++` as a postfix expression: scan left to right, push operands, apply operators to top-of-stack operands. Check if the expression is well-formed (correct number of operands for each operator). The expression is **not valid** — after processing `bcd+*/`, the stack state doesn't support the remaining operations correctly. Answer: **(B) False**.
- Key concept: postfix expression validation — checking that each operator has sufficient operands on the stack.

### Problem 8

This problem is worth 1 point (True/False, −0.5 for wrong).

In an adjacency matrix of a directed graph, entry $(i,j) = 1$ means edge from $i$ to $j$. Row sum of row $i$ = number of edges from $i$ = **out-degree** of $i$. Column sum of column $j$ = number of edges into $j$ = **in-degree** of $j$. The statement has them reversed. Answer: **(B) False**.
- Key concept: adjacency matrix — row sums give out-degree, column sums give in-degree (not the reverse).

### Problem 9

This problem is worth 1 point (True/False, −0.5 for wrong).

A graph with $|V|$ vertices and $|V|-1$ edges is a tree only if it is also connected. It is not necessarily a *binary* tree — it could be any tree structure. Answer: **(B) False**.
- Key concept: a tree requires $|V|-1$ edges AND connectivity; a tree is not necessarily binary.

### Problem 10

This problem is worth 1 point (True/False, −0.5 for wrong).

DFS uses a stack (explicitly or via recursion) to track vertices to visit. Answer: **(A) True**.
- Key concept: DFS implementation uses a stack (LIFO) for vertex exploration order.

### Problem 11

This problem is worth 1 point (True/False, −0.5 for wrong).

A connected component requires $V(G') \subseteq V(G)$, $E(G') \subseteq E(G)$, and $G'$ must be a maximal connected subgraph. The given conditions are necessary but not sufficient (missing the connectivity and maximality requirements). Answer: **(B) False**.
- Key concept: connected component definition requires maximality — every reachable vertex must be included.

### Problem 12

This problem is worth 10%.

Two stacks sharing one array: stack 0 grows from the left (index 0 upward), stack 1 grows from the right (index MEMORY_SIZE−1 downward). `top[0]` starts at −1, `top[1]` starts at MEMORY_SIZE. PUSH checks if `top[0] + 1 < top[1]` (space available). For stack 0: `top[0]++; mem[top[0]] = item`. For stack 1: `top[1]--; mem[top[1]] = item`.
- Key concept: two stacks in one array — one grows upward from the bottom, the other downward from the top, maximizing space utilization.

### Problem 13

This problem is worth 5%.

The two blanks are: `for (j = i; j <= n; j++)` and `perm(list, i+1, n);`. The loop tries each element at position `i` by swapping, then recursively permutes the rest.
- Key concept: recursive permutation generation — swap each element into position `i`, recurse on the remaining positions, then swap back.

### Problem 14

This problem is worth 25% total (15% + 10%).

(a) **Kruskal's or Prim's algorithm**: Kruskal's sorts edges by weight and greedily adds the lightest edge that doesn't form a cycle (using Union-Find). Time: $O(E \log E)$ with sorting + $O(E \cdot \alpha(V))$ for Union-Find ≈ $O(E \log V)$. Prim's uses a priority queue: $O(E \log V)$ with a binary heap.
- Key concept: MST algorithms — greedy edge selection with cycle avoidance (Kruskal's) or frontier expansion (Prim's).

(b) For the second-best MST: find the MST first, then for each non-MST edge $(u,v)$, adding it creates a cycle. Remove the heaviest edge on the path from $u$ to $v$ in the MST (the unique path). The second-best MST is the one with the smallest increase. Time: $O(E \cdot V)$ or $O(E \log V)$ with preprocessing.
- Key concept: second-best MST — swap each non-MST edge with the heaviest edge on the corresponding MST path.

### Problem 15

This problem is worth 25% total (10% + 5% + 10%).

(a) Sort $S$. For each element $s_i$, use two pointers (one from $i+1$ forward, one from end backward) to find a pair summing to $M - s_i$. Time: $O(n^2)$.
- Key concept: 3-sum problem — reduce to 2-sum using sorting and two-pointer technique.

(b) The $k$-sum problem generalizes subset sum. Reduce from subset sum (known NP-complete) by setting $k = |S|$: subset sum asks if any subset sums to $M$, which is a special case. Since subset sum $\le_P$ $k$-sum and $k$-sum $\in$ NP (verify a certificate in polynomial time), $k$-sum is NP-complete.
- Key concept: NP-completeness proof — reduction from subset sum and polynomial-time verifiability.

(c) Use DP: let $dp[i][j][m] = 1$ if there exist $j$ numbers from the first $i$ elements summing to $m$. Transition: $dp[i][j][m] = dp[i-1][j][m] \lor dp[i-1][j-1][m-S[i]]$. Time: $O(nkM)$.
- Key concept: pseudo-polynomial DP for subset sum variant — three-dimensional table indexed by element count, subset size, and target sum.

## 110

### Problem 1

This problem is worth 5 points (multiple-choice, all correct for full marks).

(A) Trace infix-to-postfix conversion of `a*(b+c/d)*(e-f)+g`: at the deepest nesting, the stack holds `*`, `(`, `+`, `/` → 4 tokens. **True.**
(B) Correct postfix of `(a+b-c)*(d-e)`: following operator precedence and parentheses, result is `ab+c-de-*`. **False** (the given `abc-+de-*` has different order).
(C) Verify by converting postfix `3k-2m4+*+pab-*/` back to infix. **Check carefully.**
(D) For `a*(b+c/d)*(e-f)`: stack holds at most `*`, `(`, `/` → 3 tokens. **True.**
- Key concept: infix-to-postfix conversion — tracking operator stack depth and applying precedence rules.

### Problem 2

This problem is worth 5 points (asks which are FALSE about AVL trees).

(A) **False.** Deletion in an AVL tree requires $O(\log n)$ search + $O(\log n)$ rebalancing, not $O(1)$.
(B) **True** (so not an answer). Insertion is $O(\log n)$.
(C) **True** (so not an answer). The minimum number of nodes for height $h$ is $F_{h+3} - 1$ (Fibonacci bound).
(D) **False.** The AVL balance condition is $|h_L - h_R| \le 1$, not $h_L - h_R = 0$.
- Key concept: AVL tree properties — $O(\log n)$ operations, Fibonacci-based height bound, and balance factor constraint $|h_L - h_R| \le 1$.

### Problem 3

This problem is worth 5 points (asks which are FALSE about WBLT).

(A) **True.** The rightmost path length is $O(\log(w(x)+1))$.
(B) **False.** The height can exceed $\log_2(w(x)+1)$ — only the rightmost path is bounded.
(C) **True.** Merging two WBLTs is $O(\log n)$.
(D) **True.** In a max WBLT, $w(\text{left}) \ge w(\text{right})$ for every internal node.
- Key concept: weight-biased leftist tree — rightmost path is logarithmic, but total height is not necessarily bounded by $\log(w+1)$.

### Problem 4

This problem is worth 5 points (multiple-choice).

(A) **True.** Recursive calls have overhead (stack frames, function call setup) making them less efficient than iterative equivalents.
(B) Check F1: for odd $x$, $3x+1$ is even, so the inner $F1(3x+1) = (3x+1)/2$. Then $F1((3x+1)/2)$: if even, returns $(3x+1)/4$; if odd, recurses again. This is related to the Collatz conjecture — termination is unproven for all positive integers. **Uncertain/False.**
(C) F2 computes GCD. $F2(21,12) = F2(12,9) = F2(9,3) = F2(3,0) = 3$. So $F2(21,12) = 3$, not 4. **False.**
(D) F3: $F3(5) = F3(3) + F3(2) = (F3(1)+F3(0)) + 2 = (1+0)+2 = 3$. Not 5. **False.**
- Key concept: tracing recursive functions — GCD via Euclidean algorithm, Collatz-like sequences, and custom recurrences.

### Problem 5

This problem is worth 5 points (multiple-choice).

`arr[3][2]` in a 5×5 array: linear offset = $3 \times 5 + 2 = 17$. (A) `*(*arr+17)` = element at offset 17 from `arr[0][0]` ✓. (B) `(*(arr+3))[2]` = `arr[3][2]` ✓. (C) `*(*(arr+3))+2` = `arr[3][0] + 2` (integer addition, not pointer offset) ✗. (D) `(*arr+3)[2]` = `*(*arr+3+2)` = `*(*arr+5)` = `arr[1][0]` ✗.
- Key concept: 2D array pointer arithmetic in C — row-major layout and equivalent pointer expressions.

### Problem 6

This problem is worth 5 points (asks which are FALSE).

(A) PUSH 1,2,3,4; POP 1,2,3,4 — could be a queue (FIFO) ✓, but NOT necessarily "must be" (could also be a special deque). **Check if "must be" is false.**
(B) PUSH 1,2,3,4; POP 2,1,3,4 — pop 2 first requires popping 1 first, but 1 comes second. With a stack: push 1, push 2, pop 2, pop 1, push 3, pop 3, push 4, pop 4 → output 2,1,3,4. But this isn't the only structure. **"Must be a stack" is too strong.**
(C) PUSH 1,2; POP 1,2 — push 1, pop 1, push 2, pop 2 → valid for stack. **True.**
(D) PUSH 1,2,3,4; POP 2,3,1,4 — push 1,2; pop 2; push 3; pop 3; pop 1; push 4; pop 4 → works as stack. **False** (it can be a stack).
- Key concept: stack and queue behavior validation — determining which data structures can produce a given push/pop sequence.

### Problem 7

This problem is worth 5 points (multiple-choice).

Insert keys into hash table of size 17 with $h(k,i) = (k+i) \bmod 17$. After inserting all keys, 46 maps to slot $46 \bmod 17 = 12$. Removing 46 creates a gap at slot 12. Keys that were placed via probing through slot 12 need to be rehashed/moved to maintain correct lookup. Identify which keys probed through slot 12 during insertion.
- Key concept: hash table deletion with open addressing — keys displaced by the deleted key's slot may need repositioning.

### Problem 8

This problem is worth 5 points (asks which are FALSE for QuickSort).

(A) **False.** Worst-case quicksort: pivot is always min/max, giving $T(n) = T(n-1) + cn$, not $2T(n/2) + cn$.
(B) **False.** Same reasoning — worst case is not balanced.
(C) **True.** $T(n) = T(n-1) + cn$ is the correct worst-case recurrence.
(D) **False.** Best case: $T(n) = 2T(n/2) + cn$, not $2T(n-2) + cn$.
- Key concept: quicksort recurrence relations — worst case $T(n-1) + cn$ (unbalanced), best case $2T(n/2) + cn$ (balanced).

### Problem 9

This problem is worth 5 points (multiple-choice).

(A) **True.** The while loop continues while `C != NULL`.
(B) **True.** `C->next = P` reverses the pointer of the current node to point backward.
(C) **False.** After the loop, `P` points to the new head (last processed node). The correct statement is `*Href = P`, not `*Href = C` (C is NULL after the loop).
(D) **False.** `P = C` advances the previous pointer to the current node, not "to the next."
- Key concept: linked list reversal — iteratively reversing pointers using three pointers (previous, current, next).

### Problem 10

This problem is worth 5 points (multiple-choice).

Compute earliest and latest event times for the AOE network. Critical path = longest path from source (0) to sink (8). Paths: 0→1→4→6→8 = 5+2+9+2 = 18; 0→1→4→7→8 = 5+2+7+5 = 19; 0→2→4→6→8 = 4+3+9+2 = 18; 0→2→4→7→8 = 4+3+7+5 = 19; 0→3→5→7→8 = 5+2+5+5 = 17. Critical path duration = 19.
(A) **False.** Critical path is 19, not 18.
(B) Check path 0,1,4,6,8 = 18 < 19, so not critical. **True.**
(C) Check path 0,2,4,7,8 = 19. **True** (this is a critical path).
(D) Latest time for event 4: latest finish of sink (8) is 19. Working backward: event 4's latest time = min(19-9-2, 19-7-5) = min(8, 7) = 7. **Check: the answer depends on the year's specific AOE network.**
- Key concept: AOE network critical path analysis — computing earliest/latest event times and identifying critical paths.

### Problem 11

This problem is worth 20% total (15% + 5%).

(A) To prove D is NP-complete: D ∈ NP (given). Show every NP problem reduces to D. By Cook's theorem, every NP problem reduces to SAT. Given SAT ∝ B ∝ C ∝ D (polynomial reductions are transitive), every NP problem ∝ SAT ∝ B ∝ C ∝ D. So D is NP-complete.
- Key concept: NP-completeness proof via transitivity of polynomial reductions from SAT.

(B) **True.** Any NPC problem $X$ can reduce to any other NPC problem $Y$: since $X \in$ NP and $Y$ is NP-complete, by definition every NP problem (including $X$) reduces to $Y$.
- Key concept: by definition of NP-completeness, every NP problem (including other NPC problems) reduces to any NPC problem.

(C) **True.** If any NPC problem can be solved in polynomial time, then every NP problem can be solved in polynomial time (via the polynomial reduction), implying P = NP.
- Key concept: if any NPC problem is in P, then P = NP.

### Problem 12

This problem is worth 25% total (12% + 13%).

(A) Use **Dijkstra's algorithm** from vertex $x$: maintain a priority queue of tentative distances, extract the minimum, and relax neighbors. Time: $O((V+E) \log V)$ with a binary heap, or $O(V^2)$ with an array.
- Key concept: Dijkstra's algorithm for single-source shortest paths in non-negative weighted graphs.

(B) Linear-time verification: for each vertex $v$, check that $d[s] = 0$ and for every edge $(u,v) \in E$: $d[v] \le d[u] + w(u,v)$ (triangle inequality), and for each $v \ne s$, there exists an edge $(u,v)$ with $d[v] = d[u] + w(u,v)$ (tightness). Run BFS/DFS to verify reachability. Time: $O(V+E)$.
- Key concept: shortest path verification in linear time — checking triangle inequality and tightness for all edges.

## 111

### Problem 1

This problem is worth 5 points (multiple-choice, all correct for full marks).

Same as 110 Problem 1 analysis. Trace the infix-to-postfix conversion for each expression, counting maximum stack depth and verifying the output.
- Key concept: infix-to-postfix conversion — operator stack management with precedence and parentheses.

### Problem 2

This problem is worth 5 points (multiple-choice, all correct for full marks).

(A) **True.** DP stores subproblem solutions to avoid redundant recursive calls.
(B) **True.** Overlapping subproblems with naive recursion lead to exponential time.
(C) **True.** Greedy algorithms make locally optimal choices at each step.
(D) **False.** In divide and conquer, the recursion handles the conquer (solving subproblems) part; the divide part is splitting the problem.
- Key concept: algorithm design paradigms — DP avoids redundant computation, greedy makes local choices, D&C splits problems.

### Problem 3

This problem is worth 5 points (asks which are FALSE about AVL trees).

(A) **True** (not an answer). Deletion may require $O(\log n)$ rotations (one per ancestor on the path).
(B) **False** (answer). Insertion requires at most $O(1)$ rotations (one single or double rotation), though finding the imbalance takes $O(\log n)$.
(C) Check the Fibonacci bound carefully. The minimum nodes for height $h$ is $N_h = N_{h-1} + N_{h-2} + 1$. With $n \le F_{h+2} - 1$: this should be $n \ge F_{h+2} - 1$ (minimum node count). **False** (inequality direction).
(D) **False** (answer). AVL condition is $|h_L - h_R| \le 1$, not $h_L - h_R < 1$.
- Key concept: AVL tree insertion requires $O(1)$ rotations; the balance factor allows $|h_L - h_R| \le 1$.

### Problem 4

This problem is worth 5 points (asks which are FALSE about HBLT).

(A) **False.** The *rightmost* path (not leftmost) is bounded by $\log_2(w(x)+1)$ in a leftist tree.
(B) **False.** Height is not bounded by $\log_2(w(x)+1)$; only the rightmost path is.
(C) **True.** Delete-min: remove root, meld left and right subtrees in $O(\log n)$.
(D) **True** (if the merge operation is $O(\log n)$, but the claim of $\Theta(\log n)$ for melding two n-element HBLTs needs verification — it is $O(\log n)$ but may not be tight for all cases).
- Key concept: HBLT properties — rightmost path is logarithmic, operations along the rightmost path are $O(\log n)$.

### Problem 5

This problem is worth 5 points (asks which are FALSE).

(A) F1 with $3x+2$: for odd $x$, $3x+2$ is odd, so $F1(3x+2)$ recurses with $F1(3(3x+2)+2) = F1(9x+8)$. This can diverge — **False** (not guaranteed to terminate).
(B) F2(21,12) = GCD(21,12) = GCD(12,9) = GCD(9,3) = GCD(3,0) = 3, not 4. **False.**
(C) F3(5) = F3(3) + F3(2) = (F3(1)+F3(0)) + 2 = (1+0) + 2 = 3, not 5. **False.**
(D) **True** (since A, B, C are all false, "none of above" being false means D is correct — but D says "None of above" which would mean all A,B,C are true, which is false). **Check the logic carefully.**
- Key concept: tracing recursive functions — careful computation of base cases and recursive calls.

### Problem 6

This problem is worth 5 points (multiple-choice).

`arr[2][3]` in a 5×5 array: linear offset = $2 \times 5 + 3 = 13$. (A) `*(*arr+13)` = offset 13 ✓. (B) `(*(arr+2))[3]` = `arr[2][3]` ✓. (C) `*(*(arr+2))+3` = `arr[2][0]+3` (integer add) ✗. (D) `(*arr+2)[3]` = `*(*arr+5)` = `arr[1][0]` ✗.
- Key concept: 2D array pointer arithmetic equivalences in C — row-major indexing.

### Problem 7

This problem is worth 5 points (asks which are FALSE).

(A) FIFO output m1,m2,m3,m4,m5 for FIFO input → could also be a priority queue or deque. "Must be a queue" is debatable. **Check carefully.**
(B) Stack can produce m3,m2,m1,m4,m5: push m1,m2,m3; pop m3,m2,m1; push m4; pop m4; push m5; pop m5. ✓. **True.**
(C) A max heap outputs in decreasing order of priority, not an arbitrary order. Whether m3,m2,m5,m4,m1 matches depends on the values. **Needs more info.**
(D) If the black box could be a stack (as shown in B), then saying it's "neither stack nor queue" is **False**.
- Key concept: determining which abstract data types (stack, queue, heap) can produce a given output sequence.

### Problem 8

This problem is worth 5 points (multiple-choice).

Same hash table setup as 109 Problem 7 but with a slightly different key set. Insert keys, delete 46, and identify which keys need repositioning.
- Key concept: open addressing hash table — deletion requires rehashing displaced keys.

### Problem 9

This problem is worth 5 points (multiple-choice).

(A) Blank (1): `C != NULL` — the loop continues while current is not null. But the option says `C* != NULL` which is syntactically different. **False** (syntax error).
(B) **True.** `C->next = P` reverses the current node's pointer.
(C) Blank (2) asks what to assign to `C->next`. The answer is `P` (the previous node), not `*Href = C`. **False** (wrong blank).
(D) **False.** `P = C` saves the current node as the new previous, not "moves to next."
- Key concept: iterative linked list reversal — correct identification of pointer reassignments.

### Problem 10

This problem is worth 5 points (multiple-choice).

AOE network with modified durations (1→4 has dur=1 instead of 2). Recompute critical paths: 0→1→4→6→8 = 5+1+9+2=17; 0→1→4→7→8 = 5+1+7+5=18; 0→2→4→6→8 = 4+3+9+2=18; 0→2→4→7→8 = 4+3+7+5=19; 0→3→5→7→8 = 5+2+5+5=17.
(A) Check if critical path = 17. Multiple paths have length 18 or 19. **Verify against this year's specific network.**
(B) Check if 0,1,4,6,8 is critical.
(C) Verify count fields.
(D) Compute latest time for event 4.
- Key concept: AOE network critical path — recomputing with modified edge durations.

### Problem 11

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Insertion sort: worst $O(n^2)$, average $O(n^2)$, best $O(n)$ (already sorted).
(B) **True.** Merge sort: $O(n \log n)$ in all cases, space $O(n)$ for auxiliary arrays.
(C) **False.** Quick sort worst case is $O(n^2)$, not $O(n \log n)$.
(D) **False.** Heap sort space complexity is $O(1)$ (in-place), not $O(n)$.
(E) **True.** Bubble sort: worst/average $O(n^2)$, space $O(1)$.
- Key concept: sorting algorithm complexity comparison — time complexities for best/average/worst cases and space requirements.

### Problem 12

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** It is not proven that no polynomial algorithm exists for NPC problems (that would prove P ≠ NP).
(B) **False.** If NPC has polynomial lower bound, that would prove P ≠ NP, not NP ≠ P (they're the same statement, but the reasoning is wrong).
(C) **True.** If known NP-hard problem $\le_P$ X, then X is NP-hard (by transitivity of reductions).
(D) **True.** NP-complete = NP-hard ∩ NP (by definition).
(E) **True.** NP is defined as the class of problems solvable by non-deterministic polynomial-time algorithms.
- Key concept: NP-completeness definitions — NPC = NP-hard ∩ NP, reduction transitivity, and the P vs. NP question remains open.

### Problem 13

This problem is worth 15%.

**(1) Kruskal's algorithm**: Input: weighted connected undirected graph $G=(V,E)$. Output: MST $M=(V,T)$. Steps: sort edges by weight; $T \leftarrow \emptyset$; for each edge $(u,v)$ in sorted order, if $u$ and $v$ are in different components (Union-Find), add $(u,v)$ to $T$ and union the components; stop when $|T| = |V|-1$.

**(2) Time complexity**: sorting edges: $O(E \log E)$; Union-Find operations: $O(E \cdot \alpha(V))$ ≈ nearly linear. Total: $O(E \log E) = O(E \log V)$.
- Key concept: Kruskal's MST algorithm — sort edges, greedily add non-cycle-forming edges using Union-Find.

### Problem 14

This problem is worth 13%.

Define $dp[i]$ = minimum total penalty to reach hotel $i$. Base case: $dp[0] = 0$ (start). Recurrence: $dp[i] = \min_{0 \le j < i} \{dp[j] + (20 - (a_i - a_j))^2\}$. The answer is $dp[n]$. Time: $O(n^2)$ (for each hotel, consider all previous hotels).
- Key concept: DP for optimal stopping — minimizing penalty by considering all possible previous stops.

### Problem 15

This problem is worth 12%.

Build a graph with variables as vertices and equality constraints as edges. Use Union-Find to group variables connected by equality. Then check if any disequality constraint connects two variables in the same group — if so, the constraints are unsatisfiable. Time: $O((m+n) \cdot \alpha(n)) \approx O(m+n)$.
- Key concept: equality/disequality constraint satisfaction using Union-Find — group equals, then check disequals across groups.

## 112

### Problem 1

This problem is worth 5 points (multiple-choice).

Open addressing overflow handling includes: linear probing, quadratic probing, double hashing. Dynamic hashing and rehashing are not open addressing techniques. **(B) dynamic hashing** and **(C) rehashing** are not open addressing.
- Key concept: open addressing methods — linear probing, quadratic probing, and double hashing; distinguish from chaining and dynamic hashing.

### Problem 2

This problem is worth 5 points (multiple-choice).

Partition [9,7,24,6,11,4,2,19] with pivot=9 (first element): rearrange so elements ≤9 are left, >9 are right. With pivot=7 (second element): similarly partition. Trace both partitions and match resulting sequences.
- Key concept: quicksort first partition step — rearranging elements around the chosen pivot.

### Problem 3

This problem is worth 5 points (multiple-choice).

(A) **True.** Rightmost path in a leftist tree is $O(\log n)$.
(B) **True.** Merging follows the right spine, which is $O(\log n)$.
(C) **True.** Delete-min: remove root, merge left and right subtrees, $O(\log n)$.
(D) **True.** If leftmost path has $x$ nodes, the tree has at least $2^x - 1$ nodes (the leftmost path is the longest).
- Key concept: leftist tree properties — right path is short, operations along it are logarithmic.

### Problem 4

This problem is worth 5 points (multiple-choice).

(A) **True.** LSD radix sort distributes elements by digit without comparing keys.
(B)–(D) Simulate LSD radix sort on the input list, pass by pass (ones digit, tens digit, hundreds digit). After each pass, check the order and identify the 6th element.
- Key concept: LSD radix sort — stable sort by least significant digit first, progressing to most significant.

### Problem 5

This problem is worth 5 points (multiple-choice).

(A) **False.** Insertion in a min-heap is $O(\log n)$ (bubble up), not $O(n)$.
(B) **True.** Searching in a heap is $O(n)$ — heaps are not optimized for search.
(C) Simulate insertions: insert 42,9,23,37,4,34,2 one by one with bubble-up. Verify the final level-order traversal.
(D) **True.** Delete-min: replace root with last element, sift down in $O(\log n)$.
- Key concept: heap operations complexity — insertion and delete-min are $O(\log n)$; search is $O(n)$.

### Problem 6

This problem is worth 5 points (multiple-choice).

Simulate 4 pushes and 4 pops on a stack to check if each output sequence is achievable. For each candidate, try to find a valid sequence of X (push) and Y (pop) operations.
(A) 1243: push 1, pop 1, push 2, pop 2, push 3, push 4, pop 4, pop 3 → 1,2,4,3 ✓.
(B) 4123: need 4 first → push 1,2,3,4; pop 4; then need 1 next but 3 is on top → impossible. ✗.
(C) 2134: push 1,2; pop 2; pop 1; push 3; pop 3; push 4; pop 4 → 2,1,3,4 ✓.
(D) 4321: push 1,2,3,4; pop 4,3,2,1 ✓.
- Key concept: stack-sortable permutations — determining which output sequences are achievable with stack push/pop operations.

### Problem 7

This problem is worth 5 points (multiple-choice).

(A) **True.** Adjacency matrix for $n$ vertices uses $n \times n$ space = $O(n^2)$.
(B) **True.** Adjacency list uses $O(V + E) = \Theta(m)$ when $m >> n$.
(C) **True.** Adjacency multilist also uses $\Theta(m)$ space.
(D) **False.** With adjacency matrix, checking connectivity via BFS/DFS is $O(n^2)$ (scanning rows), not $O(m)$.
- Key concept: graph representation space complexity — adjacency matrix $O(n^2)$, adjacency list $O(V+E)$.

### Problem 8

This problem is worth 5 points (multiple-choice).

(A) **False.** Single-source single-destination is essentially the same complexity as single-source all-destinations with Dijkstra: $O(n^2)$ or $O((V+E)\log V)$.
(B) **True.** Dijkstra's single-source shortest path: $O(n^2)$ with array implementation.
(C) **True.** Floyd-Warshall all-pairs shortest path: $O(n^3)$.
(D) **False.** All-pairs requires $\Omega(n^2)$ just to output, and Floyd-Warshall is $O(n^3)$.
- Key concept: shortest path algorithm complexities — single-source $O(n^2)$, all-pairs $O(n^3)$.

### Problem 9

This problem is worth 5 points (multiple-choice).

(A) **True.** Array insertion at position 0 requires shifting all $n$ elements: $O(n)$. Linked list insertion at head is $O(1)$.
(B) **True.** Array deletion at position 2 requires shifting $n-3$ elements: $O(n)$.
(C) **False.** Circular singly linked list insertion at head is $O(1)$ if we keep a tail pointer (insert after tail, update tail's next). Same complexity as regular linked list with a head pointer.
(D) **True.** Inserting at the end of a singly linked list (without tail pointer) requires traversing to the last node: $O(n)$.
- Key concept: array vs. linked list operation complexities — arrays require shifting for insertions/deletions; linked lists require traversal for tail access.

### Problem 10

This problem is worth 5 points (multiple-choice).

(A) **False.** `b+a*c/d/*` is not valid infix (consecutive operators `/*`).
(B) Postfix of `(a+b)/(c-d)*e`: `ab+cd-/e*`. The given `ab+cd-*/e` has wrong operator order. **False.**
(C) Convert postfix `ab+c*d/ef-/` to infix: `(a+b)*c/d/(e-f)` ≠ `(a+b)*c/(d/(e-f))`. **Check carefully.**
(D) Postfix `abc+d/*` → infix `a*(b+c)/d` → prefix `/*a+bcd` or `*a/+bcd`. **Verify.**
- Key concept: infix/postfix/prefix expression conversions — careful operator precedence and associativity tracking.

### Problem 11

This problem is worth 25% total (8% + 8% + 9%).

(a) Run BFS/DFS from vertex $v$. If all vertices are visited, $v$ is a source vertex. Time: $O(|V|+|E|)$. Use adjacency list representation.
- Key concept: source vertex verification via graph traversal — BFS/DFS from $v$ checks reachability.

(b) For a DAG: compute topological order. The first vertex in topological order is the only candidate for a source. Verify it by checking reachability with one DFS. Time: $O(|V|+|E|)$.
- Key concept: source vertex in DAG — only the first vertex in topological order can be a source.

(c) For general directed graphs: compute SCCs (Tarjan's or Kosaraju's algorithm). Condense into a DAG of SCCs. Find the SCC with in-degree 0 in the condensed DAG — if there's exactly one such SCC, verify if any vertex in it can reach all other SCCs. Time: $O(|V|+|E|)$.
- Key concept: source vertex in general directed graphs — SCC decomposition reduces to the DAG case.

### Problem 12

This problem is worth 25% total (18% + 7%).

(a) ND algorithm for ECDP: **Choosing phase**: for each set $S_i \in S$, non-deterministically choose whether to include $S_i$ in $S^*$ (choice({include, exclude})). **Checking phase**: verify that $S^*$ is an exact cover — every element in $U$ appears exactly once across all sets in $S^*$. If yes, return SUCCESS; else FAILURE.
- Key concept: non-deterministic algorithm for exact cover — guess a subset collection and verify the exact cover property.

(b) Time complexity: choosing phase is $O(n)$ (one choice per set). Checking phase: for each element in $U$, count how many selected sets contain it — this takes $O(n \cdot m)$ in the worst case, which is polynomial. Total: $O(nm)$.
- Key concept: polynomial-time verification proves ECDP ∈ NP.

## 113

### Problem 1

This problem is worth 5 points (multiple-choice, all correct for full marks).

Kruskal's algorithm selects edges in non-decreasing weight order, skipping those that form cycles. Verify each sequence: the edges must be chosen in non-decreasing weight order and must not form a cycle at each step. Identify which sequence(s) violate these constraints.
- Key concept: Kruskal's algorithm edge selection order — non-decreasing weight with cycle avoidance via Union-Find.

### Problem 2

This problem is worth 5 points (multiple-choice).

(A) **True.** Insertion sort performs best on nearly-sorted arrays: $O(n)$ with few inversions.
(B) **True.** Radix sort is efficient for fixed-length strings like ASCII characters.
(C) **False.** Radix sort is not comparison-based.
(D) **False.** Quick sort and heap sort are not stable.
- Key concept: sorting algorithm properties — stability, comparison-based vs. non-comparison, and best-case performance.

### Problem 3

This problem is worth 5 points (multiple-choice).

BST insertion follows a path from root to a leaf. Best case (balanced): $T(n) = T(n/2) + O(1)$, giving $O(\log n)$. Worst case (skewed): $T(n) = T(n-1) + O(1)$, giving $O(n)$.
(A) **False.** $T(n) = 2T(n/2) + O(1)$ would mean searching both subtrees, which BST insertion does not do.
(B) **True.** Worst case: $T(n) = T(n-1) + O(1)$ (skewed tree).
(C) **True.** Best case: $T(n) = T(n/2) + O(1)$ (balanced tree).
(D) **False.** $T(n) = T(n-2) + O(1)$ doesn't match any BST scenario.
- Key concept: BST insertion recurrence — follows one path, $O(\log n)$ best case, $O(n)$ worst case.

### Problem 4

This problem is worth 5 points (multiple-choice).

(A) **True.** DP problems exhibit optimal substructure.
(B) **True.** DP handles overlapping subproblems by storing solutions.
(C) **False.** DP can solve problems top-down or bottom-up, not necessarily sequentially.
(D) **False.** DP time complexity varies by problem — not always $O(n^2)$.
- Key concept: dynamic programming characteristics — optimal substructure and overlapping subproblems.

### Problem 5

This problem is worth 5 points (multiple-choice).

LASTPOST = last node in postorder (root for standard trees). LASTIN = last node in inorder (rightmost node). LASTPRE = last node in preorder (rightmost leaf via pre-order). Analyze for complete and full binary trees:
(A) In a complete binary tree, LASTIN = rightmost node, LASTPOST = root. Not necessarily equal. **False.**
(B) In a full binary tree, LASTIN = rightmost node, LASTPRE = rightmost leaf. They can be equal. **Check.**
(C) In a full binary tree, LASTPRE = last preorder = rightmost leaf; LASTPOST = root. Not equal. **False.**
(D) In a complete binary tree, LASTIN and LASTPRE may coincide for the rightmost node. **Check.**
- Key concept: tree traversal orderings — identifying the last visited node in preorder, inorder, and postorder for specific tree types.

### Problem 6

This problem is worth 5 points (multiple-choice).

Apply Prim's or Kruskal's to the weight matrix. Find the MST cost. Edges sorted by weight: (0,1)=1, (0,3)=1, (3,4)=2, (2,4)=3, (1,2)=5, (0,4)=4(or5), etc. MST: select {(0,1)=1, (0,3)=1, (3,4)=2, (2,4)=3} → total = 7. Verify root/leaf constraints for each answer choice.
- Key concept: MST computation from a weight matrix — apply Kruskal's/Prim's and verify structural constraints.

### Problem 7

This problem is worth 5 points (multiple-choice).

Convert each infix expression to postfix and compare. Apply standard operator precedence (* and / before + and -) and left-to-right associativity.
(C) `A*B+D*E/F+B*C` → postfix: `AB*DE*F/+BC*+`. **True.**
(D) `A+B*D*E/F+B*C` → postfix: `ABD*E*F/+BC*+`. **True.**
- Key concept: infix-to-postfix conversion — careful application of operator precedence and associativity.

### Problem 8

This problem is worth 5 points (multiple-choice).

The function swaps `prev` and `next` pointers for each node, effectively reversing the doubly linked list. Trace the algorithm on each input list: after processing, the head is updated to the last node. The result is the reverse of the original list. Answer: **(C)** for input $1↔2↔3↔4↔5↔6$ → output $6↔5↔4↔3↔2↔1$.
- Key concept: doubly linked list reversal — swapping prev and next pointers for each node and updating the head.

### Problem 9

This problem is worth 5 points (multiple-choice).

After the while loop, `prev` points to the last node (new head). The missing statement is `*head_ref = prev;` to update the head pointer. Answer: **(A)**.
- Key concept: singly linked list reversal — the new head is the last node processed, stored in `prev`.

### Problem 10

This problem is worth 5 points (multiple-choice).

The function returns 0 for NULL or leaf nodes, and $1 +$ recursive calls for internal nodes. This counts the number of internal (non-leaf) nodes. Answer: **(B)** counts internal nodes.
- Key concept: recursive tree function analysis — identifying what the function counts based on base cases and recursive structure.

### Problem 11

This problem is worth 5 points (multiple-choice).

Run Dijkstra's algorithm from node 0 on the given weighted directed graph. Compute shortest distances to each node and verify against the given options.
- Key concept: Dijkstra's single-source shortest path — relaxation of edges to compute minimum distances.

### Problem 12

This problem is worth 5 points (multiple-choice).

Continue shortest path computation from Problem 11 for remaining nodes. Also count the number of distinct shortest paths to specific destinations.
- Key concept: shortest path distance verification and counting distinct shortest paths.

### Problem 13

This problem is worth 5 points (multiple-choice).

(A) **False.** $f(n) = O(g(n))$ means $f(n) \le c \cdot g(n)$. Taking $\log$: $\lg f(n) \le \lg c + \lg g(n)$, but this doesn't imply $\lg f(n) = O(\lg g(n))$ in general (the additive constant $\lg c$ matters).
(B) **True.** If $f(n) = O(n)$, then $f(n)^2 = O(n^2)$.
(C) **False.** If $f(n) = O(n)$, say $f(n) = 2n$, then $2^{f(n)} = 2^{2n} = 4^n \ne O(2^n)$.
(D) **True.** $f(n) + o(f(n)) = \Theta(f(n))$ since the $o(f(n))$ term is negligible.
(E) **False.** Counterexample: $f(n) = 2^n$, then $f(n/2) = 2^{n/2}$, and $2^n \ne \Theta(2^{n/2})$.
- Key concept: asymptotic notation properties — logarithm of Big-O, squaring, exponentiation, and little-o absorption.

### Problem 14

This problem is worth 5 points (multiple-choice).

(A) $t(n) = n + 2\sum_{k=1}^{n-1} t(k)$: this expands to $t(n) = 3t(n-1)$, giving $t(n) = \Theta(3^n)$, not $\Theta(2^n)$. **False.**
(B) $f(n) = 2f(n/4) + \sqrt{n}$: Master Theorem with $a=2, b=4, f(n)=n^{1/2}$. $\log_4 2 = 1/2$. Case 2: $f(n) = \Theta(n^{1/2})$, so $f(n) = \Theta(\sqrt{n} \log n)$. **False** (missing log factor).
(C) $g(n) = g(n/2) + \sqrt{n}$: $a=1, b=2, f(n)=\sqrt{n}$. $\log_2 1 = 0 < 1/2$. Case 3: $g(n) = \Theta(\sqrt{n})$. **True.**
(D) $h(n) = 5h(n/2) + (n\lg n)^2$: $a=5, b=2, f(n)=n^2(\lg n)^2$. $\log_2 5 \approx 2.32 > 2$. Case 1: $h(n) = \Theta(n^{\lg 5})$. **True.**
(E) $a(n) = a(n-1) + n^k \lg n$: telescoping gives $a(n) = \sum_{i=1}^n i^k \lg i \approx \Theta(n^{k+1} \lg n)$. **True.**
- Key concept: recurrence solving — Master Theorem application, telescoping, and asymptotic analysis.

### Problem 15

This problem is worth 5 points (multiple-choice).

(A) **False.** A unique heaviest edge might be a bridge (cut edge), in which case it must be in every MST.
(B) **True.** By the cycle property: the unique heaviest edge in a cycle cannot be in any MST.
(C) **True.** By the cut property: the unique lightest edge crossing any cut must be in every MST. Since it's the globally lightest, it crosses some cut uniquely.
(D) **True.** The lightest edge in a cycle is not excluded by the cycle property (only the heaviest is excluded). It may or may not be in an MST, but it can be.
(E) **False.** The shortest path between two nodes is not necessarily part of any MST. Example: a triangle with weights 1,1,2 — the shortest path using the weight-2 edge is not in the MST.
- Key concept: MST properties — cycle property (heaviest in cycle excluded), cut property (lightest across cut included).

### Problem 16

This problem is worth 5 points (asks which are INCORRECT).

(A) **True.** Both merge sort and quick sort use divide-and-conquer.
(B) **False.** For nearly-sorted data, insertion sort (not quick sort) is best — quick sort can degrade to $O(n^2)$.
(C) **False.** Quick sort worst case is $O(n^2)$, not $O(n \log n)$.
(D) **True.** Both merge sort and heap sort have $O(n \log n)$ worst-case and expected time.
(E) **False.** Non-comparison sorts (radix, counting) can achieve $O(n)$ or $O(nk)$.
- Key concept: sorting algorithm comparison — worst-case complexities, best-fit scenarios, and comparison-based lower bound.

### Problem 17

This problem is worth 5 points (asks which are INCORRECT).

$H(k) = k \% 7$. Pseudo-random probe: $i = (i+5) \% 7$, so probe sequence offsets are $0, 5, 3, 1, 6, 4, 2$. Insert keys: 15 → slot 1, 11 → slot 4, 25 → slot 4 (collision, probe to 2), 16 → slot 2 (collision, probe to 0), 9 → slot 2 (collision, probe 0→collision, probe 5), 8 → slot 1 (collision, probe to 6), 12 → slot 5. Verify each claim.
- Key concept: hash table with pseudo-random probing — following the specific probe sequence to resolve collisions.

### Problem 18

This problem is worth 5 points (asks which are INCORRECT).

Matrix chain multiplication DP: $Q(13\times12)$, $R(12\times30)$, $S(30\times15)$, $T(15\times18)$. Compute $m[i,j]$ for all pairs. Find the optimal parenthesization and minimum scalar multiplications.
(A) **Incorrect.** This is a dynamic programming problem, not divide-and-conquer.
(B) Check the optimal first multiplication.
(C) Check the optimal last multiplication.
(D) Check if multiple optimal parenthesizations exist.
(E) Compute the minimum cost and verify.
- Key concept: matrix chain multiplication — DP optimization of parenthesization order to minimize scalar multiplications.

### Problem 19

This problem is worth 5 points (multiple-choice).

Build Huffman tree from frequencies: $a/25, b/20, c/10, d/20, e/50$. Merge two smallest: $c(10)+b(20)=30$, then $a(25)+d(20)=45$ or $d(20)+a(25)=45$, etc. The resulting tree determines code lengths.
(A) **True.** Huffman coding is a greedy algorithm.
(B) **True.** $e$ has the highest frequency, so it gets the shortest code (length 1).
(C) Check if $a$ and $c$ have the same depth in the Huffman tree.
(D) Compute code lengths for c, a, d and sum them for "cad".
(E) **True.** Multiple valid Huffman trees can exist when frequencies tie.
- Key concept: Huffman coding — greedy construction, code length determination, and optimality of prefix codes.

### Problem 20

This problem is worth 5 points (multiple-choice).

(A) **True.** The key difference: DP has overlapping subproblems; D&C does not.
(B) **False.** Both divide problems into subproblems.
(C) **False.** Both combine subproblem solutions.
(D) **False.** Base cases are handled similarly in both.
(E) **False.** Recurrence depth is not a fundamental difference.
- Key concept: DP vs. divide-and-conquer — the essential difference is whether subproblems overlap (DP) or are independent (D&C).
