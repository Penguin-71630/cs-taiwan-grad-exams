## 108

### Problem 1

This problem is worth 5 points (single-choice, −2 for wrong answer).

CPU utilization with multiprogramming = $1 - p^n$ where $p$ is the I/O wait fraction and $n$ is the degree of multiprogramming. Here $p = 0.4$, $n = 6$: utilization $= 1 - 0.4^6 = 1 - 0.004096 = 0.995904$. Answer: **(D)**.
- Key concept: multiprogramming CPU utilization formula — $1 - p^n$ models the probability that at least one process is ready to run.

### Problem 2

This problem is worth 5 points (single-choice, −2 for wrong answer).

CPI_effective = base CPI + instruction miss rate × miss penalty + data access rate × data miss rate × miss penalty = $1 + 0.02 \times 50 + 0.20 \times 0.04 \times 50 = 1 + 1 + 0.4 = 2.4$. Total cycles $H = 2.4 \times 1111 = 2666.4$. Round($H$) = $2666$, $2666 \bmod 5 = 1$. Answer: **(B)**.
- Key concept: computing effective CPI with separate instruction and data cache miss rates and applying it to total cycle count.

### Problem 3

This problem is worth 5 points (single-choice, −2 for wrong answer).

Identify the IEEE 754 bit patterns: A (largest denorm) = $0\ 00000000\ 11...1$, B (smallest positive norm) = $0\ 00000001\ 00...0$, C ($-2.25$) = $1\ 10000000\ 00100...0$, D ($+\infty$) = $0\ 11111111\ 00...0$. XOR all four 32-bit representations bit by bit, then count the number of 1s ($K$) and compute $K \bmod 5$.
- Key concept: IEEE 754 single-precision special values (denormalized, smallest normalized, infinity) and bitwise XOR operations.

### Problem 4

This problem is worth 5 points (single-choice, −2 for wrong answer).

Cache: 16 words total, 2-way set associative, 2-word blocks → 4 sets (16 words / 2 ways / 2 words per block). Block address = word address / 2; set index = block address mod 4. Simulate LRU replacement for each access. Count hits, compute $K \bmod 5$.
- Key concept: set-associative cache simulation — computing set index from word addresses and applying LRU replacement.

### Problem 5

This problem is worth 5 points (single-choice, −2 for wrong answer).

Average CPI = $1 + (0.24 \times 0.25 \times 1) + (0.20 \times 0.5 \times 1) + (0.06 \times 1) = 1 + 0.06 + 0.10 + 0.06 = 1.22$. Load-use stall: 24% loads × 25% followed by dependent instruction × 1 cycle. Branch misprediction: 20% branches × 50% mispredicted × 1 cycle. Jump delay: 6% × 1 cycle. $K = \text{Round}(1.22 \times 234) = \text{Round}(285.48) = 285$, $285 \bmod 5 = 0$. Answer: **(A)**.
- Key concept: average CPI computation with pipeline stalls from load-use hazards, branch mispredictions, and jump delays.

### Problem 6

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **True.** Superscalar machines rely on hardware (dynamic scheduling, out-of-order execution) for ILP.
(B) **False.** A page table is not a cache; it is a data structure for address translation.
(C) **True.** The TLB is a cache for page table entries.
(D) **False.** Microprogramming's advantage is flexibility and ease of modification, not speed.
(E) **True.** TLB hit means the virtual-to-physical translation is cached; the data may still not be in the data cache.
- Key concept: distinguishing hardware mechanisms — superscalar ILP, TLB as a page-table cache, and microprogramming trade-offs.

### Problem 7

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **True.** Multi-cycle CPU uses a shorter clock cycle (determined by the slowest stage, not the slowest instruction), so clock rate is higher.
(B) **True.** Single-cycle CPU completes every instruction in exactly 1 cycle.
(C) **False.** Multi-cycle CPU can have higher throughput because of shorter cycles, despite higher CPI.
(D) **False.** Multi-cycle CPU's cycle time is determined by the slowest *stage*, not the slowest instruction.
(E) **False.**
- Key concept: single-cycle vs. multi-cycle CPU design — clock rate, CPI, and cycle time determination.

### Problem 8

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **True.** `sub` reads `$s0` which is written by `add` → RAW hazard.
(B) **False.** There is no WAW hazard — different destination registers (`$s0` and `$t2`).
(C) **True.** Forwarding the ALU result of `add` to the input of `sub` resolves the RAW hazard.
(D) **False.** No WAW hazard exists to solve.
- Key concept: pipeline data hazards — identifying RAW dependencies and resolving them with data forwarding.

### Problem 9

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **False.** MIPS `jump` is unconditional.
(B) **False.** Branch target buffer is used by hardware, not the compiler.
(C) **True.** Longer pipelines mean more wasted cycles on misprediction, making prediction more critical.
(D) **True.** On misprediction, the pipeline must flush incorrectly fetched instructions and correct the PC.
- Key concept: branch prediction importance in deep pipelines and the hardware mechanisms involved.

### Problem 10

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **False.** Speedup is at most equal to the number of stages (not twice).
(B) **False.** Moving branch execution earlier (to ID stage) reduces the branch penalty, not to IF.
(C) **False.** Balanced stages improve pipeline performance; imbalanced stages waste time.
(D) **False.** Branch hazard is a control hazard, not caused by data cache miss.
(E) **True.** None of the above statements are true.
- Key concept: pipeline speedup limits, balanced stage design, and distinguishing control hazards from data hazards.

### Problem 11

This problem is worth 5 points (multiple-choice, no penalty for wrong).

In a multi-cycle MIPS CPU, different instructions use different numbers of cycles. The instruction memory is accessed in the IF stage (used by every instruction every time it starts). The data memory is used only by `lw` (15%) and `sw` (10%) = 25%. The sign-extend unit is used by `addi`, `beq`, `lw`, `sw` = 20%+25%+15%+10% = 70%. MemtoReg = 1 for `lw` only = 15%.
(A) **False** in multi-cycle context (instruction memory is used each IF cycle, which is not 50% of all cycles).
(B) **True.** Data memory used by lw + sw = 25%.
(C) **True.** Sign-extend used by addi + beq + lw + sw = 70%.
(D) **False.** MemtoReg is on for `lw` = 15%, not 25%.
- Key concept: control signal activation frequencies based on instruction mix in a multi-cycle CPU.

### Problem 12

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **True.** Software interpretation allows the OS to intercept and validate every instruction before execution.
(B) **True.** Compiler-generated protection checks can enforce privilege boundaries in software.
(C) **False.** Running on a cloud server doesn't inherently provide the OS with control over user programs on the local machine.
(D) **False.** Open-source code doesn't guarantee security or OS control.
(E) **False.** ROM-based OS prevents modification but doesn't solve the privilege mode problem.
- Key concept: software-based protection mechanisms when hardware privilege modes are unavailable.

### Problem 13

This problem is worth 5 points (multiple-choice, no penalty for wrong).

With $A < B < 0$: both rates are negative, but the running process's priority drops faster (more negative B). Processes waiting gain a relative advantage because their priority decreases more slowly. The process that has waited longest has the highest priority → **FCFS**. With $A > B > 0$: waiting processes gain priority faster than the running one. The process that has waited the longest gets the highest priority → also **FCFS**. Answer includes **(B)**.
- Key concept: dynamic priority scheduling — how priority change rates in ready vs. running states determine the effective scheduling policy.

### Problem 14

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) Fewer kernel threads than processors → some processors idle, underutilizing hardware.
(B) Equal kernel threads and processors → optimal utilization; blocking one thread may idle a processor.
(C) More kernel threads than processors → context switching overhead but good utilization when threads block.
(D) Only two kernel threads → most processors idle, severe underutilization.
(E) More kernel threads than both processors and user threads → unnecessary kernel thread overhead with no benefit.
- Key concept: many-to-many threading model — the relationship between kernel thread count, processor count, and system utilization.

### Problem 15

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **Safe.** Increasing Available can only make more resources available, never introducing deadlock.
(B) **Unsafe unless** the system remains in a safe state after the decrease; must re-run the safety algorithm.
(C) **Unsafe unless** re-checking shows the system is still in a safe state with the increased Max.
(D) **Safe.** Decreasing Max can only make the safety condition easier to satisfy.
(E) **Safe.** Fewer processes means fewer competing demands, making safe states easier to maintain.
- Key concept: Banker's algorithm safety — changes that relax resource demands (increase Available, decrease Max, fewer processes) are always safe; changes that tighten resources require re-verification.

### Problem 16

This problem is worth 5 points (multiple-choice, no penalty for wrong).

Simulate all three algorithms with 3 frames on the reference string $7, 2, 3, 1, 2, 5, 3, 4, 6, 7, 7, 1, 0, 5, 4, 6, 2, 3, 0, 1$. Count page faults for each. LRU typically gives around 18 faults, FIFO around 17, Optimal around 13. Select matching answers.
- Key concept: page replacement algorithm simulation — LRU, FIFO, and Optimal (Bélády's) algorithms on a reference string with limited frames.

### Problem 17

This problem is worth 5 points (multiple-choice, no penalty for wrong).

Threads from **different processes** do not share global variables, heap, or file descriptors (those are per-process). They also have separate PCs. Answer: **(E) None of the above.** Inter-process sharing requires explicit IPC mechanisms (shared memory, pipes, etc.).
- Key concept: process isolation — threads in different processes have separate address spaces and do not share memory or file descriptors.

### Problem 18

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **True.** Running → waiting (e.g., I/O request) triggers scheduling (nonpreemptive).
(B) **True.** Waiting → ready may trigger preemptive scheduling if the newly ready process has higher priority.
(C) **True.** Ready → running is itself a scheduling decision.
(D) **False.** There is no "decision state" in standard process state models.
- Key concept: CPU scheduling decision points — state transitions that trigger the scheduler.

### Problem 19

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **False.** Smaller pages → more pages → larger page tables.
(B) **True.** Smaller pages mean each TLB entry covers less address space, so more TLB misses occur.
(C) **False.** RR optimizes response time, not throughput.
(D) **True.** RR is preemptive — it preempts a process after its time quantum expires.
- Key concept: page size trade-offs (page table size vs. TLB coverage) and Round Robin as a preemptive scheduling algorithm.

### Problem 20

This problem is worth 5 points (multiple-choice, no penalty for wrong).

(A) **True.** ARP resolves IP addresses to MAC (hardware) addresses.
(B) **True.** DNS translates domain names to IP addresses.
(C) **True.** IPSec provides encryption and authentication for VPN tunnels.
(D) **True.** ICMP (e.g., ping, traceroute) diagnoses IP-layer errors and connectivity issues.
- Key concept: network protocol functions — ARP for address resolution, DNS for name resolution, IPSec for VPNs, ICMP for diagnostics.

## 109

### Problem 1

This problem is worth 5 points (single-choice, −2 for wrong answer).

Un-pipelined: average CPI = $0.3 \times 4 + 0.3 \times 5 + 0.4 \times 5 = 4.7$. Execution time per instruction = $4.7 \times 9 = 42.3$ ns. Pipelined: CPI = 1 (ideal), cycle time = $9 + 1 = 10$ ns. Speedup = $42.3 / 10 = 4.23$. Closest answer: **(B) 4.6** or recalculate. Actually speedup = average unpipelined time / pipelined time = $42.3 / 10 ≈ 4.23$. Check answer choices; the intended answer depends on the exact formula used.
- Key concept: pipeline speedup calculation — comparing average instruction execution time between unpipelined and pipelined implementations.

### Problem 2

This problem is worth 5 points (single-choice, −2 for wrong answer).

4 KB cache, 4-byte blocks → $4096 / 4 = 1024$ blocks. Direct-mapped → 1024 sets. Index bits = $\log_2(1024) = 10$. Offset bits = $\log_2(4) = 2$. Tag bits = $32 - 10 - 2 = 20$. Answer: **(C)**.
- Key concept: cache address field decomposition — computing tag, index, and offset bit widths from cache parameters.

### Problem 3

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Setting timer values is privileged and must be trapped in a VM.
(B) **True.** Reading the clock can reveal VM presence; it is sensitive.
(C) **True.** Clearing memory that belongs to other VMs is privileged.
(D) **True.** Trap instructions transfer control and must be intercepted.
(E) **True.** Disabling interrupts is privileged.
- Key concept: sensitive instructions in virtualization — operations that must be trapped to maintain VM isolation (Popek-Goldberg virtualization requirements).

### Problem 4

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** FCFS does not cause starvation — every process eventually gets its turn.
(B) **True.** SJF can starve long jobs if short jobs keep arriving.
(C) **False.** RR gives every process a time quantum, preventing starvation.
(D) **True.** Priority scheduling can starve low-priority processes indefinitely.
(E) **False.** Weighted fair queueing guarantees each process a minimum share, preventing starvation.
- Key concept: starvation in scheduling algorithms — SJF and priority scheduling are susceptible; FCFS, RR, and WFQ are not.

### Problem 5

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** A counting semaphore is essentially an integer variable.
(B) **False.** It is accessed through two standard operations: `wait` and `signal`.
(C) **False.** It cannot be modified simultaneously — `wait` and `signal` are atomic.
(D) **False.** Semaphores can be used to control access to critical sections.
(E) **False.** The `wait`/`signal` operations execute atomically, but the semaphore itself is not "a single uninterruptible unit."
- Key concept: counting semaphore properties — an integer variable accessed atomically via two operations (wait/signal).

### Problem 6

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) Fewer kernel threads than processors → some CPUs idle, can't fully utilize hardware.
(B) Equal → good balance; if a thread blocks on I/O, one CPU may idle.
(C) More kernel threads than processors, fewer than user threads → good utilization with some overhead.
(D) Only two → most CPUs idle, very poor parallelism.
(E) More kernel threads than user threads → unnecessary overhead, no additional benefit.
- Key concept: many-to-many threading model performance trade-offs based on kernel thread allocation relative to processor and user thread counts.

### Problem 7

This problem is worth 5 points (multiple-choice, per-option scoring).

Simulate LRU, FIFO, and Optimal on the reference string $7, 2, 3, 1, 2, 5, 3, 4, 6, 7, 1, 0, 5, 4, 6, 2, 3, 2, 3, 0, 1$ with 3 frames. Count page faults for each algorithm and match to the given options.
- Key concept: page replacement simulation — manually tracing LRU, FIFO, and Optimal algorithms to count page faults.

### Problem 8

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Resource allocation (CPU scheduling, memory management) is handled transparently by the OS.
(B) **False.** Users are typically aware of interactivity (response to input).
(C) **False.** Users are typically aware of continuity (whether a program runs smoothly).
(D) **False.** Users are typically aware of responsiveness.
- Key concept: OS abstraction — resource allocation is a core OS function that is transparent to users.

### Problem 9

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** With multi-level page tables, a TLB hit avoids multiple memory accesses (one per level), so the benefit is greater.
(B) **True.** Larger pages → fewer page table entries → smaller linear page table.
(C) **True.** With a very long time slice, RR behaves like FIFO since processes complete before being preempted.
(D) **True.** SJF can be preemptive (SRTF variant), preempting when a shorter job arrives.
- Key concept: TLB benefits with multi-level page tables, page size effects on table size, RR convergence to FIFO, and preemptive SJF.

### Problem 10

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Multiprogramming is useful even on single-user systems (e.g., running background tasks while using the foreground application).
(B) **False.** A previously blocked process moves to the READY state (not RUNNING) when I/O completes.
(C) **False.** 32-bit physical address with 4 KB pages: page offset = 12 bits, physical page number = 20 bits (not 18).
(D) **False.** Virtual pages and physical pages can differ in number — virtual address space is typically larger.
(E) **True.** None of the above are correct.
- Key concept: process state transitions, physical address decomposition, and virtual vs. physical page count independence.

### Problem 11

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** 2-level page table without TLB: 1 access for first-level table + 1 for second-level table + 1 for the actual data = 3 memory accesses.
(B) **False.** An unsafe state does not mean deadlock — it means deadlock is possible, not guaranteed.
(C) **True.** Banker's algorithm is a deadlock avoidance algorithm (checks for safe states before granting requests).
(D) **True.** Wait-for graph is used for deadlock detection in single-instance resource systems.
- Key concept: multi-level page table access cost, distinction between unsafe state and deadlock, and deadlock avoidance vs. detection.

### Problem 12

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** IPv6 header is 40 bytes; IPv4 minimum header is 20 bytes. Ratio is 2×, not 4×.
(B) **True.** IPv6 does not support broadcast — it uses multicast instead.
(C) **True.** IPsec support is mandated in the IPv6 specification.
(D) **True.** In IPv6, only the source can fragment; routers do not fragment packets.
- Key concept: IPv6 vs. IPv4 differences — header size, no broadcast, mandatory IPsec, and source-only fragmentation.

### Problem 13

This problem is worth 5 points (multiple-choice, per-option scoring). The question asks which statements are NOT true.

(A) **True** (so not an answer). Single-cycle CPUs are not used in modern mainstream processors.
(B) **NOT true** (answer). Single-cycle CPU cycle time is determined by the *longest* (not shortest) instruction path.
(C) **NOT true** (answer). Single-cycle CPU uses each functional unit at most once per instruction per cycle.
(D) **True** (so not an answer). Multi-cycle is more efficient because it doesn't waste time on shorter instructions.
(E) **True** (so not an answer). Multi-cycle is more suitable for pipelining.
- Key concept: single-cycle vs. multi-cycle CPU — the clock period is set by the longest instruction path, and functional units are used once per cycle.

### Problem 14

This problem is worth 5 points (multiple-choice, per-option scoring). The question asks which statements are NOT true.

(A) **True.** Write-back: a dirty block evicted on a read miss requires a write-back.
(B) **NOT true.** Write-through benefits from a write buffer to avoid stalling on every write.
(C) **True.** Register renaming eliminates WAW hazards by assigning different physical registers.
(D) **NOT true.** RAW hazards are true dependencies and cannot be resolved by register renaming alone (forwarding or stalling is needed).
(E) **NOT true.** Distributed shared-memory (NUMA) systems inherently have non-uniform memory access times.
- Key concept: cache write policies, write buffers, and which hazard types can/cannot be resolved by register renaming.

### Problem 15

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Dynamic scheduling's motivation includes handling out-of-order execution and completion.
(B) **False.** Separated I-cache and D-cache reduce structural hazards, not data hazards.
(C) **False.** RISC has not become obsolete; it is the dominant ISA philosophy (ARM, RISC-V).
(D) **True.** Memory wall and power limits have driven the shift to multi-core processors.
- Key concept: dynamic scheduling motivation, structural vs. data hazards, RISC relevance, and the multi-core trend.

### Problem 16

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Address translation maps virtual addresses to physical addresses, not register numbers.
(B) **False.** Physical address refers to a memory location, not a register.
(C) **True.** A TLB miss can still be resolved by looking up the page table in memory.
(D) **False.** Virtual page number bits and physical page number bits can differ (virtual space is usually larger).
(E) **True.** Context switches must protect page tables — P2 should not access P1's page table.
- Key concept: virtual memory address translation, TLB miss handling, and page table protection during context switches.

### Problem 17

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Data forwarding can partially solve RAW hazards (e.g., EX-to-EX forwarding), though not all cases (e.g., load-use).
(B) **False.** Branch prediction addresses control hazards, not data hazards.
(C) **False.** Pipelining does not reduce individual instruction latency; it increases throughput.
(D) **False.** The branch target address depends on the current PC, which is known at runtime, not compile time. However, the *offset* is encoded at compile time.
(E) **True.** Pipelining overlaps instruction execution, increasing the number of simultaneously active instructions.
- Key concept: pipeline characteristics — forwarding for RAW hazards, branch prediction for control hazards, and throughput vs. latency.

### Problem 18

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** A stack frame is the collection of all data on the stack for one subprogram call.
(B) **False.** The frame pointer stores the base address of the current stack frame, not the top. The stack pointer points to the top.
(C) **True.** Stack overflow occurs when the stack pointer exceeds the allocated stack boundary.
(D) **False.** `jal` is used to *call* a procedure (jump and link). `jr $ra` is used to *return*.
(E) **False.** `beq` uses two register operands and an immediate offset (not a register for the branch target).
- Key concept: MIPS calling convention — stack frame, frame pointer vs. stack pointer, `jal` for calls, `jr $ra` for returns.

### Problem 19

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Spatial locality means nearby addresses tend to be accessed soon (not the same address).
(B) **True.** Larger blocks capture more nearby data, exploiting spatial locality.
(C) **False.** Write-through does not need a dirty bit (every write goes to memory immediately). Write-back needs the dirty bit.
(D) **True.** Higher associativity reduces conflict misses, improving cache utilization.
(E) **False.** Direct-mapped has a higher miss rate than fully associative due to conflict misses.
- Key concept: cache design trade-offs — spatial vs. temporal locality, dirty bits with write-back, and associativity effects on miss rate.

### Problem 20

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Loop unrolling increases code size (more instructions per unrolled iteration).
(B) **False.** Register renaming's purpose is to eliminate false dependencies (WAR, WAW), not improve readability.
(C) **False.** VLIW launches multiple *independent* operations in one wide instruction, not dependent ones.
(D) **True.** Superscalar processors can issue multiple instructions per cycle from a single instruction stream.
(E) **True.** Static multiple issue relies on the compiler to schedule and bundle compatible instructions.
- Key concept: ILP techniques — loop unrolling increases code size, register renaming eliminates false dependencies, VLIW bundles independent ops, superscalar issues multiple instructions dynamically.

## 110

### Problem 1

This problem is worth 5 points (multiple-choice, per-option scoring).

64 blocks, 4-way set associative → $64/4 = 16$ sets. 16 bytes/block → offset = $\log_2(16) = 4$ bits. Index = $\log_2(16) = 4$ bits. Tag = $32 - 4 - 4 = 24$ bits. Byte address 1200: block number = $1200/16 = 75$, set = $75 \bmod 16 = 11$.
(a) **True.** Address 1200 maps to set 11.
(b) **True.** Index field requires 4 bits.
(c) **False.** Offset field is 4 bits, not 12.
(d) **False.** Tag field is 24 bits, not 16.
- Key concept: set-associative cache address decomposition — computing set index, offset, and tag field widths.

### Problem 2

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** Spatial locality refers to accessing nearby addresses, not the same address again (that's temporal locality).
(b) **False.** Larger blocks increase miss penalty (more data to transfer).
(c) **False.** Increasing associativity *increases* utilization by reducing conflict misses.
(d) **True.** A valid bit indicates whether a cache block contains valid data.
(e) **True.** A cache block is the basic unit of cache storage.
- Key concept: memory hierarchy fundamentals — spatial vs. temporal locality, miss penalty, associativity, valid bits, and cache block as the basic storage unit.

### Problem 3

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** Data forwarding passes data to *subsequent* (not previous) instructions that need it.
(b) **True.** Structural hazard occurs when a required hardware resource is busy (e.g., single memory port for IF and MEM).
(c) **True.** Compiler instruction reordering can eliminate some data hazards.
(d) **False.** Moving branch resolution to ID stage requires only 1 stall (not 2), since the branch decision is available one stage earlier.
(e) **True.** If the comparison register is the destination of the immediately preceding load, one stall is needed (load produces result at end of MEM, but ID needs it at the start of ID).
- Key concept: pipeline hazard resolution — forwarding direction, structural hazards, instruction reordering, and stall counts for branch and load-use hazards.

### Problem 4

This problem is worth 5 points (multiple-choice, per-option scoring).

Stage times: IF=200, ID=100, EXE=200, MEM=200, WB=100 ps.
(a) **True.** Single-cycle: clock = sum of all stages = $200+100+200+200+100 = 800$ ps.
(b) **True.** `sw` in single-cycle uses the same 800 ps clock cycle.
(c) **True.** Multi-cycle: clock = max stage time = 200 ps.
(d) **False.** `lw` uses 5 stages in multi-cycle: $5 \times 200 = 1000$ ps (not 800).
- Key concept: single-cycle vs. multi-cycle timing — single-cycle clock period equals the sum of all stages; multi-cycle clock period equals the slowest stage.

### Problem 5

This problem is worth 5 points (multiple-choice, per-option scoring).

16-bit I-type: op (5 bits), rs (3 bits), rt (3 bits), immediate (5 bits).
(a) **False.** 5-bit opcode supports up to $2^5 = 32$ operations, not 16.
(b) **True.** 3-bit register fields → $2^3 = 8$ registers.
(c) **False.** 5-bit 2's complement range is $-2^4$ to $2^4 - 1$ (i.e., $-16$ to $15$), not $-2^5$ to $2^5 - 1$.
(d) **False.** Register width is determined by the data path, not the instruction format field widths. The problem says 16-bit computer, so registers are 16 bits.
- Key concept: instruction format field analysis — opcode space, register file size from field width, and 2's complement range.

### Problem 6

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** Branch history table (BHT) is typically indexed by lower bits of the PC.
(b) **False.** Branch prediction buffer does not necessarily have tag checks; aliasing is tolerated.
(c) **True.** A 2-bit predictor requires two consecutive mispredictions to change its prediction.
(d) **True.** Aliasing (different branches mapping to same BHT entry) can cause mispredictions.
(e) **False.** Correlating predictors explicitly consider the behavior of other (correlated) branches.
- Key concept: dynamic branch prediction — BHT indexing, 2-bit predictor hysteresis, aliasing issues, and correlating predictors.

### Problem 7

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** RISC chips have *fewer* transistors for core logic (simpler control), using saved transistors for registers and caches.
(b) **True.** RISC's simpler instructions free up chip area for a larger register set.
(c) **True.** RISC uses uniform instruction formats, reducing decoder complexity.
(d) **False.** RISC is *well-suited* for pipelining due to uniform instruction lengths and simple operations.
(e) **False.** RISC typically supports *fewer* addressing modes than CISC.
- Key concept: RISC vs. CISC design philosophy — RISC uses simpler instructions, uniform formats, larger register files, and is pipeline-friendly.

### Problem 8

This problem is worth 5 points (multiple-choice, per-option scoring).

CPI_before = $0.25 \times 4 + 0.75 \times 1.5 = 1 + 1.125 = 2.125$. CPI_after = $0.25 \times 2 + 0.75 \times 1.5 = 0.5 + 1.125 = 1.625$. FP speedup = $4/2 = 2$. Total speedup = $2.125/1.625 = 1.307$. Amdahl's Law: speedup $= 1/((1-0.25) + 0.25/2) = 1/(0.75 + 0.125) = 1/0.875 = 1.1428$. But this applies to execution time fractions: FP fraction of time = $0.25 \times 4 / 2.125 = 0.471$, not 0.25.
(a) **True.** FP operation speedup = $4/2 = 2$.
(b) **False.** The Amdahl's Law formula shown uses instruction fraction (0.25) instead of execution time fraction.
(c) **True.** Total speedup = $2.125/1.625 ≈ 1.307$.
(d) **False.** CPI after enhancement = $1.625$, not $1.859$.
(e) **True.** CPI before = $2.125$.
- Key concept: Amdahl's Law application — distinguishing instruction frequency from execution time fraction when computing speedup.

### Problem 9

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** Loop unrolling requires that iterations be independent (no loop-carried dependencies) to correctly replicate the loop body.
(b) **False.** Register renaming resolves *false* dependencies (WAR, WAW), not true (RAW) dependencies.
(c) **True.** Anti-dependence (WAR) is when a later instruction writes a location that an earlier instruction reads.
(d) **True.** Loop unrolling reduces the number of branch instructions (loop overhead), reducing control stalls.
(e) **False.** Issuing multiple instructions per cycle reduces the effective CPI below 1.
- Key concept: ILP techniques — loop unrolling requirements, register renaming for false dependencies, anti-dependence definition, and multi-issue CPI reduction.

### Problem 10

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** Virtual memory typically uses write-back (not write-through) because writing to disk on every store would be prohibitively slow.
(b) **True.** Higher associativity reduces conflict misses by providing more placement choices.
(c) **True.** Virtual memory uses fully associative placement — any virtual page can map to any physical frame.
(d) **False.** Larger pages → fewer page table entries → smaller page table.
(e) **True.** With write-back, a dirty block evicted by a read miss must be written back to lower-level memory.
- Key concept: virtual memory uses write-back and fully associative placement; larger pages reduce page table size; dirty block eviction on read misses.

### Problem 11

This problem is worth 5 points (all-or-nothing).

(a) **True.** Page table size grows with more pages (smaller page size means more pages).
(b) **True.** Internal fragmentation is worse with larger pages.
(c) **True.** Larger pages mean each page transfer moves more data, affecting I/O overhead.
(d) **True.** Page size affects the number of page faults (spatial locality considerations).
- Key concept: page size trade-offs affect page table size, fragmentation, I/O efficiency, and page fault rate.

### Problem 12

This problem is worth 5 points (all-or-nothing).

(a) **False.** Kernel-level threads are more expensive to create (require kernel involvement) than user-level threads.
(b) **True.** User-level thread libraries can assign different scheduling priorities to threads.
(c) **False.** A blocking *user-level* thread (in many-to-one model) blocks all threads; a blocking kernel thread only blocks that specific thread.
(d) **True.** In many-to-one model, one thread blocking causes all to block since they share one kernel thread.
- Key concept: user-level vs. kernel-level thread trade-offs — creation cost, scheduling, and blocking behavior in different threading models.

### Problem 13

This problem is worth 5 points (all-or-nothing).

(a) **False.** A cycle is necessary but not sufficient for deadlock if there are multiple instances of resources.
(b) **False.** Unsafe state means there exists no safe sequence, but processes might still release resources and avoid deadlock.
(c) **True.** Ignoring deadlocks (ostrich algorithm) is a valid approach used by many systems.
(d) **False.** Banker's algorithm is a deadlock *avoidance* algorithm, not prevention.
- Key concept: deadlock concepts — cycles with multi-instance resources, unsafe ≠ deadlocked, ostrich algorithm, and avoidance vs. prevention.

### Problem 14

This problem is worth 5 points (all-or-nothing).

(a) **False.** Containers do NOT include a guest OS — they share the host OS kernel.
(b) **True.** Containers on the same machine share the same OS kernel.
(c) **False.** VMs run on a hypervisor, not as isolated processes in user space.
(d) **True.** Containers are lighter weight than VMs (no guest OS overhead).
- Key concept: containers vs. VMs — containers share the host kernel and are lightweight; VMs include a full guest OS.

### Problem 15

This problem is worth 5 points (all-or-nothing).

(a) **True.** IPv6 routers are not allowed to fragment packets; only the source host can.
(b) **True.** DHCP dynamically assigns IP addresses to hosts joining a network.
(c) **False.** NAT translates private IP addresses to public IP addresses, not IP to hardware (MAC) addresses.
(d) **False.** DNS resolves domain names to IP addresses, not hardware addresses. ARP resolves IP to MAC.
- Key concept: network protocols — IPv6 fragmentation rules, DHCP for dynamic IP assignment, NAT for IP address translation, DNS vs. ARP.

### Problem 16

This problem is worth 5 points (all-or-nothing).

Well-known ports (0–1023) are reserved for standard services (21=FTP, 23=Telnet, 80=HTTP). Client processes are typically assigned ephemeral ports (1024–65535). So **(d) 1625** is a valid port assignment for a client.
- Key concept: port number ranges — well-known ports (0–1023) are reserved; client processes use ephemeral ports (≥1024).

### Problem 17

This problem is worth 5 points (all-or-nothing).

Techniques for handling critical sections include: spinlocks, Peterson's solution, Dekker's algorithm, and preemptive/nonpreemptive kernel design choices. All of (A)–(E) are related to critical section handling. The question asks what is *not* a technique: **(A) nonpreemptive kernels** and **(B) preemptive kernels** are kernel design approaches, not specific critical section techniques per se, but they do relate to how critical sections are handled.
- Key concept: critical section handling mechanisms — mutual exclusion algorithms (Peterson's, Dekker's), spinlocks, and kernel preemption design.

### Problem 18

This problem is worth 5 points (all-or-nothing).

Effective memory access time = TLB hit rate × (TLB time + memory time) + TLB miss rate × (TLB time + 2 × memory time) = $0.9 \times (15 + 85) + 0.1 \times (15 + 2 \times 85) = 0.9 \times 100 + 0.1 \times 185 = 90 + 18.5 = 108.5$. Answer: **(A)**.
- Key concept: effective memory access time with TLB — weighting hit and miss paths by their access times.

### Problem 19

This problem is worth 5 points (all-or-nothing).

Simulate LRU on the reference string $1\ 2\ 3\ 4\ 2\ 3\ 4\ 1\ 2\ 1\ 1\ 3\ 1\ 4$ with 3 frames. Track which pages are in the frames and which was least recently used. The final configuration after all accesses determines the answer.
- Key concept: LRU page replacement simulation — tracking recency of use to determine eviction order.

### Problem 20

This problem is worth 5 points (all-or-nothing).

(a) **False.** A blocking call does NOT let the application continue — it suspends execution.
(b) **False.** A blocking call does NOT return immediately — it waits for I/O completion.
(c) **True.** A blocking system call suspends the application until the I/O operation completes.
(d) **False.** Blocking code is generally simpler and easier to understand than non-blocking code.
- Key concept: blocking vs. non-blocking system calls — blocking suspends the caller until the operation completes.

## 111

### Problem 1

This problem is worth 5 points (single-choice, −2 for wrong answer).

Amdahl's Law: speedup = $1 / ((1 - 0.4) + 0.4/10) = 1 / (0.6 + 0.04) = 1 / 0.64 = 1.5625 ≈ 1.56$. Answer: **(c)**.
- Key concept: Amdahl's Law — computing overall speedup when a fraction of computation is enhanced by a given factor.

### Problem 2

This problem is worth 5 points (single-choice, −2 for wrong answer).

16 KB, 2-way set associative, 4-byte blocks. Number of blocks = $16384/4 = 4096$. Sets = $4096/2 = 2048$. Index = $\log_2(2048) = 11$. Offset = $\log_2(4) = 2$. Tag = $32 - 11 - 2 = 19$. Answer: **(e)**.
- Key concept: set-associative cache tag bit calculation — accounting for set count and block size.

### Problem 3

This problem is worth 5 points (single-choice, −2 for wrong answer).

Un-pipelined: average instruction time = $(0.3 \times 4 + 0.3 \times 5 + 0.4 \times 5) \times 8 = 4.7 \times 8 = 37.6$ ns. Pipelined: $1 \times (8+1) = 9$ ns. Speedup = $37.6/9 ≈ 4.18$. Answer: **(b)**.
- Key concept: pipeline speedup — ratio of un-pipelined average instruction time to pipelined cycle time.

### Problem 4

This problem is worth 5 points (single-choice, −2 for wrong answer).

Each `fork()` doubles the number of processes. Loop runs 4 times: after iteration 0: 2 processes, after 1: 4, after 2: 8, after 3: 16. Total = $2^4 = 16$ processes (including the original). Answer: **(c)**.
- Key concept: `fork()` in a loop — each iteration doubles the process count, yielding $2^n$ total processes after $n$ iterations.

### Problem 5

This problem is worth 5 points (single-choice, −2 for wrong answer).

Simulate Round Robin (quantum=5) with the given arrival and burst times. Compute waiting time for each process, then average. P1 (arrival 0, burst 10): runs 0–5, preempted, waits, resumes later. P2 (arrival 1, burst 6): waits until P1's first quantum ends. P3 (arrival 12, burst 2) and P4 (arrival 13, burst 3) arrive later. Calculate each process's waiting time and average.
- Key concept: Round Robin scheduling simulation — computing waiting times with preemption and time quantum boundaries.

### Problem 6

This problem is worth 5 points (single-choice, −2 for wrong answer).

Simulate FIFO, LRU, and Optimal on reference string $1\ 5\ 4\ 6\ 4\ 1\ 5\ 4\ 1\ 6\ 2\ 3\ 1\ 6$ with 3 frames. Count page faults for each. FIFO gives 8 faults.
(a) **True.** FIFO gives 8 page faults.
(b) Check LRU simulation.
(c) Check Optimal simulation.
(d) **False.** Optimal is theoretically best but not practically implementable (requires future knowledge).
- Key concept: page replacement algorithm simulation and the theoretical optimality of Bélády's algorithm.

### Problem 7

This problem is worth 5 points (single-choice, −2 for wrong answer). The question asks which is NOT true.

(a) **True.** Kubernetes automates container deployment, scaling, and management.
(b) **Not true.** Docker virtualizes the OS (containers), not hardware. VMs virtualize hardware.
(c) **True.** Para-virtualization modifies the guest OS to cooperate with the VMM.
(d) **True.** Para-virtualization works without binary translation on older CPUs.
(e) **True.** JVM includes garbage collection.
Answer: **(b)**.
- Key concept: Docker uses OS-level virtualization (containers), not hardware virtualization — distinguishing containers from VMs.

### Problem 8

This problem is worth 5 points (single-choice, −2 for wrong answer).

In the producer-consumer problem: producer does `wait(empty)`, `wait(mutex)`, adds item, `signal(mutex)`, `signal(full)`. Consumer must do `wait(full)`, `wait(mutex)`, removes item, `signal(mutex)`, `signal(empty)`. So V1=full, V2=mutex, and signal(mutex), signal(empty). Answer: **(e) V1=full, V2=mutex**.
- Key concept: producer-consumer synchronization with semaphores — correct ordering of wait/signal on full, empty, and mutex.

### Problem 9

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** PC + adder computes the next instruction address (PC+4), not the rd register number.
(b) **True.** Sign-extending Instruction[15-0] produces a 32-bit signed integer.
(c) **False.** The Zero signal and Branch signal together determine whether to take the branch; Zero alone doesn't determine the instruction type.
(d) **False.** The MUX before "Write register" selects which register to write to (rt or rd via RegDst), not whether to write.
- Key concept: MIPS single-cycle datapath — understanding the role of each MUX, adder, and control signal.

### Problem 10

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** More exponent bits → larger dynamic range (wider range of representable magnitudes).
(b) **True.** Fewer significand bits → lower precision (fewer significant digits).
(c) Decode $0100\ 0010\ 0000\ 0000$: sign=0, exponent=$01000=8$, significand=$10\ 0000\ 0000$. Value = $(-1)^0 \times 1.10_2 \times 2^{8-15} = 1.5 \times 2^{-7}$. This is not 3. **Check carefully.**
(d) In the new format: sign=0, exponent=$10000100=132$, significand=$0000000$. Value = $1.0 \times 2^{132-127} = 2^5 = 32$. **True.**
- Key concept: floating-point format trade-offs — exponent width affects dynamic range, significand width affects precision; decoding bit patterns.

### Problem 11

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** In inclusive cache, the *bottom-level* (L2) is a superset of the top-level (L1), meaning L1 ⊆ L2. The statement says L1 is a subset of L2 — this is actually **True** (L1 is contained within L2).
(b) **False.** Evicting block A from L1 does NOT cause L2 to evict it. However, evicting from L2 forces eviction from L1.
(c) **False.** Inclusive cache wastes space by storing duplicates. Exclusive cache utilizes space more efficiently.
(d) **True.** Inclusive cache ensures that all data in L1 is also in L2, maintaining consistency for snoop-based coherence.
- Key concept: inclusive vs. exclusive cache — inclusion property, eviction behavior, space efficiency, and coherence benefits.

### Problem 12

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** Merging I-cache and D-cache increases the combined miss rate compared to the original I-cache alone (more contention).
(b) **True.** The merged cache is larger overall, so the D-cache portion's miss rate may decrease compared to a standalone smaller D-cache.
(c) **True.** Separate I-cache and D-cache allow simultaneous instruction fetch and data access.
(d) **True.** Split caches eliminate the structural hazard of IF and MEM stages accessing the same cache.
- Key concept: split vs. unified L1 cache — structural hazard elimination, simultaneous access, and miss rate trade-offs.

### Problem 13

This problem is worth 5 points (multiple-choice, per-option scoring). The question asks which are NOT true.

(a) **True** (not an answer). Dynamic scheduling handles memory latency and runtime-dependent true dependencies.
(b) **True** (not an answer). Single-cycle clock is determined by the longest instruction path.
(c) **NOT true** (answer). Register-Register architecture has *less* CPI variation (simpler, uniform instructions), not *larger* variation.
(d) **True** (not an answer). RISC is mainstream (ARM, RISC-V dominate mobile and embedded).
(e) **NOT true** (answer). Multi-cycle (not single-cycle) is more suitable for pipelining because stages are already separated.
- Key concept: ISA design trade-offs — CPI variation by architecture type, and single-cycle vs. multi-cycle pipeline suitability.

### Problem 14

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** Compilers can reorder instructions to avoid pipeline hazards (static scheduling).
(b) **True.** Split I-cache and D-cache primarily reduce structural hazards (simultaneous memory access).
(c) **True.** Register renaming resolves WAR (anti-dependence) hazards by renaming destination registers.
(d) **False.** RAW hazards are true dependencies; register renaming cannot resolve them (forwarding or stalling is needed).
- Key concept: pipeline hazard resolution — compiler scheduling, split caches for structural hazards, register renaming for WAR but not RAW.

### Problem 15

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** Increasing associativity usually reduces miss rate but *increases* hit time (more comparators and MUX delay).
(b) **True.** MIPS branch offset is in units of words (4 bytes), extending the addressable range.
(c) **False.** Increasing cache size generally increases hit rate, but there can be diminishing returns. The statement "always" is too strong.
(d) **True.** Pipeline registers are necessary to hold intermediate results between stages.
(e) **True.** MIPS instructions are fixed at 32 bits.
- Key concept: cache associativity trade-offs, MIPS branch offset encoding, and pipeline register necessity.

### Problem 16

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** System-contention scope (SCS) is scheduling kernel threads onto CPUs.
(b) **False.** Linux uses only `PTHREAD_SCOPE_SYSTEM` (SCS), not `PTHREAD_SCOPE_PROCESS`.
(c) **True.** One-to-one model maps each user thread to a kernel thread, using SCS.
(d) **False.** `PTHREAD_SCOPE_SYSTEM` uses SCS, not PCS.
- Key concept: thread scheduling scopes — SCS schedules kernel threads to CPUs; one-to-one model uses SCS; Linux uses system scope.

### Problem 17

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** Linux supports both soft and hard affinity.
(b) **True.** Load balancing may move processes between CPUs, countering processor affinity benefits (cache warmth).
(c) **True.** NUMA architecture affects affinity — accessing local memory is faster.
(d) **False.** Soft affinity means the OS *tries* to keep a process on the same CPU but doesn't guarantee it.
- Key concept: processor affinity — soft vs. hard affinity, interaction with load balancing, and NUMA considerations.

### Problem 18

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** BGP is an *inter*-AS (between autonomous systems) routing protocol, not intra-AS.
(b) **False.** HTTPS uses TLS; HTTP itself does not encrypt messages.
(c) **True.** ARP requests are broadcast on the local network.
(d) **True.** ARP spoofing associates an attacker's MAC address with a legitimate IP address.
- Key concept: network protocols — BGP for inter-AS routing, TLS for HTTPS encryption, ARP broadcast and spoofing attacks.

### Problem 19

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **False.** Threads in the same process have separate stacks.
(b) **True.** Threads in the same process share the same address space and thus the same TLB entries.
(c) **False.** Thrashing occurs when a process spends more time *paging* than executing (reversed in the statement).
(d) **False.** Local replacement limits thrashing to one process but doesn't entirely solve it.
- Key concept: thread resource sharing (separate stacks, shared TLB), thrashing definition, and local replacement limitations.

### Problem 20

This problem is worth 5 points (multiple-choice, per-option scoring).

(a) **True.** Data access locality (accessing data near where it's stored) is a reason for process migration in distributed systems.
(b) **False.** Standard UNIX pipes do not support process migration; they provide IPC within a host.
(c) **True.** Modularity is a reason for process cooperation — dividing work into cooperating modules.
(d) **True.** Shared memory is an IPC model for cooperating processes.
- Key concept: process migration motivations, IPC models (shared memory, pipes), and reasons for process cooperation.

## 112

### Problem 1

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **False.** Throughput is the number of tasks completed per unit time, not response time.
(B) **False.** CPI is an average and can be a non-integer even if clock rate is integer.
(C) **True.** New time = $0.7 + 0.3/2 = 0.7 + 0.15 = 0.85$ of original.
(D) **True.** In multi-threading, the sum of CPU time across threads can exceed wall-clock time.
- Key concept: performance metrics — throughput vs. response time, CPI as an average, Amdahl's Law, and multi-threaded CPU time.

### Problem 2

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **False.** Segmentation fault is not simply accessing heap memory — it occurs when accessing invalid/unmapped memory.
(B) **True.** Dereferencing NULL pointer causes a segfault.
(C) **True.** Uninitialized pointer `p` may point to invalid memory; dereferencing it can cause a segfault.
(D) **True.** Infinite recursion (`foo` calls itself) will overflow the stack, potentially causing a segfault.
- Key concept: segmentation fault causes — NULL dereference, uninitialized pointer access, and stack overflow from infinite recursion.

### Problem 3

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** I-type instructions have op, rs, rt, and constant fields; rs and rt are two register operands.
(B) **False.** JUMP uses the 26-bit address concatenated with the upper 4 bits of PC (not relative to SP), addressing $2^{28}$ bytes within a 256 MB region.
(C) **False.** JR is an R-type instruction (uses register fields), not J-type.
(D) **False.** Logical and arithmetic shift-left produce the same result; MIPS uses `sll` for both.
- Key concept: MIPS instruction formats — I-type register fields, J-type address calculation, JR as R-type, and shift-left equivalence.

### Problem 4

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **False.** Pipelining increases throughput but does not reduce individual instruction latency.
(B) **True.** 5-stage pipeline, 10 instructions: $(5 + 10 - 1) \times 1 = 14$ ns.
(C) **False.** Multi-cycle CPU may have higher CPI than single-cycle (which always has CPI=1); the advantage is shorter clock period.
(D) **False.** More pipeline stages increase hazard impact (more stages to flush on misprediction).
- Key concept: pipeline timing formula, latency vs. throughput, and deeper pipelines' vulnerability to hazards.

### Problem 5

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** RISC instructions are simpler and typically execute in fewer cycles than CISC instructions.
(B) **False.** CISC has *less* uniform instruction format; RISC has more uniform format.
(C) **False.** RISC is the dominant technology (ARM, RISC-V); it is not outdated.
(D) **True.** RISC uses a simpler, smaller instruction set than CISC.
- Key concept: RISC vs. CISC — RISC has simpler instructions, more uniform formats, and is the current mainstream approach.

### Problem 6

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** RAM supports both read and write operations to any address.
(B) **False.** Standard ROM is read-only (some variants like EEPROM/Flash can be rewritten, but ROM by definition is read-only).
(C) **True.** RAM is volatile — data is lost when power is removed.
(D) **True.** ROM retains data without power, suitable for persistent program storage (firmware, BIOS).
- Key concept: RAM vs. ROM characteristics — volatility, read/write capability, and persistence.

### Problem 7

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** In inclusive cache, L1 ⊆ L2 (all L1 data is also in L2).
(B) **False.** L1 cache needs short hit time, so it is *small* (not big).
(C) **True.** Reducing miss penalty (e.g., with L2 cache) decreases AMAT when L1 hit time < L2 hit time.
(D) **False.** Fully associative cache has *higher* (better) hit rate than direct-mapped due to fewer conflict misses.
- Key concept: cache hierarchy design — inclusive property, L1 size for hit time, miss penalty reduction, and associativity effects.

### Problem 8

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** Prefetching can load data before first access, reducing compulsory misses.
(B) **False.** Conflict miss occurs when multiple blocks map to the same set, not on first use (that's compulsory miss).
(C) **False.** Capacity miss occurs when the cache is too small, not due to external updates (that's coherence miss).
(D) **True.** Direct-mapped cache has more conflict misses than n-way associative (more placement restrictions).
- Key concept: cache miss classification — compulsory (first access), conflict (set mapping collisions), capacity (cache too small), and coherence (external updates).

### Problem 9

This problem is worth 5 points (multiple-choice, −1 per wrong option).

With write-through: every write goes to main memory. AMAT = read portion × (hit: cache access, miss: cache + main memory) + write portion × (always write to both). Read AMAT = $0.6 \times [H \times C + (1-H) \times (C+M)]$. Write AMAT = $0.4 \times (C + M)$ (write-through always accesses main memory). Total = $0.6[HC + (1-H)(C+M)] + 0.4(C+M)$. Simplify and match answer choices. Answer: **(E) None of the above** (or verify against given formulas).
- Key concept: average memory access time with write-through — reads depend on hit ratio, writes always access main memory.

### Problem 10

This problem is worth 5 points (multiple-choice, −1 per wrong option).

FP time fraction = 60%. Square root fraction of FP = 25%, so square root fraction of total = 15%. Option 1 (FP 1.5× faster): speedup = $1/((1-0.6) + 0.6/1.5) = 1/(0.4 + 0.4) = 1/0.8 = 1.25$. Option 2 (sqrt 8× faster): speedup = $1/((1-0.15) + 0.15/8) = 1/(0.85 + 0.01875) = 1/0.86875 ≈ 1.151$.
(A) **False.** FP enhancement (1.25×) is better than sqrt enhancement (1.15×).
(B) **True.** FP enhancement gives higher speedup.
(C) **True.** FP speedup = 1.25.
(D) **True.** Sqrt speedup ≈ 1.15.
- Key concept: Amdahl's Law comparison — enhancing a larger fraction (FP) by a smaller factor can outperform enhancing a smaller fraction (sqrt) by a larger factor.

### Problem 11

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **False.** Deadlock *avoidance* prevents deadlock from occurring; deadlock *detection* identifies deadlock after it has occurred. They are different strategies.
(B) **True.** User-level processes cannot directly modify their own page table entries (this requires kernel privilege).
(C) **False.** SRTF is optimal for minimizing average waiting time but has starvation issues and requires knowing burst times. It's not "the best" in all senses.
(D) **False.** The child process gets its own new PID; the parent's PID is not inherited as the child's PID.
- Key concept: deadlock avoidance vs. detection, page table privilege, SRTF limitations, and fork() PID behavior.

### Problem 12

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** Ordinary (unnamed) pipes are unidirectional.
(B) **False.** `pipe()` creates a unidirectional pipe, not bidirectional.
(C) **True.** Two named pipes (FIFOs) are needed for bidirectional communication.
(D) **True.** UNIX FIFOs (named pipes) transmit byte-oriented data streams.
- Key concept: UNIX pipe types — ordinary pipes are unidirectional and byte-oriented; named pipes (FIFOs) support unrelated processes.

### Problem 13

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** Web 3.0 aims for a decentralized internet using blockchain and distributed technologies.
(B) **False.** SDN is *centralized* — it separates the control plane (centralized controller) from the data plane.
(C) **True.** DNS is a distributed, hierarchical database system.
(D) **True.** IETF develops Internet standards (RFCs).
- Key concept: Web 3.0 decentralization, SDN centralized control, DNS as distributed hierarchy, and IETF's role.

### Problem 14

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **False.** A process cannot go directly from ready to waiting; it must first be running.
(B) **True.** The reference bit (used bit) is set by hardware when a page is accessed.
(C) **True.** The mode bit distinguishes user mode from kernel mode for the current instruction.
(D) **False.** The valid-invalid bit indicates whether a page is in memory; the dirty bit determines if a write-back to disk is needed for replacement.
- Key concept: process state transitions, hardware-set reference bit, mode bit function, and valid-invalid vs. dirty bit.

### Problem 15

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** Copy-on-write lets parent and child share pages initially, copying only on modification.
(B) **True.** Named pipes do not require a parent-child relationship; any process can access them by name.
(C) **True.** IPSec provides encryption and authentication for VPN tunnels.
(D) **False.** Banker's algorithm addresses deadlock avoidance, not race conditions. Race conditions are solved by synchronization primitives (mutexes, semaphores).
- Key concept: copy-on-write for efficient forking, named pipes for unrelated processes, IPSec for VPNs, and appropriate tools for race conditions vs. deadlocks.

### Problem 16

This problem is worth 5 points (multiple-choice, −1 per wrong option).

Hard real-time systems have strict, non-negotiable deadlines where missing one causes catastrophic failure.
(A) **False.** A household thermostat is soft real-time (slight delay is acceptable).
(B) **True.** Nuclear power plant control requires hard real-time guarantees.
(C) **False.** Fuel economy systems are soft real-time.
(D) **True.** Jet airliner landing systems are safety-critical and require hard real-time.
(E) **False.** Mailing systems are not real-time critical.
- Key concept: hard vs. soft real-time systems — hard real-time is required for safety-critical applications where deadline misses are catastrophic.

### Problem 17

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **False.** FCFS does not cause starvation.
(B) **True.** SJF can starve long jobs when short jobs continuously arrive.
(C) **False.** Round Robin prevents starvation by giving each process a time quantum.
(D) **True.** Priority scheduling can starve low-priority processes.
(E) **False.** Weighted fair queueing provides proportional share, preventing starvation.
- Key concept: starvation in scheduling — only SJF and priority scheduling are susceptible.

### Problem 18

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** A counting semaphore maintains an integer value.
(B) **False.** Accessed through two operations: `wait` (P) and `signal` (V).
(C) **False.** The operations are atomic; multiple threads cannot modify simultaneously.
(D) **False.** Semaphores are commonly used for critical section control.
(E) **False.** The wait/signal operations are atomic, but the semaphore as a whole is not "an uninterruptible unit."
- Key concept: counting semaphore properties — integer variable, two atomic operations, used for synchronization and mutual exclusion.

### Problem 19

This problem is worth 5 points (multiple-choice, −1 per wrong option).

Same as Problem 14 from year 108 — many-to-many threading model with varying kernel thread allocations. See year 108 Problem 14 analysis.
- Key concept: kernel thread allocation impact on performance in the many-to-many threading model.

### Problem 20

This problem is worth 5 points (multiple-choice, −1 per wrong option).

(A) **True.** Software interpretation can intercept and validate every user instruction for protection.
(B) **False.** Co-processor execution does not inherently provide privilege separation.
(C) **False.** Network server support doesn't solve the local privilege mode problem.
(D) **True.** Compiler-generated protection checks can enforce security in software.
(E) **False.** Cloud computing doesn't address the local hardware privilege issue.
- Key concept: software-based protection when hardware privilege modes are unavailable — interpretation and compiler-enforced checks.

## 113

### Problem 1

This problem is worth 5 points (all correct to receive points).

With write-through: reads have hit ratio $H$, writes always go to main memory. Read AMAT = $0.8 \times [H \cdot C + (1-H)(C+M)]$. Write AMAT = $0.2 \times (C + M)$. Total AMAT = $0.8[HC + (1-H)(C+M)] + 0.2(C+M)$. Expand and simplify, then match to answer choices.
- Key concept: AMAT calculation with write-through — reads depend on cache hit ratio, writes always access both cache and main memory.

### Problem 2

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Cache size affects how much data can be stored, directly influencing miss rate.
(B) **True.** Associativity determines placement flexibility, affecting conflict misses.
(C) **True.** Cache line (block) size affects spatial locality exploitation and miss penalty.
(D) **True.** CPU clock frequency affects the relative importance of cache hit time.
(E) **True.** All factors influence cache effectiveness.
- Key concept: factors affecting cache performance — size, associativity, line size, and clock frequency all play roles.

### Problem 3

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Non-volatile storage (NVS) retains data without power.
(B) **False.** NVS is used for persistent (not temporary) storage.
(C) **True.** HDDs and SSDs are examples of non-volatile storage.
(D) **False.** NVS has *slower* access times compared to cache memory.
- Key concept: non-volatile storage characteristics — data persistence, slower than cache, includes HDDs and SSDs.

### Problem 4

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** The MMU handles address translation, not cache data management (that's the cache controller).
(B) **True.** The MMU translates virtual addresses to physical addresses.
(C) **False.** The MMU does not control instruction flow (that's the CPU pipeline/control unit).
(D) **False.** The MMU manages address mapping, not data organization in main memory.
- Key concept: MMU function — virtual-to-physical address translation is its primary role.

### Problem 5

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Direct-mapped caches have multiple sets (one block per set), not one set.
(B) **True.** Set-associative caches allow a block to be placed in any line within its mapped set.
(C) **False.** Fully associative caches allow any block in *any* line (not a unique line per block).
(D) **True.** Direct-mapped caches generally have lower hit rates than set-associative and fully-associative due to conflict misses.
- Key concept: cache mapping policies — direct-mapped (one-way set), set-associative (n-way), and fully-associative (single set) trade-offs.

### Problem 6

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Pipelining increases throughput by overlapping instruction execution.
(B) **False.** Pipelining does not eliminate control hazards; it introduces them (branch hazards).
(C) **True.** Data hazards arise from instruction overlap when a later instruction depends on an earlier one's result.
(D) **False.** Pipelining typically requires a longer clock cycle (overhead from pipeline registers).
(E) **False.** Deeper pipelines increase individual instruction latency while improving throughput.
- Key concept: pipelining benefits and limitations — throughput improvement, hazard introduction, and pipeline register overhead.

### Problem 7

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Virtual memory treats physical memory as a cache for disk storage.
(B) **True.** TLB caches page table entries to speed up address translation.
(C) **True.** Page faults occur when the requested page is not in physical memory.
(D) **True.** Demand paging with insufficient frames causes thrashing (excessive page fault activity).
(E) **True.** Virtual memory provides process isolation and memory protection through separate address spaces.
- Key concept: virtual memory fundamentals — memory as disk cache, TLB, page faults, thrashing, and process isolation.

### Problem 8

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Cache memory has lower latency than main memory (SRAM vs. DRAM).
(B) **True.** Cache typically uses SRAM technology for fast access.
(C) **False.** Cache is *smaller* than main memory.
(D) **True.** Cache exploits temporal and spatial locality.
(E) **True.** Cache contains a subset of main memory data.
- Key concept: cache memory characteristics — low latency SRAM, smaller than main memory, exploits locality, holds a data subset.

### Problem 9

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** MIPS has a *small*, simple instruction set (RISC), not large and complex.
(B) **True.** MIPS supports register-to-register (R-type) operations.
(C) **False.** MIPS uses a general-purpose register file, not a single accumulator.
(D) **True.** MIPS uses delayed branch technique (branch delay slot).
(E) **True.** MIPS uses fixed 32-bit instruction encoding.
- Key concept: MIPS ISA features — RISC with simple instructions, register-to-register ops, delayed branches, and fixed-size encoding.

### Problem 10

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** SIMD executes the *same* instruction on multiple data points, not different instructions.
(B) **True.** SIMD is well-suited for regular, data-parallel tasks (e.g., vector operations, image processing).
(C) **True.** SIMD is commonly implemented in vector processors.
(D) **False.** SIMD uses a single instruction stream (one processor/control unit), not multiple processors on the same task.
(E) **True.** GPUs heavily use SIMD/SIMT parallelism.
- Key concept: SIMD parallelism — single instruction applied to multiple data elements, used in vector processors and GPUs.

### Problem 11

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Software interpretation allows the OS to validate every user instruction.
(B) **True.** Compiler-generated checks can substitute for missing hardware privilege modes.
(C) **False.** Cloud execution doesn't solve the local privilege mode problem.
(D) **False.** Open-source code doesn't inherently provide OS control over execution.
(E) **False.** ROM-based OS prevents modification but doesn't replace hardware privilege modes.
- Key concept: software protection mechanisms without hardware privilege support — interpretation and compiler enforcement.

### Problem 12

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** Larger pages → fewer entries → smaller page table; smaller pages → more entries → larger table.
(B) **True.** Larger pages waste more space per page (internal fragmentation).
(C) **True.** Larger pages mean more data per transfer, affecting I/O overhead per fault.
(D) **True.** Page size affects fault rate through spatial locality coverage.
- Key concept: page size design factors — page table size, fragmentation, I/O overhead, and page fault rate.

### Problem 13

This problem is worth 5 points (multiple-choice, per-option scoring).

Simulate LRU, FIFO, and Optimal on the reference string $7, 2, 3, 1, 2, 5, 3, 4, 6, 7, 7, 1, 0, 5, 4, 6, 2, 3, 0, 1$ with 3 frames. Count page faults for each algorithm and select matching answers.
- Key concept: page replacement algorithm simulation — carefully tracing frame states through a reference string.

### Problem 14

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Containers do NOT include a guest OS; they share the host kernel.
(B) **True.** Containers on the same host share the OS kernel.
(C) **False.** VMs run on a hypervisor; containers run as isolated processes.
(D) **True.** Containers are more lightweight than VMs (no guest OS overhead).
- Key concept: containers vs. VMs — containers share the host kernel, are lighter, and do not include a guest OS.

### Problem 15

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Separation of mechanism and policy doesn't directly make programming easier.
(B) **True.** Systems are easier to modify because changing policy doesn't require changing mechanism.
(C) **True.** Different policies can be applied to the same mechanism, providing flexibility.
(D) **False.** Not directly related to utilization.
(E) **False.** Not directly about response time.
- Key concept: separation of mechanism and policy — enables flexibility and easier system modification without coupling design choices.

### Problem 16

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** RAID addresses both performance (striping) and reliability (redundancy) in disk organization.
(B) **False.** RAID 5 tolerates only one disk failure; RAID 6 tolerates two.
(C) **True.** Inverted page tables reduce memory used for page tables by having one entry per physical frame.
(D) **False.** An inverted page table has one entry per *physical frame* (not per virtual page).
- Key concept: RAID levels and inverted page tables — RAID 5 single-fault tolerance, inverted page table indexed by physical frames.

### Problem 17

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** Process-contention scope (PCS) schedules user threads to kernel threads; system-contention scope (SCS) schedules kernel threads to CPUs.
(B) **True.** PCS can preempt a running thread in favor of a higher-priority one within the same process.
(C) **True.** Priority inversion occurs when a low-priority process holds a resource needed by a high-priority one.
(D) **True.** A race condition results from concurrent unsynchronized access to shared data.
- Key concept: scheduling scopes (PCS vs. SCS), priority inversion, and race conditions in concurrent systems.

### Problem 18

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** With equal-length jobs, RR adds context-switch overhead, giving worse average turnaround time than FIFO.
(B) **True.** Preemptive SJF (SRTF) can preempt a longer job when a shorter one arrives.
(C) **True.** Each queue in multilevel queue scheduling can use its own algorithm (e.g., RR for interactive, FCFS for batch).
(D) **True.** Larger pages → fewer page table entries → smaller page table.
- Key concept: scheduling algorithm comparisons, multilevel queue flexibility, and page table size dependence on page size.

### Problem 19

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **False.** The valid-invalid bit indicates whether a page is in memory, not for reducing transfer overhead.
(B) **True.** Only one process may be active inside a monitor at a time (mutual exclusion).
(C) **False.** Banker's algorithm is for deadlock *avoidance*, not prevention.
(D) **True.** Copy-on-write avoids copying pages at fork time, enabling more efficient process creation.
- Key concept: valid-invalid bit purpose, monitor mutual exclusion, Banker's algorithm as avoidance (not prevention), and copy-on-write efficiency.

### Problem 20

This problem is worth 5 points (multiple-choice, per-option scoring).

(A) **True.** ISO/IEC 27001 is an international standard for information security management systems.
(B) **False.** DoS (Denial of Service) is not a man-in-the-middle attack; it overwhelms a service with traffic.
(C) **False.** IPSec uses its own protocols (AH, ESP), not TLS. TLS operates at the transport layer.
(D) **True.** `nslookup` queries DNS to resolve domain names to IP addresses and vice versa.
- Key concept: security standards (ISO 27001), attack classifications (DoS vs. MITM), protocol layers (IPSec vs. TLS), and DNS tools.
