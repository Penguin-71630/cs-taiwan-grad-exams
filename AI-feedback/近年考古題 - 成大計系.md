## 108

### Problem 1

This problem is worth 20 points total (3 subparts).

- **(1)** [8%] For a direct-mapped cache with 16 words total, 2-word blocks → 8 blocks. Index = 3 bits (block number mod 8), tag = remaining upper bits. Block address = word address ÷ 2. Index = block address mod 8. Walk through each reference, check tag match at the indexed slot; initially all empty so first access to each index is a miss.
  - Key concept: direct-mapped cache address decomposition — tag, index, and block offset fields.

- **(2)** [8%] Four-way set-associative, 16 words, 2-word blocks → 8 blocks ÷ 4 ways = 2 sets. Index = 1 bit. Each set holds 4 blocks; on a miss with a full set, evict the LRU entry. Walk through the 12 references tracking each set's 4-way contents and LRU order.
  - Key concept: set-associative cache with LRU replacement — set indexing and eviction policy.

- **(3)** [4%] AMAT = hit time + miss rate × miss penalty. Count misses from (1) and (2), compute miss rates, then plug into the formula with respective hit times (1 cycle vs. 2 cycles) and 200-cycle miss penalty.
  - Key concept: average memory access time (AMAT) formula.

### Problem 2

This problem is worth 15 points total (3 subparts).

- **(1)** [6%] T1 and T4 are sequential (2+2=4 ms). T2 has loop-carried dependency (each iteration depends on previous), so it's hard to parallelize across cores. T3's iterations are independent → split across 4 cores: 16/4=4 ms. Total ≈ 2+8+4+2=16 ms. Speedup = 28/16 = 1.75.
  - Key concept: Amdahl's law and data dependency analysis for multicore parallelism.

- **(2)** [6%] SIMD parallelizes data-parallel inner loops. T3's inner loop does independent multiply-and-assign → 8-wide SIMD gives 16/8=2 ms. T2 has dependency → limited SIMD benefit. Total ≈ 2+8+2+2=14 ms. Speedup = 28/14 = 2.0.
  - Key concept: SIMD (Single Instruction Multiple Data) parallelism for data-level operations.

- **(3)** [3%] Compare speedups: SIMD (2.0×) > multicore (1.75×). The SIMD processor is faster because T3 (the dominant segment) benefits more from fine-grained data parallelism than from coarse-grained thread parallelism, especially when T2 has loop-carried dependencies.
  - Key concept: comparing parallelism strategies based on workload characteristics.

### Problem 3

This problem is worth 15 points total (5 subparts, 3% each).

- **(1)** [3%] **False.** GPU DRAM provides high bandwidth, not low latency. GPUs achieve performance through massive parallelism and throughput, tolerating latency via thread switching rather than reducing it.
  - Key concept: GPU memory architecture — bandwidth-oriented vs. latency-oriented design.

- **(2)** [3%] **False.** Computer clusters use message passing (e.g., MPI) for inter-node communication, not shared memory. Each node has its own memory space. Distributed shared memory (DSM) exists but is a software abstraction, not true shared memory.
  - Key concept: cluster computing communication models — message passing vs. shared memory.

- **(3)** [3%] **False.** GPUs are not always faster than CPUs. GPUs excel at massively parallel, data-parallel workloads but perform poorly on sequential, branch-heavy tasks. Power consumption comparison depends on the specific workload.
  - Key concept: CPU vs. GPU performance trade-offs — task suitability determines which is faster.

- **(4)** [3%] **False.** The purpose of L1 cache is to minimize **hit time** (small and fast), while the purpose of L2 cache is to reduce **miss rate** (larger capacity to capture more data). The statement reverses these roles.
  - Key concept: multi-level cache hierarchy design goals — L1 for speed, L2 for capacity.

- **(5)** [3%] **False.** Floating-point addition is NOT associative due to rounding errors. For example, $(1.0 + 10^{20}) + (-10^{20}) \ne 1.0 + (10^{20} + (-10^{20}))$ because of precision loss. Integer addition is associative (barring overflow).
  - Key concept: floating-point arithmetic non-associativity due to finite precision.

### Problem 4

This problem is worth 20 points total (4 subparts).

- **(1)** [4%] TLB reach = TLB entries × page size. With 64 entries: for 1 KB pages → 64 × 1 KB = 64 KB; for 4 KB pages → 64 × 4 KB = 256 KB.
  - Key concept: TLB reach — product of TLB size and page size.

- **(2)** [4%] RPM = 5400, so rotation time = 60/5400 ≈ 11.11 ms. With 40 sectors/track, time per sector = 11.11/40 ≈ 0.278 ms. Transfer rate = 512 bytes / 0.278 ms ≈ 1843 KB/s. Alternatively: 5400/60 × 40 × 512 = 1,843,200 bytes/s ≈ 1800 KB/s.
  - Key concept: disk transfer rate calculation from RPM, sectors per track, and sector size.

- **(3)** [4%] I/O time = seek time + rotational latency + transfer time. Seek = 30 ms, rotational latency (access latency) = 10 ms. For 1 KB page: transfer = 2 sectors × 0.278 ms ≈ 0.56 ms. Total ≈ 40.56 ms. For 4 KB page: transfer = 8 sectors × 0.278 ms ≈ 2.22 ms. Total ≈ 42.22 ms.
  - Key concept: page fault I/O time — sum of seek, rotational latency, and transfer components.

- **(4)** [8%] For 4 KB of data: using 1 KB pages requires 4 page faults (4 seeks + 4 rotational latencies + transfers) ≈ 4 × 40.56 = 162.2 ms. Using 4 KB pages requires 1 page fault ≈ 42.22 ms. The 4 KB page size is preferred because the overhead of repeated seeks and rotational latencies dominates.
  - Key concept: page size trade-off — larger pages amortize seek/rotational overhead for sequential data.

### Problem 5

This problem is worth 30 points total (4 subparts).

- **(1)** [10%] The two types of latencies are: (a) **Interrupt latency** — time from interrupt arrival to the start of the interrupt service routine (ISR), including saving state and determining interrupt type; (b) **Dispatch latency** — time for the dispatcher to stop one process and start the appropriate one, including preempting the current process and context switching.
  - Key concept: real-time event latency decomposition — interrupt latency and dispatch latency.

- **(2)** [5%] **Preemption-based scheduling** is preferred because it allows the OS to interrupt a currently running lower-priority process to immediately service a higher-priority event, reducing dispatch latency.
  - Key concept: preemptive vs. non-preemptive scheduling for real-time systems.

- **(3)** [10%] Using RR with quantum = 20 ms (preemptive): Execute P1(0-20), P2(20-37), P3(37-57), P4(57-77), P1(77-97), P3(97-117), P4(117-121), P1(121-134), P3(134-162). Compute waiting time = turnaround − burst for each process. Average waiting time and average turnaround time follow from the Gantt chart.
  - Key concept: Round Robin scheduling — Gantt chart construction and performance metric computation.

- **(4)** [5%] With FCFS and 4 processes arriving at the same time, the number of possible schedules is $4! = 24$ (any permutation of P1, P2, P3, P4).
  - Key concept: FCFS scheduling permutations — $n!$ orderings for $n$ simultaneously arriving processes.

## 109

### Problem 1

This problem is worth 20 points.

Discuss CPU architecture (control-heavy, few powerful cores, deep pipelines, large caches, branch prediction, out-of-order execution) vs. GPU architecture (compute-heavy, many simple cores/streaming multiprocessors, SIMT execution model, shallow pipelines, small caches, hardware thread scheduling to hide latency). CPU pipeline: 5-stage (IF/ID/EX/MEM/WB) or deeper superscalar; GPU pipeline: fetch/decode/execute with massive thread warps.
- Key concept: CPU vs. GPU architectural design philosophies — latency-oriented vs. throughput-oriented.

### Problem 2

This problem is worth 10 points (2 subparts).

- **(a)** Heterogeneous computing uses different types of processors (CPU, GPU, FPGA, DSP) together, assigning tasks to the most suitable processor type to optimize performance and energy efficiency.
- **(b)** Data parallel computing applies the same operation simultaneously to multiple data elements. Each processing unit performs the same instruction on different data (SIMD model). Examples: GPU CUDA kernels, OpenCL.
- Key concept: heterogeneous computing and data parallelism — exploiting diverse hardware for different workload characteristics.

### Problem 3

This problem is worth 20 points.

Write a C program with GPU framework (e.g., CUDA): allocate host matrices A(M×N), B(N×P), C(M×P); allocate device memory with `cudaMalloc`; copy A, B to device with `cudaMemcpy`; launch kernel with grid/block dimensions where each thread computes one element $C[i][j] = \sum_k A[i][k] \times B[k][j]$; copy result back to host; free device memory.
- Key concept: GPU-accelerated matrix multiplication — host/device memory management, kernel launch configuration, and thread-to-element mapping.

### Problem 4

This problem is worth 20 points total (4 subparts).

- **(1)** [5%] This is the first readers-writers problem (readers preference). It is NOT optimal because it can cause **writer starvation** — as long as readers keep arriving, writers may never get access to the shared data.
  - Key concept: readers-writers problem variants — reader preference leads to writer starvation.

- **(2)** [5%] Sequence: R1 enters first (acquires rmut, increments rcount to 1, acquires wmut, releases rmut, reads). W1 must wait on wmut. R2 arrives, acquires rmut, increments rcount to 2, releases rmut, reads concurrently with R1. After both R1 and R2 finish and release wmut, W1 enters. Actual order: R1 → R2 (concurrent with R1) → W1.
  - Key concept: semaphore-based reader-writer synchronization — concurrent reader access blocks writers.

- **(3)** [5%] The `test_and_set` (TAS) or `compare_and_swap` (CAS) atomic hardware instructions are used to implement semaphores. These provide atomic read-modify-write operations that prevent race conditions.
  - Key concept: hardware atomic instructions for semaphore implementation — test-and-set, compare-and-swap.

- **(4)** [5%] Without hardware atomic instructions, **Peterson's algorithm** or **Lamport's bakery algorithm** can provide mutual exclusion in software. These algorithms use shared variables with careful ordering of reads and writes to ensure atomicity of critical sections.
  - Key concept: software-based mutual exclusion — Peterson's algorithm and bakery algorithm.

### Problem 5

This problem is worth 30 points total (4 subparts).

- **(1)** [10%] Caches involved in file reads: **page cache** (OS-level cache of disk blocks in memory), **buffer cache** (for metadata and raw block I/O), and the disk controller's **hardware cache**. File writes bypass the software cache when using **direct I/O** (O_DIRECT) or **synchronous writes** (O_SYNC), which flush data directly to disk.
  - Key concept: file system caching layers — page cache, buffer cache, and conditions for unbuffered writes.

- **(2)** [10%] **Unified buffer cache** (or unified virtual memory) uses the page cache to cache both process pages and file data. When processes read large files into heap memory, this causes **double caching** or **page cache thrashing** — the large file data evicts process pages from the cache, increasing page faults and degrading performance.
  - Key concept: unified buffer cache — memory pressure from large file reads causing page cache pollution.

- **(3)** [5%] **Double buffering** (or double caching) occurs when file data is cached in both the page cache and a separate buffer cache, wasting memory and causing inconsistency.
  - Key concept: double caching problem — redundant copies of file data in different cache layers.

- **(4)** [5%] The **unified buffer cache** technique eliminates double caching by using a single cache (page cache) for both memory-mapped file I/O and read/write system calls. The block diagram shows: application → `read()`/`mmap()` → page cache → disk, with no separate buffer cache layer.
  - Key concept: unified buffer cache design — single caching layer to avoid redundancy.

## 110

### Problem 1

This problem is worth 50 points total (8 subparts).

- **(1)** [5%] MIPS assembly for `C[x+y*N] += A[x+z*N] * B[z+y*N]` with values already in `$f4`, `$f6`, `$f8`: `mul.d $f10, $f6, $f8` (multiply A and B), then `add.d $f4, $f4, $f10` (accumulate into C).
  - Key concept: MIPS floating-point instructions — `mul.d` and `add.d` for double-precision operations.

- **(2)** [5%] The **floating-point unit (FPU)** datapath should be optimized, specifically the floating-point multiplier and adder. Pipelining the FPU or adding a fused multiply-add (FMA) unit would accelerate these operations.
  - Key concept: FPU datapath optimization — pipelined multipliers and FMA units.

- **(3)** [5%] **Fixed-point arithmetic** emulates floating-point using integer operations. A fixed number of bits represents the fractional part, and standard integer add/multiply/shift operations implement the arithmetic with appropriate scaling.
  - Key concept: fixed-point arithmetic — emulating floating-point via integer operations with implicit scaling.

- **(4)** [5%] Fixed-point (integer-based) emulation is generally **more efficient** than software library emulation because it uses native integer instructions without function call overhead, while library routines involve call/return, argument passing, and complex bit manipulation.
  - Key concept: fixed-point vs. software floating-point library performance comparison.

- **(5)** [10%] **SIMD** (Single Instruction Multiple Data) instructions exploit data-level parallelism. Rewrite: load multiple consecutive elements of A and B into SIMD registers, perform packed multiply and add operations, and store results back. The inner loop processes multiple elements per iteration (e.g., 2 doubles with 128-bit SIMD).
  - Key concept: SIMD vectorization of matrix multiplication — packed operations on multiple data elements.

- **(6)** [5%] Three 64×64 matrices of 8-byte doubles = 3 × 64 × 64 × 8 = 98,304 bytes. Smallest power of 2 ≥ 98,304 is 128 KB = 131,072 bytes. But to avoid cache conflicts (direct-mapped), the cache must be large enough that different matrices don't map to the same sets. A fully-associative 128 KB cache suffices; for direct-mapped, 256 KB may be needed.
  - Key concept: cache sizing for conflict-free matrix storage — power-of-2 sizing and conflict analysis.

- **(7)** [5%] Three M×M submatrices must fit in 16 KB: $3 \times M^2 \times 8 \le 16384$. $M^2 \le 682.67$, so $M \le 26$. Maximum block size $M = 26$.
  - Key concept: cache blocking (tiling) — determining block size from cache capacity.

- **(8)** [10%] Blocked matrix multiply: add three outer loops iterating by block size M over x, y, z dimensions, with inner loops operating within M×M submatrices. This ensures temporal locality by keeping submatrices in cache.
  - Key concept: blocked (tiled) matrix multiplication — loop tiling for cache optimization.

### Problem 2

This problem is worth 50 points total (10 subparts, 5% each).

- **(1)** [5%] **In-memory indexing**: Use a hash table or B-tree in volatile memory for O(1) or O(log n) key lookups. Hash table provides fast random GET/PUT; B-tree or skip list supports efficient SCAN (range queries) via ordered traversal.
  - Key concept: in-memory index structures — hash tables for point queries, B-trees for range queries.

- **(2)** [5%] **In-disk indexing**: Use an on-disk B+tree or LSM-tree. B+tree provides O(log n) lookups and efficient range scans. LSM-tree batches writes for sequential I/O, improving write throughput at the cost of read amplification.
  - Key concept: persistent index structures — B+tree vs. LSM-tree trade-offs.

- **(3)** [5%] **Caching**: Keep frequently accessed KV pairs in memory (LRU/LFU cache). Read cache reduces disk reads for hot keys. Write-back cache batches writes to disk. Cache size is bounded by x (volatile memory).
  - Key concept: caching policies for KV stores — LRU/LFU eviction and write-back strategies.

- **(4)** [5%] **Paging**: Use virtual memory to manage the KV store's memory footprint. When total KV data exceeds x, the OS pages out cold data to disk. Memory-mapped files allow transparent paging of index and data.
  - Key concept: virtual memory paging for large-scale KV storage beyond physical memory limits.

- **(5)** [5%] **Batched I/O**: Group multiple PUT/GET operations into batches to amortize disk seek and rotational overhead. Write batching (write-ahead log) converts random writes to sequential writes, improving throughput.
  - Key concept: I/O batching — amortizing per-operation overhead through grouping.

- **(6)** [5%] **Multithreading and thread scheduling**: Use a thread pool to handle concurrent GET/PUT/SCAN requests. Assign I/O-bound requests to separate threads to overlap disk latency. Priority scheduling can prioritize latency-sensitive GET over batch PUT.
  - Key concept: concurrent request handling via thread pools and scheduling policies.

- **(7)** [5%] **Shared memory and consistency**: Multiple threads accessing the in-memory index require synchronization (read-write locks, RCU). Readers can proceed concurrently; writers acquire exclusive locks. Consistency models (linearizability vs. eventual consistency) affect performance-correctness trade-offs.
  - Key concept: shared memory synchronization for concurrent KV access — locking strategies and consistency guarantees.

- **(8)** [5%] **Encoding/Decoding**: Compress KV pairs using prefix compression, dictionary encoding, or variable-length encoding to reduce storage footprint and I/O bandwidth. Trade CPU cycles for reduced disk I/O.
  - Key concept: data compression techniques to reduce I/O in KV storage.

- **(9)** [5%] **File system block layout**: Allocate contiguous disk blocks for sequential access patterns (extent-based allocation). For random access, use indexed allocation. Align KV data to block boundaries to avoid read-modify-write overhead.
  - Key concept: disk block layout optimization — contiguous vs. indexed allocation for different access patterns.

- **(10)** [5%] **File system compaction**: Periodically merge and compact fragmented data files (similar to LSM compaction). Remove deleted/overwritten KV pairs, reclaim space, and reorganize data for sequential access. Balances write amplification against read performance.
  - Key concept: compaction in KV stores — garbage collection and data reorganization for sustained performance.

## 111

### Problem 1

This problem is worth 20 points total (10 subparts, 2% each).

- **a.** [2%] **False.** Modern processors do not have power consumption proportional to utilization. Even at low utilization, components like memory, chipset, and I/O consume significant static/leakage power. Power ∝ utilization is not linear.
  - Key concept: power consumption characteristics — static vs. dynamic power.

- **b.** [2%] **False.** Computer A: time = 10B × 1.0 / 4G = 2.5s, MIPS = 10B / (2.5 × 10^6) = 4000. Computer B: time = 8B × 1.2 / 4G = 2.4s, MIPS = 8B / (2.4 × 10^6) = 3333. B is faster (2.4s < 2.5s) but has lower MIPS. The statement says A has higher MIPS and B is faster — this is **True**.
  - Key concept: MIPS is a misleading performance metric — execution time is the true measure.

- **c.** [2%] **False.** `beq` uses a 16-bit signed offset (in words). From PC = 0x00000600, the branch target range is PC+4 ± 2^17 bytes = 0x600+4 ± 0x20000. Target 0x00020000 is 0x1FA00 away, which is within ±0x20000. Actually, offset = (0x20000 - 0x604)/4 = 0x7EFF, which fits in 16 bits. **True.** But check: 0x20000 - 0x604 = 0x1F9FC, /4 = 0x7E7F, which fits in a signed 16-bit field. **True.**
  - Key concept: MIPS branch instruction offset range — 16-bit signed word offset from PC+4.

- **d.** [2%] **False.** The bit pattern 0x01000000 = sign=0, exponent=0x02=2, mantissa=0. Value = $(-1)^0 \times 2^{2-127} \times 1.0 = 2^{-125}$. The smallest normalized positive number has exponent = 1 (0x00800000), value = $2^{-126}$. So this is not the smallest.
  - Key concept: IEEE 754 single precision — smallest normalized number has exponent field = 1.

- **e.** [2%] **False.** $-0.75 = -3/4 = -1.1_2 \times 2^{-1}$. Sign = 1, exponent = -1+127 = 126 = 0x7E = 0111_1110. Mantissa = 1000...0. Binary: 1_01111110_10000000000000000000000 = 0xBF400000 = 1011_1111_0100_0000_0000_0000_0000_0000. The given answer has mantissa all zeros (0xBF000000), which represents $-0.5$, not $-0.75$. **False.**
  - Key concept: IEEE 754 floating-point encoding — sign, biased exponent, and mantissa fields.

- **f.** [2%] **False.** The advantage of VIPT (Virtually Indexed Physically Tagged) over PIPT is **lower hit time** (can index cache in parallel with TLB lookup), not lower miss rate. The statement incorrectly says the advantage is lower miss rate.
  - Key concept: VIPT cache advantage — parallel TLB and cache index lookup reduces hit time.

- **g.** [2%] **True.** A fully-associative cache has only one set containing all blocks. Any block can go anywhere, so there are no conflict misses. Only compulsory and capacity misses occur.
  - Key concept: fully-associative cache eliminates conflict misses.

- **h.** [2%] **False.** GPUs hide DRAM latency through **massive thread-level parallelism** (hardware multithreading/warp scheduling), not deep pipelines. When one warp stalls on memory, another warp executes.
  - Key concept: GPU latency hiding — hardware multithreading, not deep pipelining.

- **i.** [2%] **True.** From the Roofline model description, Kernel 1 is positioned in the left region (low arithmetic intensity), below the memory bandwidth slope. This means its performance is limited by memory bandwidth, not compute.
  - Key concept: Roofline model — kernels below the bandwidth slope are memory-bound.

- **j.** [2%] **True.** If a process's entire working set fits in physical memory and is already mapped in the TLB, no TLB misses or page faults will occur during execution. This is possible with a small enough working set.
  - Key concept: TLB and page fault avoidance — possible when working set fits in TLB-covered memory.

### Problem 2

This problem is worth 15 points total (2 subparts).

- **a.** [5%] With NO forwarding and branch resolved at EX stage: `ADD` completes at cycle 5. `BEQ` depends on R2 from `ADD` — must wait for `ADD`'s WB (cycle 5) before `BEQ` can read in ID, so `BEQ` starts IF at cycle 1, ID stalls until cycle 6 (after ADD writes back), completes at cycle 5+stalls. With predict-taken: `BEQ` predicts taken (to Label2) but actually not taken → 2-cycle penalty (branch resolved at EX). Draw pipeline diagram accounting for data hazard stalls (no forwarding: read-after-write requires 2 stall cycles) and control hazard penalties.
  - Key concept: pipeline hazard analysis without forwarding — stall cycles for RAW hazards and branch misprediction penalties.

- **b.** [10%] With FULL forwarding and branch resolved at ID stage: `ADD` produces R2 at end of EX (cycle 3). With forwarding, `BEQ` can use R2 in ID if forwarded from EX. But branch is resolved at ID — `BEQ` needs R2 at ID stage. `ADD` result available at end of EX (cycle 3), `BEQ` ID is cycle 3 → 1-cycle stall needed (load-use type). After `BEQ` not-taken, `LW` executes. `BEQ R3,R0,Label1` needs R3 from `LW` — load-use hazard requires 1 stall. Track predict-taken penalties: second `BEQ` predicts taken and is taken → no penalty.
  - Key concept: pipeline hazard analysis with forwarding — load-use hazards and branch resolution at ID stage.

### Problem 3

This problem is worth 15 points total (3 subparts).

- **a.** [5%] Direct-mapped, 1-word block, 8 words → 8 blocks. Index = address mod 8. Walk through: 3→idx3(M), 180→idx4(M), 2→idx2(M), 43→idx3(M), 191→idx7(M), 88→idx0(M), 190→idx6(M), 14→idx6(M, replaces 190), 181→idx5(M), 88→idx0(H). Hit rate = 1/10 = 10%.
  - Key concept: direct-mapped cache simulation — index computation and replacement tracking.

- **b.** [5%] Two-way set-associative, 2-word blocks, 8 words → 4 blocks in 2 sets. Block address = word address ÷ 2. Set index = block address mod 2. Each set has 2 ways with LRU. Walk through references tracking set contents and LRU state.
  - Key concept: set-associative cache with LRU — block address computation and set-level replacement.

- **c.** [5%] Fully associative, 2-word blocks, 8 words → 4 blocks total. Block address = word address ÷ 2. Any block can occupy any of the 4 slots. LRU eviction when full. Walk through all references.
  - Key concept: fully-associative cache simulation — LRU replacement across all cache entries.

### Problem 4

This problem is worth 50 points total (10 subparts).

- **a.** [5%] Device drivers issue four types of commands: (1) **Control** commands (start, stop, reset device), (2) **Status** commands (check device readiness), (3) **Data-out** commands (write data to device), (4) **Data-in** commands (read data from device). These are issued via port-mapped I/O (`in`/`out` instructions) or memory-mapped I/O (load/store to device registers).
  - Key concept: device driver I/O commands — control, status, data transfer via port-mapped or memory-mapped I/O.

- **b.** [5%] **CPU-based (programmed I/O)** is more suitable for latency-oriented small-size data transfers. DMA has setup overhead (programming DMA controller with source, destination, count) that makes it inefficient for small transfers. For small data, the CPU can complete the transfer faster than the DMA setup time.
  - Key concept: programmed I/O vs. DMA — DMA setup overhead makes it unsuitable for small transfers.

- **c.** [5%] The **Translation Lookaside Buffer (TLB)** hardware accelerates virtual-to-physical address translation. It caches recent page table entries, allowing the DMA controller to quickly resolve physical addresses for the data transfer.
  - Key concept: TLB for accelerating virtual-to-physical address translation in DMA operations.

- **d.** [6%] (i) **Buffer** — a memory area that stores data being transferred between application and device. (ii) **Cache** — a region of fast memory holding copies of frequently accessed data. (iii) **Spool** — a memory area that holds output for a device that cannot handle interleaved streams (e.g., printer spooler).
  - Key concept: I/O service techniques — buffering, caching, and spooling.

- **e.** [4%] **Buffering** enables asynchronous I/O. The application writes data to a buffer and continues execution without waiting for the I/O to complete. The OS transfers data from the buffer to the device in the background. Double buffering allows the application to fill one buffer while the OS drains the other.
  - Key concept: asynchronous I/O via buffering — decoupling application execution from I/O completion.

- **f.** [5%] A **separate disk partition** is preferred for efficiency. It bypasses the file system overhead (no directory traversal, no file metadata management), allowing direct raw I/O to contiguous disk blocks. This provides faster swap-in/swap-out operations compared to file-system-based swap.
  - Key concept: swap partition vs. swap file — raw partition avoids file system overhead.

- **g.** [5%] **Thrashing** occurs. As the degree of multiprogramming increases with fixed swap space, each process gets fewer physical frames. Processes begin page-faulting frequently, spending more time swapping pages than executing. CPU utilization drops dramatically as the system is busy handling page faults.
  - Key concept: thrashing — excessive paging due to insufficient memory allocation per process.

- **h.** [5%] The OS suspends a process when $\text{WSS}(a) + \text{WSS}(b) + \text{WSS}(c) > d$. When the total working set size exceeds available swap/memory space $d$, the OS must suspend one or more processes to free frames and prevent thrashing.
  - Key concept: working set model — suspend processes when total WSS exceeds available memory.

- **i.** [5%] The **SCAN** (or C-SCAN/C-LOOK) family performs better in multiprogramming environments with concurrent I/O. SCAN moves the disk head in one direction servicing all requests, then reverses, providing bounded wait times and reducing total seek distance compared to FCFS (which causes random head movement).
  - Key concept: SCAN disk scheduling — bounded wait times and reduced seek distance for concurrent I/O.

- **j.** [5%] When the disk queue size is 1, **FCFS** performs best (and is equivalent to all other algorithms). With only one pending request, there is no opportunity to reorder requests, so all scheduling algorithms produce the same result. FCFS has the lowest overhead.
  - Key concept: disk scheduling degenerates to FCFS with queue size 1 — no reordering opportunity.

## 112

### Problem 1

This problem is worth 20 points total (10 subparts, 2% each).

- **(1)** [2%] **(d).** Apple's systems use capability-based protection via entitlements. Android separates users (each app gets a unique UID). Root cannot modify MAC (SELinux enforces MAC even over root). RBAC reduces risk by limiting privileges.
  - Key concept: access control mechanisms — MAC, RBAC, capabilities, and entitlements.

- **(2)** [2%] **(d).** You cannot access a file in an unmounted file system — the file system must be mounted to a mount point before its files are accessible. All other statements are correct.
  - Key concept: file system mounting — unmounted file systems are inaccessible.

- **(3)** [2%] **(a).** FAT has one entry per block, indexed by block number. Consistency check may NOT always recover structures (may lose data). NVM devices still need free-space management. Grouping is faster than linked list for finding free blocks.
  - Key concept: FAT file system structure — block-indexed allocation table.

- **(4)** [2%] **(d).** Unified virtual memory uses **page caching** to cache both process pages and file data in the same page frames.
  - Key concept: unified virtual memory — page cache for both process and file data.

- **(5)** [2%] **(b).** ISAM uses a small master index (kept in memory) pointing to disk blocks of a secondary index, which in turn points to actual file blocks.
  - Key concept: ISAM indexed structure — two-level index with master index in memory.

- **(6)** [2%] **(c).** In polling I/O, the main inefficiency is polling repeatedly when the device is rarely ready for service, wasting CPU cycles on busy-waiting.
  - Key concept: polling I/O inefficiency — wasted CPU cycles checking unready devices.

- **(7)** [2%] **(b).** Disk scheduling algorithms consider only seek distances because modern disks do not disclose the physical location (cylinder, sector) of logical blocks. The OS can only estimate seek distance from logical block numbers.
  - Key concept: disk scheduling limitation — logical-to-physical block mapping is hidden from the OS.

- **(8)** [2%] **(a).** When an instruction modifies multiple locations and a page fault occurs mid-instruction, temporary registers hold the overwritten values so the instruction can be restarted cleanly.
  - Key concept: page fault handling for multi-location instructions — temporary registers for restart capability.

- **(9)** [2%] **(c).** When a Windows dispatcher object (event) moves to signaled state, all waiting threads are moved to ready state. For mutex objects, only one thread is released.
  - Key concept: Windows dispatcher objects — event (wake all) vs. mutex (wake one) signaling behavior.

- **(10)** [2%] **(a).** Spinlocks CAN be used on single-processor machines (they just disable preemption). The statement "cannot be used" is false. All other statements about spinlocks are true.
  - Key concept: Linux spinlock behavior — on uniprocessors, spinlocks disable preemption rather than spinning.

### Problem 2

This problem is worth 10 points total (3 subparts).

- **(1)** [4%] From the dispatch table: priority 15 → time quantum = 160 ms; priority 55 → time quantum = 40 ms. Higher-priority threads get shorter time quanta (more responsive scheduling).
  - Key concept: Solaris time-sharing scheduler — priority-to-quantum mapping.

- **(2)** [3%] Priority 35 thread uses entire quantum → "time quantum expired" column: new priority = 25. The scheduler lowers the priority as punishment for CPU-bound behavior.
  - Key concept: Solaris priority adjustment — CPU-bound threads get lower priority (feedback scheduling).

- **(3)** [3%] Priority 40 thread blocks for I/O → "return from sleep" column: new priority = 55. The scheduler boosts priority to reward I/O-bound behavior, ensuring interactive responsiveness.
  - Key concept: Solaris priority boost for I/O-bound threads — multilevel feedback queue scheduling.

### Problem 3

This problem is worth 10 points total (2 subparts).

- **(1)** [5%] Four possible causes of tail latency: (a) **Garbage collection** — storage device firmware performs background GC, causing unpredictable write stalls. (b) **Write amplification** — internal data reorganization (e.g., SSD wear leveling, log-structured merge) introduces extra I/O. (c) **Queue congestion** — when many requests queue up, some experience long waiting times. (d) **Read-write interference** — large background writes block pending reads.
  - Key concept: tail latency causes in storage systems — GC, write amplification, queuing, and interference.

- **(2)** [5%] Two solutions: (a) **Request hedging** — send duplicate requests to multiple replicas and use the first response, reducing tail latency at the cost of extra load. (b) **I/O scheduling with deadline guarantees** — prioritize long-waiting requests or use deadline-based scheduling to bound worst-case latency.
  - Key concept: tail latency mitigation — hedging and deadline-aware scheduling.

### Problem 4

This problem is worth 10 points total (4 subparts).

- **(1)** [2%] Pages = $2^{32} / 2^{12} = 2^{20} = 1,048,576$ pages. Physical memory = 32 GB = $2^{35}$ bytes → 35 bits for physical address. Frames = $2^{35} / 2^{12} = 2^{23} = 8,388,608$ frames.
  - Key concept: page/frame count computation from virtual/physical address space and page size.

- **(2)** [2%] Effective access time = TLB_hit_ratio × (TLB_time + mem_time) + (1 - TLB_hit_ratio) × (TLB_time + 2 × mem_time) < 140 ns. With TLB = 20 ns, mem = 100 ns: $p(120) + (1-p)(220) < 140$. $220 - 100p < 140$. $p > 0.8$. Minimum TLB hit ratio = 80%.
  - Key concept: effective memory access time with TLB — solving for minimum hit ratio.

- **(3)** [3%] EAT = (1 - p) × 100 ns + p × 15,000,000 ns = (1 - 0.0000004) × 100 + 0.0000004 × 15,000,000 = 100 + 6 = 106 ns.
  - Key concept: effective access time under demand paging — page fault rate impact.

- **(4)** [3%] 64-bit virtual address, 8 KB = $2^{13}$ pages → 51 bits for page number. Each page entry = 8 bytes. Entries per page table page = $2^{13} / 8 = 2^{10} = 1024$, using 10 bits per level. Levels needed = $\lceil 51/10 \rceil = 6$ levels.
  - Key concept: multi-level page table depth — determined by page number bits and entries per page.

### Problem 5

This problem is worth 30 points total (4 subparts).

- **(1)** [5%] Pipeline registers must be added between each pair of adjacent stages: IF/ID, ID/EX, EX/MEM, MEM/WB. These registers buffer instruction data, control signals, and intermediate results so that multiple instructions can be in different pipeline stages simultaneously without interference.
  - Key concept: pipeline registers — buffering data between pipeline stages for concurrent instruction execution.

- **(2)** [12%] Bug: the write-back register destination number from the WB stage must be forwarded back through the pipeline registers to correctly identify which register to write. In the basic pipelined datapath, the write register number from the instruction in WB stage may be overwritten by later instructions' register numbers. Fix: propagate the destination register number through EX/MEM and MEM/WB pipeline registers, and use the MEM/WB register number for the write-back.
  - Key concept: pipeline register propagation — destination register number must travel through pipeline stages.

- **(3)** [8%] Load instruction uses all 5 stages: IF(100) + ID(50) + EX(100) + MEM(200) + WB(80) = 530 ps. Store instruction uses IF + ID + EX + MEM (no WB needed, but still passes through WB stage): 530 ps total latency (all stages). In single-cycle: clock period = max stage latency × 1 cycle = 530 ps per instruction.
  - Key concept: single-cycle instruction latency — sum of all stage delays.

- **(4)** [5%] Pipelined clock cycle = longest stage time = 200 ps (MEM stage). The clock must accommodate the slowest stage to ensure all stages complete within one cycle. Pipeline registers add slight overhead.
  - Key concept: pipelined clock period — determined by the slowest pipeline stage.

### Problem 6

This problem is worth 20 points total (3 subparts).

- **(1)** [5%] `addi x10, x14, 10` writes x10; `sub x13, x10, x14` reads x10 — RAW hazard (2-cycle gap needed without forwarding). Insert 2 NOPs: `addi x10, x14, 10` / `NOP` / `NOP` / `sub x13, x10, x14` / `xor x4, x3, x5`.
  - Key concept: NOP insertion for data hazards — 2 stall cycles for read-after-write without forwarding.

- **(2)** [5%] Reorder: `addi x10, x14, 10` / `xor x4, x3, x5` / `NOP` / `sub x13, x10, x14`. Moving `xor` (independent instruction) fills one NOP slot, but one NOP remains. The overhead cannot be completely removed because there are only 3 instructions and one independent instruction can only fill one of the two required delay slots.
  - Key concept: instruction reordering to fill hazard slots — limited by available independent instructions.

- **(3)** [10%] Loop executes 4 iterations: branch is taken 3 times (iterations 1-3) and not-taken once (iteration 4 exit). **Always-Taken**: correct 3 times, wrong 1 time → accuracy = 3/4 = 75%. **Always-Not-Taken**: correct 1 time, wrong 3 times → accuracy = 1/4 = 25%. Always-Taken predictor performs better.
  - Key concept: static branch prediction accuracy — loop branches are mostly taken.

## 113

### Problem 1

This problem is worth 20 points total (10 subparts, 2% each).

- **(1)** [2%] **(c).** Privileged instructions CAN be attempted from user mode — the attempt simply causes a trap/exception. They are not prevented from being issued; the hardware detects and traps them.
  - Key concept: privileged instruction protection — hardware traps attempts in user mode rather than preventing them.

- **(2)** [2%] **(a).** Message-passing is easier to implement for intercomputer communication because it doesn't require shared address spaces across network boundaries. Shared memory requires complex distributed memory coherence.
  - Key concept: message passing vs. shared memory for distributed systems.

- **(3)** [2%] **(a).** The major difficulty in layered OS design is appropriately defining the various layers — determining which services belong in which layer and managing dependencies between layers.
  - Key concept: layered OS design challenge — layer definition and dependency management.

- **(4)** [2%] **(a).** A boot block typically only knows the location and length of the rest of the bootstrap program. It is too small to contain the full OS loader.
  - Key concept: boot block — minimal code that locates and loads the bootstrap loader.

- **(5)** [2%] **(b).** Modules allow OS services to be loaded dynamically (loadable kernel modules). They can be loaded/unloaded at runtime without recompiling the kernel.
  - Key concept: loadable kernel modules — dynamic OS service extension.

- **(6)** [2%] **(d).** A cycle in a resource-allocation graph is a necessary AND sufficient condition for deadlock when each resource type has exactly one instance. With multiple instances, a cycle is necessary but not sufficient.
  - Key concept: resource allocation graph — cycle conditions for deadlock detection.

- **(7)** [2%] **(a).** Absolute code (with fixed memory addresses) can be generated only with compile-time binding, where the memory location is known at compile time.
  - Key concept: address binding — compile-time binding produces absolute (position-dependent) code.

- **(8)** [2%] **(d).** In dynamically linked libraries, a stub is included in the executable image for each library routine reference. The stub locates and loads the actual library routine on first call.
  - Key concept: dynamic linking — stubs replaced by actual routine addresses at runtime.

- **(9)** [2%] **(a).** `vfork()` allows the child process to use the parent's address space directly (no copying). The parent is suspended until the child calls `exec()` or `_exit()`.
  - Key concept: `vfork()` — child shares parent's address space to avoid copy overhead before exec.

- **(10)** [2%] **(d).** Belady's anomaly: for some page replacement algorithms (e.g., FIFO), increasing the number of allocated frames may actually increase the page-fault rate.
  - Key concept: Belady's anomaly — more frames can lead to more page faults with certain algorithms.

### Problem 2

This problem is worth 10 points total (10 subparts, 1% each).

- **(1)** [1%] **False.** SSDs are non-volatile storage — they retain data without power (flash memory).
  - Key concept: SSD storage classification — non-volatile (persistent).

- **(2)** [1%] **False.** A process can switch between real UID and effective UID (e.g., via `seteuid()`). The effective UID is used for permission checks but can be changed.
  - Key concept: process UID — real vs. effective UID and `setuid` behavior.

- **(3)** [1%] **True.** Mac OS X (now macOS) is a hybrid system combining the Mach microkernel with BSD UNIX components in the XNU kernel.
  - Key concept: macOS kernel architecture — hybrid Mach + BSD design.

- **(4)** [1%] **True.** Many OS treat I/O devices as special files (UNIX: everything is a file), using similar system calls (`open`, `read`, `write`, `close`) for both files and devices.
  - Key concept: UNIX device-file abstraction — unified I/O interface.

- **(5)** [1%] **False.** Named pipes (FIFOs) do NOT require a parent-child relationship. Any process that knows the pipe's name can use it. Unnamed (anonymous) pipes require parent-child relationship.
  - Key concept: named pipes vs. anonymous pipes — named pipes are accessible by any process.

- **(6)** [1%] **False.** After initial setup (`shm_open`, `mmap`), POSIX shared memory access is done through regular memory operations (load/store), NOT system calls.
  - Key concept: POSIX shared memory — direct memory access after initial mapping, no syscalls per access.

- **(7)** [1%] **False.** The first process in a typical Linux system is the `idle` process (PID 0) or the kernel `init` thread. `init` (PID 1) is the first **user-space** process, but `systemd` has replaced traditional `init` in modern systems. Strictly, the kernel's process 0 (swapper/idle) runs first.
  - Key concept: Linux boot process — PID 0 (idle/swapper) precedes PID 1 (init/systemd).

- **(8)** [1%] **True.** One-to-one model maps each user thread to a kernel thread, allowing another thread to run when one blocks. Many-to-one blocks all user threads when one makes a blocking system call.
  - Key concept: threading models — one-to-one provides better concurrency than many-to-one.

- **(9)** [1%] **True.** Implicit threading (thread pools, OpenMP, GCD, TBB) is the trend because it simplifies parallel programming by handling thread creation and management automatically.
  - Key concept: implicit threading — automatic thread management for parallel applications.

- **(10)** [1%] **False.** GCD (Grand Central Dispatch) manages thread pools internally but does not require the developer to create multiple threads explicitly. It can work with a single thread pool managed by the system. The statement is misleading — GCD uses threads internally but abstracts them away.
  - Key concept: Grand Central Dispatch — abstracts threading; system manages the thread pool.

### Problem 3

This problem is worth 5 points.

Apply the Banker's algorithm: Need = Max − Allocation. Need matrix: P0(5,4,1), P1(3,4,0), P2(4,5,2), P3(1,1,1), P4(3,0,0). Available = (2,1,2). Check: P3 needs (1,1,1) ≤ (2,1,2) ✓ → execute P3, available becomes (2,4,2). Then P1 needs (3,4,0) ≤ (2,4,2)? No. P4 needs (3,0,0) ≤ (2,4,2)? No. P0 needs (5,4,1) ≤ (2,4,2)? No. **No safe sequence exists** — the system is in an unsafe state.
- Key concept: Banker's algorithm — computing Need matrix and checking for a safe sequence.

### Problem 4

This problem is worth 15 points total (3 subparts).

- **(1)** [5%] Maximum restarts = 2. The instruction `Add A, B, C` may fault when fetching operand A (page fault, load page, restart), then fault when fetching operand B (another page fault, load page, restart). The instruction page is already resident. C's page may also fault during write, giving up to 3 restarts if A, B, and C are on different pages. But since the instruction is already in DRAM, max restarts = 3 (one for each of A, B, C's pages).
  - Key concept: demand paging instruction restart — maximum page faults per instruction equals the number of distinct pages accessed.

- **(2)** [5%] With indirect addressing `ADD (A), (B), (C)`: each operand requires two memory accesses (pointer page + data page). Minimum frames needed: 1 for instruction page + 2 per indirect operand × 3 operands = 7 frames. But with frame reuse, minimum = 1 (instruction) + 2 (for resolving each indirect reference one at a time) = at least 4 frames (instruction + pointer + data + one for replacement).
  - Key concept: minimum frame allocation for indirect addressing — counting distinct page accesses.

- **(3)** [5%] Advantages of larger pages: (1) Smaller page table (fewer entries). (2) Reduced TLB misses (each TLB entry covers more memory). (3) More efficient disk I/O (fewer seeks for same data amount). Disadvantages: (1) Greater internal fragmentation (wasted space within last page). (2) Longer page fault service time (more data to transfer per fault).
  - Key concept: large page size trade-offs — reduced overhead vs. increased fragmentation and transfer time.

### Problem 5

This problem is worth 20 points total (10 subparts, 2% each).

- **(1)** [2%] **False.** Cache is built with **SRAM** (Static RAM), not DRAM. SRAM is faster but more expensive and less dense than DRAM.
  - Key concept: cache memory technology — SRAM for caches, DRAM for main memory.

- **(2)** [2%] **False.** MIPS (Million Instructions Per Second) is NOT a reliable performance metric because it depends on the instruction set and doesn't account for instruction complexity. Execution time and CPU time are valid metrics.
  - Key concept: MIPS as a misleading metric — instruction set dependency.

- **(3)** [2%] **True.** GPR architectures (Intel x86, MIPS, RISC-V) provide general-purpose registers for storing both data and addresses, enabling flexible operand access.
  - Key concept: general-purpose register (GPR) architecture — common across major ISAs.

- **(4)** [2%] **False.** Floating-point arithmetic is not associative — parallel execution may produce different results than sequential due to rounding order differences.
  - Key concept: floating-point non-associativity affects parallel computation correctness.

- **(5)** [2%] **False.** Delayed branching is effective for short pipelines but becomes less useful for longer pipelines (more delay slots to fill, harder for compiler to find useful instructions). Pipelining is also technology-dependent (stage timing).
  - Key concept: delayed branch effectiveness decreases with longer pipelines.

- **(6)** [2%] **False.** Memory system behavior significantly affects performance. Programmers and compilers must consider cache locality, alignment, and memory access patterns to achieve high performance.
  - Key concept: memory hierarchy awareness is essential for high-performance code.

- **(7)** [2%] **False.** Modern disk drives (especially SSDs with FTL) handle their own internal scheduling. The OS may not be the best place for disk scheduling because it doesn't know the physical disk layout (especially for SSDs).
  - Key concept: disk scheduling responsibility — OS vs. device-level scheduling.

- **(8)** [2%] **False.** Instruction count and CPI are NOT independent. Different instruction mixes (more memory ops vs. ALU ops) change both the instruction count and the average CPI. Compiler optimizations trade between them.
  - Key concept: instruction count and CPI interdependence — ISA and compiler affect both.

- **(9)** [2%] **False.** Lowering clock rate generally decreases performance (fewer cycles per second). Performance improvement comes from increasing clock rate, improving CPI, or reducing instruction count.
  - Key concept: clock rate and performance — lower clock rate means slower execution.

- **(10)** [2%] **False.** A processor at 1 GHz is not necessarily faster than one at 800 MHz. Performance depends on CPI and instruction count, not just clock rate. A processor with better microarchitecture at 800 MHz could outperform a simpler 1 GHz processor.
  - Key concept: performance is not solely determined by clock frequency — CPI and ISA matter.

### Problem 6

This problem is worth 30 points total (3 subparts).

- **(1)** [10%] **Loop unrolling** reduces branch penalties by replicating the loop body multiple times, reducing the number of branch instructions. Unrolled version processes multiple iterations per loop cycle:
```c
for (i=0; i<1000; i++)
  for (j=0; j<1000; j+=4) {
    A[i][j]   = B[j][i]   * 100 + 20;
    A[i][j+1] = B[j+1][i] * 100 + 20;
    A[i][j+2] = B[j+2][i] * 100 + 20;
    A[i][j+3] = B[j+3][i] * 100 + 20;
  }
```
  - Key concept: loop unrolling — reducing branch overhead by processing multiple iterations per loop cycle.

- **(2)** [10%] Ideal speedup = 2× (dual-core). The outer loop iterations are independent (each `i` value produces independent rows of A). Split iterations across 2 cores: each core handles 500 values of `i`. No synchronization needed. Speedup ≈ 2×.
  - Key concept: parallel loop execution on multicore — speedup bounded by core count for embarrassingly parallel workloads.

- **(3)** [10%] SIMD with 128-bit registers processes 4 × 32-bit integers simultaneously. The inner loop's multiply-and-add can be vectorized: pack 4 elements of B, multiply by 100, add 20, store to 4 elements of A. Performance ratio = 4× (ideal speedup from 128-bit SIMD on 32-bit integers). Note: B's column-major access pattern may limit actual SIMD benefit due to non-contiguous memory access.
  - Key concept: SIMD performance ratio — determined by register width ÷ element width, limited by memory access patterns.
